# 20230619

http接收参数，

int Integer
int，前端传"1"也可以

Integer接收，默认值0

jackson int Integer转换，自动进行的

jackson "1" '1' 和int会自动转换吗？
在Jackson中，当JSON中的字段值为字符串类型（如"1"或'1'），并且目标属性类型为int或Integer时，Jackson会自动尝试进行转换。
具体来说，Jackson会尝试将字符串类型的字段值解析为整数，并将其转换为int或Integer类型。如果字符串可以成功解析为整数，则转换成功；否则，会抛出相应的异常。