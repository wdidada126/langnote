# 20230607

```sql
SELECT DISTINCT
	hdr.from_bill AS FROM_BILL,
	hdr.co_num_id AS CO_NUM_ID,
	hdr.type_num_id AS TYPE_NUM_ID,
	hdr.cort_num_id AS CORT_NUM_ID 
FROM
	sd_bl_co_hdr hdr
	LEFT JOIN sd_bl_co_dtl dtl ON hdr.co_num_id = dtl.co_num_id 
WHERE
	hdr.cancelsign = 'N' 
	AND dtl.cancelsign = 'N' 
	AND hdr.from_bill is not null
	AND hdr.from_bill != ""
	AND hdr.cort_num_id = 'HT03' 
	AND hdr.customer_num_id = '101556'
```



```sql
SELECT 
	DISTINCT(hdr.from_bill) AS FROM_BILL,
	hdr.co_num_id AS CO_NUM_ID,
	hdr.type_num_id AS TYPE_NUM_ID,
	hdr.cort_num_id AS CORT_NUM_ID 
FROM
	sd_bl_co_hdr hdr
	LEFT JOIN sd_bl_co_dtl dtl ON hdr.co_num_id = dtl.co_num_id 
WHERE
	hdr.cancelsign = 'N' 
	AND dtl.cancelsign = 'N' 
	AND hdr.from_bill is not null
	AND hdr.from_bill != ""
	AND hdr.cort_num_id = 'HT03' 
	AND hdr.customer_num_id = '101556'
```

错误


springmvc org.springframework.boot.context.properties.bind.BindingResult源码解析


spring框架的日志，在父类里面打印，所以行数可能不一定是子类的，也有可能是父类的
spring框架的日志，在父类里面打印，所以行数可能不一定是子类的，也有可能是父类的
spring框架的日志，在父类里面打印，所以行数可能不一定是子类的，也有可能是父类的



群友的报错
org.apache.rocketmq.client.exception.MQClientException: CODE: 17  DESC: No topic route info in name server for the topic: topic_syn_game_role

0607 springmvc hibernate-validate jboss-logging

https://blog.csdn.net/github_39060174/article/details/108777977

