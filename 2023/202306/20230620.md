# 20230620

mysql 字符集
https://blog.csdn.net/weixin_43543654/article/details/128862974

在Navicat中，可以通过以下步骤设置 MySQL 表的 Collation：

1. 打开 Navicat 并连接到 MySQL 数据库。
2. 在左侧导航栏中选择要更改 Collation 的数据库。
3. 右键单击要更改 Collation 的表并选择“设计表”选项。
4. 在“设计表”对话框中，选择要更改 Collation 的列。
5. 在“属性”选项卡中，可以看到“Collation”选项。
6. 从下拉菜单中选择所需的 Collation。
7. 点击“保存”按钮以保存更改。

注意：更改表的 Collation 可能会影响数据的排序和比较方式，因此要谨慎操作。最好先备份数据，以防止意外数据丢失。

同一个接口，微服务内部用Dubbo调用，开放给前端用http，同一个接口同时支持这两种

默认情况下，`git log` 命令输出的记录是按提交时间倒序排序的，也就是最近提交的记录排在最前面，最早提交的记录排在最后面。这个顺序是根据提交的时间戳信息来确定的。

如果想要按照其他的顺序进行排序，可以使用 `git log` 命令的一些选项来指定排序方式。例如，可以使用 `--author-date-order` 选项按作者提交时间排序，或者使用 `--topo-order` 选项按提交历史的拓扑顺序排序。

此外，还可以使用 `--reverse` 选项来反转排序顺序，将最早的提交记录放在最前面，最近的提交记录放在最后面。

ThreadLocal 处理shiro session

向kafka发送消息，kafka发起dubbo请求接收dubbo返回

调用任意dubbo server的示例代码

```java
       ReferenceConfig<GenericService> referenceConfig = new ReferenceConfig<GenericService>();
        referenceConfig.setInterface("cn.wdidada.test.fi.jiameng.service.JMFiAccCheckService");
        referenceConfig.setGeneric(true);

        ReferenceConfigCache cache = ReferenceConfigCache.getCache();
        GenericService genericService = cache.get(referenceConfig);
        Map<String, Object> map = new HashMap<>();
        map.put("tenantNumId", 18L);
        map.put("dataSign", 0L);
        map.put("reconciliationNo", "779095744134325");
        Object o = genericService.$invoke("queryItemList", new String[]{"cn.wdidada.test.fi.jiameng.request.JmFiAccountCheckQueryItemListRequest"}, new Object[]{map});
        System.out.println(JsonUtil.toJson(o));

```

kafka跨集群，dubbo不跨集群

kafka解耦的
你可以有任意个消息发送者，任意个消息接收者
接收者可以选择进行逻辑处理，也可以直接丢弃

"封版"是IT开发项目管理中的一个术语，通常指的是软件开发项目或产品的一个重要阶段，意味着该版本或产品已经完成开发，并经过充分测试和验证，准备发布给最终用户或客户使用。
在封版之前，开发团队会进行一系列的开发工作，包括需求分析、设计、编码、测试等。当开发团队认为产品已经满足了既定的需求，并且通过了内部测试和质量验证，便可以决定封版。封版后，开发团队通常会停止新增功能的开发，集中精力进行Bug修复和性能优化。
封版的意义在于标志着软件开发项目的一个重要阶段的完成，产品已经达到了可发布的状态。封版后，产品可以进入发布流程，进行正式的部署和交付给用户。此时，产品的功能和质量已经达到了可接受的水平，用户可以开始使用并享受产品带来的价值。
封版是一个关键的里程碑，标志着开发团队的努力和成果，并为产品的交付和发布奠定了基础。同时，封版后也意味着进入了新的阶段，包括支持和维护、版本更新等。

正确的Git命令是：
```shell
git reset --hard HEAD
```
在该命令中，`reset`表示重置操作，`--hard`表示将当前工作目录和暂存区都回退到指定的提交（HEAD），`HEAD`表示最近的提交。执行此命令将会抛弃当前工作目录和暂存区的所有修改，恢复到最近的提交状态。
请注意在执行此命令前确保你理解其影响，并确认你希望丢弃所有未提交的修改。该命令是不可逆的，且会删除所有未提交的更改。

git reset --hard HEAD
执行此命令将会抛弃当前工作目录和暂存区的所有修改，恢复到最近的提交状态

```shell
git pull
There is no tracking information for the current branch.
Please specify which branch you want to merge with.
See git-pull(1) for details.

    git pull <remote> <branch>

If you wish to set tracking information for this branch you can do so with:

    git branch --set-upstream-to=<remote>/<branch> feat-putong-jiameng
```

```
    @NotNull( message = "导入银行账号数据不可为空")
    @Size(min = 1,message = "导入银行账号数据不可为空")
    @Valid 
    private List<BankCardBatchImportData> dataList;
```