# 20231123

Map<String,Object> map 对象，可以判断value中数据类型

BigDecimal
Double
Long
String
挨个判断

授权管理
authority/insertAdmin

authority/getAdminAuthInfo?crtDate=1700704524152&pageSize=10&pageNum=1

authority/getMainAdminAuthInfo?crtDate=1700704524705

鲁华 这个由其他人做

```xml
    <select id="queryFailnum" parameterType="map" resultType="Integer">
        SELECT nvl(sum(case when a.STATUS='2' then 1 else 0 end),0) failnum
        FROM TB_COMMITION_DETAIL a
        WHERE SALARYBATCHNO=#{salarybatchno,jdbcType=VARCHAR}
    </select>
```

```xml

    <!-- 查询薪资组下的员工银行卡号等信息 -->
    <select id="selectStaffExistBankInfo" parameterType="map" resultType="java.util.HashMap">
        SELECT
            t1.EMP_ID AS EMP_ID,
            t1.EMP_NAME AS "NAME",
            t1.JOB_NO AS JOB_NO,
            t1.ID_NO AS ID_NO,
            t1.ID_NAME AS ID_NAME,
            t1.PHONE AS PHONE,
            t1.BANK_NUMBER AS BANK_NUMBER,
            t1.CLEARING_BANK AS CLEARING_BANK,
            t1.BANK_NAME AS BANK_NAME ,
            NVL(t2.DEP_NAME,t3.ETP_NAME) AS DEP_NAME FROM (
        SELECT t0.EMP_ID AS EMP_ID,t0.EMP_NAME AS EMP_NAME,t0.JOB_NO AS JOB_NO,t0.ID_NO AS ID_NO,t0.PHONE AS PHONE,
               t0.BANK_NUMBER AS BANK_NUMBER,t0.CLEARING_BANK AS CLEARING_BANK,t0.BANK_NAME AS BANK_NAME,
               t0.DEP_ID AS DEP_ID,t0.ID_NAME AS ID_NAME,t0.ETP_ID AS ETP_ID FROM(
        SELECT
            EMP_ID,NAME AS EMP_NAME,JOB_NO,ID_NO,PHONE,BANK_NUMBER,CLEARING_BANK,BANK_NAME,DEP_ID,ID_NAME,ETP_ID
        FROM
            ${defaultSchema}.T_CUSTOM_INFO_TD
        WHERE
            EMP_ID IN(SELECT STAFF_ID FROM ${defaultSchema}.T_MONTHLY_STAFF_SALARY
        WHERE
            GROUP_ID = #{GROUP_ID} AND MONTH = #{YEAR_MONTH}))t0
        <if test='FLAG !="A"'>
        WHERE
            t0.ID_NAME IS NULL OR t0.BANK_NUMBER IS NULL
        </if>
            )t1
        LEFT JOIN
            ${defaultSchema}.T_ETP_DEPART_INFO_TD t2 ON t1.DEP_ID = t2.DEP_ID
        LEFT JOIN
			${defaultSchema}.T_ENTERPRISE_INFO_TD t3 ON t1.ETP_ID = t3.ETP_ID
    </select>

```


hro项目的后端不光是

b-salary-piecework-center
b-salary-report-center
fuyao-salary

Oracle sql merge 语法

Oracle SQL中的MERGE语句用于将一个表的数据合并到另一个表中。它的基本语法如下：

```sql
MERGE INTO 目标表 AS T1
USING 源表 AS T2
ON (T1.匹配条件 = T2.匹配条件)
WHEN MATCHED THEN
    UPDATE SET T1.列名1 = T2.列名1, T1.列名2 = T2.列名2, ...
WHEN NOT MATCHED THEN
    INSERT (列名1, 列名2, ...)
    VALUES (T2.列名1, T2.列名2, ...);
```

其中，`目标表`是要更新或插入数据的表，`源表`是包含要合并数据的另一个表。`匹配条件`是两个表中用于匹配的列。`WHEN MATCHED`部分表示当匹配成功时，需要更新的目标表中的列值；`WHEN NOT MATCHED`部分表示当匹配失败时，需要插入到目标表中的新列值。

删除客户时，需要判断是否有生效中的协议，如果有，不能删除

访问测试环境的hro系统

新建企业 企业认证
添加白名单
开通 “智能薪酬-BETA版” 付费0元
智能费控-BETA版


已有代码用了ES
思维方式需要改变，全文检索用ES
mysql 里面的like

一对多，用','隔开，mysql like 查询的

ES，不是精准匹配的，如果查出来不应该显示的词

https://test-yt-mp.hrs361.com/fuyao-hr-api/piecework/hrofin/getEtpLedgerDtlList

增加 “关联核销记录” 字段

开发环境，测试环境连接的数据库是一个

`ASSOCIATED_SALARY_GROUP` varchar(128) DEFAULT NULL COMMENT '关联的薪资组(多个，逗号分割)',


ALTER TABLE `HRBASE_UAT2`.`T_ETP_INNER_ACC_BOOK_D` 
ADD COLUMN `ASSOCIATED_WRITEOFF_REC` varchar(256) NULL COMMENT '关联核销记录' AFTER `OPP_OPEN_BANK`;

java.util.StringJoiner
```java
        StringJoiner joiner = new StringJoiner(",");
        for (String s : treeSet) {
            joiner.add(s);
        }

        String result = joiner.toString();
```

屹通java版本
jdk1.8.0_341

同步员工数据接口
https://test-yt-mp.hrs361.com/fuyao-hr-api/staff-manage/reSynOrganization?crtDate=1700736207448&etpId=C2311231147301838934081353555974


智能人事 花名册
可以编辑员工信息 设置入职日期

屹通内存 512M，改成2G

报税主体，增加失败
新建账单，没有对应的薪资信息，直接进入“已作废”了
新建公司之后，如何新增公司下属部门  企业管理 -> 组织架构

手动新增或者导入的员工，默认是待激活状态，如何激活


