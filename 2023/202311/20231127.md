# 20231127

项目进度管理模板.xlsx

在MySQL 5.7中，可以使用INSERT语句嵌套SELECT语句来插入数据。以下是一个示例：

```sql
INSERT INTO 目标表 (列1, 列2, 列3)
SELECT 列1, 列2, 列3
FROM 源表
WHERE 条件；
```

在这个示例中，我们将从源表中选择满足条件的行，并将这些行插入到目标表中。请根据您的实际需求替换目标表、源表和列名。
