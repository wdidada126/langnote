# 20231119

SQL进阶教程(MICK.).pdf

ALTER TABLE `T_ETP_INNER_ACC_BOOK_D`.`T_ETP_INNER_ACC_BOOK_D` 
ADD COLUMN `TRAD_FLOW_NO` varchar(36) NULL COMMENT '交易流水号' AFTER `UPDATE_TIME`,
ADD COLUMN `TRADE_AIM` varchar(128) NULL COMMENT '交易用途' AFTER `TRAD_FLOW_NO`,
ADD COLUMN `WRITEOFF_STAT` varchar(1) NULL DEFAULT '0' COMMENT '核销状态 0未核销 1部分核销 2 全部核销' AFTER `TRADE_AIM`,
ADD COLUMN `CREATE_TYPE` varchar(1) NULL COMMENT '生成方式：空：自主生成 1：导入生成' AFTER `WRITEOFF_STAT`,
ADD COLUMN `TO_WRIOFF_AMT` decimal(16, 2) NULL COMMENT '待核销金额' AFTER `CREATE_TYPE`,
ADD COLUMN `WRIOFF_AMT` decimal(16, 2) NULL DEFAULT 0.00 COMMENT '已核销金额' AFTER `TO_WRIOFF_AMT`,
ADD COLUMN `OPP_OPEN_BANK` varchar(256) NULL COMMENT '对方开户行' AFTER `WRIOFF_AMT`;