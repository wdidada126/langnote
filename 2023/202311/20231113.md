# 20231113

CREATE_USER,UPDATE_USER,CREATE_TIME,LAST_MODI_TIME

客户 协议 关联薪资组 不是必填

不存在的东西，找鲁华

发起结算 选择客户名称就行了，其他的都是从协议带过来的

客户账单 

对账 付钱 发票
客户，先票后款

开票这一期先不考虑

字段 以word文档输入输出上的文档

编辑的校验规则 跟新增一样

海明

Hr系统 华跃 旗开得胜 同鑫

国内主流专业HR系统软件厂商，以朗新、嘉扬、万古、同鑫科技等为代表
国内企业综合管理软件厂商以用友、金蝶、泛微等作为代表
国内SAAS HR软件厂商以北森为代表，做的比较好的还有易路、盖雅等


chatgpt类似的产品 智谱

CREATE_TIME
LAST_MODI_TIME

鲁华
内部账户明细导入 导入的模式跟薪酬的一样，支持行选和匹配表头


方哥

CreateExcelOp
生成excdl文件

cn.com.yitong.actions.rules.RulesGroupInfoImportOp#execute


cn.com.yitong.ares.util.DateHelper#getDateString(java.util.Date, java.lang.String)

easyexcdl
com.alibaba.excel.event.AnalysisEventListener#invokeHeadMap

public void invokeHeadMap(Map<Integer, String> ExlheadMap, AnalysisContext context) easyexcel库这个函数 ExlheadMap包括哪些内容？
`ExlheadMap` 是一个包含整数键和字符串值的映射，用于表示Excel表格的表头。在EasyExcel库中，这个函数主要用于处理Excel文件的表头信息。


目前有没遇到什么问题，然后大致进度到哪个环节了，可以列举给我，胡总晚上要过项目会

李洋


讯飞消费者 面试 erp

Java中的ConcurrentHashMap类提供了线程安全的哈希表实现，它使用分段锁技术来提高并发性能。每个Segment对象都有一个独立的锁，当多个线程同时访问不同的Segment时，它们可以并行执行。这样可以在保证线程安全的同时，提高并发性能。

临时变量在堆上还是在栈上
栈

springboot启动流程

物理删除