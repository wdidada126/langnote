# 20231120

ES中文社区
https://elasticsearch.cn/
求match_phrase, match_phrase_prefix,wildcard查询的优化建议。

1、业务层面结合需求尽量使用match_phrase；
2、除非特殊情况，尽量避免使用模糊查询wildcard，尤其数据量极大的情况下：
3、实际需要你列出业务场景或者场景中遇到的问题，这样才便于进一步分析。

logstash向es里面导入mysql数据
https://elasticsearch.cn/question/4138

meilisearch and or条件搜索

https://blog.csdn.net/aaddCSDN/article/details/129342365

https://www.meilisearch.com/docs

POC是Proof of Concept的缩写，即概念验证。通常是企业进行产品选型时或开展外部实施项目前，进行的一种产品或供应商能力验证  。

方哥 团队 开晨会

屹通 tidb 数据库 是一个库，在数据库管理工具上不是一个，实际上是一个库，不用分布式事务

中电信量子 张伟 重新找我了 背调没过 重新背调的方案走不通
徐吉朋 说我是被裁员 其实是协商离职，他说话不留余地


ALTER TABLE `smart_hro`.`T_ETP_BILL_INNER_ACC_TM` 
ADD COLUMN `TM_ID` varchar(16) NULL COMMENT '主键id\r' AFTER `id`,
ADD COLUMN `ETP_ID` varchar(16) NULL COMMENT '企业ID\r' AFTER `TM_ID`,
ADD COLUMN `ACC_ID` varchar(16) NULL COMMENT '账户ID\r' AFTER `ETP_ID`,
ADD COLUMN `BILL_ID` varchar(16) NULL COMMENT '账单ID\r' AFTER `ACC_ID`,
ADD COLUMN `WRIOFF_AMT` decimal(11, 2) NULL COMMENT '单次核销金额\r' AFTER `BILL_ID`;


问个逻辑，我有个定时任务，逻辑如下：
一个企业微信下有50个企业成员，每个企业成员最多可以添加2w个客户：
先拿到企业成员的userid（50个，循环执行下面逻辑）
方案一：1、根据企业成员的userID 获取其添加的客户id列表（不分页，最多2w条数据） 2、根据客户id获取客户详情（筛选没有详情的客户id调用此接口）
方案二：根据企业成员的userID列表 直接批量获取其下所有的客户详情（分页，一页100条）全量数据，最多2w*企业成员userID数
这两方案咋选

1，影响可控 出问题也只是一家出问题

李明义总
鲁华

你抽空把客户管理的功能，相关的需求都看看，业务逻辑了自己捋一捋
这块目前的功能基本结束，现在你要给能接下来，有新的研发，可能就安排你来弄
hro项目，你得深入介入

屹通开发工具
sts-bundle.zip
