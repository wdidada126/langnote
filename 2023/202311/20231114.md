# 20231114

foxmail*exe
foxmail文件要复制到新买的ssd磁盘上

会议室2
需要弄清楚公司有哪些会议室

接口响应时间
北京到广州15ms

hro项目个人负责的接口.xlsx

```java
	map.put("ID", SeqGeneUtil.geneBusiSN());
```

D:\b-salary-piecework-center\src\main\resources\public\test\debug.html

```yaml
spring.redis.host=172.16.10.70
spring.redis.password=redis@20230804
```

6379

Redis Version: 5.0.14

		<if test="null != END_TIME and '' != END_TIME">
			<![CDATA[
			AND TRAD_DATE < #{END_TIME} ]]>
		</if> <![CDATA[ ]]> 作用

这段代码是MyBatis的动态SQL语句的一部分，用于构建SQL查询条件。

具体来说，它的作用是：如果变量`END_TIME`不为空且不为空字符串，那么在生成的SQL查询条件中添加一个子句`AND TRAD_DATE < #{END_TIME}`，表示只选择交易日期小于`END_TIME`的数据。

这里的`<![CDATA[ ]]>`是XML中的注释标记，用于告诉解析器忽略其中的文本内容，不对其进行解析和处理。

在Java中，时间格式化的HH:mm:ss和hh:mm:ss表示24小时制和12小时制的时间格式。

- HH:mm:ss是24小时制的时间格式，其中HH表示小时数，范围为00到23；mm表示分钟数，范围为00到59；ss表示秒数，范围为00到59。例如，"13:45:30"表示下午1点45分30秒。
- hh:mm:ss是12小时制的时间格式，其中hh表示小时数，范围为1到12；mm表示分钟数，范围为00到59；ss表示秒数，范围为00到59。例如，"01:45:30 PM"表示下午1点45分30秒。

因此，HH:mm:ss和hh:mm:ss的区别在于时间的显示方式不同，前者是24小时制，后者是12小时制。

时间数据，格式化成
可以的，SQL字符串比较可以直接使用 > < <= 等符号进行比较。

<trans name="in/hrofin/getEtpLedgerList" desc="实时到账-分类查询交易信息">

hrofin/getEtpLedgerDtlList
hrofin/cancelEtpLedger
实时到账-取消交易信息

器宇轩昂
刘轩
轩 xuan

刘刊

hrofin/getWrioffLedList
实时到账-查询待核销交易列表

hrofin/countLedList
实时到账-查询核销统计数据

hrofin/searchTradAcc
实时到账-查询交易账号

hrofin/wrioffLed
实时到账-核销到账信息

23
hrofin/getLedWriOffList
实时到账-查询关联核销列表

24
hrofin/getLedWriOffInfo
实时到账-查询待核销信息

25
hroprotocol/getProListBySalary
协议-根据关联信息查询协议列表

26
hroCustomManage/allCustomInfoList
客户管理-所有客户列表查询

27
hroprotocol/getProtocoCount
协议-查询未关联薪资组个数


,{"clazz":"PP15","desc":"实时到账-查询待核销交易列表","name":"实时到账-查询待核销交易列表","path":"in/hrofin/getWrioffLedList","plus":"","url":"hrofin/getWrioffLedList"}
];

端口 18021
```java
        Map<String, Object> map = ctx.getParamMap();
        logger.error("map:{}",map);
```
打印的结果


map:{server=https://qygj-test.jsbchina.cn/ecs-mobile-gateway/ares-ent-server, *required=, empType=1, PAGE_NUM=0, _CURR_TIME=16:38:57, _TIME_STAMP=20231114163857, TOTAL_NUM=0, CHECK_STAT=3, customId=c2109241923461552762489314025475, _IS_LOG=true, NEXT_KEY=, HAS_NEXT=N, appId=ZH20210917212626016330000000146, etpId=C2112070945031579433553164615686, ETP_ID=2, etpName=测试12121212, _TRAN_NAME=实时到账-分类查询交易信息, _LOG_DESC=null, _CURR_DATE=2023-11-14, teaAccToken=ZH20210917212626016330000000146, isAdmin=Y, userName=杨光宇, userId=C2110211432341562473680190918661, busiTraceNo=1, clientIp=127.0.0.1, _CURR_DATE_TIME=2023-11-14 16:38:57, TRANS_URL=/api/hrofin/getEtpLedgerDtlList.do, appSecret=4bfe0645675be67014eefbb9b1d928a9, LIST=[]}

业务代码
查询要考虑是否分页

Spring Web Flow是一个基于Spring MVC构建的框架，它允许实现Web应用程序的“流程”。这种流程封装了一系列步骤，指导用户执行某些业务任务，且具有状态性，能跨越多个HTTP请求，处理事务数据，可重用，并可能是动态的，本质上是长期运行的。 它的最佳应用场景包括需要用户按照特定顺序完成一系列步骤的应用程序，例如在线购物车、在线银行等。
此外，Spring Web Flow是Spring的一个子项目，其主要目标是解决用户与服务器之间跨越多个请求的有状态交互问题。 它与Spring MVC深度整合，最新版本2.0明确声明是基于Spring Web MVC的一个扩展。 并且，它还提供了处理Ajax事件的能力，使得在处理完成后可以刷新客户端相关界面代码。

屹通建表规范.md
TD 业务
TH 历史
TM 关系
TB 码表 城市 不经常变动的

etpId 企业id
创建时间
更新时间

{
    "head":{
        "clientIp":"127.0.0.1",
        "etpId":"C2112070945031579433553164615686",
        "userId":"C2110211432341562473680190918661",
        "userName":"杨光宇",
        "etpName":"测试12121212",
        "customId":"c2109241923461552762489314025475",
        "empType":"1",
        "isAdmin":"Y",
        "teaAccToken":"ZH20210917212626016330000000146",
        "appId":"ZH20210917212626016330000000146",
        "appSecret":"4bfe0645675be67014eefbb9b1d928a9",
        "server":"https://qygj-test.jsbchina.cn/ecs-mobile-gateway/ares-ent-server"
    },
    "body":{
    }
}

etpId在head里面

2023-11-14 20:47:51,936|INFO |main           |[org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor:181]|Initializing ExecutorService 'taskExecutor'

CREATE TABLE `HRO_CUSTOM_BILL_INFO_TD` (
  `ID` varchar(36) NOT NULL COMMENT '编号',
  `ETP_ID` varchar(36) NOT NULL COMMENT '企业编号',
  `BILL_ID` varchar(36) NOT NULL COMMENT '账单编号',
  `BILL_NAME` varchar(100) NOT NULL COMMENT '账单名称',
  `PROTOCOL_ID` varchar(36) NOT NULL COMMENT '协议ID',
  `OWE_DATE` varchar(6) NOT NULL COMMENT '所属年月(格式：yyyyMM)',
  `INVOICE_ID` varchar(36) DEFAULT NULL COMMENT '发票ID',
  `MONTH` varchar(128) NOT NULL COMMENT '薪资月(多个，逗号分割)',
  `INVOICING_FORM` char(1) DEFAULT NULL COMMENT '开票形式 1、按预收金额开票、2、按实际到账金额开票',
  `BILL_TOTAL` decimal(11,2) DEFAULT NULL COMMENT '账单总金额',
  `MANAGE_FEE` decimal(11,2) DEFAULT NULL COMMENT '管理费',
  `RECEIVE_FEE_TOTALL` decimal(11,2) DEFAULT NULL COMMENT '应收费用总额',
  `RECEIVE_SETTLE_FEE` decimal(11,2) DEFAULT NULL COMMENT '应收结算税费',
  `VERIFICATION_AMOUNT` decimal(11,2) DEFAULT NULL COMMENT '待核销金额',
  `VERIFICATED_AMOUNT` decimal(11,2) DEFAULT NULL COMMENT '已核销金额',
  `DIFF_AMOUNT` decimal(11,2) DEFAULT NULL COMMENT '差额待开票金额',
  `NON_DIFF_AMOUNT` decimal(11,2) DEFAULT NULL COMMENT '非差额待开票',
  `CREATOR` varchar(36) NOT NULL COMMENT '创建人ID',
  `REMAKR` varchar(200) DEFAULT NULL COMMENT '备注',
  `CREATE_TIME` char(26) NOT NULL COMMENT '创建时间YYYY-MM-DD HH24:MI:SS.FFFFFF',
  `LAST_MODI_TIME` char(26) NOT NULL COMMENT '最后一次更新时间YYYY-MM-DD HH24:MI:SS.FFFFFF',
  `BILL_STATUS` char(1) NOT NULL DEFAULT '1' COMMENT '账单状态(1:制作中,2:待锁定,3:待确认,4:已确认,5:待收款,6:部分收款,7:已收款,8:已作废)',
  `REFUND_AMOUNT` decimal(11,2) DEFAULT NULL COMMENT '退补金额'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_bin COMMENT='客户账单信息表';

极客时间 mysql课程 复习了

redo log 重做日志 innodb的
binlog 归档日志 mysql server的
undo log 


mysql> show variables like 'transaction_isolation';
这句写错了，应该是tx_isolation。测试了一下

作者回复: 你是不是用的5.6或更早的版本
5.7引入了transaction_isolation用来替换tx_isolation了，到8.0.3就去掉了后者了

读未提交：别人改数据的事务尚未提交，我在我的事务中也能读到。
读已提交：别人改数据的事务已经提交，我在我的事务中才能读到。
可重复读：别人改数据的事务已经提交，我在我的事务中也不去读。
串行：我的事务尚未提交，别人就别想改数据。
这4种隔离级别，并行性能依次降低，安全性依次提高。

{
    "head":{
        "clientIp":"127.0.0.1",
        "etpId":"C2112070945031579433553164615686",
        "userId":"C2110211432341562473680190918661",
        "userName":"杨光宇",
        "etpName":"测试12121212",
        "customId":"c2109241923461552762489314025475",
        "empType":"1",
        "isAdmin":"Y",
        "teaAccToken":"ZH20210917212626016330000000146",
        "appId":"ZH20210917212626016330000000146",
        "appSecret":"4bfe0645675be67014eefbb9b1d928a9",
        "server":"https://qygj-test.jsbchina.cn/ecs-mobile-gateway/ares-ent-server"
    },
    "body":{
        "ETP_ID":"C2211080933261701192952492027906",
        "WRITEOFF_STAT":"1",
        "START_TIME":"2023-06-17 09:36:32",
        "END_TIME":"2023-07-16 09:36:32"
    }
}

