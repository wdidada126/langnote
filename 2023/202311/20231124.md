# 20231124

hroCustomManage/getEtpCustList


hroprotocol/getToBeSettled

hroprotocol/getWillExpire


        <data key="*sqlId" expr="protocol.getToBeSettled" desc="Mybatis SQL Statement"/> TYPE 1


        <data key="*sqlId" expr="protocol.getToBeSettled" desc="Mybatis SQL Statement"/>  TYPE 2



HRO_PROTOCOL_INFO_TD
CREATOR 创建人
更新人

HRO_REMIND_INFO_TD
HRO提醒信息表
type 类型 1、待结算 2、将到期


	<insert id="businessOwner" parameterType="map">
		INSERT INTO ${schema.smarthro}.HRO_BUSINESS_OWNER_TM (
			ID,
			ETP_ID,
			PROTOCOL_ID,
			OWNER_ID,
			CREATE_TIME
		) VALUES  
		<foreach collection="LIST" item="item" index="index" separator="," >  
		   (
			 CONCAT(#{ID}, #{index}),
			 #{etpId},
			 #{ID},
			 #{item.EMP_ID},
			 now()
		   )
		</foreach>
	</insert>


