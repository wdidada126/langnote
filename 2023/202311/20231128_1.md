# 20231128

# 20231128

tars做服务治理，服务发现， trpc做服务实现、服务调用。

不可能的 随便一个大厂都不可能 抢功抢的那么严重 丢锅丢的那么溜 出锅了算哪个团队的？ 上面要业绩算谁的头上？

在哪都一样，不能有一套系统 只能有一个话事人。

你是懂大厂的

腾讯做trpc一个重要原因, 我理解就是因为有一些部门不可能再使用tars了, 用了不同的组件服务, 不可能都切换, 因此做了trpc能把这些都涵盖进来, 因此可扩展性是一个重要设计, 对于一般公司可能没这个需求, 实际上当你一旦选择某种名字服务以后, 以后也不太可能再切换了, 因此这个可扩展性也基本是一次性的.

https://github.com/Nepxion/Discovery

axum 0.7刚刚发布，你去看看里面的example，到处都是不兼容的例子，被注释掉了。
可能是因为hyper不兼容吧
poem 的优点是和graphhq是一个作者，缺点是 没有像 axum 那样建构在 tower hyper这一套tokio库上
tokio的文档挺好的，配合 programming rust 第二版 异步编程 那一章，就能掌握原理
tokio 有一个例子， mini-redis，手抄一边，看懂所有注释，tokio就基本能搞定了


UPDATE HRO_CUSTOM_BILL_INFO_TD 
SET UPDATEOR = CREATOR 
WHERE
	UPDATEOR IS NULL 
	AND CREATOR != NULL 
	AND CREATOR != '';

dbInsOp 这个可以用，不用写重复代码

    <node id="ceb50d97-94f0-48a3-8ae5-3d0037863b07" xPos="500" yPos="390" Width="180" Height="40">
      <desc>添加协议</desc>
      <beanId>dbInsOp</beanId>
      <data-map>
        <data key="*required" expr="" desc="必输项检查"/>
        <data key="*sqlId" expr="protocol.addProtocolInfo" desc="Mybatis SQL Statement"/>
        <data key="*dbName" expr="" desc="数据库名，指定数据源"/>
        <data key="ETP_ID" expr="${etpId}" desc="用途说明"/>
        <data key="CREATOR" expr="${currentStaffId}" desc="用途说明"/>
        <data key="UPDATEOR" expr="${currentStaffId}" desc="用途说明"/>
        <data key="PROTOCOL_STATUS" expr="1" desc="用途说明"/>
      </data-map>
    </node>

协议 新增
hroprotocol/protocolAdd

协议 续签
hroprotocol/renewal
协议 导入
hroprotocol/protocolImport

客户 新增
hroCustomManage/addEtpCustInfo
客户 导入
hroCustomManage/importEtpCustList



客户账单
hroprotocol/initiateSettlement

{"BILL_NAME":"1453","CUST_NAME":"S20231123155906018700000000936","ID":"S20231123181237018740000000375","OWE_DATE":"202311","MONTH":"202305","INVOICING_FORM":"2","ASSOCIATED_SALARY_GROUP":"2c908a908bfb2461018bfb3aa73c001d"}

@丁威 丁老师我遇到一个问题，我们在将kafka的topic从A机器迁移到B机器上时，迁移过程中发现B机器上有两个topic的producer发消息全部失败了，全部失败，其他topic正常，迁移完后，这俩topic又正常了，这可能是什么原因呢
你是怎么迁移的
如果是队列重分布，kafka是先扩容副本，进行数据同步，然后执行Leader选举，基本是平滑迁移的


idea_spring_beans.png

idea_spring_mapping.png
