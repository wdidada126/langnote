# 202304_week2

## 时间范围

- 202304 月第 2 周

## 本周记录

- 20230408：从源码中可以看出，线程池的构造函数有7个参数，分别是corePoolSize、maximumPoolSize、keepAliveTime、unit、workQueue、threadFactory、handler。下面会对这7个参数一一解释。；1、corePoolSize 线程池核心线程大小；线程池中会维护一个最小的线程数量，即使这些线程处理空闲状态，他们也不会被销毁，除非设置了allowCoreThreadTimeOut。这里的最小线程数量即是corePoolSize。任务提交到线程池后，首先会检查当前线程数是否达到了corePoolSize，如果没有达到的话，则会创建一个新线程来处理这个任务。
- 20230409：20230409 淘宝买了Java面试题库，背啊；ScheduledThreadPoolExecutor 类；ScheduledExecutorService java 1.5的接口
- 20230410：看书；mysql；SQL初学者指南2
- 20230411：主键索引，唯一索引区别；主键是一种约束，唯一索引是一种索引，两者在本质上是不同的；；一张表只能有一个主键，但可以创建多个唯一索引；
- 20230412：sql行转列；在SQL中，可以使用Pivot操作将行转换为列。下面是一个例子：；假设我们有一个名为"sales"的表，它包含以下列：
- 20230413：不是吧？我现在的公司，我当初面试的时候第一轮简单，就是电话面试。第二轮两个人给我出了一道算法题，直接坐在我面前盯着我的电脑屏幕的投影看我一行一行写出来，还和我聊了聊思路。题目也是公司业务相关的算法。第三轮给我出了一个系统设计题，让我从前端到后台的做数据库结构，api接口设计，技术选型，再让我做性能优化方案。第四轮又给我来了一个系统设计题。第五轮面我项目管理。这除了第一轮和第五轮有希望被培训机构和刷题搞定。剩下三轮机构搞不定的吧？；如果想要在表中使用自动更新时间戳功能，可以将TIMESTAMP类型字段的默认值设置为CURRENT_TIMESTAMP，每次插入或更新记录时都会自动更新该字段的值。；表字段设计是数据库设计中的一个重要方面，它决定了表的结构和数据存储的方式。以下是一些表字段设计的考虑因素：
- 20230414：补充了当日日志，暂未提炼出明确主题。
