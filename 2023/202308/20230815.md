# 20230815

spring的功能
ioc
aop
读写配置项，赋值给类中的属性
兼容或者你自己写库去兼容任何三方库


idea 时序图插件不能生成jar包里面代码的时序图

org.springframework.context.support.AbstractApplicationContext#refresh 时序图 超级复杂

有一些讲源代码的书籍，会给出函数调用时序图

ConfigurableApplicationContext_struct.png

plantuml 时序图

springmvc 从web.xml中看，context就是AnnotationConfigWebApplicationContext
spring-framework官方文档阅读


https://docs.spring.io/spring-boot/docs/2.7.14/reference/html/using.html#using


文档，spring文档可以
vert.x的文档

java8的文档
https://docs.oracle.com/javase/8/docs/api/overview-summary.html


depedendOn
拼写错误 dependsOn
depends-on用来表示一个Bean的实例化依靠另一个Bean先实例化。如果在一个bean A上定义了depend-on B那么就表示：A 实例化前先实例化 B。



<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN 2.0//EN" "http://www.springframework.org/dtd/spring-beans-2.0.dtd">
<beans> 
    <bean name="dao" class="research.spring.beanfactory.ch3.Dao" depends-on="database" > 
    </bean> 
    <bean id="database" class="research.spring.beanfactory.ch3.Database"> 
    </bean> 
</beans>


以下是一个基本的Spring XML配置示例：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 定义一个简单的bean -->
    <bean id="myBean" class="com.example.MyBean">
        <!-- 设置属性值 -->
        <property name="name" value="John Doe" />
    </bean>

    <!-- 定义另一个bean，依赖于myBean -->
    <bean id="dependentBean" class="com.example.DependentBean">
        <!-- 注入依赖 -->
        <property name="myBean" ref="myBean" />
    </bean>

</beans>
```

在上面的示例中，我们定义了两个bean：`myBean`和`dependentBean`。`myBean`是一个名为`com.example.MyBean`的类的实例，它具有一个名为`name`的属性，属性值设置为"John Doe"。

`dependentBean`是另一个名为`com.example.DependentBean`的类的实例，它依赖于`myBean`。我们通过`<property>`元素将`myBean`注入到`dependentBean`中。

请注意，上述示例中的类名和包名仅用作示例，您需要根据您自己的代码进行相应的替换。此外，您还需要根据您的项目需求添加其他必要的配置和依赖项。