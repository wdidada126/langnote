# 20230807

    <dependency>
      <groupId>com.alibaba.spring</groupId>
      <artifactId>spring-context-support</artifactId>
      <version>1.0.10</version>
    </dependency>

```
dubbo:
  registry:
    address: nacos://${spring.cloud.nacos.server-addr}?namespace=dubbo-jiameng
    # parameters:
    #   namespace: dubbo-dev
  protocol:
    name: dubbo
    threads: 400
  consumer:
    check: false
```