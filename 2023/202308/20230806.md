# 20230806

spring boot与dubbo结合，自动配置，dubbo框架就已经注入sring ioc容器了

哪些是dubbo provider？
批量注入dubbo customer？

```yaml
dubbo:
  cloud:
 #   subscribed-services: sequence-service
    subscribed-services:
    # erp-fi,erp-wm,erp-so,omp-cproduct,omp-cmasterdata,erp-scm,omp-ccache,omp-cmessagecenter-productor,sequence-service,omp-cinventory-erp,omp-cexport
```

好的,我用中文给你一个 Spring Boot 整合 Dubbo 的配置示例:

```java
@SpringBootApplication
@EnableDubbo
public class DubboProviderDemoApplication {

  public static void main(String[] args) {
    SpringApplication.run(DubboProviderDemoApplication.class, args);  
  }

  @Bean
  public DemoService demoService() {
    return new DemoServiceImpl();
  }

}

@DubboService  
public class DemoServiceImpl implements DemoService {

  @Override
  public String sayHello(String name) {
    return "Hello " + name;
  } 

}
```

Dubbo 的配置放在`application.properties`文件中:

```
# Spring Boot应用配置  
spring.application.name=dubbo-provider-demo
server.port=9090

# Dubbo配置
dubbo.scan.base-packages=com.example.dubboproviderdemo  
dubbo.protocol.name=dubbo
dubbo.protocol.port=20880

dubbo.registry.address=N/A
```

这个配置了一个基本的Dubbo提供者,通过Dubbo在20880端口暴露`DemoService`服务。

客户端类似,只是使用`@DubboReference`引用远程服务,并不暴露服务端口。

如果你需要Spring Boot整合Dubbo的相关说明,请告诉我。




在Spring Boot中使用Dubbo,需要在pom.xml中添加以下依赖:

<dependency>
    <groupId>org.apache.dubbo</groupId>
    <artifactId>dubbo-spring-boot-starter</artifactId>
    <version>2.7.3</version>
</dependency>

这个是Dubbo Spring Boot Starter的依赖,它会自动扫描@DubboService,@DubboReference注解并注册服务。

如果你需要使用Zookeeper作为注册中心,还需要添加:

<dependency>
    <groupId>org.apache.dubbo</groupId>
    <artifactId>dubbo-dependencies-zookeeper</artifactId>
    <version>2.7.3</version>
    <type>pom</type>
</dependency>

这会引入Zookeeper客户端相关的依赖。

另外,Dubbo还支持其他注册中心,如Nacos、Eureka等,如果要使用其他注册中心,需要将对应的依赖添加到pom.xml中。

最后,在Spring Boot 2.x中,需要额外添加:

<dependency>
  <groupId>org.apache.dubbo</groupId>
  <artifactId>dubbo</artifactId>
  <version>2.7.3</version> 
</dependency>

这是因为Spring Boot 2.x默认使用高版本的commons-codec,和Dubbo不兼容,需要显式指定dubbo依赖来解决冲突。


Classes

ServiceInstancePreDeregisteredEvent
ServiceInstancePreRegisteredEvent
ServiceInstanceRegisteredEvent
ServiceInstancesChangedEvent
SubscribedServicesChangedEvent


失眠了，05号早上6点多才睡，睡到下午2点
晚上10点睡，12点多醒了，凌晨4点继续睡，早上8点起来上班

一个url，同时支持xml json格式的输入
springmvc 内置了excel，pdf等视图的支持

springmvc 内置thfrif protobuf支持吗？
在Spring MVC中，并没有内置对Thrift或Protobuf的直接支持。然而，您可以通过自定义配置来集成Thrift或Protobuf到Spring MVC应用程序中。

对于Thrift，您可以使用第三方库如"thrift-spring"来实现集成。"thrift-spring"提供了Thrift服务的Spring集成支持，包括Thrift服务的定义、处理器的注册和请求的处理。您可以将其添加到您的项目的Maven依赖中，然后根据文档进行配置和使用。

对于Protobuf，您可以使用"spring-protobuf"或"spring-boot-starter-protobuf"等第三方库来实现集成。这些库提供了与Protobuf相关的Spring集成支持，包括Protobuf消息的定义、编解码器的注册和请求的处理。您可以根据您的需求选择适当的库，并按照其文档进行配置和使用。

总之，虽然Spring MVC本身没有内置Thrift或Protobuf的支持，但可以通过使用第三方库来实现集成并在Spring MVC应用程序中使用它们。

对于"thrift-spring"库的Maven坐标，以下是一个示例：

```xml
<dependency>
    <groupId>org.apache.thrift</groupId>
    <artifactId>thrift-spring</artifactId>
    <version>0.13.0</version>
</dependency>
```

您可以将上述依赖项添加到您的项目的pom.xml文件中，以使用"thrift-spring"库。请注意，版本号可能会有所不同，您可以根据需要选择适当的版本号。

请确保您的项目配置了正确的Maven仓库，以便能够从中央仓库或其他相关仓库获取该库的依赖项。


objenesis
spring core中org.springframework.objenesis这个package

讯飞销委会
1、负责公司产品在所属大区教育行业的产品的推广； 2、负责客户拓展、销售谈判，执行公司的销售政策； 3、为不断改进公司产品，主动搜集、分析、跟踪用户体验并及时反馈； 4、带领团队开拓、维护优质客户资源，与合作院校、政府建立良好的长期合作关系 ，并做好客户数据统计分析； 5、分析和预测销售市场、把握市场趋势，为决策提供准确的相关信息，开拓和发展销售市场； 6、定期对市场营销环境、目标、计划、业务活动进行核查分析，及时调整营销策略和计划，制订预防和纠正措施，确保完成营销目标和营销计划。


HttpRequestHandlerAdapter
org.springframework.web.HttpRequestHandler 接口实现类


ResourceHttpRequestHandler (org.springframework.web.servlet.resource)
DefaultServletHttpRequestHandler (org.springframework.web.servlet.resource)
PreFlightHandler in AbstractHandlerMapping (org.springframework.web.servlet.handler)
HttpInvokerServiceExporter (org.springframework.remoting.httpinvoker)
HessianServiceExporter (org.springframework.remoting.caucho)

`DefaultServletHttpRequestHandler`是Spring MVC提供的一个特殊的`HttpRequestHandler`实现，用于处理静态资源的请求。它的作用是将对静态资源（如HTML、CSS、JavaScript、图像文件等）的请求转发给应用程序的默认Servlet容器来处理。
当使用Spring MVC配置了`<mvc:default-servlet-handler>`或`<mvc:resources>`时，`DefaultServletHttpRequestHandler`会被自动注册，并且会处理与配置的静态资源位置相匹配的URL请求。
`DefaultServletHttpRequestHandler`的主要作用有以下几点：
1. 提供静态资源的访问：它将静态资源的请求转发给默认Servlet容器处理，例如在Java Web应用中通常是由容器（如Tomcat或Jetty）提供的默认Servlet。
2. 处理静态资源的缓存：`DefaultServletHttpRequestHandler`支持对静态资源的缓存控制，可以在响应中设置适当的缓存头信息（如`Cache-Control`、`Last-Modified`等）。
3. 兼容性和性能优化：将静态资源的请求交给默认Servlet容器处理，可以利用容器的优化机制和高效的静态资源处理，提高性能和兼容性。
使用`DefaultServletHttpRequestHandler`时，您需要确保配置了合适的静态资源位置和处理规则，以便它可以正确地将请求转发给默认Servlet容器，并返回相应的静态资源。
总而言之，`DefaultServletHttpRequestHandler`的作用是处理静态资源的请求，并将其转发给默认Servlet容器来处理，以提供静态资源的访问和支持缓存控制。



`org.apache.catalina.core.ApplicationDispatcher`是Apache Tomcat服务器中的一个核心组件，它扮演着请求分发器（Request Dispatcher）的角色。它的主要作用是处理HTTP请求的转发、包含和重定向，以及管理请求和响应对象的传递。
具体来说，`ApplicationDispatcher`的主要功能包括：

1. 请求分发：当一个HTTP请求到达Tomcat服务器时，`ApplicationDispatcher`负责将请求分发给正确的目标组件，例如Servlet、JSP页面或静态资源。
2. 请求包含：`ApplicationDispatcher`可以处理请求的包含（include）操作，即将当前请求的控制权转交给另一个目标组件，然后将控制权返回到原始请求的位置。这样可以在一个请求中包含多个组件的处理结果。
3. 请求重定向：`ApplicationDispatcher`可以处理请求的重定向（redirect）操作，即将当前请求重定向到另一个URL或位置。这通常用于执行页面跳转或处理其他资源的请求。
4. 请求和响应对象的传递：`ApplicationDispatcher`负责在请求之间传递请求和响应对象，以确保目标组件可以正确地访问和操作这些对象。
5. 上下文路径的管理：`ApplicationDispatcher`维护了每个请求的上下文路径，并确保在分发请求时正确解析和处理相对路径。
总之，`ApplicationDispatcher`在Tomcat服务器中扮演着请求分发器的角色，负责将HTTP请求转发、包含和重定向到正确的目标组件，并管理请求和响应对象的传递。它是Tomcat处理请求和实现Servlet容器功能的关键组件之一。


思考一下，这个涉及到一个问题，http server/web server的


希沃教室演示一体机 确实占有率高，他还有另外一个商用屏叫Maxhub很多企业会议室里用的就是这个
