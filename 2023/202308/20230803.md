# 20230803

```java
        if(endDate!= null){
            stringBuffer.append(" and item.delivery_date <= ? ");
            queryParams.add(DateFormatUtil.lastSecOfDay(endDate));
        }
        if (ObjectUtils.isNotEmpty(shipDateStart)) {
            stringBuffer.append(" and DATE(item.ship_date) >= ?");
            queryParams.add(shipDateStart);
        }
```

比较下代码

copilet 需要付费购买，但是淘宝上有github学生账号，已经花钱激活了，等待发放权益。edidada555@gmail.com这个账号


https://www.cnblogs.com/hejing-swust/p/9451512.html

springboot编译 2.3以下都是maven


AnnotationConfigServletWebApplicationContext.png

