# 20230803

```java
        if(endDate!= null){
            stringBuffer.append(" and item.delivery_date <= ? ");
            queryParams.add(DateFormatUtil.lastSecOfDay(endDate));
        }
        if (ObjectUtils.isNotEmpty(shipDateStart)) {
            stringBuffer.append(" and DATE(item.ship_date) >= ?");
            queryParams.add(shipDateStart);
        }
```

比较下代码

copilot 需要付费购买，但是淘宝上有github学生账号，已经花钱激活了，等待发放权益。edidada555@gmail.com这个账号


https://www.cnblogs.com/hejing-swust/p/9451512.html

springboot编译 2.3以下都是maven


AnnotationConfigServletWebApplicationContext.png

DispatchServlet代码中为什么要抽象出9大组件

前端网页技术有多种，需要兼容

ViewResolver

入参出参处理逻辑，以前是继承Controller接口，spring-webmvc中的org.springframework.web.servlet.mvc.Controller接口，现在是用@Controller注解实现

aop的几种用法，其中有一种是注解修饰方法，@Transactional @RequestMapping都是这样，可以认为这就是aop


    @Resource
    private DataSourceTransactionManager transactionManager;

    TransactionStatus txStatus = null;
    try{
        txStatus = transactionManager.getTransaction(TransactionUtil.newTransactionDefinition(60 * 60));
        transactionManager.commit(txStatus);
    } catch (Exception e) {
    if (Objects.nonNull(txStatus)) {
            transactionManager.rollback(txStatus);
        }
        ExceptionUtil.processException(e, response);
    }


TransactionDefinition
org.springframework.transaction.TransactionDefinition

int getPropagationBehavior() 
int getIsolationLevel()
int getTimeout()
boolean isReadOnly()
String getName()

