# 20230810


Dubbo 是一个高性能、轻量级的开源分布式服务框架，它提供了许多高级特性来支持大规模分布式系统的开发和管理。以下是 Dubbo 的一些高级特性：

1. **负载均衡**：
   Dubbo 提供多种负载均衡策略，例如随机、轮询、最少活跃调用等。这些策略可以根据实际需求选择，实现请求在集群中的分发和负载均衡。

1. **集群容错**：
   Dubbo 提供了多种集群容错机制，例如容错策略、失败自动切换、失败快速失败等。通过这些机制，可以在出现故障或异常时提供容错和自动恢复的能力。

1. **动态配置**：
   Dubbo 允许在运行时动态调整配置参数，如超时时间、并发数、重试次数等。这样可以根据实际情况灵活调整配置，提高系统的可调性和适应性。

1. **服务注册与发现**：
   Dubbo 提供了服务注册与发现的功能，可以将服务注册到注册中心，并通过注册中心进行服务的发现和订阅。这样可以实现服务的动态扩展和管理。

1. **可扩展性**：
   Dubbo 提供了可扩展的接口和插件机制，允许开发人员自定义扩展和集成第三方组件。可以编写自定义的编解码器、协议、路由策略等，以满足特定的业务需求。

1. **监控和统计**：
   Dubbo 提供了丰富的监控和统计功能，可以实时监控服务的调用情况、性能指标和错误信息。这样可以帮助开发人员快速定位和解决问题，提高系统的可观测性和稳定性。

1. **多协议支持**：
   Dubbo 支持多种通信协议，如 Dubbo 协议、HTTP 协议、RMI 协议等。可以根据需求选择适合的协议，以满足不同的通信需求和场景。

1. **分布式事务**：
   Dubbo 提供了分布式事务的支持，可以在分布式环境中实现跨服务的事务一致性。可以使用分布式事务管理器来协调多个参与者的事务操作，确保事务的正确执行和回滚。

这些高级特性使得 Dubbo 在构建可靠、高性能的分布式系统方面非常强大和灵活。使用这些特性，开发人员可以轻松实现复杂的分布式调用和管理，提高系统的可扩展性和性能。


mybatis 如何支持最新版本spring boot，支持java最新版 17？如何支持java 8新的日期时间api
nacos 
canal 丢数据


## 如何选择开源库

功能完备
性能
是否持续维护
开源社区是否活跃


org.springframework.context.annotation.ConfigurationClassPostProcessor 的作用是：

- ConfigurationClassPostProcessor 是一个 BeanFactoryPostProcessor 类型的类，它可以在 BeanFactory 的标准初始化之后修改 Bean 的定义或者元数据¹。
- ConfigurationClassPostProcessor 的主要功能是解析和处理带有 @Configuration、@ComponentScan、@Import 等注解的配置类，以及注册和增强带有 @Bean 注解的方法²。
- ConfigurationClassPostProcessor 会在应用上下文刷新的过程中被调用两次，一次是在 postProcessBeanDefinitionRegistry 方法中，用于扫描和注册配置类；另一次是在 postProcessBeanFactory 方法中，用于增强配置类³。
- ConfigurationClassPostProcessor 会使用 ConfigurationClassParser 来解析配置类，使用 ConfigurationClassBeanDefinitionReader 来注册配置类，使用 ConfigurationClassEnhancer 来增强配置类⁴。


(1) ConfigurationClassPostProcessor (Spring Framework 6.0.11 API). https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/annotation/ConfigurationClassPostProcessor.html.
(2) Error on running maven project :Cannot load configuration class org .... https://stackoverflow.com/questions/68711315/error-on-running-maven-project-cannot-load-configuration-class-org-springframew.
(3) ConfigurationClassPostProcessor —— Spring中最！最！最！重要的后置处理器！没有之一！！！. https://blog.csdn.net/qq_34436819/article/details/100944204.
(4) 谈谈Spring的ConfigurationClassPostProcessor - 苦行僧DH - 博客园. https://www.cnblogs.com/daihang2366/p/15049423.html.

spring 源代码套路
接口 Abstract类 子类
delegate类

org.springframework.context.support.AbstractApplicationContext#refresh 这个方法很重要，打断点

ClassPathXmlApplicationContext
维护了一个属性 	private Resource[] configResources;

  AbstractXmlApplicationContext
    loadBeanDefinitions(DefaultListableBeanFactory beanFactory)


AbstractRefreshableConfigApplicationContext
    spring ioc的bean afterPropertiesSet()调用refresh()

    AbstractRefreshableApplicationContext
        	private volatile DefaultListableBeanFactory beanFactory;
      AbstractApplicationContext
        ApplicationContext parent;
        ConfigurableEnvironment environment;
        List<BeanFactoryPostProcessor> beanFactoryPostProcessors
        ResourcePatternResolver resourcePatternResolver;
        LifecycleProcessor lifecycleProcessor;
        MessageSource messageSource;
        ApplicationEventMulticaster applicationEventMulticaster;
        final Set<ApplicationListener<?>> applicationListeners
        Set<ApplicationListener<?>> earlyApplicationListeners;
        Set<ApplicationEvent> earlyApplicationEvents;

        DefaultResourceLoader
          ClassLoader classLoader;
          final Set<ProtocolResolver> protocolResolvers
          final Map<Class<?>, Map<Resource, ?>> resourceCaches





- parent：这是一个 ApplicationContext 类型的属性，用于存储当前应用上下文的父上下文，如果没有父上下文，则为 null。父上下文可以用于提供共享的配置信息或者继承的 Bean 定义¹。
- environment：这是一个 ConfigurableEnvironment 类型的属性，用于存储当前应用上下文的环境信息，包括系统属性、环境变量、配置文件等。环境信息可以用于影响 Bean 的加载和初始化²。
- beanFactoryPostProcessors：这是一个 List<BeanFactoryPostProcessor> 类型的属性，用于存储当前应用上下文中注册的 BeanFactoryPostProcessor 对象。BeanFactoryPostProcessor 是一种特殊的 Bean，可以在 BeanFactory 的标准初始化之后修改 Bean 的定义或者元数据³。
- resourcePatternResolver：这是一个 ResourcePatternResolver 类型的属性，用于实现资源加载的功能，可以根据不同的资源类型（如文件、类路径、URL 等）来获取 Resource 对象，并支持 Ant 风格的路径模式。
- lifecycleProcessor：这是一个 LifecycleProcessor 类型的属性，用于管理当前应用上下文中实现了 Lifecycle 接口的 Bean 的生命周期，包括启动、停止、刷新等操作。LifecycleProcessor 可以通过配置 "lifecycleProcessor" 这个特殊的 Bean 名称来自定义。
- messageSource：这是一个 MessageSource 类型的属性，用于实现国际化和本地化的功能，可以根据不同的语言和地区来获取相应的消息内容。MessageSource 可以通过配置 "messageSource" 这个特殊的 Bean 名称来自定义。
- applicationEventMulticaster：这是一个 ApplicationEventMulticaster 类型的属性，用于管理当前应用上下文中发布和监听 ApplicationEvent 的功能，ApplicationEvent 是一种特殊的事件对象，可以在 Bean 之间传递信息或者通知。ApplicationEventMulticaster 可以通过配置 "applicationEventMulticaster" 这个特殊的 Bean 名称来自定义。
- applicationListeners：这是一个 Set<ApplicationListener<?>> 类型的属性，用于存储当前应用上下文中注册的 ApplicationListener 对象。ApplicationListener 是一种特殊的 Bean，可以监听 ApplicationEvent 并做出相应的处理。
- earlyApplicationListeners：这是一个 Set<ApplicationListener<?>> 类型的属性，用于存储在应用上下文刷新之前注册的 ApplicationListener 对象。这些监听器会在刷新过程中被移动到 applicationListeners 属性中，并处理早期发布的事件。
- earlyApplicationEvents：这是一个 Set<ApplicationEvent> 类型的属性，用于存储在应用上下文刷新之前发布的 ApplicationEvent 对象。这些事件会在刷新过程中被移动到 applicationEventMulticaster 属性中，并分发给相应的监听器。


(1) AbstractApplicationContext (Spring Framework 6.0.11 API). https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/support/AbstractApplicationContext.html.
(2) AbstractRefreshableApplicationContext (Spring Framework 6.0.11 API). https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/support/AbstractRefreshableApplicationContext.html.
(3) AnnotationConfigServletWebApplicationContext (Spring Boot 3.0.9 API). https://docs.spring.io/spring-boot/docs/3.0.x/api/org/springframework/boot/web/servlet/context/AnnotationConfigServletWebApplicationContext.html.



这些属性都是 `AbstractApplicationContext` 类中的字段，用于管理 Spring 应用上下文的各个方面。以下是每个属性的作用说明：
1. `ApplicationContext parent`: 用于表示当前应用上下文的父上下文。通过这个属性，您可以实现应用上下文之间的层级关系，以便在子上下文中能够访问父上下文中定义的 Bean。
2. `ConfigurableEnvironment environment`: 用于持有应用上下文的环境配置。环境配置包括诸如激活的配置文件、属性占位符解析器等信息。
3. `List<BeanFactoryPostProcessor> beanFactoryPostProcessors`: 用于保存要在 BeanFactory 创建过程中执行的后处理器。这些后处理器可以用来修改 BeanFactory 的配置，例如修改 Bean 的定义或属性。
4. `ResourcePatternResolver resourcePatternResolver`: 用于解析资源路径，例如在类路径下查找资源文件或者扫描类等操作。
5. `LifecycleProcessor lifecycleProcessor`: 用于控制 Bean 生命周期事件的处理器。例如，在应用上下文关闭时，该处理器会触发销毁 Bean 的方法。
6. `MessageSource messageSource`: 用于国际化消息的管理。通过该属性，可以实现在应用中支持多语言消息。
7. `ApplicationEventMulticaster applicationEventMulticaster`: 用于管理应用事件的多播（广播）。它负责分发应用中产生的事件给对应的事件监听器。
8. `Set<ApplicationListener<?>> applicationListeners`: 用于保存注册到当前应用上下文的所有事件监听器。这些监听器可以处理应用中触发的各种事件。
9. `Set<ApplicationListener<?>> earlyApplicationListeners`: 保存在应用上下文初始化过程中早期注册的事件监听器。这些监听器会在上下文的刷新（refresh）之前得到机会触发一些事件。
10. `Set<ApplicationEvent> earlyApplicationEvents`: 保存在应用上下文初始化过程中早期产生的事件。这些事件会在上下文的刷新之后被发布。

`AbstractApplicationContext` 是 Spring 框架中应用上下文的抽象基类，它提供了管理和控制应用上下文的基本结构和功能。上述属性中的每一个都有助于实现不同的应用上下文的特性和行为，从而为 Spring 应用程序提供灵活性和可扩展性。




org.springframework.boot.SpringApplication#createApplicationContext()

	public static final String DEFAULT_SERVLET_WEB_CONTEXT_CLASS = "org.springframework.boot."
			+ "web.servlet.context.AnnotationConfigServletWebServerApplicationContext";
	public static final String DEFAULT_REACTIVE_WEB_CONTEXT_CLASS = "org.springframework.boot.web.reactive.context.AnnotationConfigReactiveWebServerApplicationContext";
	public static final String DEFAULT_CONTEXT_CLASS = "org.springframework.context.annotation.AnnotationConfigApplicationContext";

org.springframework.context.support.AbstractApplicationContext#refresh
11个方法被调用
			prepareRefresh();
			prepareBeanFactory(beanFactory);
				postProcessBeanFactory(beanFactory);
				invokeBeanFactoryPostProcessors(beanFactory);
				registerBeanPostProcessors(beanFactory);
				initMessageSource();
				initApplicationEventMulticaster();
				onRefresh();
				registerListeners();
				finishBeanFactoryInitialization(beanFactory);
				finishRefresh();



`org.springframework.context.support.AbstractApplicationContext#refresh` 方法是 Spring 应用上下文的刷新过程的核心。它会在应用上下文初始化或重新加载时被调用，用于执行各种初始化和后处理步骤。下面是这些方法的作用说明：
1. `prepareRefresh()`: 在刷新之前执行的准备工作。这可能涉及设置一些刷新标志、初始化一些状态等。
2. `prepareBeanFactory(beanFactory)`: 准备 BeanFactory 的设置。在这一步中，会调用一些后处理器，以及注册一些必要的 BeanFactory 后处理器。

   - `postProcessBeanFactory(beanFactory)`: 对 BeanFactory 进行一些后处理。这是一个钩子方法，可以在子类中实现以自定义 BeanFactory 的处理。
   - `invokeBeanFactoryPostProcessors(beanFactory)`: 调用 BeanFactory 后处理器。这些处理器可以修改或增强 BeanFactory 的配置。
   - `registerBeanPostProcessors(beanFactory)`: 注册 Bean 后处理器，这些处理器在 Bean 创建时会被应用，例如 `@Autowired` 和 `@Value` 的处理。
   - `initMessageSource()`: 初始化国际化消息源。如果配置了消息资源，它会被加载到上下文中。
   - `initApplicationEventMulticaster()`: 初始化应用事件的多播器。用于分发应用中的事件给对应的监听器。
3. `onRefresh()`: 在刷新时进行的自定义处理。这是一个钩子方法，允许您在应用上下文刷新期间执行额外的操作。
4. `registerListeners()`: 注册事件监听器。将之前注册的事件监听器添加到事件多播器中，以便能够接收到应用中的事件。
5. `finishBeanFactoryInitialization(beanFactory)`: 完成 BeanFactory 的初始化。在这一步中，所有的单例 Bean 会被实例化，依赖注入会被执行。
6. `finishRefresh()`: 刷新完成后的清理工作。这可能包括通知生命周期处理器刷新完成、发布最终的刷新事件等。
总之，`refresh` 方法是 Spring 应用上下文的核心刷新流程，负责初始化和准备应用上下文，以及完成各种后处理和清理工作。这个过程确保了应用上下文的正确初始化，从而让应用能够正常运行。


看onRefresh()方法

DefaultResourceLoader (org.springframework.core.io)
    AbstractApplicationContext (org.springframework.context.support)
        GenericApplicationContext (org.springframework.context.support)
            AnnotationConfigApplicationContext (org.springframework.context.annotation)
                AnnotationConfigReactiveWebApplicationContext (org.springframework.boot.web.reactive.context)



org.springframework.context.support.GenericApplicationContext#beanFactory 重要属性 DefaultListableBeanFactory

DefaultListableBeanFactory可以直接调用构造函数

DefaultListableBeanFactory 作为bean工厂的继承关系
org.springframework.beans.factory.BeanFactory
    org.springframework.beans.factory.HierarchicalBeanFactory
        org.springframework.beans.factory.config.ConfigurableBeanFactory 接口
            org.springframework.beans.factory.support.AbstractBeanFactory
                org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory



SimpleAliasRegistry (org.springframework.core)
    DefaultSingletonBeanRegistry (org.springframework.beans.factory.support)
        FactoryBeanRegistrySupport (org.springframework.beans.factory.support)
            AbstractBeanFactory (org.springframework.beans.factory.support)
                AbstractAutowireCapableBeanFactory (org.springframework.beans.factory.support)
                    DefaultListableBeanFactory (org.springframework.beans.factory.support)
                        XmlBeanFactory (org.springframework.beans.factory.xml)


springboot项目启动流程

refresh()方法之后就跟不下去了
换项目springboothttpserver
看打印的日志
org.springframework.context.annotation.ConfigurationClassPostProcessor
ConfigurationClassPostProcessor

在Spring框架中,ConfigurationClassPostProcessor是一个很重要的后置处理器,它的主要作用是:
1. 处理被@Configuration注解的配置类,并构建出IODC(Inversion of Control)容器内部的bean定义。
2. 寻找@ComponentScan等注解,并将扫描到的组件注册为Spring Bean。
3. 处理@Import注解,导入额外的配置类。
4. 处理@Bean注解方法,注册为Bean定义。
5. 负责处理配置类之间的依赖关系,确保顺序初始化。
简单来说,它是启用注解驱动注册Bean定义的关键类,不光处理配置类,还会解析组件扫描等注解。
在Spring应用启动阶段,这个后置处理器会非常早被调用到,用来构建Spring的IOC容器,是非常重要的一个步骤。
没了它,只靠@Configuration注解的配置类是不会生效的,它起到了承上启下的作用。
所以 ConfigurationClassPostProcessor是一个关键的后置处理器,它使注解驱动的配置类可以生效,是Spring注解配置模式的基石。

```shell
2023-08-10 11:06:49.495 [main] TRACE o.s.c.annotation.ConfigurationClassPostProcessor 426 - Replacing bean definition 'springboothttpserverApplication' existing class 'com.wdidada.test.springboothttpserver.SpringboothttpserverApplication' with enhanced class 'com.wdidada.test.springboothttpserver.SpringboothttpserverApplication$$EnhancerBySpringCGLIB$$9ca2dc8a'
2023-08-10 11:06:49.499 [main] TRACE o.s.c.annotation.ConfigurationClassPostProcessor 426 - Replacing bean definition 'appConfig' existing class 'com.wdidada.test.springboothttpserver.config.AppConfig' with enhanced class 'com.wdidada.test.springboothttpserver.config.AppConfig$$EnhancerBySpringCGLIB$$dc8b055'
```

ConfigurationClassBeanDefinitionReader
org.springframework.context.annotation.ConfigurationClassBeanDefinitionReader

```shell
2023-08-10 11:06:48.714 [main] TRACE o.s.c.a.ConfigurationClassBeanDefinitionReader 169 - Registered bean definition for imported class 'org.springframework.boot.autoconfigure.context.PropertyPlaceholderAutoConfiguration'
2023-08-10 11:06:48.725 [main] TRACE o.s.c.a.ConfigurationClassBeanDefinitionReader 283 - Registering bean definition for @Bean method org.springframework.boot.autoconfigure.context.PropertyPlaceholderAutoConfiguration.propertySourcesPlaceholderConfigurer()
2023-08-10 11:06:48.726 [main] TRACE o.s.c.a.ConfigurationClassBeanDefinitionReader 169 - Registered bean definition for imported class 'org.springframework.boot.autoconfigure.websocket.servlet.WebSocketServletAutoConfiguration$TomcatWebSocketConfiguration'
2023-08-10 11:06:48.727 [main] TRACE o.s.c.a.ConfigurationClassBeanDefinitionReader 283 - Registering bean definition for @Bean method org.springframework.boot.autoconfigure.websocket.servlet.WebSocketServletAutoConfiguration$TomcatWebSocketConfiguration.websocketServletWebServerCustomizer()
2023-08-10 11:06:48.728 [main] TRACE o.s.c.a.ConfigurationClassBeanDefinitionReader 169 - Registered bean definition for imported class 'org.springframework.boot.autoconfigure.websocket.servlet.WebSocketServletAutoConfiguration'
```



236条关于ConfigurationClassBeanDefinitionReader的日志，换项目testspring32



https://gitee.com/edidada/testspring32
使用旧的 XmlBeanFactory 不会触发自动注入，而使用 ApplicationContext 则会触发 Spring 的各种自动装配机制。ApplicationContext触发自动注入的代码在哪儿？

在 Spring 框架中，`ApplicationContext` 接口的实现类会在容器初始化过程中触发自动注入（自动装配）的机制。自动注入的触发发生在以下阶段：
1. **BeanDefinition 的注册和解析阶段：** 在容器初始化过程中，`ApplicationContext` 实现类会解析配置文件中的 Bean 定义信息（例如 XML 配置文件中的 `<bean>` 元素），将它们转换为 `BeanDefinition` 对象，并注册到内部的 `BeanFactory` 中。
2. **Bean 的实例化阶段：** 一旦所有的 `BeanDefinition` 都被注册后，容器会开始实例化这些 Bean。对于需要自动注入的属性，容器会在此阶段通过适当的自动装配策略，自动将相关的依赖注入到 Bean 中。
触发自动注入的代码位于 `org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory` 类的 `populateBean` 方法内部。该方法在创建 Bean 实例后，负责对 Bean 进行属性的填充，包括自动注入。
以下是简化的代码示例，展示了在 `AbstractAutowireCapableBeanFactory` 内部的 `populateBean` 方法的部分代码，用于触发自动注入：

```java
protected void populateBean(String beanName, RootBeanDefinition mbd, @Nullable BeanWrapper bw) {
    // 获取 BeanDefinition 中的属性信息
    PropertyValues pvs = mbd.getPropertyValues();

    // 进行自动注入
    if (bw != null && !pvs.isEmpty()) {
        // 自动注入的具体实现在此处进行
        // 使用 AutowireCapableBeanFactory 的自动注入策略完成属性的填充
        // ...
    }
}
```

在 `populateBean` 方法中，Spring 会根据属性的配置信息和自动注入规则，判断是否需要执行自动注入。如果属性标记有 `@Autowired` 注解或配置了自动注入规则，Spring 将根据 Bean 的定义和容器中的其他 Bean，尝试将合适的依赖注入到当前 Bean 的属性中。
综上所述，`ApplicationContext` 实现类在容器初始化阶段会调用 `populateBean` 方法，触发自动注入的机制。这样可以实现在容器创建 Bean 时，根据配置自动完成属性的注入，提高了开发的便利性和效率。


org.springframework.beans.factory.support.AbstractBeanFactory#beanPostProcessors

beanPostProcessor.png


CommonAnnotationBeanPostProcessor
AutowiredAnnotationBeanPostProcessor


其中，AutowiredAnnotationBeanPostProcessor
Set<Class<? extends Annotation>> autowiredAnnotationTypes 有Autowired Value


org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#applyPropertyValues

`org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory#applyPropertyValues` 方法是 Spring IoC 容器中的一个核心方法，用于在创建和初始化 Bean 时，将属性值应用到 Bean 实例中。这个方法在完成属性的自动注入、属性的依赖解析以及属性值的设置等过程中起着关键作用。

下面是对 `applyPropertyValues` 方法的处理流程的简要说明：

1. 获取 BeanDefinition 中的属性信息，这些属性信息包括需要自动注入的属性、属性值等。

2. 遍历属性列表，检查每个属性是否需要自动注入。这可能涉及 `@Autowired` 注解、`@Value` 注解、`@Resource` 注解等。

3. 如果属性需要自动注入，首先尝试从容器中获取对应的依赖 Bean。如果依赖 Bean 已经在容器中存在，则将它设置到当前 Bean 实例的属性中。

4. 如果找不到依赖 Bean，可能会根据属性的类型和注解信息，创建一个适当的代理对象或者特定实例，然后将这个对象设置到当前 Bean 实例的属性中。例如，如果属性类型是接口，并且有多个实现类，Spring 可能会创建一个代理对象，根据代理的逻辑决定具体注入哪个实现类。

5. 重复以上步骤，直到所有需要自动注入的属性都完成注入。

需要注意的是，`applyPropertyValues` 方法是 Spring IoC 容器在创建和初始化 Bean 时的一部分流程。这个方法与整个 Bean 生命周期密切相关，在 `populateBean`、`initializeBean` 等阶段被调用，确保 Bean 的属性被正确注入，依赖被解析，并且属性值得到正确设置。

总之，`applyPropertyValues` 方法是 Spring IoC 容器实现属性自动注入的核心部分，它通过解析 BeanDefinition 中的属性信息，根据注解和依赖关系完成属性注入，从而构建和初始化完整的 Bean 实例。



getSingleton:183, DefaultSingletonBeanRegistry (org.springframework.beans.factory.support)
isTypeMatch:523, AbstractBeanFactory (org.springframework.beans.factory.support)
doGetBeanNamesForType:530, DefaultListableBeanFactory (org.springframework.beans.factory.support)
getBeanNamesForType:495, DefaultListableBeanFactory (org.springframework.beans.factory.support)
invokeBeanFactoryPostProcessors:87, PostProcessorRegistrationDelegate (org.springframework.context.support)
invokeBeanFactoryPostProcessors:707, AbstractApplicationContext (org.springframework.context.support)
refresh:533, AbstractApplicationContext (org.springframework.context.support)
<init>:144, ClassPathXmlApplicationContext (org.springframework.context.support)
<init>:85, ClassPathXmlApplicationContext (org.springframework.context.support)



getSingleton:183, DefaultSingletonBeanRegistry (org.springframework.beans.factory.support)
doCreateBean:607, AbstractAutowireCapableBeanFactory (org.springframework.beans.factory.support)
createBean:516, AbstractAutowireCapableBeanFactory (org.springframework.beans.factory.support)
lambda$doGetBean$0:324, AbstractBeanFactory (org.springframework.beans.factory.support)
getObject:-1, 701487403 (org.springframework.beans.factory.support.AbstractBeanFactory$$Lambda$55)
getSingleton:234, DefaultSingletonBeanRegistry (org.springframework.beans.factory.support)
doGetBean:322, AbstractBeanFactory (org.springframework.beans.factory.support)
getBean:207, AbstractBeanFactory (org.springframework.beans.factory.support)
invokeBeanFactoryPostProcessors:180, PostProcessorRegistrationDelegate (org.springframework.context.support)
invokeBeanFactoryPostProcessors:707, AbstractApplicationContext (org.springframework.context.support)
refresh:533, AbstractApplicationContext (org.springframework.context.support)
<init>:144, ClassPathXmlApplicationContext (org.springframework.context.support)
<init>:85, ClassPathXmlApplicationContext (org.springframework.context.support)



```java
@Service
public class Order {

    @Autowired
    private NotifyService notifyservice;

    public void PaySuccess(){
        notifyservice.sendMessage("客户张铁蛋完成订单2017079657付款，共人民币:97.5元");
    }
}
```

代码对应的日志

```shell
14:21:47.689 DEBUG org.springframework.beans.factory.support.DefaultSingletonBeanRegistry 225 getSingleton - Creating shared instance of singleton bean 'order'
14:21:47.689 TRACE org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory 481 createBean - Creating instance of bean 'order'
14:21:47.693 TRACE org.springframework.beans.factory.annotation.InjectionMetadata 114 checkConfigMembers - Registered injected element on class [cn.edidada.test.testspring32.scan.service.Order]: AutowiredFieldElement for private cn.edidada.test.testspring32.scan.service.NotifyService cn.edidada.test.testspring32.scan.service.Order.notifyservice
14:21:47.693 TRACE org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory 584 doCreateBean - Eagerly caching bean 'order' to allow for resolving potential circular references
14:21:47.693 TRACE org.springframework.beans.factory.annotation.InjectionMetadata 128 inject - Processing injected element of bean 'order': AutowiredFieldElement for private cn.edidada.test.testspring32.scan.service.NotifyService cn.edidada.test.testspring32.scan.service.Order.notifyservice
14:21:47.698 DEBUG org.springframework.beans.factory.support.DefaultSingletonBeanRegistry 225 getSingleton - Creating shared instance of singleton bean 'notifyServiceByCellPhoneImpl'
14:21:47.698 TRACE org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory 481 createBean - Creating instance of bean 'notifyServiceByCellPhoneImpl'
14:21:47.699 TRACE org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory 584 doCreateBean - Eagerly caching bean 'notifyServiceByCellPhoneImpl' to allow for resolving potential circular references
14:21:47.699 TRACE org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory 518 createBean - Finished creating instance of bean 'notifyServiceByCellPhoneImpl'
14:21:47.700 TRACE org.springframework.beans.factory.annotation.AutowiredAnnotationBeanPostProcessor 586 registerDependentBeans - Autowiring by type from bean name 'order' to bean named 'notifyServiceByCellPhoneImpl'
14:21:47.700 TRACE org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory 518 createBean - Finished creating instance of bean 'order'
```

这个项目 testspring32 AbstractApplicationContext refresh()
refresh:518, AbstractApplicationContext (org.springframework.context.support)
<init>:144, ClassPathXmlApplicationContext (org.springframework.context.support)
<init>:85, ClassPathXmlApplicationContext (org.springframework.context.support)
main:20, ScanMain (cn.edidada.test.testspring32.scan)


两个项目的beanDefinitionReader

ConfigurationClassBeanDefinitionReader

XmlBeanDefinitionReader



org.springframework.beans.factory.xml.XmlBeanDefinitionReader#loadBeanDefinitions(org.springframework.core.io.support.EncodedResource)
有日志


org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions
调用ConfigurationClassBeanDefinitionReader 的
			loadBeanDefinitions(configClasses);方法



loadBeanDefinitions:322, XmlBeanDefinitionReader (org.springframework.beans.factory.xml)
loadBeanDefinitions:310, XmlBeanDefinitionReader (org.springframework.beans.factory.xml)
loadBeanDefinitions:188, AbstractBeanDefinitionReader (org.springframework.beans.factory.support)
loadBeanDefinitions:224, AbstractBeanDefinitionReader (org.springframework.beans.factory.support)
loadBeanDefinitions:195, AbstractBeanDefinitionReader (org.springframework.beans.factory.support)
loadBeanDefinitions:257, AbstractBeanDefinitionReader (org.springframework.beans.factory.support)
loadBeanDefinitions:128, AbstractXmlApplicationContext (org.springframework.context.support)
loadBeanDefinitions:94, AbstractXmlApplicationContext (org.springframework.context.support)
refreshBeanFactory:130, AbstractRefreshableApplicationContext (org.springframework.context.support)
obtainFreshBeanFactory:638, AbstractApplicationContext (org.springframework.context.support)
refresh:523, AbstractApplicationContext (org.springframework.context.support)
<init>:144, ClassPathXmlApplicationContext (org.springframework.context.support)
<init>:85, ClassPathXmlApplicationContext (org.springframework.context.support)









processConfigBeanDefinitions:331, ConfigurationClassPostProcessor (org.springframework.context.annotation)
postProcessBeanDefinitionRegistry:236, ConfigurationClassPostProcessor (org.springframework.context.annotation)
invokeBeanDefinitionRegistryPostProcessors:280, PostProcessorRegistrationDelegate (org.springframework.context.support)
invokeBeanFactoryPostProcessors:96, PostProcessorRegistrationDelegate (org.springframework.context.support)
invokeBeanFactoryPostProcessors:707, AbstractApplicationContext (org.springframework.context.support)
refresh:533, AbstractApplicationContext (org.springframework.context.support)
refresh:143, ServletWebServerApplicationContext (org.springframework.boot.web.servlet.context)
refresh:758, SpringApplication (org.springframework.boot)
refresh:750, SpringApplication (org.springframework.boot)
refreshContext:397, SpringApplication (org.springframework.boot)
run:315, SpringApplication (org.springframework.boot)
run:1237, SpringApplication (org.springframework.boot)
run:1226, SpringApplication (org.springframework.boot)


这个项目也可以测试spring
https://gitee.com/edidada/springexample