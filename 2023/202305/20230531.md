# 20230531

复习 通用源码阅读指导书：MyBatis源码详解

org.springframework.jdbc.support.lob.LobHandler 作用

org.springframework.jdbc.support.lob.LobHandler是Spring JDBC中的一个实用类，它的作用是支持处理大对象（LOB）类型的数据。
在使用JDBC访问数据库时，有时需要处理大对象类型的数据，例如CLOB（字符型大对象）和BLOB（二进制型大对象）。LobHandler类可以方便地处理这些大对象类型的数据，例如读取、写入、删除等操作。
LobHandler类的主要作用有以下几点：
1. 支持不同数据库：LobHandler可以支持不同的数据库类型，例如Oracle、MySQL、SQL Server等，可以方便地处理不同类型的大对象数据。
2. 支持不同类型的大对象：LobHandler可以支持不同类型的大对象数据，例如CLOB、BLOB等。
3. 简化代码：LobHandler可以简化JDBC代码的编写，避免手动编写处理大对象类型数据的代码，从而提高了代码的可读性和可维护性。
4. 避免内存溢出：LobHandler支持流式处理大对象类型数据，避免了将整个大对象数据读入内存中的情况 。
使用LobHandler时，需要先创建一个LobHandler对象，并将其作为参数传递给JdbcTemplate或NamedParameterJdbcTemplate对象的构造方法或setter方法中。例如：

```
LobHandler lobHandler = new DefaultLobHandler();
jdbcTemplate.setLobHandler(lobHandler);
```

在上面的示例中，我们首先创建了一个DefaultLobHandler对象，并将其赋值给LobHandler类型的变量lobHandler。然后，我们使用JdbcTemplate对象的setLobHandler()方法将lobHandler对象设置为JdbcTemplate对象的lobHandler属性。
通过使用LobHandler，我们可以方便地处理大对象类型数据，避免了手动编写处理大对象类型数据的代码，从而提高了代码的可读性和可维护性。

JdbcTemplate这个类一堆日志，注意看日志

华为的 Distributed Database Middleware（DDM）分片策略有哪些
华为的 Distributed Database Middleware（DDM）支持多种分片策略，以实现数据的分布式存储和查询，提高系统的可靠性和性能。以下是常用的分片策略：
1. mod_hash 分片策略：根据数据的 hash 值将数据分配到不同的节点上。该策略适用于数据量较大的情况，可以实现数据的均匀分布和节点的负载均衡。
2. range 分片策略：根据数据的范围将数据分配到不同的节点上。该策略适用于数据按照一定范围进行查询的情况，可以提高查询性能和响应时间。
3. list 分片策略：根据数据的列表将数据分配到不同的节点上。该策略适用于数据按照列表进行查询的情况，可以提高查询性能和响应时间。
4. composite 分片策略：根据多个分片键将数据分配到不同的节点上。该策略适用于多维数据查询的情况，可以提高查询性能和响应时间。
除了以上常用的分片策略外，DDM 还支持自定义分片策略，可以根据实际的业务需求和系统性能情况进行灵活配置。
在选择分片策略时，需要综合考虑数据量、数据分布、查询模式、数据安全性和性能等因素。同时，使用 DDM 技术可以增强数据的安全性，但也需要考虑 DDM 对分片策略的影响。在确定分片策略时，建议您进行多次试验和调整，以找到最佳的方案。

```sql
CREATE TABLE `fi_bl_shop_wallet_dtl` (
  `cort_num_id` varchar(50) DEFAULT NULL COMMENT '所属公司',
  `sub_unit_num_id` varchar(50) DEFAULT NULL COMMENT '门店编码',
  `account_no` varchar(20) NOT NULL COMMENT '账户号码（公司+门店+10位流水号）',
  `amount` decimal(18,2) DEFAULT '0.00' COMMENT '调整金额',
  `before_amount` decimal(18,2) decimal(18,2) DEFAULT '0.00' COMMENT '调整前余额',
  `after_amount` decimal(18,2)  decimal(18,2) DEFAULT '0.00' COMMENT '调整后余额',
  `biz_order_no` varchar(50) DEFAULT NULL COMMENT '业务单据号',
  `biz_type_num_id` varchar(50) ali  DEFAULT NULL COMMENT '业务类型(补货预付款1001、发货结算2001、退库结算1002、解冻3001、冻结3002)',
  PRIMARY KEY (`series`) USING BTREE,
  UNIQUE INDEX `uk_account_no` (`cort_num_id`,`sub_unit_num_id`,`account_no`) USING BTREE,
  KEY `idx_account_common` (`tenant_num_id`,`data_sign`,`cort_num_id`,`sub_unit_num_id`,`account_no`) USING BTREE,
  KEY `idx_biz` (`biz_order_no`,`biz_type_num_id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci ROW_FORMAT=DYNAMIC COMMENT='门店钱包账户总览表' 
dbpartition by MOD_HASH(`CORT_NUM_ID`) 
tbpartition by MOD_HASH(`SUB_UNIT_NUM_ID`) tbpartitions 16;
```

java vlidate单独校验api，不要自己写代码去校验http接口入参

```java
        ValidatorFactory factory = Validation.buildDefaultValidatorFactory();
        Validator validator = factory.getValidator();
        List<FI_JM_RECEIPT_ORDER> receiptOrderList = new LinkedList<>();
        for(BatchSaveReceiptDTO order:receiptOrderImportDTOList ){
            Set constraintViolations = validator.validate(order,new Class[0]);
            if(constraintViolations != null && constraintViolations.size()>0) {
                ConstraintViolation error = (ConstraintViolation) constraintViolations.iterator()
                        .next();
                response.setCode(MessagePack.EXCEPTION);
                if(error != null && Objects.nonNull(error.getMessage())) {
                    response.setMessage(error.getMessage());
                }
                break;
            }
```

多总结公司工作，多看公司规范，多想自己给别人的感觉怎么样？

白板编程，java程序编译需要之间，争取一次编译全部过，c++程序编译很耗时间

int mysql 最多9位

在MySQL中，int是一种整数类型，可以使用int关键字来定义。int类型可以存储的整数的范围是-2147483648到2147483647，占用4个字节（32位）的存储空间。因此，int类型的数据可以存储的最大值为2147483647，最小值为-2147483648。
需要注意的是，如果需要存储更大的数值，可以考虑使用bigint类型，它可以存储更大范围的整数，占用8个字节（64位）的存储空间。bigint类型可以存储的最大值为9223372036854775807，最小值为-9223372036854775808。
在选择数据类型时，需要根据实际需求和数据范围进行选择，并且需要考虑存储空间和性能等因素。如果需要存储较小范围的整数，可以选择使用较小的整数类型，例如tinyint、smallint等；如果需要存储较大范围的整数，可以选择使用较大的整数类型，例如bigint。

tabnine
idea插件

在Java 8中，引入了`@FunctionalInterface`注解，用于标注函数式接口。函数式接口是指只包含一个抽象方法的接口，它可以被Lambda表达式或方法引用所使用。如果一个接口被`@FunctionalInterface`注解标注，但该接口不符合函数式接口的规范，编译器会报错。
具体而言，如果一个接口被`@FunctionalInterface`注解标注，但该接口包含多于一个抽象方法，或者包含一个抽象方法和一个重写了Object类中的方法（如toString()、equals()、hashCode()等）的方法，编译器会报错。

例如，下面的代码演示了一个不符合函数式接口规范的接口：

```
@FunctionalInterface
public interface MyInterface {
   void doSomething();
   void doAnotherThing();
}
```

在上面的代码中，`MyInterface`接口被`@FunctionalInterface`注解标注，但它包含两个抽象方法，因此不符合函数式接口的规范。如果编译这段代码，编译器会提示错误信息。
如果需要报告`@FunctionalInterface`注解的错误，可以定义一个自定义的编译器插件或者静态代码分析工具来实现。这些工具可以扫描源代码，检查被`@FunctionalInterface`注解标注的接口是否符合函数式接口的规范，如果不符合，就输出错误信息。
例如，可以使用Java编译器的插件机制，定义一个自定义的`@FunctionalInterface`检查插件。该插件可以在编译Java源代码时，扫描所有使用了`@FunctionalInterface`注解的接口，并检查接口是否符合函数式接口的规范。如果接口不符合规范，就输出错误信息。
另外，也可以使用静态代码分析工具，例如Checkstyle、FindBugs等，来检查代码中是否存在不符合函数式接口规范的接口。这些静态代码分析工具可以在编译之前或者编译之后对代码进行分析，并输出错误信息或者警告信息。
总之，在开发过程中，应该遵循函数式接口的规范，确保被`@FunctionalInterface`注解标注的接口只包含一个抽象方法，并且没有重写Object类中的方法。如果需要检查`@FunctionalInterface`注解的错误，可以使用自定义编译器插件或者静态代码分析工具来实现。