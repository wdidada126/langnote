# 202305_month

## 本月概览

- 本月共整理 31 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202305_weekly3：- 20230515：Sonatype Nexus Repository ManagerOSS 3.25.0-0；Nexus Repository 3.41.1 is available
- 202305_weekly4：- 20230522：erp-fi依赖erp-api；为了快速开发，直接先在erp-fi项目里写代码，不用先在api里面定义接口，然后刷新maven仓库，提高工作效率；public String saveAttach(@RequestParam("file") MultipartFile file)
- 202305_weekly5：- 20230529：抽象类 TransactionSynchronizationAdapter；子类；子类都是内部类，不是单独的java文件，跟
- 202305_weekly_1：- 20230501：org.springframework.web.servlet.mvc.AbstractController；需要注意的是，AbstractController已经在Spring 3.2版本中被标记为过时，建议使用更加灵活的注解驱动控制器（例如@Controller注解）来处理HTTP请求。；访问my这个url，就可以进入，MyController这个类，注意，这个my前面必须有斜杠
- 202305_weekly_2：- 20230508：BeanDefinitionRegistryPostProcessor 接口 spring-beans里面的  void postProcessBeanDefinitionRegistry(BeanDefinitionRegistry registry)；BeanFactoryPostProcessor 接口 spring-beans里面的 void postProcessBeanFactory(ConfigurableListableBeanFactory beanFactory)；BeanPostProcessor 接口 spring-beans里面的

## 本月高频主题

- org.springframework.web.servlet.mvc.AbstractController
- 需要注意的是，AbstractController已经在Spring 3.2版本中被标记为过时，建议使用更加灵活的注解驱动控制器（例如@Controller注解）来处理HTTP请求。
- 访问my这个url，就可以进入，MyController这个类，注意，这个my前面必须有斜杠
- 建议下个油猴把CSDN屏蔽掉，眼不见心不烦。
- isbn
- 9787121440014
- Spring Boot实战
- hibernate
- 请注意，Thread.sleep()方法不同于 wait() 方法，wait() 方法会释放持有的锁并进入 WAITING 状态，只有其他线程调用了该对象的 notify() 或 notifyAll() 方法时才会被唤醒。
- git config pull.rebase false 含义merge
- git config pull.rebase true rebase
- 在 Java 项目中，有许多框架可以用于创建和管理挡板程序。以下是一些常用的 Java 挡板程序框架：
