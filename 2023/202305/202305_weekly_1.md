# 202305_weekly_1

## 时间范围

- 202305 月第 1 周

## 本周记录

- 20230501：org.springframework.web.servlet.mvc.AbstractController；需要注意的是，AbstractController已经在Spring 3.2版本中被标记为过时，建议使用更加灵活的注解驱动控制器（例如@Controller注解）来处理HTTP请求。；访问my这个url，就可以进入，MyController这个类，注意，这个my前面必须有斜杠
- 20230502：建议下个油猴把CSDN屏蔽掉，眼不见心不烦。；isbn；9787121440014
- 20230503：Spring Boot实战
- 20230504：hibernate
- 20230505：请注意，Thread.sleep()方法不同于 wait() 方法，wait() 方法会释放持有的锁并进入 WAITING 状态，只有其他线程调用了该对象的 notify() 或 notifyAll() 方法时才会被唤醒。
- 20230506：git config pull.rebase false 含义merge；git config pull.rebase true rebase
- 20230507：在 Java 项目中，有许多框架可以用于创建和管理挡板程序。以下是一些常用的 Java 挡板程序框架：；Mockito：Mockito 是一个流行的 Java 挡板程序框架，它可以帮助您轻松创建和管理挡板程序，并提供了许多有用的功能和工具。Mockito 支持模拟和验证方法调用、创建和管理模拟对象、设置模拟对象的行为和状态等。；PowerMock：PowerMock 是另一个流行的 Java 挡板程序框架，它可以帮助您模拟和测试静态方法、私有方法、构造函数、终态字段等。PowerMock 支持使用 Mockito 或 EasyMock 进行模拟对象的创建和管理。
