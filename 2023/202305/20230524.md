# 20230524

ConfigurationProperties类 spring-boot项目


String serverAddr = environment
					.resolvePlaceholders("${spring.cloud.nacos.config.server-addr:}");



spring cloud dubbo自动注册的服务
com.alibaba.cloud.dubbo.service.IntrospectiveDubboMetadataService
`com.alibaba.cloud.dubbo.service.IntrospectiveDubboMetadataService`是一个Dubbo元数据服务接口，它提供了Dubbo服务的元数据信息（例如Dubbo服务接口和实现类的类名、方法名、参数类型等）。
具体来说，`IntrospectiveDubboMetadataService`接口包含以下方法：

1. `String getServiceName(String serviceInterface)`：获取指定Dubbo服务接口的服务名。
2. `String getServiceVersion(String serviceInterface)`：获取指定Dubbo服务接口的服务版本。
3. `String getServiceGroup(String serviceInterface)`：获取指定Dubbo服务接口的服务分组。
4. `String getDubboVersion()`：获取Dubbo框架的版本号。
5. `List<String> getMethodNames(String serviceInterface)`：获取指定Dubbo服务接口的所有方法名。
6. `List<String> getParameterTypes(String serviceInterface, String methodName)`：获取指定Dubbo服务接口的指定方法的参数类型。
7. `String getReturnType(String serviceInterface, String methodName)`：获取指定Dubbo服务接口的指定方法的返回值类型。

在Spring Cloud中，`IntrospectiveDubboMetadataService`接口通常用于动态生成Dubbo服务的代理类。具体来说，Dubbo服务的消费方可以通过调用`IntrospectiveDubboMetadataService`接口的方法获取Dubbo服务的元数据信息，然后根据这些元数据信息动态生成Dubbo服务的代理类。这种方式相比静态生成Dubbo服务代理类，可以更加灵活地适应Dubbo服务的变化。
例如，在Spring Cloud Alibaba中，可以使用`ReferenceAnnotationBeanPostProcessor`后置处理器来自动创建Dubbo服务的代理类，其中就使用了`IntrospectiveDubboMetadataService`接口来获取Dubbo服务的元数据信息。具体来说，`ReferenceAnnotationBeanPostProcessor`会根据`@Reference`注解自动创建Dubbo服务的代理类，并将它们注册到Spring应用程序上下文中。在创建Dubbo服务的代理类时，`ReferenceAnnotationBeanPostProcessor`会使用`IntrospectiveDubboMetadataService`接口来获取Dubbo服务的元数据信息，然后根据这些元数据信息动态生成Dubbo服务的代理类。
总之，`com.alibaba.cloud.dubbo.service.IntrospectiveDubboMetadataService`是一个Dubbo元数据服务接口，它提供了Dubbo服务的元数据信息，通常用于动态生成Dubbo服务的代理类。在Spring Cloud中，可以使用`IntrospectiveDubboMetadataService`接口来获取Dubbo服务的元数据信息，并根据这些元数据信息动态生成Dubbo服务的代理类，以适应Dubbo服务的变化。例如，在Spring Cloud Alibaba中，`ReferenceAnnotationBeanPostProcessor`后置处理器使用了`IntrospectiveDubboMetadataService`接口来创建Dubbo服务的代理类。


