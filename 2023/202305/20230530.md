# 20230530


BeanUtils spring_bean这个jar包里面的工具类
org.springframework.beans.BeanUtils#instantiateClass(java.lang.Class<T>)

抽象类，不能实例化

java，做中间件开发，一个package下面最多20个类？

一般来说,一个包中管理20~30个类已经是一个比较合理的包规模。但这不是硬性要求,完全取决于具体设计。

