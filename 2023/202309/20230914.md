# 20230914

spring-contextjar包依赖.png

```java
            RedisLock lock = null;
            try {
                String lockKey = "autoGenRecOrder-" + request.getCortNumId().toLowerCase() + "-" + request.getSubUnitNumId() + "-" + request.getDmNumId();
                lock = redisService.getRedisLock(lockKey);
                if (lock != null && lock.lock()) {
                    
                }
            } catch (Exception e) {
                ExceptionUtil.processException(e, response);
                log.error("autoGenerateReceiptOrder error...", e);
            } finally {
                if (lock != null) {
                    lock.unlock();
                }
            }
```
缓存穿透
缓存雪崩

核心概念
