# 20230911

结算单主表，有两个值是空，我没注意，应该自己发现的

徽商总行
信息科技部、大数据部、系统开发部

四、培养方向
（一）金融科技岗（总行）
入职后与徽商银行总行签订劳动合同，未来成长为金融科技专家或管理人员。
1.总行信息科技部：全行信息科技资源统筹规划、网络及系统维护和监督管理等。
2.总行大数据部：全行数据分析及模型建设、数据资产管理等。
3.总行系统开发部：全行信息系统建设、软件开发、系统集成管理等。
（二）管理培训生（分行）
培养和储备各类专业型和管理型人才，入职后通过人才潜力培养、轮岗学习、晋升机制以及分行内部选聘优先等措施，助力成为行内管理精英和业务专家。
（三）客户经理岗（南京、北京、深圳、成都、宁波分行）
入职后轮岗学习，未来从事客户经理、理财经理、产品营销、市场拓展、风险合规和财务审计等相关工作，逐步成长为精通银行相关条线的专业骨干或管理人员。
（四）定向柜员岗（分行）
从事各类柜面运营结算业务，为个人、公司客户提供运营操作服务，柜面服务期限不少于5年，定向合同制员工。
上述招聘要求为徽商银行相关岗位，徽银金融租赁和徽银理财有限公司具体招聘要求详见招聘公告。

梁成运

美国已经摸通的技术研发方向，跟随

  `reverse_amount` decimal(18,6) DEFAULT '0.000000' COMMENT '退货金额',


ALTER TABLE `fi_jm_settlement_order_dtl` 
ADD COLUMN `reverse_amount` decimal(18, 6) NULL DEFAULT 0.000000 COMMENT '退货金额' AFTER `recive_reserved_no`;

CREATE TABLE `fi_jm_settlement_order_dtl` (
  `series` bigint(20) NOT NULL COMMENT '逻辑自增主键',
  `data_sign` tinyint(4) DEFAULT '0' COMMENT '生产或测试标识',
  `tenant_num_id` int(11) DEFAULT NULL COMMENT '租户id',
  `parent_series` bigint(20) DEFAULT NULL COMMENT '父类编号',
  `reconciliation_no` varchar(20) DEFAULT NULL COMMENT '结算单号',
  `cort_name` varchar(50) DEFAULT NULL COMMENT '公司名称',
  `cort_num_id` varchar(50) DEFAULT NULL COMMENT '公司编号',
  `sub_unit_num_id` varchar(50) DEFAULT NULL COMMENT '客户编号',
  `plan_num_id` bigint(18) DEFAULT NULL COMMENT '补货计划单号',
  `paied_no` bigint(20) DEFAULT NULL COMMENT '销售订单号',
  `sale_type` tinyint(4) DEFAULT NULL COMMENT '销售类型',
  `sell_qty` decimal(12,4) DEFAULT NULL COMMENT '销售数量',
  `sale_amount` decimal(18,6) DEFAULT NULL COMMENT '销售金额-未税',
  `sale_total_amount` decimal(18,6) DEFAULT NULL COMMENT '销售金额-含税',
  `sale_tax_amount` decimal(18,6) DEFAULT NULL COMMENT '销售税额',
  `pay_amount` decimal(18,6) DEFAULT NULL COMMENT '已付款金额',
  `pay_rest_amount` decimal(18,6) DEFAULT NULL COMMENT '未付款金额',
  `cite_ship_qty` decimal(12,4) DEFAULT NULL COMMENT '出库数量',
  `cite_ship_amount` decimal(18,6) DEFAULT NULL COMMENT '出库金额-未税',
  `cite_ship_total_amount` decimal(18,6) DEFAULT NULL COMMENT '出库金额-含税',
  `cite_ship_tax_amount` decimal(18,6) DEFAULT NULL COMMENT '出库税额',
  `ship_date` datetime DEFAULT NULL COMMENT '出库日期',
  `recive_date` datetime DEFAULT NULL COMMENT '收货日期',
  `deadline_date` datetime DEFAULT NULL COMMENT '到期日',
  `pay_stat` tinyint(4) DEFAULT '0' COMMENT '支付状态(0待支付,1已支付,2未支付,3部分支付,4支付失败)',
  `contract_code` varchar(30) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '合同编码',
  `writeoff_amount` decimal(10,0) DEFAULT NULL COMMENT '已核销金额',
  `sub_unit_cort_id` varchar(50) DEFAULT NULL COMMENT '客户公司编号',
  `sub_unit_cort_num` varchar(255) DEFAULT NULL COMMENT '客户公司名称',
  `ship_reserved_no` bigint(20) DEFAULT '0' COMMENT '出库单号',
  `recive_reserved_no` bigint(20) DEFAULT '0' COMMENT '收货单号',
  `create_dtme` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `create_user_id` bigint(18) DEFAULT NULL COMMENT '创建用户号',
  `last_updtme` datetime DEFAULT NULL COMMENT '更新时间',
  `last_update_user_id` bigint(18) DEFAULT NULL COMMENT '更新用户号',
  `cancelsign` char(1) DEFAULT 'N' COMMENT '删除',
  PRIMARY KEY (`series`) USING BTREE,
  KEY `query_idx` (`cort_num_id`,`sub_unit_num_id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC COMMENT='加盟批销结算单明细' 
dbpartition by MOD_HASH(`CORT_NUM_ID`) 
tbpartition by MOD_HASH(`SUB_UNIT_NUM_ID`) tbpartitions 8;



苏州房价
尹山湖 两万左右


相亲对象ISTJ
我持保留意见，今年培训班大肆兴起。还是说你说的是音视频嵌入式方向

mysql内核开发不是人干的活，建议年轻人不要入坑！因为刚刚毕业要看优秀实践，而不是屎山

熟悉业务后，一定要去解决实际问题，尤其是部署系统，性能相关以及组件层面的问题，这才是程序员高效升级到架构师的诀窍。

中国教育
其实俞敏洪有好多讲解视频我真的觉得他说的很好的。还有复旦大学的陈果老师说的视频演讲。

https://www.zhihu.com/answer/1862026844
很多后端开发转到大数据平台开发，其实从职业发展角度，个人觉得还不如做懂大数据的数仓，系统学习一下建模理论

历史发明家是吧，周可是一点点蚕食商的，不是一次牧野之战.就解决商的，商纣王死了商朝还控制极大的土地，后面周公旦摄政翦商才算是完成了一统。
