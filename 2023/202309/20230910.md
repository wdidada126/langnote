# 20230910

专业投机原理

c++八股文.jpg
Cpp八股文.zip

以前工作过的保险公司核心系统用cobol,银行核心用C
互联网爆发之前，90%的码农都在项目外包公司，经典的就是金蝶，用友，亚信

亚信电信运营商提供IT解决方案和服务
亚信提供的软件方案和服务涉及IP、VoIP、宽带、无线、3G等等技术领域,包括:业务支撑系统(计费、客户关系管理、商业智能分析、网管系统等);电信增值应用系统以及电信级网络解决方案等。

c#
https://www.zhihu.com/answer/3190430024

Java并发编程实战 极客时间
Java7并发编程实战手册 书籍

Exchanger<V>
    Node
    Participant extends ThreadLocal<Node>

Phaser
    QNode

    public int register()
    public int bulkRegister(int parties)
    public int arriveAndAwaitAdvance()
    public void forceTermination()
    public final int getPhase()
    public int getRegisteredParties()
    public boolean isTerminated()
    public Phaser getRoot()

```java
 class FillAndEmpty {
   Exchanger<DataBuffer> exchanger = new Exchanger<DataBuffer>();
   DataBuffer initialEmptyBuffer = ... a made-up type
   DataBuffer initialFullBuffer = ...

   class FillingLoop implements Runnable {
     public void run() {
       DataBuffer currentBuffer = initialEmptyBuffer;
       try {
         while (currentBuffer != null) {
           addToBuffer(currentBuffer);
           if (currentBuffer.isFull())
             currentBuffer = exchanger.exchange(currentBuffer);
         }
       } catch (InterruptedException ex) { ... handle ... }
     }
   }

   class EmptyingLoop implements Runnable {
     public void run() {
       DataBuffer currentBuffer = initialFullBuffer;
       try {
         while (currentBuffer != null) {
           takeFromBuffer(currentBuffer);
           if (currentBuffer.isEmpty())
             currentBuffer = exchanger.exchange(currentBuffer);
         }
       } catch (InterruptedException ex) { ... handle ...}
     }
   }

   void start() {
     new Thread(new FillingLoop()).start();
     new Thread(new EmptyingLoop()).start();
   }
 }
 ```

mysql不是学习数据库的最佳实践
pg是吗？
tikv是吗？



找其他的
leveldb不支持关系运算，只能存储slice数据

leveldb没有实现关系数据库的B+索引
索引下推 优化手段
mysql
在MySQL中，索引下推是默认启用的状态。在使用InnoDB存储引擎的数据表中，索引下推只能用于二级索引。我们可以通过修改MySQL系统变量来控制索引下推是否开启。设置如下：
SET optimizer_switch = 'index_condition_pushdown=off';// 关闭
SET optimizer_switch = 'index_condition_pushdown=on';// 开启

MYDB 不支持索引
分析MYDB源码，按照包去记录


java刚问面试题
多线程，高并发
线程池相关的类
线程开启，线程状态
线程竞争
Lock

无锁
cas
AQS Lock ReadWriteLock

框架熟悉度
spring
springmvc http web server
spring boot 简化开发
spring cloud分布式

缓存
redis
mq
kafka
rocketmq
mongodb

