# 20230905

业务软件，不光要考虑代码，还要考虑数据库的正确数据脏数据，
数据校验，数据库表有完整性校验
中间件开发，版本更新功能更新记录
代码需要花流程图，复用共用的流程

华为DDM基于mycat？
效鹏：quanyi现阶段不可能维护中间件，人手不够，搞定业务问题

ALTER TABLE `fi_jm_set_pay_dtl` ADD COLUMN `recive_reserved_no` bigint(20) NULL DEFAULT 0 COMMENT '收货单号' AFTER `sale_unit`;