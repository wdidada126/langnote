# 202307_weekly_1

## 时间范围

- 202307 月第 1 周

## 本周记录

- 20230701：周末
- 20230702：周末
- 20230703：在 Vue.js 中，`.vue` 文件通常用于编写单个组件。在一个 `.vue` 文件中，可以通过 `export default` 语法导出一个 Vue 组件对象。；`export default` 语法用于将组件定义导出为默认导出。这意味着在其他地方引入该组件时，可以使用任意名称来接收它，而不需要使用特定的名称。；在一个 `.vue` 文件中，`components` 属性用于注册和引入其他组件。通过将其他组件作为键值对添加到 `components` 属性中，可以在当前组件中使用这些组件，并在模板中使用它们。
- 20230704：【跟着Mic学架构的个人空间-哔哩哔哩】 https://b23.tv/6uH860I；为什么不用声明式事务；1、占用数据库连接
- 20230705：com.github.lianjiatech.retrofit；这个框架github上star很多；问题已经定位了
- 20230706：cron；Cron 表达式通常有 6 位和 7 位两种格式，其中 6 位的格式只包含秒到周几 6 个时间字段，而 7 位的格式包含了秒到年份 7 个时间字段。具体区别如下：；6 位的 Cron 表达式只能精确到周几，无法指定具体的年份。其格式如下：
- 20230707：国内得通稿总有个毛病：；一大堆站台的专家，而这些专家和这个技术领域没点关系，更没贡献过一行代码；；PPT非常澎湃，动不动性能提升50%，有本事把社区版本的性能提升50%才是被尊重的；
