# 202307_month

## 原有内容


```xml
        <dependency>
            <groupId>com.github.lianjiatech</groupId>
            <artifactId>retrofit-spring-boot-starter</artifactId>
            <version>2.2.16</version>
        </dependency>
```
重试报错，取消重试
日志显示超时

https://github.com/edidada/retrofit-spring-boot-starter
看源码

debug代码，可以看到函数入参具体是什么值，帮助理解源代码

# 20230710




org.springframework.cloud.gateway.support.ServerWebExchangeUtils#GATEWAY_ROUTE_ATTR


            // 设置路由
            Route route = exchange.getAttribute(GATEWAY_ROUTE_ATTR);
            Route rebuildRoute = Route.async()
                    .id(route.getId())
                    .order(route.getOrder())
                    .asyncPredicate(route.getPredicate())
                    .filters(route.getFilters())
                    .replaceMetadata(route.getMetadata())
                    .uri("erp-fi".equalsIgnoreCase(route_mete_data.getString("application_name")) ? "http://localhost:9099" : ("lb://" + route_mete_data.getString("application_name"))).build();
            exchange.getAttributes().put(GATEWAY_ROUTE_ATTR, rebuildRoute);

微服务，应用有多个，url不同？
通过网关，调用方感知不到多个微服务的存在，统一的

esb的功能

生成序列号的服务

需要统计有多少接口，所以把接口的url插入数据库

《银行数字化营销与运营》
http://wjd.name/yinhang/
《银行数字化营销与运营：突围、转型与增长》是2021年机械工业出版社出版的图书，作者是金腰子。














Kafka多集群同步数据是通过Kafka broker集群之间同步数据来实现的。

生产者Producer只需要往topic发送消息,与集群个数无关。

具体来说:
1. 生产者Producer只需要知道要发送到具体的一个topic。
它不关心该topic属于哪个Kafka集群,也不会主动将消息发布到多个集群。
2. 不同Kafka集群中的同名topic,其实是两个完全独立的topic。
生产者发送的消息只会发布到指定集群的topic上。
3. 数据同步是由Kafka broker集群之间自动完成的。
当多个集群有相同名称的topic时,这些topic会形成一个"topic集群"。
4. Kafka broker会在topic集群间自动进行数据复制。
确保多集群中同名topic实时保持数据一致。
5. 当集群中的某个brokers failover 后,它会自动从其他集群中拉取最新数据。
以实现数据的恢复和备份。
所以,总的来说Kafka多集群同步数据的实现并非由生产者完成,而是由:

- Kafka broker之间自动完成topic间数据复制
- broker failover后自动从其他集群恢复数据

这两个机制共同确保多集群环境下,同名topic的数据始终保持一致。
生产者不需要知道这些细节,它只需要向一个topic发送消息即可。
希望这能帮助你理解Kafka多集群同步数据的实现原理。如果仍然存在疑问,欢迎继续提问!




nacos server，多集群部署，数据在集群中间同步
Nacos支持多集群部署,且通过数据同步机制能保证各集群间的数据同步。
具体来说:
每个Nacos集群是完全独立的,需要部署多个Nacos实例。
每个Nacos实例需要指定不同的cluster.name,从而形成不同的集群。
Nacos支持集群间数据同步。
数据同步机制:
每个集群都有一个"发布者"角色,将数据发布到全局单集
其他集群上的"订阅者"角色则订阅全局单集,获取最新的数据
主要同步的数据包括:

服务实例列表
配置信息
令牌信息
其他元数据
集群的同步关系是"点对点"的,任何一个集群都可能成为"发布者",所有集群都"订阅"它。

配置信息只允许在"发布者"集群修改,其他集群只读。
通过这种方式,Nacos可以保证各集群间的数据始终同步一致。
部署多集群的Nacos时,主要注意以下配置:
分别指定cluster.name
配置≥ 1个集群作为"发布者"
其他集群为"订阅者"
配置集群间的同步关系
Nacos支持多集群部署,且通过同步机制能保持集群间的数据一致性,提供高可用性。


把Nacos集群配置为"发布者"(Publisher)集群主要需要修改两个配置:
1. 配置cluster.name
设置每一个Nacos集群的唯一编号,比如:
- 集群1:
```
cluster.name = cluster1
```
- 集群2:

```
cluster.name = cluster2
```

2. 配置data.share.async.enable

用来指定该集群是否作为"发布者"。

- 发布者集群需要设置为:

```
data.share.async.enable = true
```

- 订阅者集群需要设置为:

```  
data.share.async.enable = false
```

即只有集群自己的数据share.async.enable设置为true时,该集群才会作为"发布者"。

订阅者集群只需设置为false即可。

以上两项配置便足以让Nacos集群作为"发布者",发布数据到其他集群。

在配置文件中, cluster.name 和 data.share.async.enable 这两个属性十分关键:

- cluster.name:用来区分不同的集群
- data.share.async.enable:用来指示本集群是否作为"发布者"

Nacos启动时便会自动发现其他"订阅者"集群,并与之建立同步关系。

希望这能帮助你正确地配置Nacos集群为"发布者"(Publisher)。

需要注意的是,为确保高可用:

- 建议部署多个"发布者"集群 
- 其他集群则都配置为"订阅者"

这样一来,即使某个"发布者"集群故障,"订阅者"集群仍然能接收其他"发布者"的数据。











RocketMQ支持多集群部署,且提供有效机制保证各集群间的数据同步。主要通过Name Server Cluster实现:

1. Name Server Cluster

RocketMQ的Name Server采用集群部署,多个Name Server形成一个Name Server Cluster。

Name Server Cluster维护整个RocketMQ集群的所有元数据,包括:

- Topic命名信息
- Broker信息 
- Message Queue信息
- 等等

2. 多集群时Name Server使用Globally Unique Name

在多集群环境中,每个集群的Name ServerCluster名称需要全局唯一。

相同集群内Name Server互相同步,不同集群Name Server间不进行数据交换。

3. 客户端根据Name Server Cluster访问指定集群

生产者和消费者通过指定Name Server Cluster,间接指定对应的RocketMQ Broker Cluster。

这样就能安全地访问特定集群中的队列和主题。

4. Name Server Cluster间数据同步

RocketMQ支持手动配置Name Server Cluster间的同步关系。

将一个集群配置成"发布者"(Publisher)集群,其他集群作为"订阅者"(Subscriber)集群。

"订阅者"集群定时从"发布者"集群拉取最新元数据,从而保持一致。

5. 集群添加后自动同步

RocketMQ还支持集群被动添加特性。

即在部署一个新的集群后,它会自动订阅已有的"发布者"集群,实现数据同步。

通过以上几点,RocketMQ就可以实现多集群之间的元数据同步,保证各集群间Topic和消息完整一致:

- Name Server Cluster全局唯一,维护每个集群元数据
- 手动或自动配置的Name Server Cluster间同步关系
- 客户端根据Name Server Cluster访问特定集群

希望这能帮助你理解RocketMQ多集群同步数据的原理。有任何问题欢迎随时提问。



责任链模式
spring cloud gateway
okhttp




职位详情：1.作为产品经理对公司财务系统进行整体设计和迭代；2.梳理公司业务流程，以财务视角对公司业财融合提出解决方案；3.配合供应商进行实施过程中的单元测试和集成测试；4.完成财务相关系统日常系统运维工作
任职要求：1.本科及以上学历，财务相关或计算机管理相关专业；2..具备良好的沟通表达能力和团队精神；3.了解SAP、EBS、Netsuite等国内外erp系统原理（熟练掌握Netsuite系统功能的优先)。4.具有良好的学习能力，文字表达能力。

具体来说，勒夏特列原理可以解释社会系统在面临不同类型的压力时可能会发生的反应。例如，一个社会系统可能受到政治、经济、文化等多种影响，这些影响可能会改变社会的平衡状态。根据勒夏特列原理，社会系统会通过调整其结构、行为等方面来抵消外界的影响，以维持社会的平衡状态。例如，在经济萧条时，社会可能会采取一些措施来缓解经济压力，例如增加财政支出、降低利率等，以保持经济稳定。
勒夏特列原理还可以用于解释社会变革的过程。社会变革可能会对社会系统产生冲击，并可能导致社会的平衡状态发生改变。根据勒夏特列原理，社会系统会通过调整其结构、行为等方面来适应变化，以重新达到平衡状态。例如，在社会经济结构发生变化时，社会可能会适应新的经济结构，通过调整产业结构、职业结构等方面来适应新的形势。
总的来说，勒夏特列原理在社会学中具有一定的应用，可以用于解释社会系统对外界影响的反应过程，以及社会变革的适应过程。

前端上传文件，前端负责解析，后端直接接收参数

js，解析excel的库



### 整理api

最近整理了mybatis mybatis-spring retrofit retrofit-springboot-starter等java库的源代码

第一次使用库的时候，就要看官网文档，跑官方demo，自己写demo，看api doc，整理用到的函数，类


复习

知识系统化，开开发者交流

| 包            |      |   自己的代码库   |
| ------------- | ---- | ---- |
| jackson       |      |    https://gitee.com/edidada/testjackson  |
| java se标准库 |      |      |
| fastjson      |      |      |
|   druid            |      |      |
|   netty            |      |      |
|   mybatis            |      |      |
|   rocketmq            |      |      |


https://javadoc.dev/online/api/org.apache.dubbo/dubbo/2.7.8/index.html


netty 源代码编译

https://gitee.com/mirrors

## 整理补充

## 本月概览

- 本月共整理 31 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202307_week5：- 20230729：编译；搬瓦工编译spring boot 2.3.4失败；找电子书，学点东西
- 202307_weekly_1：- 20230701：周末
- 202307_weekly_2：- 20230708：周末，没有学习
- 202307_weekly_3：- 20230715：周末
- 202307_weekly_4：- 20230722：bitbucket用其他算法的ssh 秘钥，不用rsa的；`org.springframework.context.annotation.ClassPathScanningCandidateComponentProvider#isConditionMatch` 是 Spring 框架中的一个方法，用于判断指定的 Bean 候选组件是否满足指定的条件。；在 Spring 框架中，可以使用条件注解（Conditional）来定义某个 Bean 是否应该被创建。这些条件注解可以应用于注解在类或方法上的任何 Spring 注解，例如 `@Bean`、`@Component`、`@Configuration` 等。当应用程序上下文启动时，Spring 框架会根据这些条件注解来确定是否应该创建某个 Bean。

## 本月高频主题

- 周末
- 周末
- 在 Vue.js 中，`.vue` 文件通常用于编写单个组件。在一个 `.vue` 文件中，可以通过 `export default` 语法导出一个 Vue 组件对象。
- `export default` 语法用于将组件定义导出为默认导出。这意味着在其他地方引入该组件时，可以使用任意名称来接收它，而不需要使用特定的名称。
- 在一个 `.vue` 文件中，`components` 属性用于注册和引入其他组件。通过将其他组件作为键值对添加到 `components` 属性中，可以在当前组件中使用这些组件，并在模板中使用它们。
- 【跟着Mic学架构的个人空间-哔哩哔哩】 https://b23.tv/6uH860I
- 为什么不用声明式事务
- 1、占用数据库连接
- com.github.lianjiatech.retrofit
- 这个框架github上star很多
- 问题已经定位了
- cron
