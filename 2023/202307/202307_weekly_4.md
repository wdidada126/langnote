# 202307_weekly_4

## 原有内容


分析 retrofit
dubbo等框架代码

spring-boot源代码分析

## 整理补充

## 时间范围

- 202307 月第 4 周

## 本周记录

- 20230722：bitbucket用其他算法的ssh 秘钥，不用rsa的；`org.springframework.context.annotation.ClassPathScanningCandidateComponentProvider#isConditionMatch` 是 Spring 框架中的一个方法，用于判断指定的 Bean 候选组件是否满足指定的条件。；在 Spring 框架中，可以使用条件注解（Conditional）来定义某个 Bean 是否应该被创建。这些条件注解可以应用于注解在类或方法上的任何 Spring 注解，例如 `@Bean`、`@Component`、`@Configuration` 等。当应用程序上下文启动时，Spring 框架会根据这些条件注解来确定是否应该创建某个 Bean。
- 20230723：`org.springframework.web.multipart.support.StandardMultipartHttpServletRequest.StandardMultipartFile`；`org.springframework.web.multipart.commons.CommonsMultipartFile`；都实现了MultipartFile接口，区别
- 20230724：星期一；熊世康 协力 离场；teamviewer现在怎么只能两台机子了
- 20230725：青龙 四号线地铁口；望江台 更靠近铁轨；西子曼城
- 20230726：jackson-databind；databind；databind.annotation
- 20230727：骑车，手机从口袋掉出来了；不能盯着别人看，不要随便思考东西，一思考就盯着一处看，别人会误会我意思；徐颖超，我盯着她看，她让我背包
- 20230728：gradle核心库，只需要声明id，不需要版本；git clone https://github.com/spring-projects/spring-boot.git；cd spring-boot
