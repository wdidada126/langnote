# 20230727

骑车，手机从口袋掉出来了

不能盯着别人看，不要随便思考东西，一思考就盯着一处看，别人会误会我意思

徐颖超，我盯着她看，她让我背包
让我用移动电风扇


mybatis ognl库

```xml
<dependency>
  <groupId>ognl</groupId>
  <artifactId>ognl</artifactId>
  <version>3.2.21</version>
</dependency>
```


编译
https://github.com/apache/dubbo/tree/dubbo-2.7.8


编译spring boot，缺少gradle插件，失败
	

		<dependency>
			<groupId>io.spring.gradle</groupId>
			<artifactId>gradle-enterprise-conventions</artifactId>
			<version>0.0.3</version>
		</dependency>


这是Java中的一种类型判断和转换的语法。

`instanceof`操作符用于检查一个对象是否是指定类型的实例。

`(dependency instanceof ModuleDependency)`这一部分是做类型判断,检查dependency对象是否是ModuleDependency类型的一个实例。

`moduleDependency`这一部分是做类型转换,如果判断成功,则将dependency对象转换成ModuleDependency类型,并将转换后的对象赋值给moduleDependency变量。

所以整个语句的意思是:

如果dependency对象是ModuleDependency类型的实例,则将其转换成ModuleDependency类型,并赋值给moduleDependency变量。

这样就可以方便地在if语句块内使用moduleDependency变量来访问ModuleDependency类型的属性和方法。

一个更完整的例子:

```java
Dependency dependency = //...

if (dependency instanceof ModuleDependency moduleDependency) {

  String group = moduleDependency.getGroup(); 
  String version = moduleDependency.getVersion();

  // 使用moduleDependency对象
}
```

这避免了需要在if块内再做一次类型转换。

这种语法在Java 12中引入,可以减少冗余的代码。type instanceof Type var的形式非常实用。


git restore --source=HEAD :/'

编译了rocketmq
rocketmq-spring 只有spring boot的	


https://repo.spring.io/ui/native/libs-milestone/

默认情况下， MyBatis允许拦截器拦截Executor的方法、 ParameterHandler的方法、 ResultSetHandler的方法以及StatementHandler的方法，这个四个对象可称为Mybatis的四大对象。

mybatis源码阅读之后，看mybatis-plus
mybatis pagehelper之类的扩展库

学java se，看api javadoc
mybatis，先用，写例子，看api java doc
两个xml文件，解析xml文件到Configuration SqlSource类中，还有mybatis接口，解析后存在Map对象中

shardingsphere 先用，写例子，官网架构图，分模块，看api java doc

分布式追踪，Skywalking

nacos 分布式算法，分布式一致性

spring springboot spring cloud太大了

其实dubbo shardingsphere也大
linux代码也大，分析其中一个模块


rocketmq java sdk client源码，类比mysql 
mysql jdbc connectier

用java se的api进行网络访问，执行sql
Statement
Connection
ResultSet

rocketmq
Message
Producer
Com

配置类，server ip

ClientConfig
消息topic，消息内容
网络连接类？
日志类
异常类
统计类