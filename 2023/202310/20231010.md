# 20231010

十月份高频面试题线程池源码及原理如何答出亮点？
全网上百万粉丝技术博主+面试大牛带你面试突击！
课程内容：《从0到1手写实现一个企业级线程池并深度分析线程池源码及原理》

mysql数据库 表 列
`sell_qty` decimal(12,4) DEFAULT NULL COMMENT '销售数量',
数量 是用bigint？还是decimal(12,4)


mq比rpc http调用好在哪儿？消息发送参数有记录，可以重试

一对多，一个消息发送者，多个消息消费者

git fetch o1 master:master

Integer payTypeId = request.getPayTypeId();
5 == payTypeId
Integer.valueOf(5).equals(payTypeId)

select r.service_name,r.method_name from platform_mq_topic l left join platform_mq_dubbo_consumer r on l.consumer_series= r.series where l.topic='FI_30815'
