# 20231007

并发控制协议_2PL_TS_OCC.mhtml

学习it技术，第一点要看，看mybtis官网，原文是英文的，但是有中文翻译，理解每个概念。其他框架英文文档也有人翻译成中文
第二点用，mybatis官方有例子，用mybtis写项目，看现存项目的代码
第三点交流，邮件列表
第四点看mybatis源代码，修bug，参与mybatis社区，mybatis功能更新
第五点比较，不用mybatis，用什么方案去写代码完成项目，mybatis优势在哪儿？
mybatis库很多人都会，小问题直接问同事。

你问别人别人说你独立解决问题，那是别人不想理你的借口托辞

```java
import org.springframework.data.redis.connection.RedisConnection;
import org.springframework.data.redis.connection.StringRedisConnection;
import org.springframework.data.redis.core.*;
import org.springframework.data.redis.core.script.DefaultRedisScript;
```
需要熟悉


```sql
select dtl.cort_name, dtl.cort_num_id, dtl.paied_no, dtl.sell_qty, hdr.reconciliation_no
	, hdr.pay_cycle, hdr.pay_cycle, hdr.last_pay_date, hdr.settle_way, dtl.cite_ship_qty
	, dtl.cite_ship_total_amount, dtl.sale_total_amount, dtl.pay_amount, dtl.pay_rest_amount, dtl.pay_stat
	, dtl.deadline_date, dtl.sale_type, dtl.plan_num_id
from fi_jm_settlement_order_hdr hdr
	inner join fi_jm_settlement_order_dtl dtl
	on dtl.tenant_num_id = hdr.tenant_num_id
		and dtl.data_sign = hdr.data_sign
		and dtl.sub_unit_cort_id = hdr.sub_unit_cort_id
		and dtl.sub_unit_num_id = hdr.sub_unit_num_id
		and dtl.reconciliation_no = hdr.reconciliation_no
		and hdr.cancelsign = 'N'
where dtl.tenant_num_id = 18
	and dtl.data_sign = 0
	and dtl.sub_unit_cort_id = 'PJ01'
	and dtl.sub_unit_num_id = 'A001'
	and dtl.parent_series in (2310070010001)
	and dtl.cancelsign = 'N'
```

if (request.getMode() == Constant.AUDIT_PASS){
改成
if (request.getMode().equals(Constant.AUDIT_PASS_INTEGER)){}
因为Integer 不能直接跟int比较

确实，在Java中，Integer是一个类，而int是一个基本数据类型。直接比较它们会导致编译错误。要比较一个Integer对象和一个int值，您需要先将Integer对象拆箱为int值，或者将int值装箱为Integer对象。以下是两种方法：

方法1：使用Integer.intValue()方法将Integer对象转换为int值

```java
Integer integerObj = new Integer(5);
int intValue = integerObj.intValue(); // 拆箱
```

方法2：使用Integer.valueOf()方法将int值装箱为Integer对象

```java
int intValue = 5;
Integer integerObj = Integer.valueOf(intValue); // 装箱
```

现在您可以比较拆箱后的int值和装箱后的Integer对象了。

```java
Integer a = new Integer(1);
Integer b = new Integer(1);
a.equals(b) 是比较地址值，还是比较熟知？
Integer是否重写hash equals toString()
```