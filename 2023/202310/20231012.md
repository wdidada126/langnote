# 20231012

甜橙金融 全亿 翼支付
旭哥，让我加班，活没干完，我说有事直接走
现在态度不行

大学，汪宇，买索尼手机，我说干嘛买索尼手机？

现在的问题是，一般纳税人的税率是多少?这个我真不知道,问我我也不知道啊
之前没有这种的,最早门店时不收税的,全部门店都是0税率.去年10月的时候国家突然之间收百分之一的税.
1%的税是小规模纳税
一般纳税人是怎么纳税,税率多少需要跟财务确认

mdms_p_product_tax_rate
税务分类

mdms_p_product_unit
分部商品结果表

java List<FI_B2C_INVOICE> invoiceWeChatWarnList   获取前10个非null的元素

```java
import java.util.List;  
import java.util.stream.Collectors;  
  
public class Main {  
    public static void main(String[] args) {  
        List<FI_B2C_INVOICE> invoiceWeChatWarnList = ...; // 获取FI_B2C_INVOICE列表  

        List<FI_B2C_INVOICE> nonNullInvoices = invoiceWeChatWarnList.stream() // 使用流API  
                                                       .filter(Objects::nonNull) // 过滤非null元素  
                                                       .collect(Collectors.toList()); // 收集非null元素到新列表

        // 如果需要获取前10个非null元素，可以继续对nonNullInvoices进行切片操作
        List<FI_B2C_INVOICE> top10NonNullInvoices = nonNullInvoices.stream().limit(10) // 取前10个元素
                                                       .collect(Collectors.toList());
    }
}
```

 limit()
 java 8 stream slice()存在吗？
 不存在
 Go语言中存在

## 第一批

123101200010015

282310120020008

182310120010014

2310120010014

192310120010013

HTTP协议入门-阮一峰的网络日志.mhtml

在Vue中发送HTTP请求时，可以通过设置Accept-Language头部来指定所接受的内容语言。Accept-Language头部用于告诉服务器客户端能够接受的语言，以便服务器可以根据需要进行相应的处理。

对于区分中文和英文的请求，可以在发送请求时设置Accept-Language头部来指定所接受的语言。以下是一些示例：

发送中文请求：
```js
axios.get('https://example.com/api/data')  
  .headers({  
    'Accept-Language': 'zh-CN'  
  })  
  .then(response => {  
    // 处理响应数据  
  })  
  .catch(error => {  
    // 处理错误  
  });
```
上述代码中，通过设置Accept-Language头部为zh-CN，告诉服务器客户端希望接收中文内容。

发送英文请求：

```
axios.get('https://example.com/api/data')  
  .headers({  
    'Accept-Language': 'en-US'  
  })  
  .then(response => {  
    // 处理响应数据  
  })  
  .catch(error => {  
    // 处理错误  
  });
```
上述代码中，通过设置Accept-Language头部为en-US，告诉服务器客户端希望接收英文内容。
需要注意的是，当发送Accept-Language头部时，服务器会根据所接受的语言来返回相应语言的内容。因此，确保服务器端能够处理相应的语言并根据需要进行返回。

pistache c++库，如何支持中英文
Rust web框架如何支持？
Go web框架如何支持

gin gorm go-zero grpc

go写http web api，用啥框架？
gin多一点


## k8s 描述文件

```yaml
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: '928'
  creationTimestamp: '2021-10-19T03:19:00Z'
  generation: 934
  labels:
    name: erp-fi
  managedFields:
    - apiVersion: apps/v1
      fieldsType: FieldsV1
      fieldsV1:
        'f:metadata': {}
        'f:spec':
          'f:selector': {}
          'f:strategy': {}
          'f:template':
            'f:metadata': {}
            'f:spec':
              'f:affinity': {}
              'f:containers':
                'k:{"name":"erp-fi"}':
                  'f:env': {}
                  'f:ports': {}
                  'f:resources': {}
                  'f:securityContext': {}
                  'f:volumeMounts': {}
              'f:imagePullSecrets': {}
              'f:volumes':
                'k:{"name":"jdk-data"}': {}
                'k:{"name":"timezone"}': {}
      manager: Mozilla
      operation: Update
      time: '2022-06-08T05:41:39Z'
    - apiVersion: apps/v1
      fieldsType: FieldsV1
      fieldsV1:
        'f:metadata': {}
        'f:status':
          'f:conditions': {}
      manager: kube-controller-manager
      operation: Update
      time: '2023-10-11T09:55:33Z'
  name: erp-fi
  namespace: qy-beta
  resourceVersion: '511904020'
  selfLink: /apis/apps/v1/namespaces/qy-beta/deployments/erp-fi
  uid: 305fd05c-f449-4be3-a11e-82bb1ecdcb68
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      name: erp-fi
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/restartedAt: '2023-10-11T17:55:24+08:00'
      labels:
        name: erp-fi
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: name
                      operator: In
                      values:
                        - erp-fi
                topologyKey: kubernetes.io/hostname
              weight: 100
      containers:
        - command:
            - /bin/bash
            - '-c'
            - >-
              java  -Xms3g -Xmx6g -Dspring.profiles.active=beta 
              -Duser.timezone=GMT+08 -Ddubbo.shutdown.hook=true
              -Ddubbo.reference.check=false
              -javaagent:/usr/local/apps/skywalking-agent/skywalking-agent.jar
              -classpath /usr/local/apps/classes:/usr/local/apps/lib/*
              com.ykcloud.soa.erp.fi.FiMain
          env:
            - name: SW_AGENT_NAME
              value: erp-fi
            - name: SW_AGENT_COLLECTOR_BACKEND_SERVICES
              value: 'oap.qy-beta:11800'
          image: 'qy-dev-harbor.myquanyi.com/qy-dev/erp-fi:latest-dev'
          imagePullPolicy: Always
          name: erp-fi
          ports:
            - containerPort: 22222
              name: http
              protocol: TCP
          resources:
            limits:
              cpu: '1'
              memory: 6Gi
            requests:
              cpu: 300m
              memory: 4Gi
          securityContext:
            privileged: false
            seccompProfile:
              type: RuntimeDefault
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          volumeMounts:
            - mountPath: /etc/localtime
              name: timezone
              readOnly: true
            - mountPath: /storage
              name: jdk-data
      dnsPolicy: ClusterFirst
      imagePullSecrets:
        - name: qy-beta-harbor
      nodeSelector:
        myapp: jd
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
      volumes:
        - hostPath:
            path: /etc/localtime
            type: ''
          name: timezone
        - name: jdk-data
          persistentVolumeClaim:
            claimName: jdk-pv-claim
status:
  availableReplicas: 1
  conditions:
    - lastTransitionTime: '2023-07-27T06:41:32Z'
      lastUpdateTime: '2023-07-27T06:41:32Z'
      message: Deployment has minimum availability.
      reason: MinimumReplicasAvailable
      status: 'True'
      type: Available
    - lastTransitionTime: '2021-10-19T03:19:00Z'
      lastUpdateTime: '2023-10-11T09:55:33Z'
      message: ReplicaSet "erp-fi-78784b767" has successfully progressed.
      reason: NewReplicaSetAvailable
      status: 'True'
      type: Progressing
  observedGeneration: 934
  readyReplicas: 1
  replicas: 1
  updatedReplicas: 1
```

SpringBoot是微服务的基础，也是面试必问的技术，特别是自动配置原理，启动原理，以及条件注解实现原理🔥
今晚周瑜老师一节课带大家手写模拟SpringBoot底层实现，帮你顺利拿下十月面试！

读书
RocketMQ分布式消息中间件：核心原理与最佳实践.pdf
RocketMQ分布式消息中间件：核心原理与最佳实践 2020 (李伟).epub

```sql
EXPLAIN SELECT
IF
	( IFNULL( a1.cort_num_id, '' )= '', 0, a1.cort_num_id ) AS cort_num_id,
IF
	( IFNULL( a1.supply_num_id, '' )= '', 0, a1.supply_num_id ) AS supply_num_id,
IF
	( IFNULL( a1.org_unit, '' )= '', 0, a1.org_unit ) AS org_unit,
IF
	( IFNULL( a1.in_storage, '' )= '', 0, a1.in_storage ) AS in_storage,
IF
	( IFNULL( a1.buy_unit, '' )= '', 0, a1.buy_unit ) AS buy_unit,
IF
	( IFNULL( a1.sale_unit, '' )= '', 0, a1.sale_unit ) AS sale_unit,
IF
	( IFNULL( a1.inv_cort, '' )= '', 0, a1.inv_cort ) AS inv_cort,
IF
	( IFNULL( a1.pay_cort, '' )= '', 0, a1.pay_cort ) AS pay_cort,
IF
	( IFNULL( a1.out_storage, '' )= '', 0, a1.out_storage ) AS out_storage,
IF
	( IFNULL( a1.pur_officer_uid, '' )= '', 0, a1.pur_officer_uid ) AS pur_officer_uid,
IF
	( IFNULL( a1.create_user_id, '' )= '', 0, a1.create_user_id ) AS create_user_id,
IF
	( IFNULL( a1.last_update_user_id, '' )= '', 0, a1.last_update_user_id ) AS last_update_user_id,
	a1.series,
	a1.po_num_id,
	a1.owner_code,
	a1.pur_group,
	a1.pur_officer,
	a1.pur_tel,
	a1.supply_name,
	a1.theme,
	a1.split_type,
	a1.status_num_id,
	a1.confirm_status,
	a1.rec_status,
	a1.fi_status,
	a1.advance_status,
	a1.advance_approve_user,
	a1.delivery_date,
	a1.sup_confirm_date,
	a1.type_num_id,
	a1.order_date,
	a1.so_from_type,
	a1.pre_pay_amount,
	a1.audit_user_id,
	a1.audit_dtme,
	a1.create_dtme,
	a1.last_updtme,
	a1.cancelsign,
	a1.tenant_num_id,
	a1.data_sign,
	a1.rec_total_amount,
	a1.write_off_amount,
	a1.no_write_off_amount,
	a1.origin_place,
	a1.total_unpay_amount,
	a1.cort_name,
	a1.remark,
	a1.channel,
	round( sum( dtl.cost_price * dtl.qty ), 2 ) total_order_amount,
IF
	( IFNULL( a1.contract_code, '' )= '', 0, a1.contract_code ) contract_code,
	a1.payment_type 
FROM
	scm_rep_po_hdr a1
	LEFT JOIN scm_rep_po_dtl dtl ON a1.po_num_id = dtl.po_num_id 
	AND a1.cort_num_id = dtl.cort_num_id 
	AND a1.sub_unit_num_id = dtl.sub_unit_num_id 
	AND a1.tenant_num_id = dtl.tenant_num_id 
	AND a1.data_sign = dtl.data_sign 
WHERE
	a1.cancelsign <> 'Y' 
	AND a1.tenant_num_id = '18' 
	AND a1.data_sign = '0' 
	AND a1.cort_num_id = 'JY01' 
	AND DATE_FORMAT( a1.create_dtme, '%Y-%m-%d' ) >= '2023-09-09' 
	AND DATE_FORMAT( a1.create_dtme, '%Y-%m-%d' ) <= '2023-10-09' 
GROUP BY
	a1.po_num_id 
ORDER BY
	create_dtme DESC 
	LIMIT 0,
	10
```

问题
group by后聚合函数是如何处理空值的？
不带空值？
空值是0？

MySQL提供了许多聚合函数，包括AVG()，COUNT()，SUM()，MIN()，MAX()等。除COUNT函数外，其它聚合函数在执行计算时会忽略NULL值。

如果要统计null值，可以设置成0？

```sql
SELECT
	count(*) 
FROM
	(
	SELECT
		item_num_id,
		batch_id,
		expiry_date,
		date_format( create_dtme, '%Y-%m-%d' ) AS create_dtme,
		sum( purchase_qty ) AS purchase_qty,
		sum( sell_qty ) AS sell_qty,
		sum( return_qty ) AS return_qty,
		sum( change_qty ) AS change_qty 
	FROM
		(
		SELECT
			item_num_id,
			batch_id,
			expiry_date,
			create_dtme,
			sum( reserved_qty ) purchase_qty,
			0 AS sell_qty,
			0 AS return_qty,
			0 AS change_qty 
		FROM
			scm_acc_inv_own_stk_batch_loc_tsc 
		WHERE
			tenant_num_id = 18 
			AND data_sign = 0 
			AND cort_num_id = 'HG01' 
			AND sub_unit_num_id = 'G130' 
			AND bill_type_num_id = 'RECEIPT' 
		GROUP BY
			item_num_id,
			batch_id,
			expiry_date,
			date_format( create_dtme, '%Y-%m-%d' ) UNION ALL
		SELECT
			item_num_id,
			batch_id,
			expiry_date,
			create_dtme,
			0 AS purchase_qty,
			sum( reserved_qty ) AS sell_qty,
			0 AS return_qty,
			0 AS change_qty 
		FROM
			scm_acc_inv_own_stk_batch_loc_tsc 
		WHERE
			tenant_num_id = 18 
			AND data_sign = 0 
			AND cort_num_id = 'HG01' 
			AND sub_unit_num_id = 'G130' 
			AND balance_function = 111001 
		GROUP BY
			item_num_id,
			batch_id,
			expiry_date,
			date_format( create_dtme, '%Y-%m-%d' ) UNION ALL
		SELECT
			item_num_id,
			batch_id,
			expiry_date,
			create_dtme,
			0 AS purchase_qty,
			0 AS sell_qty,
			sum( reserved_qty ) AS return_qty,
			0 AS change_qty 
		FROM
			scm_acc_inv_own_stk_batch_loc_tsc 
		WHERE
			tenant_num_id = 18 
			AND data_sign = 0 
			AND cort_num_id = 'HG01' 
			AND sub_unit_num_id = 'G130' 
			AND bill_type_num_id = 'SHIP' 
		GROUP BY
			item_num_id,
			batch_id,
			expiry_date,
			date_format( create_dtme, '%Y-%m-%d' ) UNION ALL
		SELECT
			item_num_id,
			batch_id,
			expiry_date,
			create_dtme,
			0 AS purchase_qty,
			0 AS sell_qty,
			0 AS return_qty,
			sum( reserved_qty ) AS change_qty 
		FROM
			scm_acc_inv_own_stk_batch_loc_tsc 
		WHERE
			tenant_num_id = 18 
			AND data_sign = 0 
			AND cort_num_id = 'HG01' 
			AND sub_unit_num_id = 'G130' 
			AND bill_type_num_id IN ( 'RECEIPT', 'SHIP', 'STOCKADJUST' ) 
		GROUP BY
			item_num_id,
			batch_id,
			expiry_date,
		date_format( create_dtme, '%Y-%m-%d' )) tsc 
	GROUP BY
		item_num_id,
		batch_id,
	expiry_date,
	date_format( create_dtme, '%Y-%m-%d' )) T
```

```sql
SELECT
IF
	( IFNULL( a1.cort_num_id, '' )= '', 0, a1.cort_num_id ) AS cort_num_id,
IF
	( IFNULL( a1.supply_num_id, '' )= '', 0, a1.supply_num_id ) AS supply_num_id,
IF
	( IFNULL( a1.org_unit, '' )= '', 0, a1.org_unit ) AS org_unit,
IF
	( IFNULL( a1.in_storage, '' )= '', 0, a1.in_storage ) AS in_storage,
IF
	( IFNULL( a1.buy_unit, '' )= '', 0, a1.buy_unit ) AS buy_unit,
IF
	( IFNULL( a1.sale_unit, '' )= '', 0, a1.sale_unit ) AS sale_unit,
IF
	( IFNULL( a1.inv_cort, '' )= '', 0, a1.inv_cort ) AS inv_cort,
IF
	( IFNULL( a1.pay_cort, '' )= '', 0, a1.pay_cort ) AS pay_cort,
IF
	( IFNULL( a1.out_storage, '' )= '', 0, a1.out_storage ) AS out_storage,
IF
	( IFNULL( a1.pur_officer_uid, '' )= '', 0, a1.pur_officer_uid ) AS pur_officer_uid,
IF
	( IFNULL( a1.create_user_id, '' )= '', 0, a1.create_user_id ) AS create_user_id,
IF
	( IFNULL( a1.last_update_user_id, '' )= '', 0, a1.last_update_user_id ) AS last_update_user_id,
	a1.series,
	a1.po_num_id,
	a1.owner_code,
	a1.pur_group,
	a1.pur_officer,
	a1.pur_tel,
	a1.supply_name,
	a1.theme,
	a1.split_type,
	a1.status_num_id,
	a1.confirm_status,
	a1.rec_status,
	a1.fi_status,
	a1.advance_status,
	a1.advance_approve_user,
	a1.delivery_date,
	a1.sup_confirm_date,
	a1.type_num_id,
	a1.order_date,
	a1.so_from_type,
	a1.pre_pay_amount,
	a1.audit_user_id,
	a1.audit_dtme,
	a1.create_dtme,
	a1.last_updtme,
	a1.cancelsign,
	a1.tenant_num_id,
	a1.data_sign,
	a1.rec_total_amount,
	a1.write_off_amount,
	a1.no_write_off_amount,
	a1.origin_place,
	a1.total_unpay_amount,
	a1.cort_name,
	a1.remark,
	a1.channel,
	round( sum( dtl.cost_price * dtl.qty ), 2 ) total_order_amount,
IF
	( IFNULL( a1.contract_code, '' )= '', 0, a1.contract_code ) contract_code,
	a1.payment_type 
FROM
	scm_rep_po_hdr a1
	LEFT JOIN scm_rep_po_dtl dtl ON a1.po_num_id = dtl.po_num_id 
	AND a1.cort_num_id = dtl.cort_num_id 
	AND a1.sub_unit_num_id = dtl.sub_unit_num_id 
	AND a1.tenant_num_id = dtl.tenant_num_id 
	AND a1.data_sign = dtl.data_sign 
WHERE
	a1.cancelsign <> 'Y' 
	AND a1.tenant_num_id = '18' 
	AND a1.data_sign = '0' 
	AND a1.cort_num_id = 'JY01' 
	AND DATE_FORMAT( a1.create_dtme, '%Y-%m-%d' ) >= '2023-09-09' 
	AND DATE_FORMAT( a1.create_dtme, '%Y-%m-%d' ) <= '2023-10-09' 
GROUP BY
	a1.po_num_id 
ORDER BY
	create_dtme DESC 
	LIMIT 0,
	10
```




SELECT
	so_no,
	o2o_platform,
	o2o_status,
	o2o_action,
	channel_num_id,
	total_pay_money,
	freight,
	consumer_name,
	consumer_invoice_sign,
	create_dtme,
	stock_up_sign,
	tml_num_id,
	status_num_id,
	operate_status_num_id,
	tran_type_num_id,
	customer_so_no,
	verification_code,
	delivery_time 
FROM
	sd_bl_so_tml_hdr hdr 
WHERE
	tenant_num_id = 18 
	AND data_sign = 0 
	AND cancelsign = 'N' 
	AND so_from_type = 17 
	AND type_num_id = 1 
	AND cort_num_id = 'JY01' 
	AND sub_unit_num_id = 'S094' 
ORDER BY
	create_dtme DESC 
	LIMIT 0,
	10






SELECT
	count(*) 
FROM
	sd_bl_so_tml_hdr 
WHERE
	tenant_num_id = 18 
	AND data_sign = 0 
	AND cort_num_id = 'HG01' 
	AND sub_unit_num_id = 'G409' 
	AND status_num_id = 6 
	AND DATE_FORMAT( order_date, '%Y-%m-%d' ) = '2023-10-08' 
	AND cancelsign = 'N'


在MySQL的EXPLAIN结果中，select_type和type是两个不同的列，它们提供了有关查询执行计划的一些重要信息。

select_type：这个列描述了查询中的特殊查询类型。以下是select_type可能的值：

SIMPLE：这是最简单的情况，当查询只包含一个简单的SELECT语句，没有子查询或联合查询。
PRIMARY：当查询包含子查询，并且子查询不能与主查询分开时，主查询被标记为PRIMARY。
SUBQUERY：当主查询作为子查询的一部分，且子查询不能与主查询分开时，主查询被标记为SUBQUERY。
UNION：当两个或更多的SELECT语句合并在一起时，每个SELECT语句的查询计划都会被列出，并且这个查询被标记为UNION。
UNION ALL：类似于UNION，但是它不会去除重复的结果行。
DEPENDENT UNION：当UNION或UNION ALL的任何一个子查询依赖于主查询中的列或常量时，这个查询被标记为DEPENDENT UNION。
DEPENDENT UNION ALL：当UNION ALL的任何一个子查询依赖于主查询中的列或常量时，这个查询被标记为DEPENDENT UNION ALL。
NULL：当没有从主查询返回任何结果行时，这个查询被标记为NULL。

type：这个列描述了MySQL如何执行查询，给出了执行计划的详细信息。以下是type可能的值：
ALL：全表扫描。MySQL将检查整个表来寻找匹配的结果行。
index：全索引扫描。MySQL将扫描整个索引来寻找匹配的结果行。这通常比全表扫描快，但可能会增加内存使用。
range：范围扫描。MySQL将使用索引来查找满足某个范围的行。这是在你知道结果行在一个范围内时最常用的方法。
ref：索引引用。MySQL将使用索引来查找匹配某个单个列或多列的行。它通常比范围扫描和全表扫描快。
eq_ref：唯一索引引用。这表示MySQL使用一个唯一索引来查找一个精确匹配的行。这是最快的查找方法，但仅适用于唯一索引或PRIMARY KEY。
const、system和NULL：这些类型表示在执行查询时没有任何表或索引的读取操作。
这些类型的主要区别在于它们描述了MySQL是如何访问和查找数据的。理解这些类型可以帮助你优化查询性能，特别是当你注意到查询执行得非常慢时。

ifnull()
if()


在MySQL中，IFNULL()函数用于返回两个参数中的第一个非NULL参数。它的语法如下：

```
IFNULL(expr1, expr2)
```
其中，expr1和expr2是两个参数。如果expr1不是NULL，则返回expr1的值；如果expr1是NULL，则返回expr2的值。

下面是一个使用IFNULL()函数的示例：

```sql
SELECT IFNULL(column_name, default_value) FROM table_name;
```
在这个示例中，如果column_name的值为NULL，则返回default_value；否则，返回column_name的值。

另外，MySQL还提供了IF()函数，用于根据条件执行不同的操作。它的语法如下：

```sql
IF(condition, value_if_true, value_if_false)
```
其中，condition是要测试的条件，如果condition为真，则返回value_if_true；否则，返回value_if_false。

下面是一个使用IF()函数的示例：

```sql
SELECT IF(column_name > 10, 'greater than 10', 'less than or equal to 10') FROM table_name;
```
在这个示例中，如果column_name的值大于10，则返回'greater than 10'；否则，返回'less than or equal to 10'。


是的，MySQL 5.7对SQL语句是大小写敏感的。这意味着如果你在SQL语句中使用大写或小写字母，它们将被视为不同的字符。

例如，以下两个SQL语句在MySQL 5.7中是不同的：

```sql
SELECT * FROM mytable;  
SELECT * FROM MYTABLE;
```
第一个SQL语句将选择"mytable"表中的所有行，而第二个SQL语句将选择"MYTABLE"表中的所有行（如果存在）。这是因为MySQL将大写和小写视为不同的字符。

然而，在某些情况下，MySQL对表和数据库的名称不区分大小写。例如，以下两个SQL语句在MySQL 5.7中是等效的：

```sql
USE mydatabase;  
USE MYDATABASE;
```
这因为在MySQL中，表名和数据库名不区分大小写。但是，请注意，表的列名和函数名是区分大小写的。

需要注意的是，MySQL的默认设置是不区分大小写，但也可以通过设置适当的配置参数来更改大小写敏感性。

explain_result.png

202308 202309月份，也太摸鱼了

看公司代码，每一行代码都需要了解含义，没看懂的话需要问别人

中行这种大型企业，it项目必须要有人维护，还需要分a b角色
翼支付，国家法定节假日只能是正式员工值班，不能是外包员工值班

实现了四个标准的隔离级别，默认级别是可重复读(REPEATABLE READ)。在可重复读隔离级别下，通过多版本并发控制(MVCC)+ 间隙锁(Next-Key Locking)防止幻影读。

全亿健康，昨晚账期支付上线，搞到了晚上12点
