# 20231207

	<select id="getNotInvoiceAmt" parameterType="map" resultType="java.math.BigDecimal">


下面是一个例子：
对
	<select id="getInvoiceTdDetail" parameterType="map" resultType="map">
		select INVOICE_TYPE,
			   INVOICING_TIME,
			   INVOICE_CODE,



错
	<select id="getInvoiceTdDetail" parameterType="map" resultType="list">
		select INVOICE_TYPE,
			   INVOICING_TIME,
			   INVOICE_CODE,




{"ID":"S20231206105902018320000000004"}


AUDIT_STATUS
审批中 1  01
审批拒绝 2 03
已撤销 3 04
审批通过 4 02


01待审批
02已通过
03已拒绝
04已撤销
05已退回
06已删除


ALTER TABLE `HRO_PROTOCOL_INFO_TD` 
MODIFY COLUMN `AUDIT_STATUS` varchar(8) CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NULL DEFAULT NULL COMMENT '审批状态 01待审批 02已通过 03已拒绝 04已撤销 05已退回 06已删除' AFTER `AUDIT_CODE`;



	@Autowired
	private InitFieldsOp initFieldsOp;

出现的问题：发票的需求，详细设计的不对，代码写完了，还需要改

详细设计好跟鲁华对一下

每天都有工作成果

在办公室，不要大声说话，轻声细语

浦发银行的客户来办公室