# 20231214

strings /usr/local/lib/libpistache.a | c++filt
nm



cd /Users/ibqo/.ssh

ssh-keygen -t rsa -C "sandisks555@gmail.com" -f id_rsa_aliyun2_wdidada
odb-mysql库，需要看文档，谷歌搜索不到资料，还要注意版本的区别
odb --version
