# 202312_week4

## 时间范围

- 202312 月第 4 周

## 本周记录

- 20231222：jackson扩展性强；对json标准的支持力度大；架构师思维
- 20231223：补充了当日日志，暂未提炼出明确主题。
- 20231224：bazel仓库；c/c++代码打包deb rpm msi exe文包含哪些内容；配置文件 AndroidMainfest.xml
- 20231225：有没有MySQL数据库大佬MySQL_store_result()的结果在什么时候会返回null呢？数据库语句正确；但是数据库是空的；为啥返回的不是null呢
- 20231226：IDEA自带的代码质量检查；易道云c++全套.jpg；cd boost_*/
- 20231227：@PostMapping(path = "/queryEmployeesFilter")；public PagingExtResponse<InsuredEmployeeVO> queryEmployeesFilter(@ApiIgnore @SessionValue(SessionAttrs.CURRENT_ACCOUNT) CurrentAccount account, @RequestBody InsuredQueryFilter filter) {；springmvc http接口绑定的方法，增加注解，获取值
- 20231228：鲁棒是Robust的音译，也就是健壮和强壮的意思。它是在异常和危险情况下系统生存的能力。比如说，计算机软件在输入错误、磁盘故障、网络过载或有意攻击情况下，能否不死机、不崩溃，就是该软件的鲁棒性。所谓“鲁棒性”，也是指控制系统在一定（结构，大小）的参数摄动下，维持其它某些性能的特性。根据对性能的不同定义，可分为稳定鲁棒性和性能鲁棒性。以闭环系统的鲁棒性作为目标设计得到的固定控制器称为鲁棒控制器。；github actions编译；这个项目大量使用c++ modues
