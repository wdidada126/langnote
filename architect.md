# architect

通常架构师/开发认为语言只是实现细节，重点是业务建模思想

DCI

MVC模式已死
MVC模式将被DCI架构替代，控制器被Context替代。
DCI(Data Context Interaction)

DCI（Data Context Interaction）架构是一种特别关注行为的模式，它强调将数据、场景和交互结合在一起，以便更好地理解和设计软件系统。
在DCI架构中，数据是核心，场景是数据的表现形式，而交互则是场景之间的联系。通过将这三者结合在一起，DCI架构可以帮助开发人员更好地理解软件系统的行为，并为其设计出更加高效、可靠和易于维护的解决方案。
DCI架构的出现主要是为了解决一些传统架构模式（如MVC模式）的问题。MVC模式虽然被广泛使用，但随着Web 2.0、社会化媒体以及游戏等频繁交互应用的发展，它已经不适合高度交互注重行为的应用了。相比之下，DCI架构更加关注行为模式的设计和实现，因此更适合于现代高度交互的应用。
总之，DCI架构是一种关注行为模式的架构，它强调数据、场景和交互之间的联系和组合，以便更好地设计和实现软件系统。

尽管在开源项目中，DCI（Data Context Interaction）架构的实现并没有直接对应的项目，但你可以在一些主流的开源项目中看到DCI架构的理念和精神的应用。
例如，在一些框架或库的设计和实现中，可能会把数据（Data）、上下文（Context）和交互（Interactions）作为核心考虑因素，从而构建出更加清晰、灵活和易于维护的代码结构。
举一个例子，Spring框架中的Spring MVC就非常注重模型（Model）、视图（View）和控制器（Controller）的分离，这种分离在一定程度上体现了数据、上下文和交互的关注点。
再比如，在数据库交互层面，像JPA（Java Persistence API）这样的ORM（Object-Relational Mapping）框架，也体现了数据和交互的核心思想。
虽然并没有直接体现DCI架构的开源项目，但你可以借鉴各种优秀开源项目的经验，结合DCI架构的理念，来设计和实现你的应用。

阿里中台架构图 注意图
https://zhuanlan.zhihu.com/p/81410765

monolithic(巨石、整体性和铁板一块)架构切换到Go语言 microservice(微服务)架构



架构师 书籍

https://www.zhihu.com/answer/245214823





https://www.cnblogs.com/zhoujg/archive/2010/01/23/1654721.html





《Expert One-on-One J2EE Design and Development》

这本书是Rod Johnson的成名著作，非常经典，从这本书中的代码诞生了springframework



《敏捷软件开发原则、模式与实践》

Uncle Bob的名著，敏捷的经典名著，这本书比较特别，与其说是讲软件开发过程的书，不如说讲软件架构的书，本书用了很大篇幅讲各种面向对象软件开发的各种模式，个人以为看了这本书，就不必看GoF的《设计模式》了。



《DAMA数据管理知识体系指南》

DAMA数据资产管理体系

TOGAF架构开发方法论


参加大牛的技术分享会

https://www.zhihu.com/answer/1209698711

Expert One-on-One J2EE Design and Development






Redis 是一个优秀的高性能分布式缓存服务器：在实际应用场景中，每秒 QPS 能够达到 4.5 万 ～5 万



绝大多数时候系统的瓶颈在架构和数据库



**亚军 异地双活**

CZone Qzone RZone

单元化



**程序员转型架构师，推荐你读这几本**



**一、方法论**

**《架构整洁之道》**

**《领域驱动设计》**

**《微服务设计》**

**《架构即未来：现代企业可扩展的Web架构、流程和组织》**

**二、架构落地实践**

**《淘宝技术这十年》**

**《分布式服务框架：原理与实践》**

**《大型网站技术架构：核心原理与案例分析》**

**《大型网站系统与Java中间件实践》**

**《企业IT架构转型之道：阿里巴巴中台战略思想与架构实战》**



**《尽在双11：阿里巴巴技术演进与超越》**

比起语言，切换更累的是工作方式，比如一个以前搞敏捷开发的，转成V周期开发，或者去搞形式化开发。
awk处理json 
**UNICODE,GBK,UTF-8区别**
unicocd是传输，显示的格式
UTF-8是存储格式
https://www.cnblogs.com/gavin-num1/p/5170247.html





一台 32 核 64GB 内存的机器，性能测试数据显示每秒处理 Hello World 的 HTTP 请求不超过 2 万，实际业务机器处理HTTP请求每秒可能才几百 QPS，而互联网业务并发超过 1 万是比较常见的，遇到双十一、过年发红包这些极端场景，每秒可以达到几十万的请求。




https://github.com/toutiaoio/awesome-architecture




- CRG架构（支付宝
- 微服务



微服务 单进程的？



**对防火墙安全策略的检查**

防火墙可以用来保护重要数据免遭非法访问和限制黑客攻击范围。防火墙的一般原理是允许信任的流量通过，拒绝其余所有不信任的流量。

在图 1 中，防火墙 1 上面是互联网，我们一般称之为红区（red zone），是最不受控的区域。

防火墙 1 和 防火墙 2 之间，我们一般称之为黄区（yellow zone），也叫隔离区 (DMZ)，一般 Web 服务器，负载均衡服务器位于此区。

防火墙 2 和 防火墙 3 之间，我们一般称之为绿区（green zone），一般放置应用服务器和数据库服务器。

防火墙 3 之下，我们一般称之为蓝区（blue zone）， 蓝区一般和企业内网直连，一般放置测试环境和登台服务器（staging server）。

一般有两种类型的防火墙，一种是 协议防火墙，基于 TCP/IP 协议或 HTTP 协议去过滤，另一种是 XML 防火墙，基于 XML 的内容来过滤。目前大部分生产环境使用协议防火墙，本文将针对协议防火墙来论述。



ibm 架构师技术路线

ibm oracle 认证



https://www.cnblogs.com/sucia-panda/p/7874300.html

http://www.ithov.com/server/127372_8.shtml


https://www.sohu.com/a/361845189_465221

Ionut Balosin

Luxoft 















感觉是个机会记录一下几年前在果壳网期间做架构设计方面的积累了。

首先要定义问题，千万PV是什么概念？每天一千万PV，以正常网站高峰期经验系数每天10小时计算，对应的是278qps(reQuest Per Second)。考虑到当前的网站大多有Ajax API、爬虫、网络扫描等不会记录PV的请求，可以取整到1000qps。这1000qps就是网站要完成的动态页面请求数，但不包含静态文件请求。静态文件虽然数量更多，但对服务器负载几乎没有影响，尤其是用好CDN更是可以忽略不计了。

很多人看到1000qps其实心底就有底了，感觉也不是很多。当然这要分两方面考虑。一来是当代很多Web开发框架，都可以轻松在单核心下跑无后端时的处理能力超过1000qps。更何况可以用缓存。反之，如果是没有后端请求(数据库、数据中间层等)，那也就无所谓动态网站了。所以，对于这个1000qps，我们可以做这样一个归纳，就是平均每个页面有20-60个动态数据项是来自于数据库的，缓存另算。算个平均数40吧，那么意味着无缓存情况下数据库的负载就是40000qps。从这个数字看就超出很多数据库的负载能力了。所以要把整个系统做到目标，缓存是必不可少的。

果壳网首次上线时，最初的两周是没有缓存的，那也是时间所限的无奈选择。也正是那时，让我见识到了一个完全没有缓存的动态网站可以慢到什么地步。一个首页的打开要10秒往上，甚至20秒也不奇怪。真是让人十分痛苦。这也是源于首页动态加载数据项过多，有近百个数据库请求。所以在用户量不大的时候也让人难以忍受。

总的来说，要实现目标要做的步骤如下：


1. 页面拆分：一个页面如果整体缓存，也意味着整体缓存失效后的全部查询都要重做，所以将页面拆分成多个模块，分别缓存。在用户请求时再高效率的组装起来响应就成了高效的方法。选择合适而高性能的页面组装方案就显得很重要了。

2. 分块缓存：如上条所讲，各个资源分块缓存就成了必然。缓存块足够小带来很多好处，比如小块资源重新查询的速度也很快，缓存体系的控制也更容易。各个小块可以错开缓存溅出也避免了整个页面重新加载带来的大量密集请求。

3. 高频页面静态化：在流量高峰到达时，可以用的一个临时处理方案就是把高频访问页面临时改为静态页面。比如首页，或者一些列表页。生成HTML后，用nginx做URL映射到HTML文件，可以顶得住很高的压力。带来的坏处就是要在这段时间手工维护页面更新。但这类静态页面顶到100000qps都很容易，压力也很小。

4. 静态资源版本化：这是个很大的话题，就是将静态资源做成浏览器永久缓存，避免非临时静态文件缓存的304请求。对于一个大型网站，客户端发来的304请求也是个灾难。2008年晚期的土豆网就曾经为此郁闷，并交由我开发了土豆网的静态资源版本化系统。想要静态资源永久缓存，但又要面对不定期的网站更新和改版，就需要在每次修改静态资源后，直接使得该资源的URL也修改，使得浏览器可以通过加载不同的URL来获得最新版。而静态资源版本化就是负责给每个静态资源的URL加上个版本号，让HTML页面引用带有版本号的静态资源。




正是通过如上的若干技术手段，果壳网在2011年早期的一次流量高峰中，单台服务器轻松顶过了470万PV。且当时CPU、内存、硬盘带宽等若干指标占比不到50%。

页面拆分和组装方面使用的nginx SSI技术，也后来发展成了果壳网的商务专区系统。使得BD可以轻松的通过修改HTML页面就能像拼装积木一样生成各种商务专区，而且性能高的一塌糊涂，稳定的请求性能可以在Dell R620上跑到4500qps。上文所用到的缓存体系和静态资源版本化的主题过大，会在后续文章仔细讨论。

nginx SSI是Server Side Include，类似于网页模板技术。使用类似HTML注释的语法，通过一个URL来加载页面的一部分，最终是在nginx内部完成整个页面的组装。功能极其简单，带来了极高的效率。使得相对于整个流程，SSI运行时间可以忽略不计。

在2010年之前的几年，几位做架构的朋友就已经做到了单服务器千万PV负载能力，虽然技术方案略有差别，但总体思路无外乎如上几点。在2008年那个时代，单服务器千万PV，单聊天服务器千万连接，都是一个架构师的无上荣耀。待我2011年做到时，硬件已经强大了许多，但这样的成绩还是让我很高兴的。

在云计算开始流行以后，不仅对运维工程师的需求少了。就连对后端工程师的需求也在减少。而后端工程师是通往架构师之路上最直接的途径。让人很伤心的是，这些年云计算的流行，使得后端工程师和架构师的产量都在明显下降，越来越多的平台稳定性也开始成为了问题。甚至一些云计算平台都开始面临招聘难题。也正是在这样的环境里，各个公司的后端事故频出。前些天的Amazon AWS S3故障的发生虽然事出偶然，但若仔细考察Amazon AWS下若干服务的设计和稳定性，这又是必然的。比如AWS Cognito的设计就很成问题。

*Cognito* ( *AWS* 的身份认证解决方案)




大部分互联网服务都是io型的（与其它服务、数据库、缓存、队列的通信），很多性能瓶颈都是传统基于socket的阻塞框架导致的。io模型一换，基本上基础层面能优化的就不多了，更多的还是看读写架构设计。



支付宝的架构
我想关心分布式系统设计的人都曾被下面这些问题所困扰过：

支付宝海量支付背后最解渴的设计是啥？换句话说，实现支付宝高 TPS 的最关键的设计是啥？
LDC 是啥？LDC 怎么实现异地多活和异地灾备的？
CAP 魔咒到底是啥？P 到底怎么理解？
什么是脑裂？跟 CAP 又是啥关系？
什么是 PAXOS，它解决了啥问题？
PAXOS 和 CAP 啥关系？PAXOS 可以逃脱 CAP 魔咒么？
Oceanbase 能逃脱 CAP 魔咒么？




https://zhuanlan.zhihu.com/p/102537064




LDC（logic data center）与传统的（Internet Data Center-IDC）架构对比与优势（淘宝双十一经典案例解析）

https://blog.csdn.net/unforgettable2010/article/details/104337894



支付宝将单元分成了三类（也称 CRG 架构）：



- **RZone（Region Zone）：**直译可能有点反而不好理解。实际上就是所有可以分库分表的业务系统整体部署的最小单元。每个 RZone 连上数据库就可以撑起一片天空，把业务跑的溜溜的。
- **GZone（Global Zone）：**全局单元，意味着全局只有一份。部署了不可拆分的数据和服务，比如[系统配置等。实际情况下，GZone 异地也会部署，不过仅是用于灾备，同一时刻，只有一地 GZone 进行全局服务。GZone 一般被 RZone 依赖，提供的大部分是读取服务。](https://link.zhihu.com/?target=https%3A//jq.qq.com/%3F_wv%3D1027%26k%3D54YpQCg)
- **CZone（City Zone）：**顾名思义，这是以城市为单位部署的单元。同样部署了不可拆分的数据和服务，比如用户账号服务，客户信息服务等。理论上 CZone 会被 RZone 以比访问 GZone 高很多的频率进行访问。
  CZone 是基于特定的 GZone 场景进行优化的一种单元，它把 GZone 中有些有着”写读时间差现象”的数据和服务进行了的单独部署，这样 RZone 只需要访问本地的 CZone 即可，而不是访问异地的 GZone。



程序员转型架构师，推荐你读这几本

一、方法论
《架构整洁之道》
《领域驱动设计》
《微服务设计》
《架构即未来：现代企业可扩展的Web架构、流程和组织》
二、架构落地实践
《淘宝技术这十年》
《分布式服务框架：原理与实践》
《大型网站技术架构：核心原理与案例分析》
《大型网站系统与Java中间件实践》
《企业IT架构转型之道：阿里巴巴中台战略思想与架构实战》

《尽在双11：阿里巴巴技术演进与超越》



第1层：能做成东西（能运行）

第2层：做的东西能长时间或高负荷地运行

第3层：做的东西能长时间在高负荷下运行


第4层：能预先知道什么才是客户/行业需要的功能，并以最符合的代价（金钱、硬件、期限、人力）实现



一般应用来说，并发估算公式如下：

qps = 5 * 日pv / 86400

5是通用峰值倍数，如果你有高峰值特性自行调整，比如秒杀功能。

mysql通常在实体机的读写综合qps在几千左右，具体你可以自己压测一下，跟机器配置有关。

一般除以10万



软件架构方面 为什么要做到单元化

更高性能更低成本

当然除了性能收益，如果你做到了，你会发现还有很多收益，比如更好的隔离性，包括请求隔离和资源隔离，比如更友好的升级，产品可以灰度发布等。

https://www.infoq.cn/article/how-weibo-do-unit-architecture



单元化架构，为性能和隔离性而设计，上层节点访问指定下层节点



粉丝服务平台是微博的内容推送系统（代号 Castalia 实现了单元化







数据库连接不复用

数据库连接池爆了，好多库要连接



在于数据库连接不够用了。一个应用可能有多个事务，主流的商业数据库，连接都不是共享的，就是说一个事务必须独占一个连接。



OceanBase 在当时如果成熟，可以很好的解决 Sharding 带来的连接数瓶颈问题，因为 OceanBase 是分布式数据库，连接可以共享。



摩尔定律失效，cpu都在加核心数，单机性能到达极限了

具体:sql语句执行耗时太长

有重新开发数据库的，tidb

有用中间件的

本质上由原来的单机，变成集群

SQl parse



[如何通过SQL语句生产干净的AST树](https://www.zhihu.com/question/61569096/answer/319129542)



银行 Aix Oracle



异地多活架构的应用场景



备份系统平常没有流量，如果直接上线可能触发平常测试不到的故障。

再实时的系统也会有数据延时，如果涉及到金融这种系统，仍然是不敢直接切换的。

系统运行过程中会有很多中间数据，缓存数据等。系统不经过预热直接把流量倒过来，大流量会直接把系统拖垮。



异地多活架构的应用场景



备份系统平常没有流量，如果直接上线可能触发平常测试不到的故障。

再实时的系统也会有数据延时，如果涉及到金融这种系统，仍然是不敢直接切换的。

系统运行过程中会有很多中间数据，缓存数据等。系统不经过预热直接把流量倒过来，大流量会直接把系统拖垮。



2019年10月9日，某部委人士在公开会议上指出，“OceanBase测试指标虽高，但在关键领域仍不能使用”、“互联网和银行场景完全不同”、“不能支持跑批（批处理业务）”。问题本质是“什么样的分布式数据库在关键领域可用”？

 

从用户的角度，答案很明确，兼容Oracle功能且满足性能要求。兼容Oracle，意味着“不改造应用系统无缝升级模式”，用户责任小，风险低。满足性能要求，意味着业务可运行。

 

那OceanBase是不是这样一个产品呢？

 

先说Oracle的兼容性：

 

数据库核心功能，OceanBase在分布式架构下，不兼容Oracle的存储过程、触发器、视图、多表关联、大表关联等常用数据库核心功能，需要通过大规模改造应用系统来弥补功能缺口，工程繁复，且不保证改造一定成功；

 

隔离等级，OceanBase不支持Oracle的隔离等级“可重复读”，存在不可知数据错误风险及高失败率；
锁机制，和Oracle严苛锁机制相比，OceanBase是松散锁机制，在有数据冲突的金融场景，必然导致跑批（批处理业务）中断，存在业务连续性风险；

结论：OceanBase完全不兼容Oracle，其缺口源于结构性差异，不可能通过适配解决。

再说性能，分布式数据库性能的关键是处理分布式事务的效率：

两次tpc-c测试，分布式事务均不是由OceanBase数据库完成的。按tpc-c规则，存在随机15%和1%跨仓交易，如果完全随机，总交易量的6.896%，即8小时共有520.017798亿个交易，成为跨数据库节点的分布式事务。蚂蚁金服披露“OceanBase1557节点集群时，压测tpmC/理论tpmC=0.987”，集群与单机相比性能0损耗，即分布式架构却完全没有分布式开销，显然tpc-c测试里的分布式事务不是由OceanBase数据库节点完成的。
2019年6月，中国信通院和中国软件评测中心搞过一次分布式数据库的公开摸底考试，不允许大规模修改应用系统，OceanBase性能不佳，没有进入复试。

支付宝场景，有专业人士认为：“网络支付场景，更多是连接，而资金的清算早期在商业银行，现在在人行网联平台，而非支付公司。相反，说明银行的核心系统大有进步。”支付场景与金融场景差异明显，OceanBase分布式事务能力仍需证明。

OceanBase多个外部测试场景，目前均未见到OceanBase单独完成分布式事务，更多是由应用系统分担，OceanBase作为数据存储。

高斯分布式数据库与OceanBase同属一类，实战效果不佳，已下架。

小结：没有直接证据证明OceanBase分布式事务处理性能。
综上所述，OceanBase完全不兼容Oracle，分布式数据库性能尚待证明。结构上更像是一个数据库存储而非完整数据库，就像没有发动机的裸底盘，替换高端整车Oracle缺乏理论支撑和实践证明。


## 周志明 凤凰架构

硬件，网络，开发人员能力有相对偏弱的，造成it系统不稳定
it系统不稳定，所以要容错，falt tolr
为了达到ha，各个组件是可以替换的
健康检查，剔除不健康的节点，在适当时机重启某个节点的服务
