# 20200102

cmake官网文档  ---  CMake的设计目的是跨平台 支持Windows Linux MacOS，甚至是Android

不同平台 c/cpp的编译器 IDE不同

CMake 生成Xcode VS配置文件

`cmake .. -G "Xcode"`

`cmake .. -G "Visual Studio 14 2015 Win64"`

cmake -makefile make

ninja

cmake支持make ninja

.cmake 文件

xmake lua

ss
openstrace
mockio

Mockito是一个针对Java的mocking框架

https://github.com/mockito/mockito

