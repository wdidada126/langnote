# 20201016

caict 中国信通院

腾讯联合科大讯飞，TARS 正式支持 Google ProtoBuf
讯飞用tars

mybatis
1、mybatis-spring区别开来，虽然工作上用spring比较多，用Spring的公司基本都会用mybatis-spring
2、mybatis的分页 mybatis pagehelper
如何配置分页插件呢，有两种方式，一种是作为mybatis的插件配置，另一种是作为spring的拦截器配置

https://blog.csdn.net/zhaozhirongfree1111/article/details/84955858

3、mybatis单独使用
xml配置 java配置
xml配置中的namespace

MySQL的自增ID用完了，怎么办
https://mp.weixin.qq.com/s/yoBfSMNWpvlMjxuObeA9wA

17:28:03    insert into t1 **values**(**null**) Error Code: 1062. Duplicate entry '4294967295' **for** key 'PRIMARY'    0.00054 sec

由于这里定义的是intunsigned，所以最大可以达到2的32幂次方-1=4294967295

解决办法
**建议采用 bigint unsigned**，这个数字就大了。
**不过，还存在另一种情况，如果在创建表没有显示申明主键，会怎么办？**
如果是这种情况，InnoDB 会自动帮你创建一个不可见的、长度为 6 字节的 row_id，而且 InnoDB 维护了一个全局的 dictsys.row_id，所以未定义主键的表都共享该 row_id，每次插入一条数据，都把全局 row_id 当成主键 id，然后全局 row_id 加 1
该全局 row_id 在代码实现上使用的是 bigint unsigned 类型，但实际上只给 row_id 留了 6 字节，这种设计就会存在一个问题：**如果全局 row_id 一直涨，一直涨，直到 2 的 48 幂次 - 1 时，这个时候再 + 1，row_id 的低 48 位都为 0，结果在插入新一行数据时，拿到的 row_id 就为 0，存在主键冲突的可能性。**