# 20200911

sql查询大量数据json序列化打印日志导致dubbo线程池满，cpu占用率100%

解决方案sql分页查询

相关的问题，如何写出一个占用cpu100%的程序
写出一个占用CPU100%的程序在很多情况下并不是一个实际的需求，因为这样的程序会使得你的电脑变得非常缓慢，甚至无法进行其他任务。如果你只是想测试电脑的CPU性能，那么你可能需要一个像Cinebench这样的基准测试工具。

然而，如果你是在学习或者研究目的下尝试创建高CPU使用率的程序，以下是一个简单的Python程序，它使用了无限递归。这个程序会一直运行，直到被强制停止。
```python
python
def recursive_function():
    recursive_function()
```
recursive_function()
当你运行这个程序时，它会一直调用自身，占用大量的CPU资源。

但请注意，这是一个非常简单的例子，并且在实际开发中，我们通常会避免创建这样的程序，因为它们可能会导致系统崩溃或者其它不可预期的后果。
另外，如果你想让程序占用特定比例的CPU资源，而不是全部资源，那么你需要更复杂的编程技巧，这通常涉及到使用多线程或异步编程，以及控制线程或进程的数量和运行时间。
递归调用，循环调用


dubbo线程池相关代码netty线程池https://www.cnblogs.com/xhj123/p/9095278.html
Dubbo学习笔记8：Dubbo的线程模型与线程池策略-徐浩进-博客园.mhtml

类似的问题：springmvc线程池

tomcat线程池

SpringMVC框架使用了Spring的线程池实现，具体实现可以参考ThreadPoolTaskExecutor类。
在SpringMVC中，线程池的使用通常是在配置文件中进行配置，而不是在代码中直接使用。下面是一个示例配置：

```java
@Configuration
public class TaskExecutorConfig {  
    @Bean("taskExecutor")  
    @ Qualifier("taskExecutor")  
    public TaskExecutor taskExecutor() {  
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();  
        executor.setCorePoolSize(5);  
        executor.setMaxPoolSize(10);  
        executor.setQueueCapacity(25);  
        executor.setThreadNamePrefix("taskExecutor-");  
        executor.initialize();  
        return executor;  
    }  
}
```
在上述代码中，我们定义了一个名为taskExecutor的TaskExecutor实例，并将其注入到需要使用它的组件中。ThreadPoolTaskExecutor是Spring提供的线程池实现，我们可以设置线程池的核心线程数、最大线程数、队列容量和线程名称前缀等参数。
在Spring MVC中，控制器方法通常不会直接使用线程池，而是通过使用@Async注解将方法标记为异步执行。例如：
```java
@Service  
public class MyService {  
    @Autowired  
    private TaskExecutor taskExecutor;  
  
    @Async  
    public void asyncMethod() {  
        // 异步执行的方法体  
    }  
}
```
在上述代码中，我们使用了@Autowired注解将TaskExecutor注入到MyService中，然后在需要异步执行的方法上添加了@Async注解。当调用该方法时，该方法会在一个新的线程中异步执行，并且使用了我们配置的线程池。
Tomcat的线程池是在Tomcat启动时初始化的，并创建一定数量的线程，这些线程会被放在线程池中等待执行。当有请求到达时，Tomcat的连接器会接收请求，并将其传递给线程池。线程池中的线程会被调度来处理请求。如果线程池中有空闲线程，将会有一个线程去执行该请求。如果线程池中没有可用线程，请求将进入等待队列。请求处理完成后，线程将返回到线程池中，变为可用状态，以供处理下一个请求。
Tomcat线程池的工作原理可以总结为以下步骤：
初始化线程池：在Tomcat启动时，会初始化线程池，并创建一定数量的线程。
接收请求：当客户端发起请求时，Tomcat的连接器将接收到请求，并将其传递给线程池。
线程池调度：线程池中的线程会被调度来处理请求。如果线程池中有空闲线程，将会有一个线程去执行该请求。如果线程池中没有可用线程，请求将进入等待队列。
请求处理：选中的线程会处理请求，包括解析请求、执行相应的业务逻辑等。该线程会一直处理请求直到完成。
请求完成：处理完请求后，线程将返回到线程池中，变为可用状态，以供处理下一个请求。
等待队列：如果线程池中没有空闲线程，新到达的请求将会进入等待队列。请求按照先到先服务的顺序排队等待处理。等待队列的长度由acceptCount参数决定。
线程重用：线程池中的线程会被重复使用，避免了频繁创建和销毁线程的开销。这个点跟普通线程池不一样。

服务端程序，考虑cpu使用了，多线程，甚至是协程

Dubbo

容错机制

深入理解ApacheDubbo与实战

Netty实战

Netty权威指南（第2版）

DirectByteBuffe
Channel
ChannelHandler
ChannelPinpile
ChannelInHandler
ChannelOutHandler



Netty底层基于JDK 的NIO，我们为什么不直接基于JDK的NIO或者其他NIO框架：
使用JDK自带的NIO需要了解太多的概念，编程复杂。
Netty底层IO模型随意切换，而这一切只需要做微小的改动。
Netty自带的拆包解包，异常检测等机制让我们从 NIO 的繁重细节中脱离出来，只需关心业务逻辑即可。
Netty解决了JDK 的很多包括空轮训在内的 Bug。
Netty底层对线程，Selector 做了很多细小的优化，精心设计的 Reactor 线程做到非常高效的并发处理。

自带各种协议栈，让我们处理任何一种通用协议都几乎不用亲自动手。
Netty社区活跃，遇到问题随时邮件列表或者 issue。
Netty已经历各大RPC框架（Dubbo），消息中间件（RocketMQ），大数据通信（Hadoop）框架的广泛的线上验证，健壮性无比强大。
Netty所用的堆外内存只是Java NIO的DirectByteBuffer类
https://blog.csdn.net/u010359663/article/details/84816103



Boostrap



自定义解码器 编码器 tcp udp http websocket 自定义协议

线程池



netty 3/4 

org.jboss.netty

io.netty



NioServerSocketChannel

https://www.jianshu.com/p/97d38824b444



lib_netty_transport_native_epoll_x86_64.so



https://netty.io/wiki/native-transports.html



\- bootstrap

\- buffer

\- channel

\- traffic

\- util



找一下netty sample 自己写的 https://github.com/edidada/testnetty

https://github.com/edidada/Netty-study

https://github.com/edidada/nettybook2



workGroup select

bossGroup  io



Output

Input



尽管本书是专门介绍NIO框架Netty的，但是，并不意味着所有的Java网络编程都必须要选择NIO和Netty，具体选择什么样的IO模型或者NIO框架，完全基于业务的实际应用场景和性能诉求，如果客户端并发连接数不多，周边对接的网元不多，服务器的负载也不重，那就完全没必要选择NIO做服务端；如果是相反情况，那就要考虑选择合适的NIO框架进行开发。

http://ifeve.com/author/linfeng/

java nio 1 1.4

java nio 2 1.7



没事多看看netty doc

https://netty.io/4.1/api/index.html



https://netty.io/wiki/user-guide-for-4.x.html

netty example 指南



netty

4.1.43.Final 需要jdk11

jdk8不行



os.detected.classifier  环境变量

${os.detected.classifier}

https://www.oschina.net/p/os-maven-plugin

https://github.com/trustin/os-maven-plugin 说明书



io.netty.buffer.Unpooled 静态方法 构造Bytebuf



io.netty.channel.ChannelDuplexHandler



ChannelHandler



ChannelHandler用于处理Channel对应的事件

 ChannelHandler接口里面只定义了三个生命周期方法，我们主要实现它的子接口ChannelInboundHandler和ChannelOutboundHandler，为了便利，框架提供了ChannelInboundHandlerAdapter，ChannelOutboundHandlerAdapter和ChannelDuplexHandler这三个适配类，在使用的时候只需要实现你关注的方法即可。

https://www.jianshu.com/p/96a50869b527



模型相关的，都是不可重复的



安卓蓝牙向windows 10发送文件，windows 10需要准备接受



Netty权威指南



c/c++中有channel概念

Unix的5种I/O模型



ByteBuf  缓冲区



Char short buf
一个多路复用器Selectior可以对接多个Channel
突破1024/4028个数限制



Channel子类



ServerSocketChannel



netty 已经实现http协议





Nettt api

BiteBuf



Java api

BiteBuffer

二者不能混淆

Netty 引用计数算法