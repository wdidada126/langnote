# 20190731

grep 'IsomerizationAccessServiceImpl' isomerization-proxy.2019-07*.log | grep access | grep PARAMETER | grep contracts-ocr -c
grep 'IsomerizationAccessServiceImpl' isomerization-proxy.log | grep access | grep PARAMETER | grep contracts-ocr -c
