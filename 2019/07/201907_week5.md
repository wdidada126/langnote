# 201907_week5

## 原有内容


## 时间范围

- 201907 月第 5 周

## 本周记录

- 20190729：数据库索引；primary key；unique key
- 20190730：学习grep保存信息到文件；[grep查找的内容输出到文件](https://my.oschina.net/calmsnow/blog/2962160)；grep -o OcrServiceImpl authcenter-medium-center.2019-07-28.*.log -c >> ./log/count.log
- 20190731：grep 'IsomerizationAccessServiceImpl' isomerization-proxy.2019-07*.log | grep access | grep PARAMETER | grep contracts-ocr -c；grep 'IsomerizationAccessServiceImpl' isomerization-proxy.log | grep access | grep PARAMETER | grep contracts-ocr -c

## 整理补充

## 时间范围

- 201907 月第 5 周

## 本周记录

- 20190729：数据库索引；primary key；unique key
- 20190730：学习grep保存信息到文件；[grep查找的内容输出到文件](https://my.oschina.net/calmsnow/blog/2962160)；grep -o OcrServiceImpl authcenter-medium-center.2019-07-28.*.log -c >> ./log/count.log
- 20190731：grep 'IsomerizationAccessServiceImpl' isomerization-proxy.2019-07*.log | grep access | grep PARAMETER | grep contracts-ocr -c；grep 'IsomerizationAccessServiceImpl' isomerization-proxy.log | grep access | grep PARAMETER | grep contracts-ocr -c
