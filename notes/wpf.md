# wpf

## 官方文档
https://learn.microsoft.com/zh-cn/dotnet/desktop/wpf/overview/?view=netdesktop-6.0

WPF仅在Windows上运行

<TargetFramework>net6.0-windows</TargetFramework>

wpf原生支持多点触控，原生可以区分鼠标和触摸，我用wpf的理由之一就是很有可能在哪里上个大触摸屏逼格高，键鼠时就不弹键盘，触摸时弹触摸键盘。

C#
WPF是Windows Presentation Foundation的缩写，是微软公司推出的基于.NET Framework 3.0的用户界面框架之一。它是一种全新的基于XAML的界面设计语言，可以帮助开发人员创建具有高度交互性和动态性的Windows应用程序。WPF支持多种编程语言，包括C#、VB.NET等。

https://github.com/edidada/WpfApp1

https://gitee.com/edidada/wpfapp1

了解如何在.NET 7 上使用Windows Presentation Foundation (WPF)（适用于Windows的开源图形用户界面）。
.net 6上如何使用wpf
.net framework上使用wpf

WPF有两种实现：
.Net版本（本指南）：
GitHub上托管的WPF的开源实现，在.NET上运行。适用于XAML设计器最低要求Visual Studio 2019 版本 16.8。 但根据 .NET 的版本，可能需要使用较新版本的 Visual Studio。
尽管 .NET 是一种跨平台技术，但 WPF 仅在 Windows 上运行。

.NET Framework 4 版本：
受 Visual Studio 2019 和 Visual Studio 2017 支持的 WPF 的 .NET Framework 实现。
.NET Framework 4 是仅限 Windows 的 .NET 版本，被视为一个 Windows 操作系统组件。 此版本的 WPF 随 .NET Framework 一起分发。 有关 WPF 的 .NET Framework 版本的详细信息，请参阅适用于 .NET Framework 的 WPF 简介。

## XAML
XAML 是一种基于 XML 的标记语言，以声明形式实现应用程序的外观。 通常用它定义窗口、对话框、页面和用户控件，并填充控件、形状和图形。

https://learn.microsoft.com/zh-cn/dotnet/desktop/xaml-services/?view=netdesktop-6.0