# 第 11 章 并发编程实践（10 个坑）（原书 pp.284-344）

> 实践篇，全书最"值钱"的一章：前 10 章学机制，这一章学**别人踩过的坑**。
> 本章按「坑 → 现象 → 根因 → 正确写法」组织，每条都给出可复现代码。
> 延伸专篇：[concepts/ThreadLocal与内存泄漏.md](concepts/ThreadLocal与内存泄漏.md) · [concepts/CompletableFuture与异步编排.md](concepts/CompletableFuture与异步编排.md)

## 一、本章地图

| # | 坑 | 页码 | 根因一句话 |
| --- | --- | --- | --- |
| 11.1 | 线程池 + `ArrayBlockingQueue` 死锁 | 284 | 任务之间互相等待，池里没有多余线程 |
| 11.2 | `CountDownLatch` 忘 `countDown()` | 289 | 异常路径没走到 |
| 11.3 | `SimpleDateFormat` 非线程安全 | 295 | 内部持有 `Calendar` 可变状态 |
| 11.4 | `Timer` 单线程阻塞 | 302 | 见 [09 章](09-ScheduledThreadPoolExecutor.md) |
| 11.5 | `ConcurrentHashMap` 的**复合操作**不原子 | 308 | `get` 与 `put` 之间仍有窗口 |
| 11.6 | `CopyOnWriteArrayList` 误用 | 315 | 写时复制，写多则灾难 |
| 11.7 | `FutureTask` 的异常被吞 | 320 | 不调 `get()` 就永远看不到 |
| 11.8 | 死锁与 `jstack` 定位 | 326 | 锁顺序不一致 |
| 11.9 | `CompletableFuture` 的默认线程池陷阱 | 333 | 共用 `ForkJoinPool.commonPool()` |
| 11.10 | `ThreadLocal` 内存泄漏 | 339 | 线程池线程不销毁 + 弱引用 key |

## 二、11.1 线程池与有界队列的互等死锁

```java
// ❌ 死锁：父任务等待子任务，子任务却在队列里排队
ExecutorService pool = new ThreadPoolExecutor(
    1, 1, 0, SECONDS, new ArrayBlockingQueue<>(10));

pool.submit(() -> {
    Future<String> f = pool.submit(() -> "child");   // 子任务进队列
    return f.get();                                   // 父任务占着唯一线程等子任务
});
```

**根因**：池中唯一线程被父任务占用 → 子任务永远无法获得线程 → 父任务 `get()` 永不返回。
这是「**线程饥饿死锁**（thread starvation deadlock）」，与普通死锁（A 等 B 的锁）不同，它是**等线程**而非等锁。

**解法**：

1. 不要把相互依赖的任务提交到**同一个**池
2. 用无界/大容量队列（把死锁转成内存问题，也不好）
3. 最好：**用 `CompletableFuture` 编排**，让子任务的完成驱动父任务，而不是阻塞等待

> ⚠️ **虚拟线程能救这个场景吗？** 不能根治。
> `Executors.newVirtualThreadPerTaskExecutor()` 下每个任务一个新虚拟线程，父任务 `get()` 不会饿死子任务——
> 确实某种程度上缓解了。但这仍是**坏设计**，应当用异步编排替代阻塞等待。

## 三、11.2 `CountDownLatch` 忘 `countDown()`

```java
// ❌
for (Task t : tasks) {
    pool.submit(() -> {
        doWork(t);          // 抛异常 → countDown() 永不执行 → await() 永久阻塞
        latch.countDown();
    });
}
latch.await();
```

**正确写法**：

```java
pool.submit(() -> {
    try { doWork(t); }
    finally { latch.countDown(); }      // 必须 finally
});
// 且 await 要带超时，避免永久挂起
if (!latch.await(30, TimeUnit.SECONDS)) {
    log.error("timeout, {} tasks not finished", latch.getCount());
}
```

> **通用原则**：任何「配对操作」（`countDown`/`await`、`release`/`acquire`、`unlock`/`lock`），
> **释放侧一律放 `finally`**，**等待侧一律带超时**。这两条能消灭 90% 的并发挂起事故。

## 四、11.3 `SimpleDateFormat` 非线程安全

```java
// ❌ 经典错误：static 共享
private static final SimpleDateFormat SDF = new SimpleDateFormat("yyyy-MM-dd");
// 并发下会抛 NumberFormatException 或产出错误日期
```

**根因**：`SimpleDateFormat` 内部持有一个 `Calendar` 字段，`parse()`/`format()` 会**先 `calendar.clear()` 再逐步 set**，
多线程交错执行会互相污染中间状态。

四种正确解法（推荐度递减）：

```java
// ① 最佳：JDK 8+ 的 DateTimeFormatter，不可变且线程安全
private static final DateTimeFormatter FMT = DateTimeFormatter.ofPattern("yyyy-MM-dd");
String s = LocalDate.now().format(FMT);

// ② ThreadLocal 隔离（老代码迁移常用）
private static final ThreadLocal<SimpleDateFormat> TL =
    ThreadLocal.withInitial(() -> new SimpleDateFormat("yyyy-MM-dd"));

// ③ 每次 new（开销可接受时）
new SimpleDateFormat("yyyy-MM-dd").format(date);

// ④ synchronized / 锁（最差，串行化）
```

> ⚠️ **`ThreadLocal` 方案在线程池下有泄漏风险**（见 11.10），且虚拟线程下代价被放大。
> JDK 8+ 一律优先用 `java.time`（`DateTimeFormatter` / `LocalDateTime`），它是不可变的，天生线程安全。

## 五、11.5 `ConcurrentHashMap` 的复合操作不原子

```java
ConcurrentHashMap<String, Long> map = new ConcurrentHashMap<>();

// ❌ 仍然是竞态：get 与 put 之间有时间窗口
if (!map.containsKey(k)) map.put(k, v);

// ✅ 用原子方法
map.putIfAbsent(k, v);
map.computeIfAbsent(k, kk -> load(kk));      // 最常用
map.compute(k, (kk, vv) -> vv == null ? 1L : vv + 1);
map.merge(k, 1L, Long::sum);                 // 计数的最佳写法
```

**`computeIfAbsent` 的两个大坑**：

1. **递归更新会死锁/抛 `IllegalStateException`**：在 `computeIfAbsent` 的映射函数里再次修改同一个 map → JDK 8 下可能死循环，JDK 9+ 抛 `IllegalStateException("Recursive update")`。
2. **映射函数里的阻塞操作会锁住 bin**：`computeIfAbsent` 会持有该 bin 的锁，若映射函数做 IO/远程调用，会阻塞其他访问同一 bin 的线程。
   → **映射函数必须快、且不能再改本 map**。

**计数场景的正确选择**：

```java
map.merge(k, 1L, Long::sum);          // 低争用
// 高争用下 LongAdder 更快（见第 4 章）：
ConcurrentHashMap<String, LongAdder> counters = new ConcurrentHashMap<>();
counters.computeIfAbsent(k, kk -> new LongAdder()).increment();
```

> 详见 [concepts/ConcurrentHashMap演进.md](concepts/ConcurrentHashMap演进.md) 与 [concepts/LongAdder与分段累加.md](concepts/LongAdder与分段累加.md)。

## 六、11.6 `CopyOnWriteArrayList` 的误用

```java
CopyOnWriteArrayList<String> list = new CopyOnWriteArrayList<>();
// ✅ 适合：读极多、写极少（监听器列表、白名单、路由表）
// ❌ 不适合：写频繁（每次 add 都复制整个数组，O(n) 且产生大量垃圾）
```

| 维度 | 表现 |
| --- | --- |
| 写 | **O(n) 全数组复制**，加锁，内存放大（同时存在新旧两个数组） |
| 读 | 无锁，快照语义（迭代器**不反映**后续修改，也不会抛 `ConcurrentModificationException`） |
| 内存 | 写操作期间峰值 2×；频繁写会造成严重 GC 压力 |

> **判据**：写频率超过「每分钟几次」就别用。监听器/配置表这类场景才是它的主场。

## 七、11.7 `FutureTask` 的异常被吞

```java
Future<?> f = pool.submit(() -> { throw new RuntimeException("boom"); });
// 不调用 f.get() → 异常被 FutureTask 吞掉，日志里什么都没有
```

`submit()` 把异常封进 `FutureTask` 的 `outcome` 字段，只有 `get()` 才会把它以 `ExecutionException` 重新抛出。

**解法**：

```java
// ① 显式 get 并处理
try { f.get(); }
catch (ExecutionException e) { log.error("task failed", e.getCause()); }

// ② 用 execute() 而不是 submit()：异常直接打到线程的 UncaughtExceptionHandler
pool.execute(() -> { throw new RuntimeException("boom"); });   // 会打印堆栈

// ③ 全局兜底：自定义 ThreadFactory 设置 UncaughtExceptionHandler
new ThreadFactory() {
    public Thread newThread(Runnable r) {
        Thread t = new Thread(r);
        t.setUncaughtExceptionHandler((th, ex) -> log.error("uncaught in " + th.getName(), ex));
        return t;
    }
}

// ④ 最推荐：CompletableFuture，异常在编排链上显式传播
CompletableFuture.runAsync(() -> doWork(), pool)
    .exceptionally(ex -> { log.error("failed", ex); return null; });
```

> ⚠️ **周期定时任务的异常也是同样机制**（见 [09 章](09-ScheduledThreadPoolExecutor.md)第四节）——
> 你通常不会去 `get()` 定时任务的结果，于是异常静默丢失 + 任务永久停止。

## 八、11.8 死锁与定位

四个必要条件（Coffman 条件）：互斥、占有且等待、不可抢占、循环等待。
**破坏任意一个即可避免**，工程上最实用的是**破坏循环等待**——**全局统一的加锁顺序**。

```java
// ❌ 可能死锁：两个线程加锁顺序相反
// T1: lock(A); lock(B);
// T2: lock(B); lock(A);

// ✅ 统一顺序：按对象的 identityHashCode 排序加锁
void transfer(Account from, Account to, long amount) {
    Account first  = System.identityHashCode(from) < System.identityHashCode(to) ? from : to;
    Account second = (first == from) ? to : from;
    synchronized (first) {
        synchronized (second) { /* 转账 */ }
    }
}
// （极小概率 hash 冲突，可再加一个全局 tie-break 锁）
```

**线上定位三板斧**：

```bash
# ① jstack 抓栈，JVM 会自动打印死锁
jstack <pid> | grep -A 20 "Found one Java-level deadlock"

# ② JFR 直接看哪些线程 BLOCKED 在哪个 monitor
jcmd <pid> JFR.start duration=60s settings=profile filename=app.jfr
#   打开后看 jdk.JavaMonitorEnter / jdk.ThreadPark 事件

# ③ async-profiler 采锁竞争火焰图
```

> JDK 21+ 虚拟线程下 `jstack` 看不到虚拟线程的完整栈（数量太多），
> 要用 `jcmd <pid> Thread.dump_to_file -format=json <file>`。

## 九、11.9 `CompletableFuture` 的默认线程池陷阱

```java
// ❌ 危险：不传 Executor 时用 ForkJoinPool.commonPool()
CompletableFuture.supplyAsync(() -> blockingIoCall());
```

三个问题：

1. **`commonPool` 是全局共享的**，一个地方用了它做阻塞 IO，会拖垮**整个 JVM** 里所有用 `commonPool` 的代码（包括 parallelStream）
2. **线程数 = CPU 核数 - 1**，对 IO 密集严重不足
3. **`commonPool` 的线程是守护线程**，不阻止 JVM 退出；且它**不会因空闲而扩容**

```java
// ✅ 总是显式传 Executor
CompletableFuture.supplyAsync(() -> blockingIoCall(), myIoPool);
```

> 另见 [concepts/CompletableFuture与异步编排.md](concepts/CompletableFuture与异步编排.md) 与
> [concepts/ForkJoin与工作窃取.md](concepts/ForkJoin与工作窃取.md)。

## 十、11.10 `ThreadLocal` 内存泄漏

这是实践篇**最重要的一个坑**，也是本书点到但未讲透的：

```java
static ThreadLocal<byte[]> TL = new ThreadLocal<>();
// 线程池场景下：线程不销毁 → ThreadLocalMap 一直存在 → value 强引用 → 内存泄漏
```

**根因链**（详见 [concepts/ThreadLocal与内存泄漏.md](concepts/ThreadLocal与内存泄漏.md)）：

```
Thread → ThreadLocalMap → Entry(key=弱引用ThreadLocal, value=强引用)
                                    ↓
           ThreadLocal 被 GC 后 key=null，但 value 仍被强引用
                                    ↓
           线程池线程存活几十年 → value 永不释放 → 泄漏
```

**三条防御**：

```java
// ① 用完必须 remove（最根本）
try { TL.set(v); ... } finally { TL.remove(); }

// ② 声明为 static final（避免 ThreadLocal 自身被提前 GC 导致 key 变 null）
private static final ThreadLocal<X> TL = new ThreadLocal<>();

// ③ 不要往 ThreadLocal 里放大对象（尤其不要放整个 Request 上下文）
```

**虚拟线程下的问题被放大**：百万级虚拟线程 × 每个带 `ThreadLocal` = 灾难性内存占用。
→ JDK 21+ 用 **`ScopedValue`**（JEP 506 在 JDK 25 转正）替代：

```java
// JDK 25+
private static final ScopedValue<User> CURRENT_USER = ScopedValue.newInstance();

ScopedValue.where(CURRENT_USER, user).run(() -> {
    // 作用域内可读，退出自动清除，不可变，无泄漏
    handle();
});
```

详见 [concepts/结构化并发与ScopedValue.md](concepts/结构化并发与ScopedValue.md)。

## 十一、JDK 版本演进（实践篇相关）

| 版本 | 变化 |
| --- | --- |
| JDK 5 | `Future`/`FutureTask`、`ConcurrentHashMap` 重写、`CopyOnWriteArrayList` |
| JDK 7 | `Phaser`、`TransferQueue`；`ConcurrentHashMap` 内部改进 |
| **JDK 8** | 🔴 `java.time`（`DateTimeFormatter` 线程安全，`SimpleDateFormat` 时代结束）、`CompletableFuture`、`LongAdder`、`StampedLock` |
| JDK 9 | `CompletableFuture.orTimeout()`/`completeOnTimeout()`；`Optional.or()`；`Flow`（Reactive Streams） |
| JDK 15 | 偏向锁废弃（影响 `synchronized` 的性能直觉） |
| **JDK 21** | 🔴 虚拟线程 GA、结构化并发预览、`ScopedValue` 孵化 |
| **JDK 25** | 🔴 **`ScopedValue` GA**（JEP 506）——`ThreadLocal` 的现代替代正式可用 |

## 十二、经典论文 / 原始文献

| 主题 | 文献 | 出处 |
| --- | --- | --- |
| 死锁的必要条件 | Coffman, Elphick, Shoshani, *System Deadlocks* | **Computing Surveys 3(2), 1971** —— 四个必要条件的原始表述 |
| 死锁的避免/检测 | Havender, *Avoiding Deadlocks in Multitasking Systems* | IBM Systems Journal, 1968（加锁顺序法的出处） |
| 银行家算法 | Dijkstra, EWD-108 / Habermann, *System Deadlocks* | 1968-1969 |
| 线程安全与不可变 | Bloch, *Effective Java*（Item 15/17：最小化可变性） | Addison-Wesley，第 3 版 2018 |
| 并发 bug 的实证研究 | **Lu, Park, Seo, Zhou, *Learning from Mistakes: A Comprehensive Study on Real World Concurrency Bug Characteristics*** | **ASPLOS 2008** —— 对 4 个大型开源软件（MySQL、Apache、Mozilla、OpenOffice）的 105 个真实并发 bug 的分类研究，**本章 10 个坑的实证依据** |
| 并发 bug 检测的综述 | **张等, *Concurrency Bug Detection, Reproduction, and Traps*** | 相关综述见 IEEE 软件工程汇刊 |
| 无锁与事务内存 | Herlihy & Moss, *Transactional Memory: Architectural Support for Lock-Free Data Structures* | **ISCA 1993** |

> **ASPLOS 2008 那篇值得单独读**：它的核心结论是——**约 1/3 的并发 bug 是「原子性违反」（atomicity violation），
> 而不是死锁或顺序问题**，这正好对应本章 11.5（`ConcurrentHashMap` 复合操作）与 11.3（`SimpleDateFormat`）这两类坑。

## 十三、近年研究与工业界前沿（2020-2026）

**同行评审论文**

- **并发 bug 的实证研究持续更新**：ASPLOS 2008 之后，针对 Go、Rust、分布式系统的并发 bug 研究（如 *Understanding Real-World Concurrency Bugs in Go*, ASPLOS 2019）给出了跨语言对比——
  **Go 的 channel 并不比 Java 的锁更安全**，消息传递引发的阻塞类 bug 反而更多。这是近年最反直觉、也最有价值的结论之一。
- **Rust 的所有权模型的实证评估**：多项研究（含 ICSE/PLDI 上的工作）表明，`Send`/`Sync` 编译期检查确实消除了数据竞争，
  但**逻辑竞态（atomicity violation）依然存在**——语言能防的只是内存安全这一层。

**工业界资料（非同行评审）**

- **OpenJDK `jcstress`**：验证 JMM / happens-before 结论的官方压力测试工具，本书所有内存可见性结论都应跑一遍。
  https://github.com/openjdk/jcstress
- **OpenJDK `jmh`**：唯一可信的微基准写法（规避 JIT 死代码消除与预热误差）。
  https://github.com/openjdk/jmh
- **async-profiler / JFR**：线上定位锁竞争与线程 park 的标配。
  https://github.com/async-profiler/async-profiler
- **error-prone / SpotBugs / IntelliJ  inspections**：`SimpleDateFormat` 静态字段、忘记 `remove()` 等坑都有现成静态检查规则，**应在 CI 里强制开启**。
- **《阿里巴巴 Java 开发手册》**：本章多个坑（线程池、SimpleDateFormat、ThreadLocal）在国内的普及主要靠它。

## 十四、常见误区 / 本书需修正之处

1. **⚠️ `SimpleDateFormat` 的解法已过时**：本书建议 `ThreadLocal<SimpleDateFormat>`，
   **JDK 8+ 应直接用 `DateTimeFormatter`**（不可变、线程安全、无泄漏风险）。
2. **⚠️ 本书未充分强调「配对操作放 finally、等待操作带超时」这两条通用原则**——它们是消灭并发挂起的最高效手段。
3. **⚠️ `ThreadLocal` 泄漏的根因是线程池 + 弱引用 key 的组合**，本书只说「要 remove()」未解释为什么，导致很多人记不住。
4. **`computeIfAbsent` 的递归更新与阻塞映射函数**是本书未覆盖的新坑（JDK 8 后才有）。
5. **`CompletableFuture` 默认用 `commonPool`** 是本书未提及的重大陷阱（本书成书时 `CompletableFuture` 刚普及）。
6. **虚拟线程改变了「线程池调优」但没改变「正确性」**：本章 10 个坑里，除了 11.1（线程饥饿）在虚拟线程下有所缓解，
   其余**全部依然存在**——竞态、可见性、原子性违反与线程模型无关。
7. **静态检查 > 人工 review**：本机应把 SpotBugs / error-prone 接进 CI，很多并发坑靠人眼是看不出来的。
