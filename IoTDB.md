# IoTDB



实际有 iotdb时序数据库就是java写的 清华大学的项目 目前这个在物联网方面应用很多
IoTDB使用了层次化的时序数据模型,可以方便地组织和管理不同类型和来源的时序数据,并且支持多维索引和元数据缓存,提高查询性能。


java数据库

问题只说数据库，没说是 RDBMS/SQL 还是 NoSQL，从一些场景来讲，Lucene、Solr、ElasticSearch、Neo4j 等都是数据库，这些都是 Java 开发的，而且都不局限于通过 Java 调用，Python、PHP 都可以调。我司（小作坊）就直接拿 Lucene、Neo4j 当数据库用。为升级和数据安全起见，还是会在关系数据库里存一份，但应用层都是直接读 Lucene、Neo4j 的。要讲点缺点的话，RDBMS 这个 R 确实好用，在 Lucene 里关联是很麻烦的，内部提供的关联太严格、太鸡肋；采用 IN 的模式，条件规模受限制，且无法关联排序。Neo4j 补足了这个缺点，能玩的花样比关系数据库多多了，只是二者要结合用好的话在应用层面比较麻烦。Lucene、Solr、ElasticSearch 操作繁琐？封装呗，我给 Lucene 做了一个类似 MongoDB 的 JSON 封装，同时，也给我们的关系数据库做了一个相同的 JSON 封装，所以二者用起来基本上一样的，大部分简单场景使用时甚至不必关心底下存哪种库里了。在统计上，数据量不太大的情况，可以封装出媲美 SQL 的 group by/count/sum 等，而且条件约束上基本上不必关心索引优化，毕竟 Lucene 就是为解决索引而生的。至于磁盘空间、零散文件这些小问题在如今都不是问题，磁盘不值钱（相对一个不太大的数据规模）。没精力折腾 Lucene？用 ElasticSearch 嘛，开箱即用。我拿 Lucene 折腾只是因为用的比较早，习惯了，也舍不得丢了。至于为啥“国内”的程序员没做这些，也许有的，名气小吧，小团队光做这个养活自己都难，只能当个副业。我了解到有些准大厂基于 ES、Lucene、Hadoop 等封装类 RDBMS，能用常规 SQL 那种，没开源而已吧；或者开源了，像我们这样的小作坊用不着，大厂又不可能用竞争对手的。


