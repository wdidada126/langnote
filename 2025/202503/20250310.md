# 20250310
https://github.com/edidada/testdrogon

Git patch文件是纯文本文件。

### 详细解释
- 纯文本格式：Git patch 文件以纯文本形式存储，它包含了一系列对文件内容的修改信息，如文件的添加、删除、修改等操作。这种纯文本格式使得 patch 文件易于阅读和编辑，并且可以方便地在不同系统和工具之间传输和应用。
- 内容结构：patch 文件通常由一系列的“块（hunk）”组成，每个块描述了对一个文件的具体修改。每个块包含了文件名、文件的旧版本和新版本的相关信息，以及具体的修改内容。例如，下面是一个简单的 patch 文件示例：
```diff
diff --git a/file.txt b/file.txt
index 1234567..89abcde 100644
--- a/file.txt
+++ b/file.txt
@@ -1,3 +1,4 @@
 This is the first line.
 This is the second line.
+This is a new line.
 This is the third line.
```
在这个示例中，`diff --git` 表示这是一个对文件 `file.txt` 的修改；`index` 行显示了文件的旧版本和新版本的哈希值；`---` 和 `+++` 分别表示旧文件和新文件；`@@ -1,3 +1,4 @@` 表示修改的位置和范围；以 `-` 开头的行表示删除的内容，以 `+` 开头的行表示添加的内容。

### 用途
- 分享修改：可以将 patch 文件发送给其他开发者，让他们在自己的代码库中应用这些修改。
- 版本控制：在代码审查和合并过程中，patch 文件可以用于记录和跟踪代码的变化。

### 示例代码
在 Git 中生成 patch 文件的命令如下：
```bash
# 生成从某个提交到当前的 patch 文件
git diff <commit-hash> > changes.patch

# 生成两个提交之间的 patch 文件
git diff <commit-hash1> <commit-hash2> > changes.patch
```
应用 patch 文件的命令如下：
```bash
# 应用 patch 文件
git apply changes.patch
```

综上所述，Git patch文件是纯文本文件，它以一种易于理解和处理的方式记录了代码的修改信息。

产出分的定义
产出分是通过具体数据衡量员工工作成果的评分体系，常见于KPI（关键绩效指标）或OKR（目标与关键成果）考核中。
典型指标包括：
任务完成量（如代码行数、项目交付数）
工作质量（如错误率、客户满意度）
效率（如任务耗时、资源利用率）
创新性（如提出优化方案、技术突破）

drogon orm print sql
全局日志改成
TRACE

openssl/1.1.1t: WARN: replace_in_file didn't find pattern '/MTd"' in 'Configurations\10-main.conf' file.
openssl/1.1.1t: RUN: C:/Users/wdidada/.conan2/p/straw898fb56546adc/p/bin/perl.exe ./Configure "VC-conan-Debug-Windows-x86_64-msvc-193" no-shared --prefix=/ --openssldir="res" no-unit-test threads PERL=C:/Users/wdidada/.conan2/p/straw898fb56546adc/p/bin/perl.exe no-tests --debug --libdir=lib no-md2
conanvcvars.bat: Activating environment Visual Studio 17 - amd64 - winsdk_version=None - vcvars_ver=14.3

`vcvars_ver=14.3`和VS Build Tools都与Visual Studio的编译环境相关，以下是它们的作用及相关命令行介绍：

### `vcvars_ver=14.3`的作用
- `vcvars_ver=14.3`用于指定Visual C++ 生成工具的版本为14.3。在Visual Studio的生态系统中，不同版本的C++生成工具具有不同的特性、功能和对语言标准的支持程度。通过指定`vcvars_ver=14.3`，可以确保项目使用特定版本的工具进行编译等操作，以保证项目构建的稳定性和一致性，也能让开发者利用该版本工具所提供的特定功能和优化。

### VS Build Tools的作用
- 构建工具集：提供了一系列用于构建C++、C#、Python等多种编程语言项目的工具和环境，包括编译器、链接器、资源编译器等，能将源代码转换为可执行文件、库文件等目标产物。
- 项目配置与管理：帮助开发者管理项目的属性、设置编译选项、链接库路径等，支持多种项目类型，如控制台应用程序、Windows应用程序、动态链接库等的创建和管理。
- 依赖管理：可以处理项目之间的依赖关系，自动解析和链接所需的库文件和头文件，确保项目在构建过程中能够正确找到并使用所需的依赖项。
- 与其他工具集成：能与Visual Studio IDE、命令行工具以及其他开发工具进行良好的集成，方便开发者根据自己的工作流程和需求选择合适的方式进行项目开发和构建。

### 相关命令行
- vcvarsall.bat相关命令行
    - `vcvarsall.bat x86`：用于在32位环境下设置Visual C++的编译环境，激活`vcvars_ver`指定版本的32位编译工具链，使得可以在命令行中使用相应的编译器和工具来构建32位的项目。
    - `vcvarsall.bat x64`：与上面类似，不过是用于设置64位的编译环境，以便构建64位的项目。
    - `vcvarsall.bat amd64_x86`：用于设置交叉编译环境，从64位环境交叉编译为32位的目标程序，常用于需要在64位系统上构建32位应用程序或库的场景。
- VS Build Tools的MSBuild命令行
    - `msbuild.exe MyProject.csproj`：用于构建C#项目文件`MyProject.csproj`，执行项目的编译、链接等一系列构建操作。可以在命令行中指定项目文件的路径和名称，MSBuild会根据项目文件中的配置信息进行构建。
    - `msbuild.exe /t:Rebuild MyProject.vcxproj`：`/t:Rebuild`参数表示执行重新构建操作，对于C++项目文件`MyProject.vcxproj`，会先清理之前的构建结果，然后重新进行编译和链接等操作。
    - `msbuild.exe /p:Configuration=Release MyProject.sln`：`/p:Configuration=Release`参数用于指定构建配置为发布版本，对于解决方案文件`MyProject.sln`，会按照发布版本的配置进行构建，通常会进行优化等操作。

不同版本的cl.exe

C:/Program Files/Microsoft Visual Studio/2022/Enterprise/VC/Tools/MSVC/14.38.33130/bin/Hostx64/x64/cl.exe  
C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC\14.29.30133
C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC\14.43.34808

超聚变数字技术有限公司（英文名xFusion Digital Technologies Co., Ltd.），简称：超聚变，是一家全球领先的算力基础设施与算力服务提供者，成立于2021年9月13日，总部位于河南省郑州市郑东新区龙子湖智慧岛正商博雅广场1号楼9层，法定代表人为刘宏云。

在 vcpkg 中自定义三方库的编译选项可以通过以下几种方式实现：
1.使用`--feature`选项
许多 vcpkg 包提供了功能选项（features），可以在安装时通过指定这些选项来启用或禁用特定功能。例如：

```bash
vcpkg install <package>[<feature1>,<feature2>]
```

例如，安装带有特定功能的 Boost：

```bash
vcpkg install boost[filesystem,system]
```

2.修改`triplet`文件
`triplet`文件用于指定平台相关的编译设置。你可以通过修改或创建自定义`triplet`文件来自定义编译选项。例如：

1. 复制默认的`triplet`文件并重命名为`x64-windows-custom.cmake`：

```bash
   cp <vcpkg_root>/triplets/x64-windows.cmake <vcpkg_root>/triplets/community/x64-windows-custom.cmake
```


2. 在文件中修改 CMake 变量，例如：

```cmake
   set(VCPKG_CMAKE_OPTIONS "-DSOME_OPTION=ON")
```


3. 安装包时指定自定义的`triplet`：

```bash
   vcpkg install <package>:x64-windows-custom
```

3.修改`portfile.cmake`
你可以直接编辑包的`portfile.cmake`文件来自定义编译选项。例如：

```cmake
vcpkg_configure_cmake(
    SOURCE_PATH ${SOURCE_PATH}
    PREFER_NINJA
    OPTIONS
        -DSOME_OPTION=ON
)
```

安装该包时将使用你自定义的编译选项。

4.使用`overlay-ports`
你可以创建一个自定义的`overlay-port`，在其中定义或修改包的`portfile.cmake`，并指定编译选项。例如：

```bash
vcpkg install <package> --overlay-ports=<path_to_custom_ports>
```

5.使用`--editable`选项
通过`--editable`标志，你可以在安装包时使其以可编辑模式构建，从而手动干预编译选项。例如：

```bash
vcpkg install <package> --editable
```

安装完成后，你可以在构建目录中修改相关的 CMake 文件或编译选项，然后重新构建。

6.使用`vcpkg.json`清单文件
如果你在项目中使用`vcpkg.json`清单文件，可以在该文件中指定包的特定选项。例如：

```json
{
    "dependencies": [
        {
            "name": "zlib",
            "features": ["custom-feature"],
            "default-features": false
        }
    ]
}
```

运行以下命令安装依赖项：

```bash
vcpkg install
```

总结
以上方法提供了不同层次的自定义选项，从简单的功能启用到全面的编译配置修改。你可以根据需求选择适合的方式。
