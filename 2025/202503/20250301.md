# 20250301


// 自定义线程池类，继承自 Poco::ThreadPool
class MyThreadPool : public Poco::ThreadPool {
public:
    using Poco::ThreadPool::ThreadPool; // 继承基类的构造函数

    // 定义一个公共方法来访问受保护的 getThread 函数
    Poco::Thread* getThreadPublic(int index) {
        return getThread(index);
    }
};
