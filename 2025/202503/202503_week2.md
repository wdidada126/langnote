# 202503_week2

## 时间范围

- 202503 月第 2 周

## 本周记录

- 20250308：周六休息
- 20250309：周日休息
- 20250310：Git patch文件是纯文本文件。；纯文本格式：Git patch 文件以纯文本形式存储，它包含了一系列对文件内容的修改信息，如文件的添加、删除、修改等操作。这种纯文本格式使得 patch 文件易于阅读和编辑，并且可以方便地在不同系统和工具之间传输和应用。；内容结构：patch 文件通常由一系列的“块（hunk）”组成，每个块描述了对一个文件的具体修改。每个块包含了文件名、文件的旧版本和新版本的相关信息，以及具体的修改内容。例如，下面是一个简单的 patch 文件示例：
- 20250311：`cl.exe` 和 `MsBuild.exe` 都是在 Windows 平台上与 Visual Studio 开发环境紧密相关的工具，但它们的作用有着明显的区别，下面为你详细介绍：；`cl.exe` 是 Microsoft Visual C++ 编译器，主要负责将 C、C++ 源代码文件编译成目标文件（`.obj`），或者直接生成可执行文件。它是 Visual C++ 开发工具集的核心组件之一，专注于单个源文件或一组源文件的编译工作。；编译源代码：读取 C 或 C++ 源文件（`.c`、`.cpp` 等），进行词法分析、语法分析、语义分析等操作，将源代码转换为汇编代码，再进一步生成目标文件（`.obj`）。
- 20250312：云服务商，web上像命令行shell一样操作linux；conan 2编译drogon，设置；ERROR: There are invalid packages:
- 20250313：git clone https://code.videolan.org/rist/librist.git；cd librist；sudo apt install -y meson ninja-build
- 20250314：补充了当日日志，暂未提炼出明确主题。
