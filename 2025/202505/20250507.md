# 20250507

复习std::any
### `std::any` 所属的 C++ 标准版本
`std::any` 是在 C++17 标准中引入的。在 C++11 和 C++14 标准里并没有这个特性。它被定义在 `<any>` 头文件中，其目的在于提供一种类型安全的方式来存储任意类型的值。

### `std::any` 产生的背景
在 `std::any` 出现之前，开发者若要存储任意类型的值，通常会采用以下几种不太理想的方式：
- 使用 `void*` 指针：`void*` 可以指向任意类型的对象，但它不具备类型信息，使用时需要手动进行类型转换，这不仅麻烦，还容易引发类型安全问题。因为在进行类型转换时，若转换错误，程序可能会在运行时崩溃。
- 使用联合（`union`）：联合可以在同一内存位置存储不同类型的值，但联合要求所有成员类型的大小和对齐方式必须兼容，并且需要手动管理类型信息，使用起来较为复杂。

`std::any` 的出现解决了这些问题，它提供了一种类型安全的方式来存储任意类型的值，同时隐藏了底层的类型信息，使得代码更加简洁和安全。

### `std::any` 使用示例
以下是一个简单的 `std::any` 使用示例，展示了如何存储不同类型的值，并在需要时进行类型检查和转换：
```cpp
#include <iostream>
#include <any>
#include <string>

int main() {
    // 创建一个 std::any 对象，并存储一个整数
    std::any value = 42;

    // 检查存储的值是否为整数类型
    if (value.type() == typeid(int)) {
        // 进行类型转换并输出值
        int intValue = std::any_cast<int>(value);
        std::cout << "存储的整数是: " << intValue << std::endl;
    }

    // 存储一个字符串
    value = std::string("Hello, World!");

    // 检查存储的值是否为字符串类型
    if (value.type() == typeid(std::string)) {
        // 进行类型转换并输出值
        std::string stringValue = std::any_cast<std::string>(value);
        std::cout << "存储的字符串是: " << stringValue << std::endl;
    }

    // 尝试进行错误的类型转换
    try {
        double doubleValue = std::any_cast<double>(value);
        std::cout << "存储的双精度浮点数是: " << doubleValue << std::endl;
    } catch (const std::bad_any_cast& e) {
        std::cout << "类型转换错误: " << e.what() << std::endl;
    }

    return 0;
}
```
### 代码解释
1. 存储不同类型的值：
    - `std::any value = 42;`：创建一个 `std::any` 对象，并存储一个整数。
    - `value = std::string("Hello, World!");`：将 `std::any` 对象的值更新为一个字符串。

2. 类型检查和转换：
    - `value.type() == typeid(int)` 和 `value.type() == typeid(std::string)`：使用 `type()` 方法检查 `std::any` 对象中存储的值的类型。
    - `std::any_cast<int>(value)` 和 `std::any_cast<std::string>(value)`：使用 `std::any_cast` 进行类型转换。如果转换成功，将返回存储的值；如果转换失败，会抛出 `std::bad_any_cast` 异常。

3. 异常处理：
    - 使用 `try-catch` 块捕获 `std::bad_any_cast` 异常，处理类型转换错误。

通过这个示例，你可以看到 `std::any` 提供了一种类型安全的方式来存储和处理任意类型的值。 

https://github.com/ruanyf/weekly
科技爱好者周刊

redis 6.2.14 cluster模式启动，有三个机器，ip分别是10.82.5.97 10.82.5.98 10.82.5.99，java代码spring工程redis.conf配置如下，

redis.cluster.node1.host=10.82.5.97
redis.cluster.node1.port=6379
redis.cluster.node2.host=10.82.5.97
redis.cluster.node2.port=6479
redis.cluster.node3.host=10.82.5.98
redis.cluster.node3.port=6379
redis.cluster.node4.host=10.82.5.98
redis.cluster.node4.port=6479
redis.cluster.node5.host=10.82.5.99
redis.cluster.node5.port=6379
redis.cluster.node6.host=10.82.5.99
redis.cluster.node6.port=6479

redis.cluster.timeout=2000
redis.cluster.max-redirects=8
redis.cluster.password=RedisP@ssW0rd  
 如何配置并启动三台redis，如何测试启动是否成功，是否需要配置集群主从节点？

以下将详细介绍如何配置并启动三台Redis节点组成的集群，测试启动是否成功，以及是否需要配置集群主从节点。
### 1. 配置并启动三台 Redis 节点
#### 步骤 1：准备 Redis 配置文件
在每台机器上，为每个 Redis 实例创建配置文件。例如，在 `10.82.5.97` 机器上，创建两个配置文件 `redis6379.conf` 和 `redis6479.conf`，内容如下：

redis6379.conf
```plaintext
port 6379
cluster-enabled yes
cluster-config-file nodes-6379.conf
cluster-node-timeout 5000
appendonly yes
requirepass RedisP@ssW0rd
masterauth RedisP@ssW0rd
```

redis6479.conf
```plaintext
port 6479
cluster-enabled yes
cluster-config-file nodes-6479.conf
cluster-node-timeout 5000
appendonly yes
requirepass RedisP@ssW0rd
masterauth RedisP@ssW0rd
```

解释：
- `port`：指定 Redis 实例监听的端口。
- `cluster-enabled yes`：开启 Redis 集群模式。
- `cluster-config-file`：指定集群配置文件的名称。
- `cluster-node-timeout`：集群节点超时时间。
- `appendonly yes`：开启 AOF 持久化。
- `requirepass` 和 `masterauth`：设置访问密码。

在 `10.82.5.98` 和 `10.82.5.99` 机器上也做同样的操作，分别创建两个配置文件。

#### 步骤 2：启动 Redis 实例
在每台机器上，使用对应的配置文件启动 Redis 实例。例如，在 `10.82.5.97` 机器上执行以下命令：
```bash
redis-server redis6379.conf
redis-server redis6479.conf
```
同样，在 `10.82.5.98` 和 `10.82.5.99` 机器上执行相同的启动命令。

#### 步骤 3：创建集群
使用 `redis-cli` 工具创建 Redis 集群。在任意一台机器上执行以下命令：
```bash
redis-cli --cluster create 10.82.5.97:6379 10.82.5.97:6479 10.82.5.98:6379 10.82.5.98:6479 10.82.5.99:6379 10.82.5.99:6479 --cluster-replicas 1 -a RedisP@ssW0rd
```
`--cluster-replicas 1` 表示为每个主节点分配一个从节点。

### 2. 测试启动是否成功

#### 使用 `redis-cli` 连接测试
在任意一台机器上，使用 `redis-cli` 连接到集群中的一个节点，例如：
```bash
redis-cli -c -h 10.82.5.97 -p 6379 -a RedisP@ssW0rd
```
`-c` 表示以集群模式连接。连接成功后，执行 `cluster info` 命令查看集群信息，执行 `cluster nodes` 命令查看集群节点信息。

#### 方法二：Java 代码测试
在 Spring 工程中，使用 `Lettuce` 或 `Jedis` 客户端进行测试。以下是使用 `Lettuce` 的示例代码：

```java
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisClusterConfiguration;
import org.springframework.data.redis.connection.RedisPassword;
import org.springframework.data.redis.connection.lettuce.LettuceConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.serializer.GenericJackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.StringRedisSerializer;

import java.util.Arrays;
import java.util.List;

@Configuration
public class RedisConfig {

    @Value("${redis.cluster.node1.host}")
    private String node1Host;
    @Value("${redis.cluster.node1.port}")
    private int node1Port;
    @Value("${redis.cluster.node2.host}")
    private String node2Host;
    @Value("${redis.cluster.node2.port}")
    private int node2Port;
    @Value("${redis.cluster.node3.host}")
    private String node3Host;
    @Value("${redis.cluster.node3.port}")
    private int node3Port;
    @Value("${redis.cluster.node4.host}")
    private String node4Host;
    @Value("${redis.cluster.node4.port}")
    private int node4Port;
    @Value("${redis.cluster.node5.host}")
    private String node5Host;
    @Value("${redis.cluster.node5.port}")
    private int node5Port;
    @Value("${redis.cluster.node6.host}")   
    private String node6Host;
    @Value("${redis.cluster.node6.port}")
    private int node6Port;

    @Value("${redis.cluster.password}")
    private String password;

    @Bean
    public LettuceConnectionFactory redisConnectionFactory() {
        List<String> nodes = Arrays.asList(
                node1Host + ":" + node1Port,
                node2Host + ":" + node2Port,
                node3Host + ":" + node3Port,
                node4Host + ":" + node4Port,
                node5Host + ":" + node5Port,
                node6Host + ":" + node6Port
        );
        RedisClusterConfiguration clusterConfig = new RedisClusterConfiguration(nodes);
        clusterConfig.setPassword(RedisPassword.of(password));
        return new LettuceConnectionFactory(clusterConfig);
    }

    @Bean
    public RedisTemplate<String, Object> redisTemplate() {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(redisConnectionFactory());
        template.setKeySerializer(new StringRedisSerializer());
        template.setValueSerializer(new GenericJackson2JsonRedisSerializer());
        return template;
    }
}
```

在测试类中使用 `RedisTemplate` 进行操作：
```java
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Component;

@Component
public class RedisTest {

    @Autowired
    private RedisTemplate<String, Object> redisTemplate;

    public void testRedisCluster() {
        redisTemplate.opsForValue().set("testKey", "testValue");
        Object value = redisTemplate.opsForValue().get("testKey");
        System.out.println("Value from Redis: " + value);
    }
}
```

### 3. 是否需要配置集群主从节点
在创建集群时，使用 `--cluster-replicas 1` 参数已经自动配置了主从节点。Redis 集群会自动将 6 个节点划分为 3 个主节点和 3 个从节点，每个主节点对应一个从节点。当主节点出现故障时，从节点会自动提升为主节点，保证集群的高可用性。
通过以上步骤，你可以成功配置并启动 Redis 集群，测试集群是否正常工作，并且已经自动配置了主从节点。
