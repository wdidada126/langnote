# 202505_week2

## 原有内容


## 时间范围

- 202505 月第 2 周

## 本周记录

- 20250508：华为鸿蒙PC发布；redis；sentinel模式启动
- 20250509：cat /etc/os-release；要查看 Git 仓库中某个文件夹的更新记录，你可以使用 `git log` 命令并指定路径。以下是具体步骤和一些常用的命令选项：；要查看特定文件夹下的所有更改记录，可以使用以下命令：
- 20250510：周六休息；尝试升级brew，失败；尝试恢复Mac，没找对移动硬盘，东芝那个
- 20250511：周日休息；iPhone xr支持快充，我一直用慢充；mac os，先安装操作系统，然后恢复备份，可以把原先用户文件完全删除
- 20250512：B站，Golang做流量调度的；在Go语言（Golang）中，进行流量调度相关的开发时，有多个开源库可以提供帮助。这里列出一些可能对你有用的开源库：；gorouter：虽然这不是一个直接的“流量调度”库，但它是Cloud Foundry使用的Go语言编写的路由，它可以根据请求的内容动态地将HTTP请求路由到不同的后端服务。
- 20250513：libssl；pkg-config --modversion openssl；结论：
- 20250514：以下是一个使用Java操作Apache Derby数据库的示例，模拟Nacos中可能的数据库操作（如用户认证表）。代码包含数据库连接、表创建、数据读写等基本操作。；数据库连接：；使用嵌入式模式（`jdbc:derby:nacos-data;create=true`）

## 整理补充

## 时间范围

- 202505 月第 2 周

## 本周记录

- 20250508：华为鸿蒙PC发布；redis；sentinel模式启动
- 20250509：cat /etc/os-release；要查看 Git 仓库中某个文件夹的更新记录，你可以使用 `git log` 命令并指定路径。以下是具体步骤和一些常用的命令选项：；要查看特定文件夹下的所有更改记录，可以使用以下命令：
- 20250510：周六休息；尝试升级brew，失败；尝试恢复Mac，没找对移动硬盘，东芝那个
- 20250511：周日休息；iPhone xr支持快充，我一直用慢充；mac os，先安装操作系统，然后恢复备份，可以把原先用户文件完全删除
- 20250512：B站，Golang做流量调度的；在Go语言（Golang）中，进行流量调度相关的开发时，有多个开源库可以提供帮助。这里列出一些可能对你有用的开源库：；gorouter：虽然这不是一个直接的“流量调度”库，但它是Cloud Foundry使用的Go语言编写的路由，它可以根据请求的内容动态地将HTTP请求路由到不同的后端服务。
- 20250513：libssl；pkg-config --modversion openssl；结论：
- 20250514：以下是一个使用Java操作Apache Derby数据库的示例，模拟Nacos中可能的数据库操作（如用户认证表）。代码包含数据库连接、表创建、数据读写等基本操作。；数据库连接：；使用嵌入式模式（`jdbc:derby:nacos-data;create=true`）
