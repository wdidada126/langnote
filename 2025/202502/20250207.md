# 20250207

nginx proxy manager
彻底搞懂反向代理神器Nginx Proxy Manager的配置和使用

除了DNS的A类型记录外，常见的还有以下类型：
1. AAAA记录（IPv6 Address Record）：用于将域名映射到IPv6地址，随着IPv6的广泛应用，它允许设备使用新的互联网协议版本进行通信，提供更多的地址空间和更好的安全性。
2. MX记录（Mail Exchanger Record）：即邮件交换记录，指向一个邮件服务器，用于电子邮件系统发邮件时根据收信人的地址后缀来定位邮件服务器。
3. CNAME记录（Canonical Name Record）：即别名记录，允许将多个名字映射到同一台计算机，也就是可以为一个主机设置多个别名，这些别名最终都指向同一个主机名。
4. NS记录（Name Server Record）：即域名服务器记录，用来指定该域名由哪个DNS服务器来进行解析。
5. TXT记录：可以存储任意文本信息，通常用于存储元数据或配置信息，如用于SPF（发送者策略框架）反垃圾邮件，向收信者表明哪些邮件服务器是经过某个域名认可会发送邮件的。
6. SRV记录（Service Record）：用于指定特定服务的服务器位置和端口号，比如指定一个LDAP服务器的地址和端口。
7. PTR记录（Pointer Record）：是A记录的逆向，将IP地址映射回域名，通常用于反向DNS查找，确定一个IP地址属于哪个域名。
8. SOA记录（Start of Authority Record）：标志着DNS区域的开始，并包含有关区域的信息，如主DNS服务器和区域的管理员联系信息。
9. DNSSEC记录：即DNS安全扩展记录，用于提供DNS数据的完整性和来源认证，包括DS（Delegation Signer）、DNSKEY（DNS密钥）和RRSIG（资源记录签名）等。
10. 其他记录：如LOC（地理位置）记录用于标识与域名相关的地理位置信息、NAPTR（名称服务器指针转换）记录用于处理电话号码映射等特定应用场景、SSHFP（SSH公钥指纹）记录用于验证SSH服务器的公钥等。


在Java中使用 ESAPI（OWASP Enterprise Security API） 处理JSON数据中的XSS漏洞时，需确保对JSON中的所有字符串字段进行适当的编码或验证。以下是针对JSON数据的XSS防护方案，结合ESAPI实现：

### 1. 核心思路
- 遍历JSON结构：递归处理JSON中的每个字段（包括嵌套对象和数组），对所有字符串值进行XSS过滤。
- 上下文敏感编码：根据数据最终的使用场景（HTML、JavaScript、URL等），选择合适的编码方式。
- 避免破坏JSON结构：确保编码操作不会损坏JSON的语法（如引号、转义符等）。

### 2. 依赖配置
确保项目中包含ESAPI依赖：
```xml
<dependency>
    <groupId>org.owasp.esapi</groupId>
    <artifactId>esapi</artifactId>
    <version>2.5.0.0</version> <!-- 使用最新版本 -->
</dependency>
```

---

### 3. 实现代码示例
#### (1) 递归处理JSON数据
使用`org.json`库或其他JSON解析工具（如Jackson/Gson），遍历所有字符串字段并编码：

```java
import org.owasp.esapi.Encoder;
import org.owasp.esapi.reference.DefaultEncoder;
import org.json.JSONObject;
import org.json.JSONArray;

public class JsonXssFilter {
    private static final Encoder encoder = DefaultEncoder.getInstance();

    public static Object sanitizeJson(Object data) {
        if (data instanceof JSONObject) {
            return sanitizeJsonObject((JSONObject) data);
        } else if (data instanceof JSONArray) {
            return sanitizeJsonArray((JSONArray) data);
        } else if (data instanceof String) {
            // 根据上下文选择合适的编码（此处以HTML为例）
            return encoder.encodeForHTML((String) data);
        }
        return data;
    }

    private static JSONObject sanitizeJsonObject(JSONObject jsonObject) {
        JSONObject sanitized = new JSONObject();
        for (String key : jsonObject.keySet()) {
            Object value = jsonObject.get(key);
            sanitized.put(key, sanitizeJson(value));
        }
        return sanitized;
    }

    private static JSONArray sanitizeJsonArray(JSONArray jsonArray) {
        JSONArray sanitized = new JSONArray();
        for (int i = 0; i < jsonArray.length(); i++) {
            Object value = jsonArray.get(i);
            sanitized.put(sanitizeJson(value));
        }
        return sanitized;
    }
}
```

#### (2) 使用示例
```java
// 原始JSON数据（可能包含XSS payload）
String rawJson = "{\"name\": \"<script>alert(1)</script>\", \"nested\": {\"data\": \"javascript:alert(1)\"}}";
JSONObject input = new JSONObject(rawJson);

// 执行XSS过滤
JSONObject sanitized = JsonXssFilter.sanitizeJsonObject(input);

// 输出结果：{"name":"&lt;script&gt;alert(1)&lt;/script&gt;","nested":{"data":"javascript:alert(1)"}}
System.out.println(sanitized.toString());
```

---

### 4. 上下文敏感编码
根据数据最终使用场景选择ESAPI编码方法：

| 上下文         | ESAPI编码方法                     | 示例场景                     |
|---------------------|---------------------------------------|------------------------------|
| HTML Body       | `encodeForHTML()`                    | 数据插入到HTML元素内容中      |
| HTML Attribute  | `encodeForHTMLAttribute()`           | 数据作为HTML属性值（如`href`）|
| JavaScript      | `encodeForJavaScript()`              | 数据嵌入到JavaScript代码中    |
| URL             | `encodeForURL()`                     | 数据作为URL参数              |
| CSS             | `encodeForCSS()`                     | 数据插入到CSS样式            |

---

### 5. 进阶优化
#### (1) 白名单验证
在编码前，可通过正则表达式或白名单策略验证数据格式（如邮箱、电话号码）：
```java
public static String validateAndEncode(String input, String regex) {
    if (!input.matches(regex)) {
        throw new IllegalArgumentException("Invalid input format");
    }
    return encoder.encodeForHTML(input);
}
```

#### (2) 自定义序列化（Jackson/Gson）
在JSON序列化阶段自动应用编码：
```java
// 使用Jackson自定义序列化器
public class XssSafeSerializer extends JsonSerializer<String> {
    @Override
    public void serialize(String value, JsonGenerator gen, SerializerProvider provider) 
      throws IOException {
        String encoded = encoder.encodeForHTML(value);
        gen.writeString(encoded);
    }
}

// 注册到ObjectMapper
ObjectMapper mapper = new ObjectMapper();
SimpleModule module = new SimpleModule();
module.addSerializer(String.class, new XssSafeSerializer());
mapper.registerModule(module);
```

#### (3) 性能优化
- 避免重复解析JSON，尽量在一次遍历中完成所有操作。
- 使用缓存机制减少Encoder实例化开销。

---

### 6. 注意事项
1. 不要过度编码：确保编码后的JSON仍为合法结构（如保留引号`"`）。
2. 区分数据类型：仅对字符串类型编码，避免误处理数字、布尔值等。
3. 前端协作：即使后端已编码，前端也需根据上下文再次处理（防御深度防御策略）。

---

通过上述方法，可以系统化地防御JSON数据中的XSS攻击。需根据实际场景调整编码策略，并配合其他安全措施（如CSP、输入验证）形成完整防护体系。


warning: in the working copy of 'src/main/resources/mybatis/oracle/mapper/apprflow/apprflow.xml', LF will be replaced by CRLF the next time Git touches it


git reset：取消暂存区的所有文件，但保留工作区的更改。
git reset <file>：取消暂存区的特定文件。
git reset --hard：取消暂存区的所有文件，并将工作区恢复到最近一次提交的状态。
git reset --soft：取消暂存区的所有文件，但保留工作区的更改


cd D:\work\yitong\codes\ecs-platform-batch
git checkout --orphan develop-beijing
git reset
git add -A
git commit -m "init"
git push origin develop-beijing -u

在 Git 中，使用 git checkout 命令可以创建一个新的空分支。以下是具体步骤和说明：
创建空分支的命令
bash复制
git checkout --orphan <new-branch-name>
参数说明
--orphan：这个选项用于创建一个全新的分支，该分支没有任何父提交（即它是一个空分支）。
<new-branch-name>：这是你想要创建的新分支的名称。
示例
假设你想创建一个名为 new-empty-branch 的空分支，可以运行以下命令：
bash复制
git checkout --orphan new-empty-branch
创建空分支后的情况
分支为空：创建后的新分支没有任何历史记录，它是一个完全独立的分支，没有任何父提交。
工作区状态：在切换到新分支后，工作区中的文件状态会保持不变（即之前分支中的文件仍然存在）。如果需要一个完全干净的工作区，可以运行以下命令删除所有文件：
bash复制
git rm -rf .
使用场景
创建空分支通常用于以下场景：
从头开始开发新功能：当你需要从一个全新的起点开始开发一个功能，而不依赖于当前仓库的任何历史记录时。
初始化新的子模块或项目：当你需要在当前仓库中初始化一个新的子模块或项目时，空分支可以提供一个干净的起点。
希望这些信息对你有帮助！


以下是 Java Servlet 中 `forward`（请求转发）和 `redirect`（重定向）的完整代码示例及区别说明：

---

### 1. Forward（请求转发）
#### 代码示例
```java
@WebServlet("/sourceServlet")
public class SourceServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 设置请求属性（传递数据）
        request.setAttribute("message", "Hello from SourceServlet!");
        
        // 获取 RequestDispatcher 对象，并转发到目标 Servlet/JSP
        RequestDispatcher dispatcher = request.getRequestDispatcher("/targetServlet");
        dispatcher.forward(request, response);
    }
}

@WebServlet("/targetServlet")
public class TargetServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 获取转发来的数据
        String message = (String) request.getAttribute("message");
        response.getWriter().println("Forwarded Message: " + message);
    }
}
```

#### 关键特性
- 服务器端跳转：客户端浏览器无感知，地址栏 URL 不变。
- 共享请求数据：通过 `request.setAttribute()` 传递数据。
- 路径规则：使用相对路径或绝对路径（以 `/` 开头时，相对于当前 Web 应用的根目录）。

---

### 2. Redirect（重定向）
#### 代码示例
```java
@WebServlet("/redirectServlet")
public class RedirectServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response) 
            throws ServletException, IOException {
        
        // 将数据存入 Session（用于跨请求传递）
        HttpSession session = request.getSession();
        session.setAttribute("redirectMessage", "Hello from RedirectServlet!");
        
        // 重定向到目标地址
        response.sendRedirect(request.getContextPath() + "/targetPage.jsp");
    }
}
```

#### 目标 JSP 页面 (`targetPage.jsp`)  
```jsp
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<html>
<head>
    <title>Target Page</title>
</head>
<body>
    <%
        // 从 Session 中获取数据
        String message = (String) session.getAttribute("redirectMessage");
    %>
    <h1>Redirected Message: <%= message %></h1>
</body>
</html>
```

#### 关键特性
- 客户端跳转：浏览器会发起新请求，地址栏 URL 变化。
- 无法直接共享请求数据：需通过 `Session` 或 URL 参数（如 `response.sendRedirect("target?param=value")`）传递数据。
- 路径规则：必须使用绝对路径（`request.getContextPath()` 确保路径正确）。

---

### 3. 对比总结
| 特性              | Forward（转发）                     | Redirect（重定向）               |
|-----------------------|-----------------------------------|---------------------------------|
| 请求次数           | 1 次（服务器内部处理）             | 2 次（浏览器发起新请求）         |
| 地址栏变化         | 否                                | 是                              |
| 数据传递方式       | `request.setAttribute()`          | `Session` 或 URL 参数           |
| 目标资源类型       | 同一 Web 应用内的资源              | 任意 URL（可跨应用或外部站点）   |
| 性能               | 更高（无额外网络请求）             | 较低（两次请求）                 |

---

### 4. 使用场景
- Forward：  
  - 需要共享请求数据的场景（如表单提交后跳转到结果页）。  
  - 隐藏实际资源路径（如权限控制后转发到登录页）。  

- Redirect：  
  - 防止表单重复提交（POST-REDIRECT-GET 模式）。  
  - 跳转到外部站点或不同 Web 应用的资源。  

---

### 5. 配置说明
#### XML 配置（替代 `@WebServlet` 注解）
```xml
<!-- web.xml -->
<servlet>
    <servlet-name>SourceServlet</servlet-name>
    <servlet-class>com.example.SourceServlet</servlet-class>
</servlet>
<servlet-mapping>
    <servlet-name>SourceServlet</servlet-name>
    <url-pattern>/sourceServlet</url-pattern>
</servlet-mapping>
```

---

通过以上代码和对比，可以清晰理解 `forward` 和 `redirect` 的用法及适用场景。




以下是一些开源的工业级C++11线程池库推荐：


1.task-thread-pool

• 特点：

• 快速且轻量级，专注于正确性、易用性、简洁性和性能。

• 单头文件库，易于集成，许可证宽松。

• 支持多种任务提交方式，包括函数、lambda表达式、`std::packaged_task`、`std::function`等。

• 支持并行化标准库算法（如`std::for_each`），方便与现有代码结合。

• 适用场景：适用于需要高性能、轻量级线程池的项目，尤其是对任务提交灵活性要求较高的场景。

• 安装方式：支持直接复制头文件、CMake或通过vcpkg安装。


2.ThreadPool

• 特点：

• 基于C++11标准，使用`std::shared_ptr`和`std::function`，代码简洁。

• 无外部依赖，易于移植到不同平台（如Linux、OSX、Unix等）。

• 提供了线程池的基本功能，如任务提交、线程管理等。

• 适用场景：适合对线程池功能需求较为基础，但需要跨平台支持的项目。

• 安装方式：直接下载源码并集成到项目中。


3.Win32 Thread Pools

• 特点：

• 基于Windows的Threadpool API封装，简化了API的使用。

• 提供了工作项、IO项、定时器项等多种任务类型。

• 支持自动销毁和手动管理两种模式。

• 适用场景：仅适用于Windows平台，适合需要利用Windows线程池API的项目。

• 安装方式：通过GitHub下载。


4.Boost.ThreadPool

• 特点：

• Boost库的一部分，功能强大且经过广泛测试。

• 提供了丰富的线程池功能，如任务调度、线程管理等。

• 与Boost库的其他组件（如Boost.Bind、Boost.Function）无缝集成。

• 适用场景：适合对线程池功能有较高要求的项目，尤其是已经使用Boost库的项目。

• 安装方式：通过Boost库安装。


5.RuedigerHelsch/ThreadPool

• 特点：

• 简单易用，基于C++11标准。

• 提供了基本的线程池功能，如任务提交、线程管理等。

• 适用场景：适合对线程池功能需求较为基础的项目。

• 安装方式：直接下载源码并集成到项目中。


总结

• 如果你需要一个轻量级且高性能的线程池，推荐使用`task-thread-pool`。

• 如果你的项目基于Windows，并且需要利用Windows的线程池API，可以选择`Win32 Thread Pools`。

• 如果你的项目已经使用了Boost库，或者需要更强大的线程池功能，可以考虑`Boost.ThreadPool`。

希望这些推荐对你有帮助！




双Token验证是一种常见的身份验证机制，通常使用两个令牌（Token）：访问令牌（Access Token）和刷新令牌（Refresh Token）。以下是双Token验证的基本原理和实现方式：


基本原理

1. 登录时生成双Token：用户登录成功后，服务器会生成两个Token，一个是有效期较短的访问令牌（Access Token），另一个是有效期较长的刷新令牌（Refresh Token）。访问令牌用于访问受保护的资源，而刷新令牌用于在访问令牌过期时重新获取新的访问令牌。

2. 访问令牌过期：当访问令牌过期时，客户端会使用刷新令牌向服务器请求新的访问令牌。服务器会验证刷新令牌的有效性，如果验证通过，则生成新的访问令牌和刷新令牌，并返回给客户端。

3. 刷新令牌过期：如果刷新令牌也过期或无效，用户需要重新登录。


优点

• 安全性更高：访问令牌有效期短，即使被窃取，攻击者也无法长期使用。刷新令牌虽然有效期长，但通常存储在服务器端（如Redis），不会直接暴露在网络中。

• 用户体验更好：通过刷新令牌自动获取新的访问令牌，用户无需频繁重新登录。


实现方式
以下是一个基于Spring Boot和JWT的双Token实现示例：

1. 登录接口：用户登录成功后，生成访问令牌和刷新令牌，并将它们返回给客户端。

2. 请求拦截器：在每次请求中，检查访问令牌的有效性。如果访问令牌过期，则使用刷新令牌请求新的访问令牌。

3. 刷新令牌接口：当访问令牌过期时，客户端调用刷新令牌接口，服务器验证刷新令牌的有效性，并生成新的访问令牌和刷新令牌。


示例代码片段

• 登录接口：

  ```java
    String accessToken = JWTUtil.getJwtToken(userDetail.getUsername(), TokenConstant.ACCESS_TOKEN_EXPIRATION_TIME);
    String refreshToken = JWTUtil.getJwtToken(userDetail.getUsername(), TokenConstant.REFRESH_TOKEN_EXPIRATION_TIME);
    // 将refreshToken保存到Redis
    redisTemplate.opsForValue().set(userDetail.getUsername() + TokenConstant.REFRESH_TOKEN_START_TIME, String.valueOf(System.currentTimeMillis()), TokenConstant.REFRESH_TOKEN_EXPIRATION_TIME, TimeUnit.MILLISECONDS);
    ```


• 刷新令牌接口：

  ```java
    @PostMapping("/refresh")
    public R<UserLoginVO> refresh(@RequestHeader(Constants.GATEWAY.REFRESH_TOKEN) String refreshToken) {
        return R.success(this.memberService.refresh(refreshToken));
    }
    ```


双Token验证机制在现代Web应用中被广泛应用，能够有效提升系统的安全性和用户体验。

