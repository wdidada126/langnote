# 202502_week2

## 时间范围

- 202502 月第 2 周

## 本周记录

- 20250208：补充了当日日志，暂未提炼出明确主题。
- 20250209：fortran.md；清华deepseek入门到精通
- 20250210：redis的key最好有前缀；spring::session::expires；redis sentianl的数据，有不同的序列化反序列化器
- 20250211：在Spring Boot中，RedisConnectionFactory是用于创建与 Redis 数据库连接的工厂类，它是 RedisTemplate 的依赖组件。RedisConnectionFactory 的来源主要有以下几种方式：；Spring Boot 自动配置；当你的 Spring Boot 项目中引入了 spring-boot-starter-data-redis 依赖时，Spring Boot 会自动配置一个 RedisConnectionFactory 实例。你只需要在 application.properties 或 application.yml 文件中配置 Redis 的连接信息，例如：
- 20250212：补充了当日日志，暂未提炼出明确主题。
- 20250213：cookie.md；Hutool库中的 `FileUtil.getAbsolutePath` 方法本身并不能直接解决文件路径包含 `../` 这类可能引发文件遍历漏洞的问题，但 Hutool 在处理路径时采用了一些机制来减少这类风险，下面为你详细分析：；文件遍历漏洞（也称为目录遍历漏洞）通常是由于应用程序没有对用户输入的文件路径进行充分的验证和过滤，导致攻击者可以通过构造包含 `../` 的路径来突破预期的目录限制，访问或操作系统中的其他敏感文件。例如，在一个允许用户指定文件下载路径的应用中，如果没有对用户输入的路径进行检查，攻击者可能会输入 `../../etc/passwd` 来尝试获取系统的敏感信息。
- 20250214：CPU with SSE4.1；SSE4.1 是 Streaming SIMD Extensions 4.1 的缩写，即单指令多数据扩展指令集 4.1，是英特尔公司开发的一种CPU 指令集扩展技术，具有以下特点和作用：；技术特点
