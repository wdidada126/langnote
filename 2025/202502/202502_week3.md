# 202502_week3

## 时间范围

- 202502 月第 3 周

## 本周记录

- 20250215：poco；自定义类；cursor ai编程功能
- 20250216：周日休息；安装cursor；mybatis，自定义redis cache
- 20250217：补充了当日日志，暂未提炼出明确主题。
- 20250218：Cursor的两种AI编程模式，Chat和Composer。；逆向这一块，确实很赚钱，但是太累太累了，倒不是说工作时长，而是脑力负荷比较高，心累。比如和前端开发相比，在熟练掌握以后，每天就是按部就班的来写代码即可，工作的时候，CPU（大脑）负荷保持在30%左右，而逆向研究，往往需要大脑负荷保持在100%，全神贯注，集中精力，反复调试，有时候还连续几天没有进展。；另一方面，很多普通程序员，其实只会输入与输出，即拿过教程和资料来学习，然后照葫芦画瓢的写代码完成正向开发。教程和资料没有的东西，网上搜不到的东西，就搞不定了。这种人其实占大多数，我以前的同事大部分都是这样的。逆向这一块，很多时候是没有参考资料的，网上也搜不到，大部分都得靠自己去研究，这一关卡住了很多人。
- 20250219：腾讯元宝；使用* model DeepSeek-R1来切换满血版R1模型。；netsh wlan set hostednetwork mode=allow ssid=dada-wins10 key=5Edidada
- 20250220：补充了当日日志，暂未提炼出明确主题。
- 20250221：在 C++ 中，`virtual void pureVirtualFunction() = 0;` 和 `virtual void pureVirtualFunction();` 虽然都涉及虚函数，但它们有着显著的区别，下面从定义、功能、使用场景等方面为你详细分析。；当在基类中声明一个虚函数时，在函数声明的末尾加上 `= 0`，这个函数就被定义为纯虚函数。包含纯虚函数的类被称为抽象类。示例代码如下：；定义接口：纯虚函数为派生类定义了一个必须实现的接口。基类通过纯虚函数规定了派生类需要实现的功能，但不提供具体的实现代码。
