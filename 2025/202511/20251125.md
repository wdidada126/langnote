# 20251125

cmake可以设置c c++标准，maven可以设置java标准，make如何设置？
antlr-runtime需要cpp17
make设置了没生效？

斯坦福

cs143
cs243
cs343

clang，读作k lang
可以查看源代码对应的语法树
汇编
clang可以查看词法，语法，语义，中间代码生成等阶段的中间产物
llvm ir
x86汇编
arm汇编
risv汇编

arm汇编可以用qemu模拟器运行

一个java jar同时支持gradle maven，jar包要存两份
/usr/local/下的三方库，如何同时兼容 pkg-config cmake？


github.com/fravenx/MIT-6.824-2023
强烈推荐，这个笔记写得很清晰 http://github.com/AccumulateMore/CV


make如何设置c++为14 17等版本，保证编译器支持？
在 Makefile 中设置 C++ 版本并确保编译器支持，有多种方法。以下是详细的实现方案：

🛠️ 方法一：直接设置 CXXFLAGS（推荐）

# 检查编译器是否支持C++17
CXX := g++
CPP_STD := c++17

# 测试编译器是否支持指定的C++标准
SUPPORTED := $(shell $(CXX) -std=$(CPP_STD) -E -x c++ /dev/null >/dev/null 2>&1 && echo "yes")

ifeq ($(SUPPORTED),yes)
    CXXFLAGS += -std=$(CPP_STD)
else
    # 如果不支持，尝试降级到C++14
    CPP_STD := c++14
    SUPPORTED := $(shell $(CXX) -std=$(CPP_STD) -E -x c++ /dev/null >/dev/null 2>&1 && echo "yes")
    ifeq ($(SUPPORTED),yes)
        CXXFLAGS += -std=$(CPP_STD)
        $(warning C++17 not supported, falling back to C++14)
    else
        # 如果还不支持，使用默认标准并报警告
        $(warning Neither C++17 nor C++14 supported, using compiler default)
    endif
endif

CXXFLAGS += -Wall -Wextra
TARGET = myapp
OBJS = main.o utils.o

$(TARGET): $(OBJS)
	$(CXX) -o $@ $(OBJS)

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

clean:
	rm -f $(OBJS) $(TARGET)

🔧 方法二：使用 feature_test 宏自动检测

CXX := g++
TARGET = myapp

# 自动检测编译器支持的最高C++标准
detect_std = $(shell \
    if $(CXX) -std=c++20 -E -x c++ /dev/null >/dev/null 2>&1; then \
        echo "c++20"; \
    elif $(CXX) -std=c++17 -E -x c++ /dev/null >/dev/null 2>&1; then \
        echo "c++17"; \
    elif $(CXX) -std=c++14 -E -x c++ /dev/null >/dev/null 2>&1; then \
        echo "c++14"; \
    elif $(CXX) -std=c++11 -E -x c++ /dev/null >/dev/null 2>&1; then \
        echo "c++11"; \
    else \
        echo ""; \
    fi \
)

CPP_STD := $(detect_std)

ifneq ($(CPP_STD),)
    CXXFLAGS += -std=$(CPP_STD)
    $(info Using C++ standard: $(CPP_STD))
else
    $(warning Could not detect supported C++ standard, using compiler default)
endif

# 强制指定版本（如果自动检测不满足需求，取消注释下面一行）
# CXXFLAGS += -std=c++17

CXXFLAGS += -Wall -Wextra -O2

OBJS = main.o utils.o

$(TARGET): $(OBJS)
	$(CXX) -o $@ $(OBJS) $(LDFLAGS)

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

.PHONY: check-compiler
check-compiler:
	@echo "Compiler: $(CXX)"
	@echo "Version: $(shell $(CXX) --version | head -1)"
	@echo "Supported C++ standard: $(CPP_STD)"

📋 方法三：支持命令行参数覆盖

CXX ?= g++
CXX_STD ?= c++17  # 默认使用C++17，可通过命令行覆盖

# 验证指定的标准是否支持
check_std = $(shell $(CXX) -std=$(1) -E -x c++ /dev/null >/dev/null 2>&1 && echo "supported")

# 检查用户指定的标准
ifneq ($(call check_std,$(CXX_STD)),supported)
    # 如果不支持，尝试一些备选方案
    ifeq ($(call check_std,c++17),supported)
        CXX_STD := c++17
        $(warning Requested C++ standard not supported, using C++17 instead)
    else ifeq ($(call check_std,c++14),supported)
        CXX_STD := c++14
        $(warning Requested C++ standard not supported, using C++14 instead)
    else ifeq ($(call check_std,c++11),supported)
        CXX_STD := c++11
        $(warning Requested C++ standard not supported, using C++11 instead)
    else
        $(error No supported C++ standard found!)
    endif
endif

CXXFLAGS += -std=$(CXX_STD)
CXXFLAGS += -Wall -Wextra -O2

TARGET = myapp
OBJS = main.o utils.o

$(TARGET): $(OBJS)
	$(CXX) -o $@ $(OBJS)

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

# 使用示例: make CXX_STD=c++14 来覆盖默认标准

🧪 验证编译器支持的完整测试

# 完整的编译器能力检测
CXX ?= g++

# 测试各种C++标准支持
SUPPORTED_STDS :=

# 检查特定标准是否支持
check_std = $(shell $(CXX) -std=$(1) -E -x c++ /dev/null >/dev/null 2>&1 && echo $(1))

# 测试所有现代C++标准
ifneq ($(call check_std,c++23),)
    SUPPORTED_STDS += c++23
endif
ifneq ($(call check_std,c++20),)
    SUPPORTED_STDS += c++20
endif
ifneq ($(call check_std,c++17),)
    SUPPORTED_STDS += c++17
endif
ifneq ($(call check_std,c++14),)
    SUPPORTED_STDS += c++14
endif
ifneq ($(call check_std,c++11),)
    SUPPORTED_STDS += c++11
endif

# 选择最高支持的标准
ifneq ($(words $(SUPPORTED_STDS)),0)
    HIGHEST_STD := $(firstword $(SUPPORTED_STDS))
else
    $(error No C++ standard supported by compiler!)
endif

# 使用最高支持的标准
CXXFLAGS += -std=$(HIGHEST_STD)
CXXFLAGS += -Wall -Wextra

$(info Using C++ standard: $(HIGHEST_STD))
$(info All supported standards: $(SUPPORTED_STDS))

TARGET = myapp
SRCS = main.cpp

$(TARGET): $(SRCS:.cpp=.o)
	$(CXX) -o $@ $^

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

.PHONY: info
info:
	@echo "Compiler: $(CXX)"
	@echo "Version: $(shell $(CXX) --version | head -1)"
	@echo "Highest supported C++ standard: $(HIGHEST_STD)"
	@echo "All supported standards: $(SUPPORTED_STDS)"

💡 使用示例

# 使用默认设置
make

# 指定特定的C++标准
make CXX_STD=c++14

# 使用不同的编译器
make CXX=clang++

# 查看编译器信息
make info

🎯 关键点总结

1. 兼容性检查：使用 
"-E -x c++ /dev/null" 来测试编译器是否支持特定标准而不实际编译代码
2. 优雅降级：当请求的标准不被支持时，自动降级到较低版本
3. 灵活性：支持通过命令行参数覆盖默认设置
4. 错误处理：提供清晰的警告和错误信息

这些方法确保了你的 Makefile 在不同环境和编译器下都能正常工作。



代码优化
啃源代码的，没上课，llvm社区里的资源很多，看代码实操结合社区里的文章和视频，慢慢积累的。

https://www.zhihu.com/question/509541648/answer/3278220857?share_code=HOxn4qqaAjZO&utm_psn=1976713292987786946
看见 using namespace std 的代码就一把掌扇过去，尤其是头文件里using namespace


cmake后缀的文件可以相互包含

clang目前是Gentoo Linux的衍生项目, Google ChromiumOS的默认编译器, 也是Android、FreeBSD、OpenBSD、NetBSD、macOS、iOS的默认编译器.

后来接触到国外的教材体系，才有一种豁然开朗的感觉。比如学线性代数，国内很多书上来就是行列式的定义，算逆矩阵。但你看Axler的那本或者Strang的书，人家告诉你线性代数是关于线性变换的，矩阵只是基底选定后的某种表达。这种视角的转换，直接决定了你日后处理高维数据时的思维高度。
疫情期间，有个去西班牙留学的女人，网爆自己父亲，怪父亲给她的生活费少了。
面试内容：算法题，CPU/GPU体系结构，LLVM，编译器算法（表调度，图着色，活跃区间分析，数据流分析...)
你是瞧不起旁边的云南吗？不说省委书记、市委书记了，光一个宣威帮你贵州就望尘莫及
不是写的最菜的程序员才回去国企吗？基友在联通一周1小时代码量，用的架构外面私企5年前都淘汰了。
创新中心刚成立还好，现在听说越来越卷了，而且只有大模型岗薪资给的多，其他岗也就那样
他的思维还停留在做题家阶段，原子化的城市中产阶级印记在他身上很明显，搞另一套的水平很低，跟胡百龙、周劼之类的比差远了。
工作能力是基础，除非天线可以对本系统的主要领导降维打击
https://gitee.com/wan3574489/Lex-and-Yacc/
为什么同黑人交往同居过的女子无人愿意接盘，这是为什么呢？
因为我国今年发现了之前从未在国内发现的HIV、HPV类型。

刘强东前妻龚小京，十几年前就辞职了，因为父辈人走茶凉再无升迁可能，，估计不只是前途无望，估计也被排挤过的不咋地。但是龚小京已经是非常争气的后代了，那个年代凭本事考人大啊。所以在中国想传承其实比登天还难是真的
