# 20251125

antlr4
.g4 不要从头开始写
从已有的抄

.l
.y也是的


clang -E


编译器构造2024
编译器构造实验
单招文

proj4-IRoptimizer/中间代码优化(2) 08:32
proj4-IRoptimizer/中间代码优化(1) 22:18
proj3-IRgen/中间代码生成 15:45
proj0-overview/VSCode开发框架介绍 20:58
proj1-lexer/词法分析器 09:24
proj2-parser(antlr)/语法分析器 14:05
proj2-parser(bison)/语法分析器 22:46
YatCC-现代小型编译器构建/毕昇杯RISC-V 34:23
autograder/自动评测机 03:38


antlr4_generate(
  task2-antlr # 唯一标识名
  ${CMAKE_CURRENT_SOURCE_DIR}/SYsUParser.g4 # 输入文件
  PARSER # 生成类型：LEXER/PARSER/BOTH
  FALSE # 是否生成 listener
  FALSE # 是否生成 visitor
  "" # C++ 命名空间
)


cmake antlr4_generate()是内置的还是用户自定义的？
`antlr4_generate` 是 ANTLR4 C++ 运行时库提供的 CMake 函数，而不是 CMake 内置命令。它定义在 ANTLR4 的 CMake 模块中（如 `Antlr4Package.cmake`），用于在构建过程中自动调用 ANTLR 生成词法分析器和解析器源文件。

要使用它，需要先通过 `find_package(antlr4-runtime)` 或 ExternalProject 集成 ANTLR4，然后调用 `antlr4_generate()` 函数生成目标文件。例如：

```cmake
find_package(antlr4-runtime REQUIRED)
antlr4_generate(
    TARGET my_parser
    INPUT MyGrammar.g4
    VISITOR  # 或 LISTENER，根据需要
)
```

这会创建生成目标（如 `antlr4cpp_generation_my_parser`），并设置相关的包含目录和源文件变量。更多细节可参考 ANTLR4 的官方 GitHub 仓库（runtime/Cpp/cmake）。