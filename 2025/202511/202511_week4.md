# 202511_week4

## 原有内容


## 时间范围

- 202511 月第 4 周

## 本周记录

- 20251122：这个问题涉及到字符编码的细节。<0xa0> 是一个特殊的空白字符，与普通空格有重要区别。；字符本质区别；普通空格（Space）
- 20251123：在Flex词法分析器中，要获取某个token所在的行号，你可以使用Flex内置的全局变量 yylineno。下面是一个快速参考表格，帮助你理解相关的变量和API：；变量/方法 说明 示例/备注；yylineno 全局变量，通常记录当前行号（从1开始）。 需在Flex文件定义部分用%option yylineno声明。
- 20251124：ANTLR4中的fragment关键字用于定义词法规则片段，这些规则本身不生成token，而是被其他词法规则引用。；fragment的基本概念；作用：
- 20251125：antlr4；.g4 不要从头开始写；从已有的抄
- 20251126：中间件，编程语言的选择；kafka，jvm语言；大数据组件，Java，后来觉得gc影响性能
- 20251127：详细讲解 XML 的 DOM 解析方式，包括其特点、与其他解析方式的对比，以及一个生成 XML 的例子。；DOM 解析 XML 文件的特点；DOM（Document Object Model，文档对象模型）解析方式的核心思想是：将整个 XML 文档一次性加载到内存中，并在内存中构建一个树形结构（DOM 树）来表示该文档。
- 20251128：实现clang -E xxx.c xxx.i；antlr；flex

## 整理补充

## 时间范围

- 202511 月第 4 周

## 本周记录

- 20251122：这个问题涉及到字符编码的细节。<0xa0> 是一个特殊的空白字符，与普通空格有重要区别。；字符本质区别；普通空格（Space）
- 20251123：在Flex词法分析器中，要获取某个token所在的行号，你可以使用Flex内置的全局变量 yylineno。下面是一个快速参考表格，帮助你理解相关的变量和API：；变量/方法 说明 示例/备注；yylineno 全局变量，通常记录当前行号（从1开始）。 需在Flex文件定义部分用%option yylineno声明。
- 20251124：ANTLR4中的fragment关键字用于定义词法规则片段，这些规则本身不生成token，而是被其他词法规则引用。；fragment的基本概念；作用：
- 20251125：antlr4；.g4 不要从头开始写；从已有的抄
- 20251126：中间件，编程语言的选择；kafka，jvm语言；大数据组件，Java，后来觉得gc影响性能
- 20251127：详细讲解 XML 的 DOM 解析方式，包括其特点、与其他解析方式的对比，以及一个生成 XML 的例子。；DOM 解析 XML 文件的特点；DOM（Document Object Model，文档对象模型）解析方式的核心思想是：将整个 XML 文档一次性加载到内存中，并在内存中构建一个树形结构（DOM 树）来表示该文档。
- 20251128：实现clang -E xxx.c xxx.i；antlr；flex
