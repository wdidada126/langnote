# 202507_week3

## 时间范围

- 202507 月第 3 周

## 本周记录

- 20250715：conan install . --build=missing -pr=default -of=build_conan；在 C++ 中，`std::vector` 是一个非常常用的动态数组容器。它的迭代器失效（iterator invalidation）问题是开发中常见的陷阱之一。；当 `vector` 的内存重新分配或元素被修改时，指向该 `vector` 元素的迭代器、指针、引用可能会变成“无效”，继续使用这些迭代器会导致 未定义行为（Undefined Behavior, UB）。
- 20250716：补充了当日日志，暂未提炼出明确主题。
- 20250717：王道c++微服务培训，北京；shared_ptr与weak_ptr结合使用解决循环引用问题 给出讲解和可执行的main函数源代码；在 C++ 中，`std::shared_ptr` 是一种智能指针，它通过引用计数来管理对象的生命周期。当最后一个 `shared_ptr` 被销毁时，所管理的对象也会被删除。
- 20250718：git clone https://github.com/DOCGroup/ACE_TAO.git；cd ACE_TAO/ACE；git checkout 675c2f88cf84591e0dcf995f6d0dddd2fd807df0
- 20250719：new_local_repository；本地代码库规则；new_local_repository
- 20250720：bazel，src下面有build文件，就不能添加src下面的源代码文件？；Rust Go sdk编译的库如何提供给使用方？；在Rust和Go中，如果你希望提供编译好的库给使用方而不暴露源代码，可以通过编译生成二进制文件或库文件，并提供相应的接口文档和头文件（对于 C FFI）。以下是针对这两种语言的具体做法：
- 20250721：C:\Users\wdidada\AppData\Local\Google\Chrome\User Data\Profile 3\Extensions；一串类似乱码的东西，找到manifest.json文件，然后看；"manifest_version": 3,
