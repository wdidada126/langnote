# 202507_week2

## 时间范围

- 202507 月第 2 周

## 本周记录

- 20250708：conda；python包管理器；虽然 Conda 最初是为了支持 Python 而开发的，但它也支持其他编程语言，如 R、Ruby、Lua、Scala、Java、JavaScript、C/ C++ 等。
- 20250709：/usr/local/musl/bin/musl-gcc -L/usr/local/musl/lib hello.c -o hello -lc；./configure --prefix=/usr/local/musl --enable-shared --enable-static --enable-wrapper=no --target=x86_64-linux-musl；make
- 20250710：在使用 `git clone` 时，如果你希望保留代码中的换行符为 Linux 风格的 LF（而不是 Windows 上默认转换成的 CRLF），你需要配置 Git 的 自动换行符处理行为。；> 这样 Git 在提交时不会自动把 LF 转为 CRLF，在检出时不转回 LF。适合跨平台协作、服务器部署是 Linux 的场景。；你不需要做特别配置，默认就是：
- 20250711：最近高德好多组都在转rust；尤其是嵌入式的C,芯片型号不一样很可能就用不成,我几乎没有找到过拿来就能用的库,都是手撸. 手撸过BootLoader,调度器,消息队列,事件组啥的,用到啥写啥；上世纪金融软件主要使用C语言而非C++或Fortran，主要有以下几个原因：
- 20250712：阿里云统一云管平台（Apsara Uni-manager）是面向阿里云专有云的企业级云管理平台，提供全方位的云资源供给、运维和运营管理能力，具备一体化管控、自动化运维、智能化分析及个性化扩展等核心竞争力；致力于为客户提供优质的用户体验，简化专有云管理，加速政企数字化转型。；Bevy 是一个用 Rust 编写的开源游戏引擎，具有模块化、跨平台、高性能等特点，适合 2D/3D 游戏开发。；git@github.com:edidada/testcompress.git
- 20250713：你问的是这两行 C++ 代码的异同：；这两行代码在功能上是完全等价的，都会动态分配一个 `NotFoundHandler` 类型的对象并返回其指针。唯一的区别是：；> 第一行多写了关键字 `class`，而第二行省略了它。
- 20250714：vcredist_x64.exe是Microsoft Visual C++ Redistributable 的 64 位版本,它提供了运行基于 Visual C++ 编写的应用程序所需的库文件；`VC_redist.x64.exe` 是 Microsoft Visual C++ Redistributable for Visual Studio 的可再发行组件安装包，专为 64位（x64）Windows 系统设计。；这个文件的作用是 在目标计算机上安装运行使用 Microsoft Visual C++ 开发的应用程序所必需的运行时组件。
