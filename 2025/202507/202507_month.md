# 202507_month

## 本月概览

- 本月共整理 32 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202507_week1：- 20250701：OceanBase、TiDB 和高斯数据库（GaussDB）都是当前主流的分布式数据库系统，它们在分布式事务的实现上各有特点，同时也存在一些相似之处。以下是它们在分布式事务实现机制、联系与区别的详细分析：；OceanBase 采用 Paxos + 两阶段提交（2PC） 的机制来实现分布式事务，并结合 多版本并发控制（MVCC） 和 全局时钟服务（GTS） 来保证隔离性和一致性：；两阶段提交（2PC）：OceanBase 使用 2PC 确保事务的原子性，协调者（Coordinator）负责管理事务的提交或回滚。
- 202507_week2：- 20250708：conda；python包管理器；虽然 Conda 最初是为了支持 Python 而开发的，但它也支持其他编程语言，如 R、Ruby、Lua、Scala、Java、JavaScript、C/ C++ 等。
- 202507_week3：- 20250715：conan install . --build=missing -pr=default -of=build_conan；在 C++ 中，`std::vector` 是一个非常常用的动态数组容器。它的迭代器失效（iterator invalidation）问题是开发中常见的陷阱之一。；当 `vector` 的内存重新分配或元素被修改时，指向该 `vector` 元素的迭代器、指针、引用可能会变成“无效”，继续使用这些迭代器会导致 未定义行为（Undefined Behavior, UB）。
- 202507_week4：- 20250722：antd vue版本；支持服务端渲染；安卓，iOS如何组件化？
- 202507_week5：- 20250729：C:\Users\wdidada> npm i nrm -g；added 32 packages in 20s；8 packages are looking for funding

## 本月高频主题

- OceanBase、TiDB 和高斯数据库（GaussDB）都是当前主流的分布式数据库系统，它们在分布式事务的实现上各有特点，同时也存在一些相似之处。以下是它们在分布式事务实现机制、联系与区别的详细分析：
- OceanBase 采用 Paxos + 两阶段提交（2PC） 的机制来实现分布式事务，并结合 多版本并发控制（MVCC） 和 全局时钟服务（GTS） 来保证隔离性和一致性：
- 两阶段提交（2PC）：OceanBase 使用 2PC 确保事务的原子性，协调者（Coordinator）负责管理事务的提交或回滚。
- LearnCpp.com
- 学 C++ 的那段时间，我一边看教程，一边跟不上群里的聊天，别人聊 STL、智能指针、模板编程，我连这玩意干嘛用都搞不明白。
- 那时候为了找一套看得懂、学得下去的资料，我真的是在 B 站、博客、知乎之间来回跳。后来终于找到了几家我现在还在用的“常驻网站”，今天就来分享下。
- 你遇到的问题是：
- > “DATA 表需要在子查询中使用，但是访问不了”
- 从你的 SQL 看来，你在外层查询中引用了 `DATA` 子查询中的字段（如 `DATA.year`, `DATA.type`），但 `DATA` 是内嵌在 `FROM (...) DATA` 中的一个别名，并没有被暴露到最外层，因此无法直接访问。
- 自己源代码安装了tinyrpc，在cmake文件夹下新建xx.cmake.文件，库，如果找到，打印相关信息，没找到，报错
- 你想要实现一个自定义的 CMake 模块（比如 `FindTinyRPC.cmake`），用于检测系统中是否安装了你自己源码编译并安装的 `tinyrpc` 库。如果找到，打印相关信息；如果没有找到，则报错。
- 假设你是通过以下方式安装的：
