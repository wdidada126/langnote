# 202507_week1

## 时间范围

- 202507 月第 1 周

## 本周记录

- 20250701：OceanBase、TiDB 和高斯数据库（GaussDB）都是当前主流的分布式数据库系统，它们在分布式事务的实现上各有特点，同时也存在一些相似之处。以下是它们在分布式事务实现机制、联系与区别的详细分析：；OceanBase 采用 Paxos + 两阶段提交（2PC） 的机制来实现分布式事务，并结合 多版本并发控制（MVCC） 和 全局时钟服务（GTS） 来保证隔离性和一致性：；两阶段提交（2PC）：OceanBase 使用 2PC 确保事务的原子性，协调者（Coordinator）负责管理事务的提交或回滚。
- 20250702：LearnCpp.com；学 C++ 的那段时间，我一边看教程，一边跟不上群里的聊天，别人聊 STL、智能指针、模板编程，我连这玩意干嘛用都搞不明白。；那时候为了找一套看得懂、学得下去的资料，我真的是在 B 站、博客、知乎之间来回跳。后来终于找到了几家我现在还在用的“常驻网站”，今天就来分享下。
- 20250703：你遇到的问题是：；> “DATA 表需要在子查询中使用，但是访问不了”；从你的 SQL 看来，你在外层查询中引用了 `DATA` 子查询中的字段（如 `DATA.year`, `DATA.type`），但 `DATA` 是内嵌在 `FROM (...) DATA` 中的一个别名，并没有被暴露到最外层，因此无法直接访问。
- 20250704：自己源代码安装了tinyrpc，在cmake文件夹下新建xx.cmake.文件，库，如果找到，打印相关信息，没找到，报错；你想要实现一个自定义的 CMake 模块（比如 `FindTinyRPC.cmake`），用于检测系统中是否安装了你自己源码编译并安装的 `tinyrpc` 库。如果找到，打印相关信息；如果没有找到，则报错。；假设你是通过以下方式安装的：
- 20250705：D:\develops\git\github\c\testlibevent\cmake_third_party\libevent；file:///D:/develops/git/github/c/testlibevent/cmake_third_party/libevent/html/index.html；libevent
- 20250706：截至25年6月，C++23暂未达到生产可用，但可在个人项目尝鲜。；以下是几个基于 C++23 的开源项目推荐（部分可能使用较新的 C++20/23 特性），这些项目在 GitHub 上活跃，适合学习和参考：；语言标准：C++17/20/23（部分优化使用新特性）
- 20250707：特斯拉Autopilot项目的重要人物之一，Jingdong Hou；华人大牛离开特斯拉 事件的主人公名叫Zheng Gao，在特斯拉工作了8年之久。过去三年，Zheng Gao一直担任特斯拉自动驾驶硬件工程总监；find_package(Threads REQUIRED)
