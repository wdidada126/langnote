# 202510_week4

## 时间范围

- 202510 月第 4 周

## 本周记录

- 20251022：git remote prune gitee是一个Git命令，它的主要作用是清理本地仓库中那些在远程仓库（gitee）中已经不存在，但本地仍然有记录的过时分支引用。；简单来说，这个命令会：；• 与指定的远程仓库（这里是 gitee）进行通信
- 20251023：关键字表算法的核心就是利用高效的查找结构 (如哈希表) 来快速区分一个形如标识符的字符序列到底是语言保留的关键字还是用户自定义的标识符。；提供一个使用CMake. 构建的 C++ 示例项目，该项目使用 `std::unordered_map` 来实现关键字表算法。；这个例子模拟了词法分析器的一个核心功能：识别输入字符串中的关键字和标识符。
- 20251024：编译原理（Compilers）课程的大作业通常涉及词法分析、语法分析、中间代码生成、优化和目标代码生成等环节，许多学生会实现小型编译器或解释器。这些开源项目多基于C/C++实现，适合本科生/硕士生参考学习（包括实验报告、测试用例和框架）。我从GitHub等平台筛选了高质量的学生项目，按难度和完整度分类。建议克隆仓库后阅读README，结合龙书（Compilers: Principles, Techniques, and Tools）验证实现。；这些项目聚焦前端（lexer/parser），使用Flex/Bison工具，适合初学者快速上手。；| 项目名称 | 描述 | 语言 | 链接 | 亮点 |
- 20251025：C/C++项目编译耗时长，尤其是大型项目（如 Chromium、LLVM），分布式编译可以将编译任务分发到多台机器（本地或远程），显著加速构建过程。常见构建工具包括 Make、CMake、Ninja 等，这些工具支持并行编译（通过 `-j` 选项），但分布式需结合专用工具如distcc、icecc（icecream）、ccache（缓存加速）、FASTBuild、yadcc 等。ccache常与分布式工具结合使用，提供缓存机制，进一步提升效率。；分布式编译的核心原理：客户端预处理源代码，将独立编译任务（.c/.cpp 文件）分发到远程节点编译，远程节点返回对象文件（.o），客户端完成链接。挑战包括网络延迟、工具链一致性（编译器版本、库依赖）和防火墙配置。；以下基于开源工具和行业实践，详细说明常见工具及其设置方法。数据来源于GitHub、CSDN、Stack Overflow等公开资源（2025 年最新）。推荐从distcc + ccache开始，适合初学者。
- 20251026：编译原理大作业（其他） 计算器；谷歌开源编译工具bazel如何支持分布式编译的？；/home/runner/work/testre2/testre2/src/thompson.cpp:70:5: error: no template named 'function'
- 20251027：sudo apt install -y qtbase5-dev qtbase5-dev-tools libqt5widgets5 libqt5gui5 libqt5core5a；你的问题涉及编译原理中正则表达式（Regular Expression, RE）及其引擎在词法分析中的应用，以及如何从正则表达式逐步转换为非确定有限自动机（NFA）和确定有限自动机（DFA）来处理源代码。这个过程确实是编译器前端（词法分析）的核心部分。以下详细解答你的问题，结合编译原理的核心概念，逐步解释正则表达式、RE引擎、NFA和DFA在处理源代码中的作用，并分析其实现逻辑和意义。；在编译原理中，正则表达式和正则引擎是词法分析（Lexical Analysis）的核心工具，用于将源代码分解为词法单元（Tokens），如关键字、标识符、运算符等。你的描述“写一个RE，加上一个RE引擎来处理源代码”是对词法分析过程的简化表达，但非常贴近实际。下面具体分析：
- 20251028：这个错误表明程序运行时找不到 libre2.so.11 动态链接库。以下是完整的解决方案：；问题原因；您通过源码安装了 RE2 库（安装到 /usr/local/lib）
