# 202504_month

## 原有内容


## 原有内容


conan复习

conan center官方的，只保留最近几个版本的编译文件

压缩c库写例子

antlr4 c++
antlr4 词法器,解析器,分开的,词法器生成tokens,解析器依赖tokens
在 ANTLR 中，你可以在语法文件里嵌套 Java 或者 C++ 代码，这些代码通常用于实现动作（actions）和监听器（listeners）等功能。
.g4 add c++ code
https://github.com/antlr/antlr4/blob/dev/doc/cpp-target.md

学习antlr这个话题很大，你的需求不同，学习的深度也不一样。antlr是一个cc(compiler compiler)，你需要有一点点的编译原理知识，但是相信我真的不需要很多。如果你的文本是确定性的，完全没有语法错误的文本。那么你要做的大概率就是一个反序列化的过程，即把文本变成一个结构化的内存数据。这种情况下，你只需要保证语法、词法的定义即可。如果你是要做一个类似编译器的东西，希望可以像rust一样有比较丰富的报错信息，那么你就需要处理报错相关的内容了，这个时候可以看一下 @zxh404 大佬推荐的《ANTLR 4权威指南》。这本书里面就有介绍，在报错时如何处理（关键词 ErrorListener）

ANTLR v4 - Plugins | JetBrains 好用到爆炸的IDEA plugin，输入待解析的文本，自己一边编写语法文件，一边通过解析树查看是否符合预期，另外还有红色背景色高亮，来提示自己写的文件是否有问题。非常舒适

对于快速入门我的建议是，自己手写一个json的解析器。目标是：

能够自己独立写出g4文件
能够自己独立写出词法和句法的语法定义文件
能够知道如何遍历解析出来的语法树，通过API访问自己需要访问的部分

https://abcdabcd987.com/notes-on-antlr4/

https://abcdabcd987.com/using-antlr4/

ANTLR4笔记.mhtml

## 整理补充

## 本月概览

- 本月共整理 30 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202504_week1：- 20250401：redis sentinel；curl -X POST 'https://qygj-uat.cdrcb.com/ecs-mobile-gateway/ares-ent-server/approval/queryApprPendingList.do' -H 'Content-Type: application/json' -H 'Cookie: SESSION=MjU1ZDExMDEtZWI5MC00OWQxLTg1YWItNDBjMDZkOThhZjE0' -d '{；"head": {
- 202504_week2：- 20250408：mcp.md；CoAP（Constrained Application Protocol）；简介：专为受限设备和网络设计的应用层协议，基于 RESTful 架构风格，与 HTTP 协议有一定的相似性。
- 202504_week3：- 20250415：Spring Security 配置不再难！基于流程图的定制化配置全攻略；本文试图通过图表和代码样例详细分析基本配置，为进一步的定制提供了一个起点。；单机版OceanBase，中小规模业务中的“性价比之王”
- 202504_week4：- 20250422：chrome查看书签同步记录；chrome.md；wxWidgets

## 本月高频主题

- redis sentinel
- curl -X POST 'https://qygj-uat.cdrcb.com/ecs-mobile-gateway/ares-ent-server/approval/queryApprPendingList.do' -H 'Content-Type: application/json' -H 'Cookie: SESSION=MjU1ZDExMDEtZWI5MC00OWQxLTg1YWItNDBjMDZkOThhZjE0' -d '{
- "head": {
- D:\develops\git\cpp\ffmpeg_qt_project
- find html -mindepth 1 -not -path 'html/wxcp*' -exec cp -r {} html2 \;
- C++实战
- 在C++中，模板类的实现通常放在头文件中，而不是单独的源文件中。这是因为模板类的实例化需要在编译时完成，而编译器需要看到模板的完整定义才能正确生成代码。以下是关于模板类在头文件中实现的一些要点和示例。
- 为什么模板类的实现需要放在头文件中？
- 模板类的代码生成依赖于具体的模板参数。当使用模板类时，编译器需要根据传入的模板参数（如类型、值等）来生成具体的代码。如果模板类的实现放在源文件中，编译器在编译源文件时无法知道所有可能的模板实例化情况，因此无法生成完整的代码。而将模板类的实现放在头文件中，可以确保编译器在处理任何使用模板类的地方时都能看到完整的定义。
- 清明节放假
- 清明节放假
- [清华大学研究人员开源了 CogAgent-9B-20241220](https://www.infoq.cn/news/vXo69hgVtfViNa7ALrVz)

## 整理补充

## 本月概览

- 本月共整理 30 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202504_week1：- 20250401：redis sentinel；curl -X POST 'https://qygj-uat.cdrcb.com/ecs-mobile-gateway/ares-ent-server/approval/queryApprPendingList.do' -H 'Content-Type: application/json' -H 'Cookie: SESSION=MjU1ZDExMDEtZWI5MC00OWQxLTg1YWItNDBjMDZkOThhZjE0' -d '{；"head": {
- 202504_week2：- 20250408：mcp.md；CoAP（Constrained Application Protocol）；简介：专为受限设备和网络设计的应用层协议，基于 RESTful 架构风格，与 HTTP 协议有一定的相似性。
- 202504_week3：- 20250415：Spring Security 配置不再难！基于流程图的定制化配置全攻略；本文试图通过图表和代码样例详细分析基本配置，为进一步的定制提供了一个起点。；单机版OceanBase，中小规模业务中的“性价比之王”
- 202504_week4：- 20250422：chrome查看书签同步记录；chrome.md；wxWidgets
- 202504_week5：- 20250429：`MySqlLexer.interp` 和 `MySqlLexer.tokens` 这两个文件是 ANTLR（Another Tool for Language Recognition）在生成词法分析器（Lexer）时产生的辅助文件，下面分别介绍它们的作用。；用途：这是一个解释器数据文件，主要存储着 ANTLR 生成的词法分析器的状态机信息。状态机在词法分析过程里用于识别输入文本中的词法单元（Token），像关键字、标识符、运算符等。；工作原理：在词法分析时，词法分析器借助 `MySqlLexer.interp` 文件中的状态机信息，对输入的字符流进行扫描和处理。状态机依据输入字符的不同，在不同状态之间进行转换，从而识别出各种词法单元。

## 本月高频主题

- redis sentinel
- curl -X POST 'https://qygj-uat.cdrcb.com/ecs-mobile-gateway/ares-ent-server/approval/queryApprPendingList.do' -H 'Content-Type: application/json' -H 'Cookie: SESSION=MjU1ZDExMDEtZWI5MC00OWQxLTg1YWItNDBjMDZkOThhZjE0' -d '{
- "head": {
- D:\develops\git\cpp\ffmpeg_qt_project
- find html -mindepth 1 -not -path 'html/wxcp*' -exec cp -r {} html2 \;
- C++实战
- 在C++中，模板类的实现通常放在头文件中，而不是单独的源文件中。这是因为模板类的实例化需要在编译时完成，而编译器需要看到模板的完整定义才能正确生成代码。以下是关于模板类在头文件中实现的一些要点和示例。
- 为什么模板类的实现需要放在头文件中？
- 模板类的代码生成依赖于具体的模板参数。当使用模板类时，编译器需要根据传入的模板参数（如类型、值等）来生成具体的代码。如果模板类的实现放在源文件中，编译器在编译源文件时无法知道所有可能的模板实例化情况，因此无法生成完整的代码。而将模板类的实现放在头文件中，可以确保编译器在处理任何使用模板类的地方时都能看到完整的定义。
- 清明节放假
- 清明节放假
- [清华大学研究人员开源了 CogAgent-9B-20241220](https://www.infoq.cn/news/vXo69hgVtfViNa7ALrVz)
