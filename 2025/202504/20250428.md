# 20250428

testcppantlr4
https://github.com/edidada/testcppantlr4

    static constexpr size_t EPSILON = std::numeric_limits<size_t>::max() - 1;

java命令行生成antlr的g4文件对应的程序代码失败
antrl的maven插件生产Java代码成功

mysql_8_doc.md

grammar Json;

lexer grammar MySqlLexer;
parser grammar MySqlParser;

options {
    tokenVocab = MySqlLexer;
}