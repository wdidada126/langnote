# 202504_week1

## 原有内容


## 时间范围

- 202504 月第 1 周

## 本周记录

- 20250401：redis sentinel；curl -X POST 'https://qygj-uat.cdrcb.com/ecs-mobile-gateway/ares-ent-server/approval/queryApprPendingList.do' -H 'Content-Type: application/json' -H 'Cookie: SESSION=MjU1ZDExMDEtZWI5MC00OWQxLTg1YWItNDBjMDZkOThhZjE0' -d '{；"head": {
- 20250402：D:\develops\git\cpp\ffmpeg_qt_project；find html -mindepth 1 -not -path 'html/wxcp*' -exec cp -r {} html2 \;；C++实战
- 20250403：在C++中，模板类的实现通常放在头文件中，而不是单独的源文件中。这是因为模板类的实例化需要在编译时完成，而编译器需要看到模板的完整定义才能正确生成代码。以下是关于模板类在头文件中实现的一些要点和示例。；为什么模板类的实现需要放在头文件中？；模板类的代码生成依赖于具体的模板参数。当使用模板类时，编译器需要根据传入的模板参数（如类型、值等）来生成具体的代码。如果模板类的实现放在源文件中，编译器在编译源文件时无法知道所有可能的模板实例化情况，因此无法生成完整的代码。而将模板类的实现放在头文件中，可以确保编译器在处理任何使用模板类的地方时都能看到完整的定义。
- 20250404：清明节放假
- 20250405：清明节放假；[清华大学研究人员开源了 CogAgent-9B-20241220](https://www.infoq.cn/news/vXo69hgVtfViNa7ALrVz)；项目链接：https://github.com/inclusionAI/AReaL
- 20250406：清明节放假
- 20250407：补充了当日日志，暂未提炼出明确主题。

## 整理补充

## 时间范围

- 202504 月第 1 周

## 本周记录

- 20250401：redis sentinel；curl -X POST 'https://qygj-uat.cdrcb.com/ecs-mobile-gateway/ares-ent-server/approval/queryApprPendingList.do' -H 'Content-Type: application/json' -H 'Cookie: SESSION=MjU1ZDExMDEtZWI5MC00OWQxLTg1YWItNDBjMDZkOThhZjE0' -d '{；"head": {
- 20250402：D:\develops\git\cpp\ffmpeg_qt_project；find html -mindepth 1 -not -path 'html/wxcp*' -exec cp -r {} html2 \;；C++实战
- 20250403：在C++中，模板类的实现通常放在头文件中，而不是单独的源文件中。这是因为模板类的实例化需要在编译时完成，而编译器需要看到模板的完整定义才能正确生成代码。以下是关于模板类在头文件中实现的一些要点和示例。；为什么模板类的实现需要放在头文件中？；模板类的代码生成依赖于具体的模板参数。当使用模板类时，编译器需要根据传入的模板参数（如类型、值等）来生成具体的代码。如果模板类的实现放在源文件中，编译器在编译源文件时无法知道所有可能的模板实例化情况，因此无法生成完整的代码。而将模板类的实现放在头文件中，可以确保编译器在处理任何使用模板类的地方时都能看到完整的定义。
- 20250404：清明节放假
- 20250405：清明节放假；[清华大学研究人员开源了 CogAgent-9B-20241220](https://www.infoq.cn/news/vXo69hgVtfViNa7ALrVz)；项目链接：https://github.com/inclusionAI/AReaL
- 20250406：清明节放假
- 20250407：补充了当日日志，暂未提炼出明确主题。
