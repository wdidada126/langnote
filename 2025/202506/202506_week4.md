# 202506_week4

## 原有内容


## 时间范围

- 202506 月第 4 周

## 本周记录

- 20250622：周日休息；库名	特点；Boost.Process	跨平台，功能丰富
- 20250623：std::print 还不是 C++23 的正式标准功能！；虽然 C++23 引入了 std::print，但目前主流编译器（如 MSVC、GCC、Clang）尚未完全支持这一特性。特别是你在使用的是 MSVC Visual Studio 2022 (v14.38)，它还不支持 std::print。；在 Oracle SQL 中，如果你的 `UPDATE` 语句中某个字段值包含特殊字符 `&`（例如字符串 `'Tom&Jerry'`），你需要特别处理它，否则：
- 20250624：vtable.md；`-fdump-class-hierarchy` 是 GCC 编译器 提供的一个调试选项，用于 输出 C++ 类的继承结构和虚函数表（vtable）信息。这个选项对于理解多态、虚函数调用、类布局等底层机制非常有用。；| 参数 | 作用 |
- 20250625：sudo apt-get install libfilezilla-dev libwxbase3.0-dev gnutls-dev libdbus-1-dev；sudo apt-get install libwxgtk3.0-gtk3-dev；sudo apt-get install libgtk-3-dev
- 20250626：SCADA.md
- 20250627：代码风格良好，熟悉Win32编程，熟悉MFC框架，熟悉常用Windows API和编程风格，熟悉GDI编程，熟悉Visual  Studio调试；熟悉C++，最好熟悉C++11；熟悉网络编程和多线程编程
- 20250628：周六休息；/usr/lib/libstdc++.6.dylib；To use the bundled libc++ please add the following LDFLAGS:

## 整理补充

## 时间范围

- 202506 月第 4 周

## 本周记录

- 20250622：周日休息；库名	特点；Boost.Process	跨平台，功能丰富
- 20250623：std::print 还不是 C++23 的正式标准功能！；虽然 C++23 引入了 std::print，但目前主流编译器（如 MSVC、GCC、Clang）尚未完全支持这一特性。特别是你在使用的是 MSVC Visual Studio 2022 (v14.38)，它还不支持 std::print。；在 Oracle SQL 中，如果你的 `UPDATE` 语句中某个字段值包含特殊字符 `&`（例如字符串 `'Tom&Jerry'`），你需要特别处理它，否则：
- 20250624：vtable.md；`-fdump-class-hierarchy` 是 GCC 编译器 提供的一个调试选项，用于 输出 C++ 类的继承结构和虚函数表（vtable）信息。这个选项对于理解多态、虚函数调用、类布局等底层机制非常有用。；| 参数 | 作用 |
- 20250625：sudo apt-get install libfilezilla-dev libwxbase3.0-dev gnutls-dev libdbus-1-dev；sudo apt-get install libwxgtk3.0-gtk3-dev；sudo apt-get install libgtk-3-dev
- 20250626：SCADA.md
- 20250627：代码风格良好，熟悉Win32编程，熟悉MFC框架，熟悉常用Windows API和编程风格，熟悉GDI编程，熟悉Visual  Studio调试；熟悉C++，最好熟悉C++11；熟悉网络编程和多线程编程
- 20250628：周六休息；/usr/lib/libstdc++.6.dylib；To use the bundled libc++ please add the following LDFLAGS:
