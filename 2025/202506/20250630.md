# 20250630

真正的C++布道者吧，免得大家被某些只会利用信息差”借鉴“外文著作的人割韭菜：
吴咏炜，李建忠，B站双笙子佯谬，陈硕，Rainer Grimm，Jason Turner，Mike Shah，Cherno，谢丙堃，d2learn社区发起人SPeak.......github上还有不少开源教程，以及有一些C++教程博客均很优秀强烈推荐吴咏炜，谢丙堃两位大佬，这两位均是有架构师和技术专家履历的实战派；是被C++ summit特邀认可的顶级专家：
1）前者在极客时间有一套与时俱进且贴近工程实践的现代C++教程，聚焦于生产环境上最实用的C++11~20特性；
2）后者在B站上免费放出了《现代C++语言核心特性解析》的视频讲解，是新手初学现代C++特性的最优教程
3）如果还不满意，也想做一个语法律师，模仿什么也够黑，也够白的，就对着cppref或者下面这个仓库，查漏补缺：AnthonyCalandra/modern-cpp-features
注：截至25年6月；C++23暂未达到生产可用，但可在个人项目尝鲜
------------------------------------------------------------------------------------------------------------------有些想搞嵌入式和底层开发的同学需要了解高度透明的内存管理方式，有些场景不建议使用C++中某些会进行隐式内存管理的特性，推荐下C/C++98+STL的教程：C：
1）阮一峰的C语言教程元编程：
1）《Template Metaprogramming with C++ 》；
陈晓伟在github翻译了中文版本2）gitbook：magicbowen
3）gitbook：rayonicks
自己搓一个小型STL：
1）B站双笙子佯谬
2）d2learn的手搓数据结构开源教程

git clone https://github.com/AnthonyCalandra/modern-cpp-features.git
D:\develops\git\github\cpp\modern-cpp-features

不要在头文件里using namespace

C++程序设计实践教程 :新国标微课版 :new international standard with micro-lecture
出版社：华中科技大学出版社
ISBN：9787568094825
出版年：2023
作者：马光志
资源类型：图书细分
类型：中文文献

如何在Drogon中使用C++14/17特性
在 Drogon 中，开发者可以充分利用 C++14 和 C++17 带来的便利。例如，通过 lambda 表达式的使用，可以轻松定义事件处理器或路由回调函数，使得代码逻辑更加清晰直观。下面是一个简单的示例，展示了如何使用 lambda 函数来响应 HTTP 请求：

```cpp
#include <drogon/drogon.h>
// 定义一个简单的 GET 请求处理器
app.get("/hello", [](const HttpRequestPtr &req) {
    return HttpResponse::newHttpResponse()
        ->setBody("Hello, World!")
        ->setStatusCode(k200OK);
});
```

在这个例子中，我们使用了一个 lambda 函数来处理所有指向 /hello 路径的 GET 请求。当请求到达时，函数会生成一个新的 HTTP 响应对象，并设置响应体和状态码。这样的编写方式不仅简洁，而且易于理解和维护。

此外，Drogon 还支持使用 C++17 的结构化绑定来简化变量声明，例如在处理数据库查询结果时：

```cpp
auto [rows, err] = db->execSql("SELECT * FROM users WHERE id = ?", userId);
if (err) {
    // 处理错误情况
} else {
    for (const auto &[id, name, email] : rows) {
        // 使用结构化绑定直接访问查询结果
    }
}
```

通过这种方式，我们可以直接获取查询结果中的每一项，而无需显式地遍历每一列，大大减少了代码量，提高了开发效率。Drogon 对 C++14/17 特性的支持，使得开发者能够在构建高性能 Web 应用的同时，享受现代 C++ 带来的诸多便利。
