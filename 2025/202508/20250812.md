# 20250812

git clone https://github.com/tmux/tmux.git
cd tmux
sh autogen.sh
./configure && make

tmux depends on libevent 2.x, ncurses

Netty框架（Java网络库）支持Multireactor模式，通过BossGroup（主Reactor线程池）和WorkerGroup（从Reactor线程池）实现。

c++库，windows平台

C:\Program Files (x86)\Windows Kits\10\Lib\10.0.22621.0\um\arm\WS2_32.Lib
C:\Program Files (x86)\Windows Kits\10\Lib\10.0.22621.0\um\arm64\WS2_32.Lib
C:\Program Files (x86)\Windows Kits\10\Lib\10.0.22621.0\um\x64\WS2_32.Lib
C:\Program Files (x86)\Windows Kits\10\Lib\10.0.22621.0\um\x86\WS2_32.Lib
C:\Program Files (x86)\Windows Kits\10\Lib\10.0.26100.0\um\arm64\WS2_32.Lib
C:\Program Files (x86)\Windows Kits\10\Lib\10.0.26100.0\um\x64\WS2_32.Lib
C:\Program Files (x86)\Windows Kits\10\Lib\10.0.26100.0\um\x86\WS2_32.Lib

写个Rust版本的sqlite来结束Rust学习，学习东西，要有代码验证自己的学习是否过关

通过-XX:+PrintAssembly输出JIT生成的汇编代码（需HSDIS插件）
jcstress（Java Concurrency Stress Test）是 OpenJDK 提供的工具，专门用于测试并发代码的正确性和性能。

jdk8.md

ConcurrentNavigableMap接口

这是一个非常精彩的问题！Go的 `channel`是其并发模型的核心，它结合了通信（Communication）和同步（Synchronization），实现了 “不要通过共享内存来通信，而应该通过通信来共享内存” 的理念。

在Java 8中，虽然没有原生的`channel`语法，但我们可以通过`java.util.concurrent`包中的工具类来模拟Go channel的核心行为。

## 一、Go Channel 核心特性

| 特性 | 说明 |
|------|------|
| 1. 线程安全 | 多 goroutine 可并发读写 |
| 2. 阻塞操作 | `ch <- data` 和 `<-ch` 可阻塞 |
| 3. 缓冲 vs 无缓冲 | 有缓冲 channel 可异步传递 |
| 4. 关闭（close） | 可关闭 channel，读取方能检测 |
| 5. 范围遍历（range） | `for v := range ch` |
| 6. Select 多路复用 | 多 channel 选择（Java 难模拟） |

## 二、Java 8 模拟方案

我们用 `BlockingQueue` 作为核心数据结构来模拟 `channel`。

### 推荐：使用 `BlockingQueue<E>`

| Go Channel | Java 对应 |
|-----------|----------|
| `ch := make(chan int)` | `BlockingQueue<Integer>` |
| `ch <- x` | `queue.put(x)` |
| `x = <-ch` | `x = queue.take()` |
| `x, ok = <-ch` | `x = queue.poll(timeout, unit); ok = (x != null)` |
| `close(ch)` | `queue.put(POISON_PILL)` 或 `queue = null`（需配合标志） |

## 三、Java 8 模拟代码实现

```java
import java.util.concurrent.*;
import java.util.concurrent.atomic.AtomicBoolean;

// 模拟 Go 的 channel
public class GoChannel<T> {
    private final BlockingQueue<T> queue;
    private final AtomicBoolean closed = new AtomicBoolean(false);
    private static final Object POISON_PILL = new Object(); // 用于表示关闭

    // 无缓冲 channel
    public GoChannel() {
        this(0);
    }

    // 有缓冲 channel
    public GoChannel(int capacity) {
        if (capacity == 0) {
            this.queue = new SynchronousQueue<>(); // 类似无缓冲
        } else {
            this.queue = new ArrayBlockingQueue<>(capacity);
        }
    }

    // ch <- data
    public void send(T data) throws InterruptedException {
        if (closed.get()) {
            throw new IllegalStateException("channel closed");
        }
        queue.put(data);
    }

    // data = <-ch
    public T receive() throws InterruptedException {
        T data = queue.take();
        if (data == POISON_PILL) {
            return null; // 表示 channel 已关闭
        }
        return data;
    }

    // data, ok = <-ch
    public ReceiveResult<T> tryReceive(long timeout, TimeUnit unit) throws InterruptedException {
        T data = queue.poll(timeout, unit);
        if (data == null) {
            return new ReceiveResult<>(null, false); // 超时或关闭
        }
        if (data == POISON_PILL) {
            return new ReceiveResult<>(null, false); // 已关闭
        }
        return new ReceiveResult<>(data, true);
    }

    // close(ch)
    public void close() {
        if (closed.compareAndSet(false, true)) {
            queue.offer((T) POISON_PILL); // 发送关闭信号
        }
    }

    // 检查是否关闭（非精确）
    public boolean isClosed() {
        return closed.get() && queue.isEmpty();
    }

    // 接收结果封装
    public static class ReceiveResult<T> {
        public final T value;
        public final boolean ok;

        public ReceiveResult(T value, boolean ok) {
            this.value = value;
            this.ok = ok;
        }
    }
}
```

## 四、使用示例（对比 Go vs Java）

### 场景 1：基本发送/接收

```go
// Go
ch := make(chan int, 2)
ch <- 1
ch <- 2
fmt.Println(<-ch) // 1
```

```java
// Java
GoChannel<Integer> ch = new GoChannel<>(2);
ch.send(1);
ch.send(2);
System.out.println(ch.receive()); // 1
```

### 场景 2：关闭 channel

```go
// Go
close(ch)
x, ok := <-ch // ok == false if closed and empty
```

```java
// Java
ch.close();
GoChannel.ReceiveResult<Integer> result = ch.tryReceive(1, TimeUnit.SECONDS);
if (!result.ok) {
    System.out.println("channel closed or empty");
}
```

### 场景 3：生产者-消费者

```go
// Go
go func() {
    for i := 0; i < 5; i++ {
        ch <- i
    }
    close(ch)
}()

for v := range ch {
    fmt.Println(v)
}
```

```java
// Java
GoChannel<Integer> ch = new GoChannel<>();

// 生产者
new Thread(() -> {
    try {
        for (int i = 0; i < 5; i++) {
            ch.send(i);
        }
        ch.close();
    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
    }
}).start();

// 消费者
try {
    GoChannel.ReceiveResult<Integer> result;
    while ((result = ch.tryReceive(1, TimeUnit.SECONDS)).ok) {
        System.out.println(result.value);
    }
} catch (InterruptedException e) {
    Thread.currentThread().interrupt();
}
```

## 五、Java 模拟的局限性

| Go 特性 | Java 模拟难度 | 说明 |
|--------|---------------|------|
| `select` 多路复用 | ❌ 难 | Java 无 `select` 等价物 |
| `context` 取消 | ⚠️ 需手动 | 用 `Thread.interrupt()` 或 `Future.cancel()` |
| `range` 遍历关闭的 channel | ✅ 可模拟 | 通过 `tryReceive` 返回 `ok=false` |
| 性能 | ⚠️ 稍低 | `BlockingQueue` 有锁或 CAS 开销 |
| 内存模型 | ✅ 一致 | JMM 保证可见性 |

## 六、进阶：模拟 `select`（多 channel 选择）

Java 没有原生 `select`，但可以用 `Poller` 模式或 `ExecutorService` 模拟：

```java
// 简单轮询（低效）
while (true) {
    var r1 = ch1.tryReceive(10, TimeUnit.MILLISECONDS);
    var r2 = ch2.tryReceive(10, TimeUnit.MILLISECONDS);
    if (r1.ok) { /* handle ch1 */ }
    if (r2.ok) { /* handle ch2 */ }
}
```

或者使用 `CompletableFuture` 实现非阻塞选择：

```java
CompletableFuture.supplyAsync(() -> {
    try { return ch1.receive(); } 
    catch (InterruptedException e) { return null; }
}).applyToEither(
    CompletableFuture.supplyAsync(() -> {
        try { return ch2.receive(); } 
        catch (InterruptedException e) { return null; }
    }),
    value -> { System.out.println("Got: " + value); return null; }
);
```

但复杂度高，不推荐。

## 七、总结：Java 如何模拟 Go Channel

| 目标 | Java 实现方案 |
|------|----------------|
| 线程安全通信 | `BlockingQueue<E>`（如 `ArrayBlockingQueue`, `SynchronousQueue`） |
| 阻塞发送/接收 | `put()` / `take()` |
| 非阻塞尝试 | `offer()` / `poll()` |
| 关闭 channel | 发送特殊标记对象（如 `POISON_PILL`）+ `closed` 标志 |
| 检测关闭 | 接收时检查是否为 `null` 或特殊值 |
| 缓冲 channel | `ArrayBlockingQueue(capacity)` |
| 无缓冲 channel | `SynchronousQueue`（生产者消费者直接交接） |

## 最佳实践建议

1. 优先使用 `BlockingQueue`：它是 Java 并发包中最接近 channel 的组件。
2. 用 `POISON_PILL` 模拟关闭：避免 `null` 值歧义。
3. 注意中断处理：`InterruptedException` 要正确处理。
4. 高并发场景用 `LinkedTransferQueue`：性能更好，支持 `transfer()`（类似无缓冲）。
5. 不要过度模拟 `select`：Java 的并发模型不同，用 `ExecutorService` + `Future` 更自然。

如果你想看：
- 如何用 `VarHandle` 实现无锁 channel
- `Reactive Streams`（如 Project Reactor）如何更优雅地替代 channel
- Go channel 底层实现 vs Java `BlockingQueue` 源码对比

我可以继续深入！Go 的并发哲学和 Java 的并发工具，各有千秋，理解它们的映射关系，能让你在两种语言间游刃有余。

现在面试音视频开始要会FEC了
FEC（前向纠错，Forward Error Correction）是一种数据编码技术，用于提高音视频传输的可靠性，特别是在网络条件不稳定的情况下。以下是FEC的详细介绍：

