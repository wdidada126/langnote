# 202508_week3

## 时间范围

- 202508 月第 3 周

## 本周记录

- 20250815：vcpkg-cmake；vcpkg-cmake-config；是 vcpkg 工具链中用于集成 CMake 的“支持模块”或“内部组件”。
- 20250816：testdrogon项目；zlib.lib(zlib1.dll) : error LNK2005: crc32_z 已经在 zlib.lib(crc32.obj) 中定义 [C:\Users\wdidada\.conan2\p\b\libmy5b67081fdfd66\b\build\utilities\comp_err.vcxproj]；C:\Users\wdidada\.conan2\p\b\libmy5b67081fdfd66\b\build\runtime_output_directory\Debug\comp_err.exe : fatal error LNK1169: 找到一个或多个多重定义的符号 [C:\Users\wdidada\.con
- 20250817：Changes to Microsoft Authenticator autofill；面试，熟悉Java生态；Android开发生态
- 20250818：[迭代]高性能流式ASR C++实现；oracle数据库；rownum
- 20250819：redis 6.2.17 clion wsl调试；Oracle分页查询，查询的结果有重复的，北京生产环境，；在Oracle分页查询中出现数据重复的问题，是一个常见但容易被忽视的技术细节。你提到“加上某字段排序就解决了”，这正是问题的关键所在。下面我将结合Oracle的机制，详细解释为什么会出现重复数据，以及为何加上排序字段（特别是唯一性字段）可以解决。
- 20250820：ALB和SLB的特点和使用场景；Application Load Balancer (ALB)；ALB 是 AWS Elastic Load Balancer 中的一种，是一种基于 OSI 模型第七层的负载均衡器。它支持 HTTP、HTTPS、WebSocket 等协议的请求进行负载均衡，并具有以下特点：
- 20250821：跳表是一种随机化的有序数据结构，它通过建立多级索引来加速查找操作，使得平均时间复杂度可以达到O(log n)。与传统的平衡树结构相比，跳表具有以下核心优势：；实现简单：跳表的实现逻辑比红黑树、AVL树等平衡树结构简单得多，代码量少，易于理解和维护。平衡树通常需要复杂的旋转操作来维持平衡，而跳表通过随机层数来保持近似平衡。；并发友好：跳表的修改操作只影响局部链表节点，不需要像平衡树那样进行全局的旋转操作，这使得它更适合并发环境下的实现。Java的ConcurrentSkipListMap就是基于跳表实现的无锁并发结构。
