# js

js内存模型

https://www.cnblogs.com/sachen/p/6659680.html

## 数据类型
int
array

数组

## 关键字
let

await?


## 标准/版本
ECMAScript
ECMAScript 是什么？
ECMAScript（简称 ES）是 JavaScript 的标准化规范，由 Ecma International 组织制定。
JavaScript 是 ECMAScript 规范的一种实现（其他实现还包括 ActionScript 等）。
因此，当我们说“JavaScript 标准”时，通常指的是 ECMAScript 标准。

版本	发布时间	重要特性
ES1	1997	初始版本
ES3	1999	增加正则表达式、异常处理等（ES2 未广泛采用）
ES5	2009	JSON 支持、strict mode、数组方法（map, filter 等）
ES6 / ES2015	2015	重大更新：let/const、箭头函数、类（class）、模块化、Promise、解构赋值等
ES2016 (ES7)	2016	Array.prototype.includes, 指数运算符（**）
ES2017 (ES8)	2017	async/await, Object.values(), String.prototype.padStart/End()
ES2018 及以后	每年更新	每年发布一个新版本，包含新特性和语法改进（如 Promise.finally, flat(), optional chaining (?.), nullish coalescing (??) 等）



Babel等转译工具将新语法转换为兼容旧环境的代码