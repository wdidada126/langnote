# 20210311



mysql doc optimize部分 了解



mysql router

mysql cluster windows安装包 下载



mmm

mha

周云彦 拉勾教育





update news set project_code ='prov440000';

全真练 添加项目

news数据，扩展到多个项目 分项目显示
广东的全真练的数据 复制到中山一份

[修改代码]增加了news表项目字段，设置了默认查询参数

Mybatis插件，Mapper接口类自定义注解

tk.mapper

common.model.page.PageBean

	//当前页码
	private int pageNum;
	//页大小
	private int pageSize;
	//总记录数
	private int total;
	//总页数
	private int pages;
	//数据
	private List<T> list;


package .common.annotation;

import org.springframework.stereotype.Component;

import java.lang.annotation.*;

/**
 * 标识MyBatis的DAO,方便{@link org.mybatis.spring.mapper.MapperScannerConfigurer}的扫描。
 * 
 */
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@Documented
@Component
public @interface MyBatisRepository {
	String value() default "";
}



    <!-- 扫描basePackage下所有以@MyBatisRepository标识的 接口-->
    <bean class="tk.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com...**.dao"/>
        <property name="annotationClass" value="com...common.annotation.MyBatisRepository"/>
        <property name="markerInterface" value="com...common.dao.BaseDao"/>
    </bean>


        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="projectCode" column="project_code" javaType="java.lang.String"/>
    </resultMap>

jdbcType
javaType 不同

2021-03-11 10:37:18,463 ERROR [main] c.i.j.c.c.sql.FixedSqlSessionFactory:20 - sqlSessionFactory加载出错: 
org.springframework.core.NestedIOException: Failed to parse mapping resource: 'URL [jar:file:/data/service/jk-cuser-srv/lib/cuser-service-core-1.0-SNAPSHOT.jar!/com///cuser/core/dao/mapper/NewsMapper.xml]'; nested exception is org.apache.ibatis.builder.BuilderException: Error parsing Mapper XML. Cause: org.apache.ibatis.builder.BuilderException: Error resolving JdbcType. Cause: java.lang.IllegalArgumentException: No enum constant org.apache.ibatis.type.JdbcType.java.lang.String
	at org.mybatis.spring.SqlSessionFactoryBean.buildSqlSessionFactory(SqlSessionFactoryBean.java:468)


MybatisInterceptor
ServiceDubboConsumerFilter



sql hint

辅助优化的
例子

强制索引 FORCE INDEX
SELECT * FROM TABLE1 FORCE INDEX (FIELD1) …



gcc5.4 ucloud编译



Alex 鹅厂 搭建rpm包镜像 开发库最新版