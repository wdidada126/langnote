# 20210423

## tomcat类加载机制
Tomcat的类加载机制违反了双亲委托原则，对于一些未加载的非基础类（Object,String等），各个web应用自己的类加载器（WebAppClassLoader）会优先加载，加载不到时再交给CommonClassLoader走双亲委托。对于标准类库中的类，会让系统类加载器加载，然后一直委托到启动类加载器，这个过程是没有违背双亲委派的。Tomcat的类加载机制启动类加载器（Bootstrap）和扩展类加载器（Extension ClassLoader），应用程序类加载器（Application ClassLoader），这三个类加载器默认的一致。CommonClassLoader、CatalinaClassLoader、SharedClassLoader和WebappClassLoader则是Tomcat自己定义的类加载器。
tf
proth

dubbo springcloud rpc
cxf webservice
服务发现 都有
负载均衡 cxf没有

jndi spi

跑java程序遇到的问题
jar包版本不兼容
fegign         9   10
feign-form版本 3.5 3.8

feign抓包

   IPv4 地址 . . . . . . . . . . . . : 10.179.92.17
   子网掩码  . . . . . . . . . . . . : 255.255.255.0
   默认网关. . . . . . . . . . . . . : 10.179.92.254

route add 10.179.92.17 mask 255.255.255.255 10.179.92.254
route delete 10.179.92.17 mask 255.255.255.255 10.179.92.254

## 需要分析一下apache-http的源码

import java.nio.file.Files;

Hi is anybody working on this issue?
I was looking at implementations of Apache and Okhttp clients.

in Apache [4.5.x]:
--ContentBody
---AbstractContentBody
----ByteArrayBody
----FileBody
----InputStreamBody
----StringBody
for multipart:
--AbstractMultipartForm
--FormBodyPart
--FormBodyPartBuilder
--MultipartEntityBuilder

in Okhttp:
--RequestBody
for multipart:
---FormBody
---MultipartBody

## java consul客户端

ui
ucloud上部署了

actuator spring cloud的jar包
Get "http://WDIDADA:8501/actuator/health": dial tcp: lookup WDIDADA on 10.23.255.1:53: no such host

feign 发送form-data File类型的数据 只能发送文件元数据，不能发送二进制数据 wireshark抓包得到

String packageName = A.class.getPackage().getName();
或者：
packageName = new A().getClass().getPackeage().getName();
