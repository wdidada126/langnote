# 20210427

我，后端Java工程师，现在美团点评工作。
爱健身，爱技术，也喜欢写点文字。
个人网站: http://kailuncen.me
公众号: KailunTalk (凯伦说)

https://github.com/kailuncen/mybatis-cache-demo

https://segmentfault.com/a/1190000022662110

mapper.xml
cache存在configuration. <String, Cache> caches = new StrictMap<Cache> 这个属性里面

org.apache.ibatis.builder.xml.XMLMapperBuilder#parse
org.apache.ibatis.builder.xml.XMLMapperBuilder#configurationElement
org.apache.ibatis.builder.xml.XMLMapperBuilder#cacheElement
org.apache.ibatis.builder.MapperBuilderAssistant#useNewCache

    <cache eviction="FIFO" readOnly="true" size="256" flushInterval="60000"/>
eviction 驱逐; 驱赶;

```java
    Cache cache = new CacheBuilder(currentNamespace)
        .implementation(valueOrDefault(typeClass, PerpetualCache.class))
        .addDecorator(valueOrDefault(evictionClass, LruCache.class))
        .clearInterval(flushInterval)
        .size(size)
        .readWrite(readWrite)
        .blocking(blocking)
        .properties(props)
        .build();
```

org.apache.ibatis.builder.xml.XMLMapperBuilder#cacheRefElement

MapperAnnotationBuilder
XMLMapperBuilder

mysql-connector-java.md

raft
pasox节点角色 pasox协议变种
Multi Paxos
首先我们来回顾一下Multi Paxos，Multi Paxos在Basic Paxos的基础上确定一系列值，其决议过程如下：
proposer -> leader -> acceptor -> learner
https://www.cnblogs.com/bangerlee/p/6189646.html

fcl
flow
cancidate
leader

MyBatis 注解用法
如果不写，就不会理解mybatis源码

在netty的架构这块我们使用一种bossGroup加workerGroup的方式，bossGroup只负责请求的转发，workerGroup是具体的数据处理，其实netty整个框架使用的是Reactor(响应器)的设计模式。
