# scl


scl --list 

管理多个版本工具的
ruby的rvm


Software Collections（SCL）可以帮助解决 RHEL/CentOS 下的软件安装、软件依赖、多版本并存等问题，因为SCL
的创建就是为了给 RHEL/CentOS
用户提供一种以方便、安全地安装和使用应用程序和运行时环境的多个（而且可能是更新的）版本的方式，同时避免把系统搞乱。与之相对的是第三方源，它们可能会在已安装的包之间引起冲突。


