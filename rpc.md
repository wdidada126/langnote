# rpc
服务注册与发现：
客户端向服务注册中心注册自己的服务信息。
客户端发起服务发现请求，获取可用服务提供者的地址信息。
负载均衡：
客户端根据负载均衡策略选择一个服务提供者。
路由与流量控制：
客户端根据路由策略确定请求的目标服务实例。
客户端执行流量控制，确保不超过预设的流量限制。
熔断降级：
在请求发起前，客户端进行熔断降级决策，避免调用已经熔断的服务实例。
限流：
客户端进行请求的限流处理，确保服务提供者不会因过多请求而过载。
安全认证与授权：
客户端进行安全认证，确保请求合法且服务提供者有权限处理该请求。
请求发送与服务调用：
客户端将请求发送到选定的服务提供者。
服务提供者接收请求并执行相应的服务逻辑。
服务提供者返回响应给客户端。
监控与告警：
客户端和服务提供者记录请求日志，监控服务调用的性能和状态。
监控系统定期查询日志数据，发现异常情况并触发告警。
日志与追踪：
客户端和服务提供者记录详细的日志信息，用于追踪请求处理过程和排查问题。
自动化运维：
运维系统根据监控数据自动触发运维操作，例如服务扩容、缩容等。

## go rpc

golang 有官方的rpc库(net/rpc)
golang的rpc只支持go语言开发的服务器和客户端交互
https://gitee.com/zth_python/rpc-go/blob/master/rpc%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.md
https://gitee.com/dennis-kk/rpc-go-backend

校招面试碰到好几份简历写了自己开发rpc框架的，我就问服务治理请求顺序相关的问题，结果一个都没答上

服务治理在微服务架构和分布式系统中扮演着至关重要的角色，它主要负责服务的注册、发现、管理、监控和优化等一系列功能。以下是服务治理的主要功能：

服务注册与发现：
服务提供者（Provider）在启动时，会向注册中心（如Eureka、Zookeeper等）注册自己提供的服务信息，包括服务地址、端口、版本号等。
服务消费者（Consumer）在需要调用服务时，会从注册中心查询所需的服务信息，并根据这些信息找到对应的服务提供者进行调用。
注册中心负责维护服务的元数据信息，并提供服务查询的接口。
负载均衡：
在有多个服务提供者的情况下，服务治理框架需要实现负载均衡机制，以确保请求能够合理地分配到各个服务提供者上，避免单一服务提供者过载。
常见的负载均衡策略包括随机、轮询、最少活跃连接等。
容错与断路器：
当服务提供者出现故障或响应时间过长时，服务治理框架需要能够识别这些问题，并采取相应的容错措施，如重试、快速失败等。
断路器模式是一种常用的容错机制，它可以在服务调用失败率达到一定阈值时，自动断开服务调用链路，避免系统雪崩。
服务监控与告警：
服务治理框架需要能够实时监控服务的运行状态和性能指标，如响应时间、吞吐量、错误率等。
当服务状态异常或性能指标超出预设阈值时，系统能够自动触发告警，以便运维人员及时介入处理。
服务路由与配置管理：
服务治理框架支持基于路由规则的服务调用，可以根据不同的请求条件将请求路由到不同的服务提供者上。
同时，它也支持服务的配置管理，允许运维人员在运行时动态调整服务的配置信息，而无需重启服务。
关于请求顺序的面试题，服务治理本身并不直接保证RPC请求的顺序性，因为RPC调用通常是异步的，并且设计用来提高系统的可伸缩性和容错性。然而，在某些特定场景下，如果需要保证请求的顺序性，可以通过以下几种方式来实现：

客户端控制：在客户端使用单线程或同步调用，确保所有需要顺序处理的RPC调用都在同一个线程中执行。
序列号标记：在发送RPC请求时附带序列号或时间戳，服务端根据这些标记来保证处理顺序。
队列处理：在服务端为特定客户端或业务逻辑设置一个队列，所有请求都按照到达顺序处理。
需要注意的是，这些方法可能会引入额外的性能开销或复杂性，因此在设计系统时需要权衡这些因素。同时，在面试中回答此类问题时，可以结合具体的业务场景和需求来阐述解决方案的优缺点和适用场景。

请求顺序:
在分布式系统中,由于网络、系统资源等因素的影响,请求的执行顺序可能与预期不同。
需要考虑幂等性、事务性等特性,确保请求的执行顺序不会导致业务问题。
可以使用消息队列、分布式锁等技术手段来控制请求的执行顺序。
同时需要考虑错误处理、重试机制等,以提高分布式系统的整体可靠性。

在分布式系统中，特别是使用Dubbo这样的RPC（远程过程调用）框架时，处理高并发下的请求顺序是一个挑战。Dubbo本身主要关注于服务的注册、发现、负载均衡、通信等RPC框架的核心功能，而不直接提供严格的请求顺序保证机制。这主要是因为RPC调用通常是异步的，并且设计用来提高系统的可伸缩性和容错性，而不是保证严格的请求顺序。

然而，如果你的应用场景确实需要保证多个RPC请求的顺序，你可以通过以下几种方式来实现：

客户端控制：
单线程或同步调用：最简单的方法是确保所有需要顺序处理的RPC调用都在同一个线程中同步执行。这可以通过在客户端使用单线程来执行所有RPC调用，或者通过同步控制（如使用锁或信号量）来确保请求的顺序。但这种方法会显著降低并发性能。
序列号标记：在发送RPC请求时，可以附带一个序列号或时间戳，然后在服务端根据这个序列号或时间戳来保证处理顺序。但这种方法依赖于服务端能够正确处理这些标记，并且需要处理可能的并发问题（如两个请求具有相同的序列号或时间戳）。
服务端控制：
队列处理：在服务端，可以针对来自同一客户端或特定业务逻辑的所有请求使用一个队列。这样，所有请求都会按照它们到达的顺序被处理。但这种方法可能会引入额外的延迟和性能开销。
状态机或事务控制：如果业务逻辑允许，可以使用状态机或事务控制来确保操作的顺序和一致性。这通常涉及到更复杂的状态管理和错误恢复机制。
使用消息队列：
将RPC调用转换为消息发布到消息队列中，然后在服务端从队列中顺序消费这些消息。这种方法可以有效地解耦服务间的调用关系，并提供更好的容错性和可伸缩性。但是，它也会增加系统的复杂性和延迟。
利用Dubbo的扩展点：
Dubbo提供了丰富的扩展点，如Filter、Invoker等，你可以通过实现这些扩展点来添加自定义的逻辑，如请求排队、优先级处理等。
业务逻辑设计：
在某些情况下，重新设计业务逻辑以避免对请求顺序的依赖可能是一个更好的选择。例如，使用幂等性操作、最终一致性模型等。
总之，保证RPC请求的顺序通常需要根据具体的应用场景和需求来选择合适的方法。在追求高性能和高并发的分布式系统中，完全保证请求顺序可能会带来额外的复杂性和性能开销。因此，在设计系统时，需要权衡这些因素，并找到最适合你应用场景的解决方案。

性能的话 主要还是长连接连接复用+ pb 编解码+尽量内存复用，另外rpc性能只能一个方面，功能上可以去看brpc和kitex 的官方文档，试着实现几个服务治理特性

完成RPC 需要两个协议： 对象序列化协议  和 调用控制协议

常见例子举例：
1.zeroC ICE，拥有自己的网络通信框架 + ICE 调用控制协议和对象序列化协议,同时也涵盖了服务组件的抽象部署等功能。
2.thrift，有自己的网络通信框架+thrift 对象序列化协议+thrift 调用控制协议
3.probuff，只是 对象序列化协议
4.XMLRPC ，jsonRPC，常见的语境是利用HTTP协议作为调用控制协议,XML 和 JSON 作为对象序列化之后的格式。

云原声的rpc协议 grpc

后端前景

云服务把标准化的后端比如数据库／缓存／邮件／监控都给你做好了，后端业务代码还得写啊，云厂家又不可能帮你写出一个在线支付程序。

## rpc的功能
qos 监控 兼容开源监控方案Prometheus

openfigen相当于rpc

一个完善的rpc框架，在实际生产环境中统计诸如qps、连接数等各种数值是必须的，也是服务监控的很重要的一部分

其实rpc不是一种协议，rpc是一种调用过程的方案/范式/实现。
http+retrofit同样也可以实现rpc风格的http调用。
dubbo框架同样也支持http(2)传输协议。

拿rpc和http对比没啥意义，应该是拿rpc底层的通信协议（如dubbo/grpc）对http，序列化协议（如hessian/protobuf）对json这样比较才有意义。

我觉得你说到点上了。rpc这个概念可能太多地方用了。如果是作为IPC、LPC这一级别概念来说，只是说互联网两台不同主角之间的调用来说。目前前后端那种或者说后端微服务那种都算。但是目前的rpc更多的是一种设计风格和规范，把方法和参数放在一起传递过去，他其实对标的是restful规范，资源方法用uri指定，参数另外放。http协议对标thrift。http的json序列化对标thrift的binaryprotocal。至于他们所说的rpc用http传输本质上都没说到点上，其实就是基于http协议实现rpc的设计风格，要是有功夫你基于thrift协议实现一套restful风格也没毛病
是的，其实这个可以从dubbo的invoker分层中可以很直观了解到，protocol/exchange/serialize/remoting等，开头也很不理解这种分层，直到后面接触异构通信多了才明白这样分层真的很有意义。

其实rpc不是一种协议，rpc是一种调用过程的方案/范式/实现。
http+retrofit同样也可以实现rpc风格的http调用。
dubbo框架同样也支持http(2)传输协议。
拿rpc和http对比没啥意义，应该是拿rpc底层的通信协议（如dubbo/grpc）对http，序列化协议（如hessian/protobuf）对json这样比较才有意义。
mq http比较

架构师的例子

复杂
bhrpc

简单
https://github.com/edidada/EasyRPC

找比较流行的RPC框架（例如Protobuf，Thrift，Avro），从API Document开始读，然后到Stackoverflow上读相关的热门讨论，自己动手写些toy，还想深入就读源码。

这是我能找到的最有效的方法。每一本出版的书都有对你来说是废话的篇章，以及你想知道却没有包括的内容。

https://www.ibm.com/developerworks/cn/aix/library/au-rpc_programming/
