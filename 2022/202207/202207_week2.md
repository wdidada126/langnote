# 202207_week2

## 时间范围

- 202207 月第 2 周

## 本周记录

- 20220708：科目一 备考
- 20220709：mysql 日志；redo log；undo log
- 20220710：ServerBootstrap 类是 Netty 用于启动 NIO 的辅助类，能够方便开发。通过 group 方法将线程组传递到 ServerBootstrap 中，设置 Channel 为 NioServerSocketChannel，接着设置 NioServerSocketChannel 的 TCP 参数，最后绑定 I/O 事件处理类 ChildChannelHandler。；辅助类完成配置之后调用 bind 方法绑定监听端口，Netty 返回 ChannelFuture；相比传统的简单模型，Reactor增加了一定的复杂性，因而有一定的门槛，调试相对复杂。
- 20220711：netty可以配置reactor模式；tomcat 6可配置成nio；tomcat8默认是nio reactor
- 20220712：cpp11才有线程类 之前用linux c api提供的线程类；pthread_rwlock_init；pthread_rwlock_destroy
- 20220713：mvcc 多版本并发控制；java代码实现；yes的练级攻略
- 20220714：数据库表为什么需要记录更新时间；更新的时候，可能更新多个字段，；被更新的数据，可能包括不准备更新的数据
