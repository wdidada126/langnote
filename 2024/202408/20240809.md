# 20240809

ffmpeg zlmediakit srs webrtc obs vlc 

vp8 vp9 h264 hevc av1 avs2 aac ac3 opus

mp4 flv mkv ts avi mov 

rtmp rtsp hls httpflv rtp/rtcp

nvcuvid videotoolbox qsv rkmedia

音视频又多又杂又细，协议、网络、渲染、硬件全部都能扯上，没啥新人爱干

ffmpeg 的文档很全 但是缺少傻瓜式文档(.[捂嘴]

https://www.zhihu.com/question/51359754/answer/3279884618?utm_psn=1805232178361139201

他说的是一个开源项目，叫青龙面板，专门跑定时任务的
2023年了，C++20已经三年了，为什么现在搜相关书还是11的，自学学完11之后该怎样提升自己？

https://www.zhihu.com/question/595810238/answer/3261197259

export http_proxy=http://your.proxy.server:port  
export https_proxy=https://your.proxy.server:port

$env:HTTP_PROXY="http://127.0.0.1:10800"
$env:HTTPS_PROXY="https://127.0.0.1:10800"

conan install . --build=missing -pr=default_debug
======== Input profiles ========
Profile host:
[settings]
arch=x86_64
build_type=Debug
compiler=gcc
compiler.cppstd=gnu17
compiler.libcxx=libstdc++11
compiler.version=9
os=Linux

Profile build:
[settings]
arch=x86_64
build_type=Release
compiler=gcc
compiler.cppstd=gnu14
compiler.libcxx=libstdc++11
compiler.version=9
os=Linux

======== Computing dependency graph ========
Graph root
    conanfile.py (restfulapp/0.1): /home/wdidada/tmp/rest_description/conanfile.py
Requirements
    apr/1.7.4#393fa2a2b547ac6297b0374b6d31f860 - Cache
    apr-util/1.6.1#f80d0e3100c59089a52612f2b25b7d1e - Cache
    autoconf/2.71#f9307992909d7fb3df459340f1932809 - Cache
    automake/1.16.5#058bda3e21c36c9aa8425daf3c1faf50 - Cache
    date/3.0.1#032e24ad8bd1fd136dd33c932563d3d1 - Cache
    expat/2.6.2#2d385d0d50eb5561006a7ff9e356656b - Cache
    gflags/2.2.2#48d1262ffac8d30c3224befb8275a533 - Cache
    libcurl/8.9.1#9e940595377416e8d16e813a21e687a0 - Cache
    libiconv/1.17#73fefc1b696e069df90fd1d18aa63edd - Cache
    libmysqlclient/8.0.25#ed863d60bbfc60cf74c0d730d536d38c - Cache
    libtool/2.4.7#08316dad5c72c541ed21e039e4cf217b - Cache
    log4cxx/0.12.0#1af87cb5c57fc3c6243644a5f4cf4641 - Cache
    m4/1.4.19#b38ced39a01e31fef5435bc634461fd2 - Cache
    odbc/2.3.11#84d32d803c970271eb2fc58d3e3cc2b5 - Cache
    openssl/3.2.1#64189bfad173ff6dfcf60441d4e26bbe - Cache
    pistache/0.0.5#0694ad2dd0378748015cb997764eb6f9 - Cache
    rapidjson/cci.20230929#8dc0392af2b3aaea7312095f0ba53467 - Cache
    zlib/1.2.13#4e74ebf1361fe6fb60326f473f276eb5 - Cache
Build requirements
    autoconf/2.71#f9307992909d7fb3df459340f1932809 - Cache
    automake/1.16.5#058bda3e21c36c9aa8425daf3c1faf50 - Cache
    gnu-config/cci.20210814#dc430d754f465e8c74463019672fb97b - Cache
    libtool/2.4.7#08316dad5c72c541ed21e039e4cf217b - Cache
    m4/1.4.19#b38ced39a01e31fef5435bc634461fd2 - Cache
    meson/1.2.2#d43246711a59751b79c614da4afe2d82 - Cache
    meson/1.3.1#a56bddbb88ae75429477f85709f010e0 - Cache
    ninja/1.11.1#77587f8c8318662ac8e5a7867eb4be21 - Cache
    pkgconf/2.0.3#f996677e96e61e6552d85e83756c328b - Cache
    pkgconf/2.1.0#27f44583701117b571307cf5b5fe5605 - Cache
Resolved version ranges
    libcurl/[>=7.78 <9]: libcurl/8.9.1
    openssl/[>=1.1 <4]: openssl/3.2.1
    zlib/[>=1.2.11 <2]: zlib/1.2.13
Overrides
    expat/2.5.0: ['expat/2.6.2#2d385d0d50eb5561006a7ff9e356656b']
    expat/[>=2.6.2 <3]: ['expat/2.6.2']
ERROR: Version conflict: libmysqlclient/8.0.25->openssl/1.1.1w, restfulapp/0.1->openssl/3.2.1.

其中，
Resolved version ranges
    openssl/[>=1.1 <4]: openssl/3.2.1

sudo apt-get install libmysqlclient-dev libgflags-dev odb libodb-dev libodb-mysql-dev -y

cmake --install .
cmake . -t main_exe
conan2可以创建cmake组织的library
https://docs.conan.io/2/tutorial/creating_packages.html

```shell
conan inspect .
author: <Put your name here> <And your email here>
default_options:
    shared: False
    fPIC: True
description: <Description of ddrpc package here>
generators: []
label: 
license: <Put the package license here>
name: ddrpc
options:
    fPIC: True
    shared: False
options_definitions:
    shared: ['True', 'False']
    fPIC: ['True', 'False']
package_type: library
requires: []
revision_mode: hash
settings: ['os', 'compiler', 'build_type', 'arch']
topics: ['<Put some tag here>', '<here>', '<and here>']
url: <Package recipe repository url here, for issues about the package>
version: 0.0.1
```

CMake wrapper for conan C and C++ package manager
https://github.com/conan-io/cmake-conan

cd cmake-conan/example
mkdir build
cmake -B build -S . -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES=../conan_provider.cmake -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release

conandata.yml
https://docs.conan.io/1/reference/config_files/conandata.yml.html

conan和cmake的版本对应关系
conan2重新ci/cd github上c c++项目

编译器
c++ vs 2022
gcc 4.8/4.9 7 9
clang 12 13?
java 8
考虑升级11 14 17
bash脚本，在~/.conan2目录下找到protoc文件，入职protoc到/usr/local/bin/文件夹下，如果有多个，复制第一个，不用保存.sh  文件，全部都是命令行，我需要在ci cd系统中用

Error:fatal: detected dubious ownership in repository at 'E:/CLionProjects/cpp11thread'
'E:/CLionProjects/cpp11thread' is owned by:
	BUILTIN/Administrators (S-1-5-32-544)
but the current user is:
	DESKTOP-DAF8ST0/edida (S-1-5-21-138818386-1372801434-984294972-1001)
To add an exception for this directory, call:
	git config --global --add safe.directory E:/CLionProjects/cpp11thread

git config --global --add safe.directory E:/CLionProjects/calc
git config --global --add safe.directory E:/CLionProjects/clhQueue

git config --global --add safe.directory E:/CLionProjects/threadpoolcpp
git config core.autocrlf input

PS E:\CLionProjects\threadpoolcpp> git remote rename origin gitee
fatal: detected dubious ownership in repository at 'E:/CLionProjects/threadpoolcpp'
'E:/CLionProjects/threadpoolcpp' is owned by:
        BUILTIN/Administrators (S-1-5-32-544)
but the current user is:
        DESKTOP-DAF8ST0/edida (S-1-5-21-138818386-1372801434-984294972-1001)
To add an exception for this directory, call:

        git config --global --add safe.directory E:/CLionProjects/threadpoolcpp

是的，CMake 的 `-S` 选项可以指定源目录，但不能直接指定具体的文件名。不过，你可以通过一些技巧来实现类似的效果。让我解释一下相关的用法和替代方案：
1. 标准用法：
   `-S` 选项用于指定包含 CMakeLists.txt 文件的源目录。
   ```
   cmake -S /path/to/source -B /path/to/build
   ```
2. 指定特定的 CMakeLists.txt：
   虽然不能直接用 `-S` 指定文件名，但你可以使用 `-C` 选项来预加载一个 CMake 脚本：
   ```
   cmake -C /path/to/custom/InitialCache.cmake -S /path/to/source -B /path/to/build
   ```
3. 使用环境变量：
   设置 `CMAKE_HOME_DIRECTORY` 环境变量可以改变 CMake 寻找 CMakeLists.txt 的位置：
   ```
   export CMAKE_HOME_DIRECTORY=/path/to/custom/CMakeLists.txt
   cmake -S /path/to/source -B /path/to/build
   ```
4. 在命令行中指定项目文件：
   虽然不是使用 `-S`，但你可以直接在命令行中指定 CMakeLists.txt 文件：
   ```
   cmake /path/to/custom/CMakeLists.txt -B /path/to/build
   ```
5. 使用 `-P` 选项执行脚本：
   如果你只想执行一个 CMake 脚本而不是完整的配置过程，可以使用 `-P` 选项：
   ```
   cmake -P /path/to/script.cmake
   ```
6. 自定义入口点：
   在你的主 CMakeLists.txt 文件中，你可以包含其他 CMake 文件：
   ```cmake
   include(/path/to/custom/cmake/file.cmake)
   ```
7. 使用 `CMAKE_SOURCE_DIR` 变量：
   在 CMakeLists.txt 中，你可以使用 `CMAKE_SOURCE_DIR` 变量来引用源目录，然后包含特定的文件：
   ```cmake
   include(${CMAKE_SOURCE_DIR}/path/to/specific/file.cmake)
   ```
总结：
虽然 `-S` 选项本身不能直接指定文件名，但 CMake 提供了多种方式来灵活地控制配置过程和包含特定的 CMake 文件。根据你的具体需求，你可以选择上述方法中最适合的一种。如果你有特定的使用场景，可以提供更多详情，我可以给出更具体的建议。

E:\CLionProjects\testcytpo\src\testcytpo.cpp(44,13): warning C4267: “初始化”: 从“size_t”转换到“int”，可能丢失数据 [E:\CLionProjects\testcytpo\build\testcytpo.vcxproj]
string str ;int len = str.length();如何解决这个warn

这个警告是因为 `std::string::length()` 返回 `size_t` 类型，而你将其赋值给 `int` 类型。`size_t` 通常是无符号的，并且在 64 位系统上可能比 `int` 大。这可能导致在处理非常长的字符串时丢失数据。
解决这个警告的方法有几种，取决于你的具体需求：
1. 使用 `size_t` 类型（推荐）：
```cpp
std::string str;
size_t len = str.length();
```
这是最直接和推荐的方法，因为它保持了类型的一致性。
2. 如果你确定字符串长度不会超过 INT_MAX，可以使用强制类型转换：
```cpp
std::string str;
int len = static_cast<int>(str.length());
```
3. 如果你需要在不同的整数类型之间进行安全转换，可以使用 `gsl::narrow_cast`（来自 Guidelines Support Library）：
```cpp
#include <gsl/gsl>

std::string str;
int len = gsl::narrow_cast<int>(str.length());
```
4. 如果你确定字符串长度不会超过 INT_MAX，但想要在调试模式下进行检查，可以使用断言：
```cpp
#include <cassert>

std::string str;
assert(str.length() <= INT_MAX);
int len = static_cast<int>(str.length());
```
5. 如果你使用的是 C++20 或更高版本，可以考虑使用 `std::ssize`：
```cpp
#include <iterator>

std::string str;
auto len = std::ssize(str);
```
这会返回一个有符号的整数类型，通常是 `ptrdiff_t`。

6. 如果你确定不需要处理超过 INT_MAX 长度的字符串，可以在项目级别禁用这个警告（不推荐，但有时可能是必要的）：
在你的 CMakeLists.txt 中：
```cmake
if(MSVC)
    add_compile_options(/wd4267)
endif()
```

或者在代码中：

```cpp
#pragma warning(disable : 4267)
std::string str;
int len = str.length();
#pragma warning(default : 4267)
```

总的来说，使用 `size_t` 是最安全和最推荐的方法。如果由于某些原因你必须使用 `int`，那么使用 `static_cast` 并确保你的代码能够正确处理可能的溢出情况。

C:\Users\edida.conan2\p\b C:\Users\edida.conan2\p两个文件夹下面文件区别

C:\Users\edida.conan2\p
直接下载的头文件和库文件
C:\Users\edida.conan2\p\b
编译成的头文件和库文件

如果您正在寻找用于在 Windows 上管理 SQLite 数据库的工具，以下是一些常用的工具：
1. DB Browser for SQLite:
   - 简介：一个免费的开源工具，提供用于创建、设计和管理 SQLite 数据库的图形界面。
   - 官方网站：[DB Browser for SQLite](https://sqlitebrowser.org/)
2. SQLiteStudio:
   - 简介：另一个免费的开源工具，提供创建、管理和浏览 SQLite 数据库的功能。
   - 官方网站：[SQLiteStudio](https://sqlitestudio.pl/)
3. Navicat for SQLite:
   - 简介：一个功能强大的商业数据库管理工具，支持多种数据库，包括 SQLite。
   - 官方网站：[Navicat for SQLite](https://www.navicat.com/en/products/navicat-for-sqlite)
这些工具都提供了图形界面，使得在 Windows 系统上管理 SQLite 数据库变得更加容易和直观。您可以根据自己的需求和偏好选择适合您的工具。

findStr等于linux 的grep

在Windows PowerShell中，您可以使用以下命令来设置HTTP代理：

```powershell
$env:http_proxy="http://127.0.0.1:20800"
```

这个命令会将`http_proxy`环境变量设置为`http://127.0.0.1:10800`，指定了HTTP代理的地址和端口。

如果您需要同时设置HTTPS代理，可以使用以下命令：

```powershell
$env:https_proxy="http://127.0.0.1:20800"
```

这会将`https_proxy`环境变量设置为相同的代理地址和端口。

请注意，这些设置仅在当前的PowerShell会话中有效。如果您希望在每次打开PowerShell时自动应用这些设置，可以将这些命令添加到您的 PowerShell配置文件中。您可以编辑PowerShell配置文件，通常位于`$PROFILE`变量指定的位置。

要查看您的 PowerShell 配置文件的位置，可以运行以下命令：

```powershell
$PROFILE
```

然后，将相应的环境变量设置命令添加到该文件中，以便在启动 PowerShell 时自动应用HTTP代理设置。

cpu占用大户
Antimalware Service Executable
Microsoft Defender Antivirus Service

https://github.com/iluwatar/java-design-patterns

https://github.com/JakubVojvoda/design-patterns-cpp

https://github.com/liu-jianhao/Cpp-Design-Patterns

https://github.com/FengJungle/DesignPattern

https://github.com/yogykwan/design-patterns-cpp


win32
cmake宏

只有在win平台定义
其他平台没有

全亿健康，药店通，税务系统升级，前端http接口入参加上税务系统版本号

判断os，cmake也设计了内置变量
if(CMAKE_SYSTEM_NAME MATCHES "Darwin")
    # 针对 Darwin 的配置
    message(STATUS "Configuring for Darwin")
    # 可以在这里添加针对 macOS 的特定设置
else()
    # 非 Darwin 系统的默认配置
    message(STATUS "Configuring for another system")
endif()

https://cmake.org/cmake/help/latest/variable/CMAKE_SYSTEM_NAME.html

```c
#if defined(_WIN32)  
    // Windows特有的代码  
#elif defined(__linux__)  
    // Linux特有的代码  
#elif defined(__APPLE__) && defined(__MACH__)  
    // macOS特有的代码  
#else  
    // 其他系统或未知系统的代码  
#endif
```