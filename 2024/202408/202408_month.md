# 202408_month

## 本月概览

- 本月共整理 31 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202408_week1：- 20240801：C++中的struct可以拥有method。；在C++中，struct和class的主要区别在于默认的访问级别和能否继承。尽管在C语言中，struct主要用于封装数据，不允许包含函数（即method），但C++扩展了这一概念，允许在struct中定义成员函数，这些函数在C++中被视为method。因此，C++中的struct不仅可以包含数据成员，还可以包含函数成员，这使得struct在C++中变得更加灵活和强大。此外，C++中的struct默认的访问级别是public，而class的默认访问级别也是public，但在C++中并没有严格的区分struct和class在功能上的差异，它们的主要区别在于语法糖和历史原因。在面向对象的编程中，struct可以被看作是一个没有默认实现的接口的类，或者是一个轻量级的类，它不支持继承（除非明确声明为继承自其他类）。；总的来说，C++中的struct是一种用户定义的数据类型，它可以包含数据成员和函数成员，这些函数成员在C++中被视为method，提供了对数据的操作和访问。这与C语言中的struct形成了对比，后者在C语言中仅用于封装数据而不包含函数
- 202408_week2：- 20240808：生产中落地，不用c++用python？python各种库tensorflow pytorth之类底层不是c++？；他说纯数据科学家的话有部分可能用不上？虽然大多是会点的。不过那种不需要物理场景落地的项目，可能写个pytorch就够了。不是那些行业内的，不过想象中做金融/生物的可能不需要那么追求部署性能吧；大厂里分工细一点的话，可能做偏学术的不用c++也行吧。需要考虑硬件性能/响应速度的话基本还是c++/cuda主流了
- 202408_week3：- 20240815：C也没有公认的ABI。 C 的统一 ABI 是相对于操作系统的。操作系统通常会规定自己的 C ABI ，但 C++ 就缺少这种待遇了。；（这其实是语言本体之外的东西……）；其他主要是不严谨了，例如我认为长城汽车在研发方面还是有一定投入的，坦克500的3.0t，v6基本上可以说是目前卖的最好，最经受住市场考验的国产3.0tv6了。
- 202408_week4：- 20240822：C++玩转模板之——函数萃取function traits；Eigen 线代库，发现里面大量使用了这种模式；自动驾驶开发之中间件-OpenPilot的cereal
- 202408_week5：- 20240829：字符串转int；atoi 入参是char *；std::stoi

## 本月高频主题

- C++中的struct可以拥有method。
- 在C++中，struct和class的主要区别在于默认的访问级别和能否继承。尽管在C语言中，struct主要用于封装数据，不允许包含函数（即method），但C++扩展了这一概念，允许在struct中定义成员函数，这些函数在C++中被视为method。因此，C++中的struct不仅可以包含数据成员，还可以包含函数成员，这使得struct在C++中变得更加灵活和强大。此外，C++中的struct默认的访问级别是public，而class的默认访问级别也是public，但在C++中并没有严格的区分struct和class在功能上的差异，它们的主要区别在于语法糖和历史原因。在面向对象的编程中，struct可以被看作是一个没有默认实现的接口的类，或者是一个轻量级的类，它不支持继承（除非明确声明为继承自其他类）。
- 总的来说，C++中的struct是一种用户定义的数据类型，它可以包含数据成员和函数成员，这些函数成员在C++中被视为method，提供了对数据的操作和访问。这与C语言中的struct形成了对比，后者在C语言中仅用于封装数据而不包含函数
- 我怀疑是大公司的技术架构的顶层决策如此，比如技术兼容要让位于新设备普及等等。
- 能进到大公司的人又不是傻子，苹果这种公司也不至于如此官僚，所以我认为背后一定会有更重要的原因，以至于最后把一个基本的行业共识给破坏掉了。
- 是的，毕竟技术的优先级远远低于业务
- B站谦谦
- 换行，在业务为王的银行搞这个，只是个辅助性岗位
- 谦谦不是被辞了。
- 小彭老师C++实战演练系列之“自己实现所有STL容器”。比起艰深的STL源码解读，不如自己动手实现STL，更能生动直观地掌握底层技术细节，破除模棱两可心理，了解常见的最佳实践。这是本系列试水的第六课，自己实现std::optional可选值容器。本期视频中我们介绍了如何借助tag类、union惰性初始化、万能引用、完美转发等技巧，实现一个可以存放任意类型的可选值容器。最终借助所学知识，封装出了和标准库一样的可选值容器std::optional，完美支持存放任意类型的值，同时支持为空。in_place和nullopt等tag类重载构造函数的妙用真是醍醐灌顶，我们不仅实现了value、value_or、emplace、*和->运算符，还实现了C++23新增的transform、and_then、or_else，彻底迈向函数式！介绍了C++17的CTAD机制，感受if-auto语法的简洁有力。最终，还顺便介绍了名字空间的ADL机制如何帮助多态，并实现了自适配swap。且C++14就能编译，如果你的编译器无法升级到C++17，可以集成小彭老师的Optional类，直奔C++23。如果反响较好，点赞超过300立即加更下一期。
- 课程的源码已经全部开源：https://github.com/parallel101/stl1weekend
- C++ 11为什么引入nullptr？
