# 20240805

Android开发
G:\gradle\cache\caches\modules-2\files-2.1\org.hamcrest\hamcrest-library\1.3\4324046c5f99f3dc91b5370899fa3ae65fd137d2\hamcrest-library-1.3-javadoc.jar

c++ primer5随书代码，make组织的，mac
gdb调试没有授权，mac ubuntu24虚拟机eclipse卡死

make内置变量
cmake内置变量

eclipse c++ mac版本支持meson

 C++ 中拷贝语义的基本概念。了解哪些成员变量可能需要深拷贝（如动态分配的内存），哪些可以简单地浅拷贝（如基本数据类型）。
 悬垂指针


在 C++ 中，如果一个类没有显式地定义拷贝构造函数，编译器会为这个类生成一个默认的拷贝构造函数。这个默认的拷贝构造函数会逐个成员地拷贝对象的内容，对于基本数据类型（如 int, float 等）和指针来说，这意味着简单地复制它们的值。然而，对于动态分配的内存（如使用 new 分配的内存），这种简单的拷贝可能会导致问题，如双重释放或悬垂指针。

如果你在使用 CLion 或 Eclipse 这样的 IDE，并希望查看或分析一个类的默认拷贝构造函数的行为，你实际上无法直接“查看”编译器生成的代码，因为这些代码是在编译时由编译器自动生成的，并不直接以源代码的形式存在。但是，你可以通过几种方法来理解和验证默认拷贝构造函数的行为：

理解拷贝语义：首先，理解 C++ 中拷贝语义的基本概念。了解哪些成员变量可能需要深拷贝（如动态分配的内存），哪些可以简单地浅拷贝（如基本数据类型）。
使用调试工具：在 CLion 或 Eclipse 中，你可以设置断点，并在对象被拷贝时观察其成员变量的值。这可以帮助你理解拷贝构造函数是如何工作的。
编写测试代码：编写测试代码来验证拷贝构造函数的行为。例如，你可以创建一个对象，修改它的某些成员，然后创建这个对象的另一个拷贝，并检查两个对象的状态是否如你所预期。
使用编译器的特定功能：某些编译器（如 GCC 和 Clang）提供了工具或选项来查看或生成与默认构造函数、拷贝构造函数等相关的代码。例如，GCC 的 -fdump-tree-all 选项可以生成编译过程中的中间表示，但这对于一般用户来说可能过于复杂和低级。
自定义拷贝构造函数：为了更清楚地了解拷贝构造函数的行为，你可以显式地为你的类定义一个拷贝构造函数。这不仅可以让你控制拷贝过程，还可以作为学习如何正确实现拷贝构造函数的好机会。
使用静态分析工具：使用如 Clang Static Analyzer 或 Cppcheck 这样的静态分析工具可以帮助你发现潜在的拷贝相关的错误，如未初始化的成员变量或潜在的内存泄漏。
记住，虽然你无法直接查看编译器生成的默认拷贝构造函数的源代码，但你可以通过理解 C++ 的拷贝语义、编写测试代码、使用调试工具和静态分析工具来确保你的类在拷贝时表现正确。


在C++中，拷贝语义主要涉及对象的复制过程，特别是拷贝构造函数和拷贝赋值运算符的使用。理解哪些成员变量需要深拷贝（即复制资源本身，如动态分配的内存）和哪些可以浅拷贝（即仅复制指针或引用）是编写健壮C++代码的关键。

基本数据类型和指针的拷贝
基本数据类型（如int、float、double等）和固定大小的数组通常可以安全地浅拷贝，因为它们不包含动态分配的资源。
指针在默认情况下是浅拷贝的，即拷贝指针的值（即内存地址），而不是指针所指向的数据。如果指针指向动态分配的内存，并且多个对象共享这个内存，那么在不适当的时机释放内存或修改数据可能会导致问题（如双重释放、悬垂指针或数据损坏）。
代码例子
浅拷贝示例（基本数据类型）
```cpp
#include <iostream>  
  
class ShallowCopyExample {  
public:  
    int value;  
  
    ShallowCopyExample(int val) : value(val) {}  
  
    // 编译器会生成默认的拷贝构造函数和拷贝赋值运算符  
    // 这里的value是基本数据类型，所以拷贝是浅拷贝且安全的  
};  
  
int main() {  
    ShallowCopyExample obj1(10);  
    ShallowCopyExample obj2 = obj1; // 使用拷贝构造函数进行浅拷贝  
  
    std::cout << "obj1.value: " << obj1.value << std::endl;  
    std::cout << "obj2.value: " << obj2.value << std::endl; // 输出与obj1相同  
  
    return 0;  
}
```
深拷贝示例（动态分配的内存）
```cpp
#include <iostream>  
  
class DeepCopyExample {  
public:  
    int* data;  
  
    DeepCopyExample(int val) {  
        data = new int(val); // 动态分配内存  
    }  
  
    // 自定义拷贝构造函数以进行深拷贝  
    DeepCopyExample(const DeepCopyExample& other) {  
        data = new int(*other.data); // 分配新内存并复制值  
    }  
  
    // 自定义拷贝赋值运算符以进行深拷贝  
    DeepCopyExample& operator=(const DeepCopyExample& other) {  
        if (this != &other) { // 自赋值检查  
            delete data; // 释放旧资源  
            data = new int(*other.data); // 分配新资源并复制值  
        }  
        return *this;  
    }  
  
    ~DeepCopyExample() {  
        delete data; // 释放资源  
    }  
  
    void setValue(int val) {  
        *data = val;  
    }  
  
    int getValue() const {  
        return *data;  
    }  
};  
  
int main() {  
    DeepCopyExample obj1(10);  
    DeepCopyExample obj2 = obj1; // 使用自定义拷贝构造函数进行深拷贝  
  
    std::cout << "obj1.value: " << obj1.getValue() << std::endl;  
    std::cout << "obj2.value: " << obj2.getValue() << std::endl; // 输出与obj1相同，但内存是独立的  
  
    obj2.setValue(20);  
    std::cout << "After modification, obj1.value: " << obj1.getValue() << std::endl; // 仍然是10  
    std::cout << "After modification, obj2.value: " << obj2.getValue() << std::endl; // 现在是20  
  
    return 0;  
}
```
在上面的深拷贝示例中，DeepCopyExample类包含一个指向动态分配内存的指针。我们为该类提供了自定义的拷贝构造函数和拷贝赋值运算符，以确保在拷贝对象时，动态分配的内存也被正确复制，从而避免了浅拷贝可能导致的问题。

sql 更新 aa字段，在原aa字段前面加上7个0，一个逗号


5.6.4

1)形式一
常量表达式
#if
程序段 //当"常量表达式"非零时编译本程序段
#endif
2)形式二
常量表达式
#if
程序段1 //当"常量表达式"非零时编译本程序段
#else
程序段2//当"常量表达式"为零时编译本程序段
#endif
3)形式三
#if常量表达式1
程序段1 //当"常量表达式1"非零时编译本程序段常量表达式
2#elif
程序段2 //当"常量表达式1"为零、"常量表达式2"非零时编译本程序段
#elif常量表达式n
程序段 n //当"常量表达式 1"、……、"常量表达式 n-1"均为零,//"常量表达式n"非零时编译本程序段
#else
程序段 n+1 //其他情况下编译本程序段
#endif

4)形式四
#ifdef标识符
程序段1
#else
程序段2
#endif
如果“标识符”经#defined 定义过,且未经 undef删除,则编译程序段1,否则编译程序段2。如果没有程序段2,则#else可以省略:
标识符#ifdef
程序段1
#endif
5)形式五
#ifndef识符
程序段1
#else
程序段2
# endif

您的描述基本正确，但有一些小错误和格式上的问题。下面我将逐一修正并解释C++中的条件编译指令：

1. 形式一
修正后的形式：

cpp
#if 常量表达式  
程序段 // 当"常量表达式"非零时编译本程序段  
#endif
解释：这个形式是最基本的条件编译指令，用于在编译时根据常量表达式的值决定是否编译某段代码。

2. 形式二
修正后的形式：

cpp
#if 常量表达式  
程序段1 // 当"常量表达式"非零时编译本程序段  
#else  
程序段2 // 当"常量表达式"为零时编译本程序段  
#endif
解释：这个形式提供了两种选择，根据常量表达式的值决定编译哪个程序段。

3. 形式三
修正后的形式：

cpp
#if 常量表达式1  
程序段1 // 当"常量表达式1"非零时编译本程序段  
#elif 常量表达式2  
程序段2 // 当"常量表达式1"为零且"常量表达式2"非零时编译本程序段  
#elif 常量表达式n  
程序段n // 当所有前面的常量表达式都为零且"常量表达式n"非零时编译本程序段  
#else  
程序段n+1 // 其他情况下编译本程序段  
#endif
解释：这个形式提供了多个条件分支，允许根据多个常量表达式的值选择性地编译不同的程序段。

4. 形式四
修正后的形式：

cpp
#ifdef 标识符  
程序段1  
#else  
程序段2  
#endif
或者，如果不需要else部分：

cpp
#ifdef 标识符  
程序段1  
#endif
解释：这个形式检查是否已定义某个标识符（通常通过#define定义）。如果已定义，则编译程序段1；否则，如果提供了#else部分，则编译程序段2。

5. 形式五
修正后的形式（注意ifndef中的n应该是误打，应为ifndef）：

cpp
#ifndef 标识符  
程序段1  
#else  
程序段2  
#endif
解释：这个形式与#ifdef相反，它检查某个标识符是否未被定义。如果未定义，则编译程序段1；如果已定义，且提供了#else部分，则编译程序段2。

注意：条件编译指令#if、#ifdef、#ifndef、#else、#elif和#endif都是预处理指令，它们在编译之前由预处理器处理，不会出现在编译后的代码中。这些指令主要用于条件性地包含或排除代码段，以适应不同的编译环境或条件。


#undef 的作用是删除由#define定义的宏，使之不再起作用。

#ifndef BLOB_H
#define BLOB_H
#endif

属于形似5


#include <iterator>
using std::begin; using std::end;

#include <list>
using std::list;

#include <vector>
using std::vector; 

#include <iostream>
using std::cout; using std::endl; 

#include <string>
using std::string;

#include <initializer_list>
using std::initializer_list;

#include <cstddef>
using std::size_t;
