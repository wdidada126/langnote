# 20240829

c++ poco.data odb sqlpp11
三个库对比
sqlpp11不支持多线程
odb不跨平台

https://github.com/wdidada126/rest_libevent

1. libgda
简介: libgda 是一个 C 语言数据库访问库，提供了对多种数据库的支持，包括 MySQL、PostgreSQL 和 SQLite。
支持多线程: libgda 支持多线程操作。
维护状态: libgda 是 GNOME 项目的一部分，依然在维护中。
链接: https://gitlab.gnome.org/GNOME/libgda
2. libdbi
简介: libdbi 是一个 C 语言数据库接口库，提供了统一的 API 来操作多种数据库。
支持多线程: libdbi 支持多线程操作。
维护状态: 虽然 libdbi 的活跃度可能不如一些更现代的项目，但仍在维护中。
链接: https://libdbi.sourceforge.net/



/workspaces/rest_libevent/src/utils/dynamic_ini_reader.c:24:12: error: ‘ini_handler’ redeclared as different kind of symbol
   24 | static int ini_handler(void *user, const char *section, const char *name, const char *value) {
      |            ^~~~~~~~~~~
In file included from /workspaces/rest_libevent/src/utils/dynamic_ini_reader.c:4:
/home/codespace/.conan2/p/b/inihc5eed2cc24fad/p/include/ini.h:56:15: note: previous declaration of ‘ini_handler’ was here
   56 | typedef int (*ini_handler)(void* user, const char* section,
      |               ^~~~~~~~~~~
make[2]: * [CMakeFiles/dynamic_ini_reader.dir/build.make:90: CMakeFiles/dynamic_ini_reader.dir/src/utils/dynamic_ini_reader.c.o] Error 1
make[1]: * [CMakeFiles/Makefile2:251: CMakeFiles/dynamic_ini_reader.dir/all] Error 2
make: * [Makefile:91: all] Error 2

这个错误的根本原因是由于在 `dynamic_ini_reader.c` 文件中重新定义了 `ini_handler` 函数，而这个函数名已经在 `ini.h` 头文件中被定义为了一个函数指针类型。C 语言中，函数和类型不能共享同一个名字，因此会引发这个编译错误。

### 解决方法

你需要重命名 `dynamic_ini_reader.c` 文件中的 `ini_handler` 函数，以避免与 `ini.h` 中的 `ini_handler` 类型名称冲突。可以按如下方式修改代码：

1. 重命名 `ini_handler` 函数：
   
   例如，将 `ini_handler` 函数重命名为 `handle_ini_setting` 或其他合适的名字。

### 修改后的代码示例：

```c
#include <stdio.h>
#include <stdlib.h>
#include "ini.h"

// 原先的 ini_handler 函数重命名为 handle_ini_setting
static int handle_ini_setting(void *user, const char *section, const char *name, const char *value) {
    // 处理 ini 文件内容的逻辑
    if (strcmp(section, "db") == 0 && strcmp(name, "user") == 0) {
        printf("db.user = %s\n", value);
    } else if (strcmp(section, "server") == 0 && strcmp(name, "port") == 0) {
        printf("server.port = %s\n", value);
    }
    return 1;
}

int main(int argc, char *argv[]) {
    // 读取 INI 文件
    if (ini_parse("config.ini", handle_ini_setting, NULL) < 0) {
        printf("Can't load 'config.ini'\n");
        return 1;
    }
    return 0;
}
```

### 编译：
确保在 `CMakeLists.txt` 文件中正确地包含 `inih` 库的头文件路径，并链接必要的库。

```sh
gcc dynamic_ini_reader.c -o dynamic_ini_reader -I/path/to/inih -L/path/to/lib -linih
```

这样就可以避免函数命名冲突的问题，成功编译项目。



https://github.com/HardySimpson/zlog

https://log4c.sourceforge.net/

xmake -y
checking for Microsoft Visual Studio (x86) version ... 2022
checking for Microsoft C/C++ Compiler (x86) version ... 19.38.33130
checking for Microsoft Visual Studio (x64) version ... 2022
checking for Microsoft C/C++ Compiler (x64) version ... 19.38.33130
note: the following packages are unsupported on windows/x86:
  -> zlog 1.2.18


https://github.com/SergiusTheBest/plog
c++的

https://github.com/google/glog/
c++的

https://github.com/KjellKod/g3log
c++的

https://github.com/MengRao/fmtlog
c++的


wrk -t12 -c400 -d30s http://101.43.12.32:8090/user/list
Running 30s test @ http://101.43.12.32:8090/user/list
  12 threads and 400 connections
  Thread Stats   Avg      Stdev     Max   +/- Stdev
    Latency   260.53ms  357.41ms   2.00s    83.99%
    Req/Sec   106.92     80.98   848.00     71.64%
  35514 requests in 30.08s, 5.08MB read
  Socket errors: connect 0, read 0, write 0, timeout 664
Requests/sec:   1180.78
Transfer/sec:    172.97KB



lsof -i:8090
rest_libe 528337 wdidada  743u  IPv4 3215998      0t0  TCP VM-12-8-ubuntu:8090->112.26.202.18:7268 (CLOSE_WAIT)
rest_libe 528337 wdidada  744u  IPv4 3216000      0t0  TCP VM-12-8-ubuntu:8090->112.26.202.18:7154 (CLOSE_WAIT)
rest_libe 528337 wdidada  745u  IPv4 3216002      0t0  TCP VM-12-8-ubuntu:8090->112.26.202.18:7145 (CLOSE_WAIT)
rest_libe 528337 wdidada  746u  IPv4 3216004      0t0  TCP VM-12-8-ubuntu:8090->112.26.202.18:7262 (CLOSE_WAIT)


查看mysql server执行了哪些sql？
要记录 MySQL 服务器执行的所有 SQL 语句，可以使用 MySQL 5.7 提供的 `general_log` 功能。这个功能会将所有执行的 SQL 语句记录到一个日志文件或数据库表中。下面是如何将这些日志记录到 MySQL 数据库表中的步骤：

### 1. 启用 `general_log`
   首先，确保 MySQL 的 `general_log` 功能是开启的。你可以通过以下命令检查和启用它。

   ```sql
   -- 查看当前 `general_log` 状态
   SHOW VARIABLES LIKE 'general_log%';

   -- 启用 `general_log`
   SET GLOBAL general_log = 'ON';
   ```

### 2. 将日志记录到表
   默认情况下，`general_log` 记录到文件中。你可以将其配置为记录到 MySQL 数据库的表中。

   ```sql
   -- 将日志记录方式设置为 TABLE
   SET GLOBAL log_output = 'TABLE';
   ```

   这样，MySQL 将会把所有执行的 SQL 语句记录到 `mysql.general_log` 表中。

### 3. 查看日志
   你可以直接查询 `mysql.general_log` 表来查看所有执行过的 SQL 语句。

   ```sql
   SELECT * FROM mysql.general_log;
   ```

### 4. 优化建议
   - 日志表可能增长得很快：因为记录了所有 SQL 语句，`general_log` 表可能会很快变得非常大。建议定期清理或归档此表。
   - 性能影响：启用 `general_log` 会增加 MySQL 的 I/O 负载，可能会对数据库性能产生负面影响。只在必要时启用，并在不需要时禁用它。

   ```sql
   -- 禁用 `general_log`
   SET GLOBAL general_log = 'OFF';
   ```

### 5. 配置持久化
   如果你想让这些设置在 MySQL 重启后依然有效，需要将这些配置添加到 MySQL 的配置文件（`my.cnf` 或 `my.ini`）中。

   在 `[mysqld]` 部分添加以下内容：

   ```ini
   [mysqld]
   general_log = 1
   log_output = TABLE
   ```

这样设置后，MySQL 服务器将会记录所有的 SQL 语句到 `mysql.general_log` 表中，你可以随时查询这些日志。

select argument from general_log limit 100;
SELECT event_time, user_host, CONVERT(argument USING utf8) AS argument_text FROM mysql.general_log order by event_time desc LIMIT 10;
