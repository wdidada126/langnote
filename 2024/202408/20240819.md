# 20240819

chatgpt根本不是理解，是用类似于用贝叶斯公式不断试错训练，往概率大的方向发展，得出的结论不是逻辑的百分百确定，而是chatgpt认为类似更高概率的结论，这种东西用在工业这种低容错的情况根本就是不合适的，而且工业很多时候需要往回找错误，chatgpt一旦出错，很难朔源，chatgpt要用在工业上还差得多，也未必比我们走的快，大概率像可灵一样，结果是我们我们自己工业ai先应用
现在国内就是这么个理，达摩院都卖给高校了，私企研发尖端科技的运营模式支撑不起来，老板没有这个耐心，现阶段国内大环境也不允许大手笔投入，让你去做不能马上落地赚钱的东西，只是堆出来一个营销噱头，指望这些公司大炼模型赶英超美根本就是外行人痴人说梦

内存少＋轻量级＋喜欢老婆插件的
Vs Code 要装Unreal Engine 4 Snippets，C/C++拓展

高效率，大内存，颜值高
Rider，唯一缺点是内存占用大。我这边直接4个g了都

效率不错，内存中等，经典
Visual Studio 2022，记得装Visual Assist或者ReSharper

还有一个重要的，UE的宏自动补全只有ReSharper和Rider能做到，比如用的最多的UPROPERTY，而且用IDE编译得开一次UE，这里可以用UE自带编译，或者Live Coding（Live Coding更改不会保存，适合快速调试，但是IDE编译不能和Live Coding一起开启）

而且C++同一句话的不同语义可太多了，变量类型不同是一个意思，很多时候加个括号又是别的意思。语义不明这一点上，标准库也有很多坑，比如std::string的replace方法居然有14种重载的形式（参考cppreference，还没算上默认参数）。
其实我想表达的是: 
先吃到饭，再谈吃好饭。
会Rust并不能保证你吃到饭。它是我们在吃好饭这个阶段解决问题的工具。至少我目前还在这两个阶段来回横跳，我的主力语言还是C艹，至少在用到MKL，Slurm，libibverbs的时候，Rust确实没有靠谱的crate。这些高性能计算相关的生态，我只能去找C或者C艹第三方库。
就没人吐槽下freetype的代码吗？几乎所有的游戏、游戏引擎的文本渲染都是基于freetype的，但是那代码，不忍直视。
curl的也是

可以参考下政彩云的技术调研分享 https://mp.weixin.qq.com/s/qGDSgcaTonqnXoB5rKeJIg

谷歌
现在都用自家的absl lib了。

回复 @一墨青云 :Squirrel-foundation，up 主给的那个链接里面也有介绍

://github.com/ICKelin/zta/releases/tag/v0.0.1-mvp
MVP版本构建的二进制文件，欢迎自取。
项目地址：https://github.com/ICKelin/zta 项目是基于go语言实现的，只要你会用go语言，观看完本视频你就能自己写一个内网穿透软件。

1、初步安排
2、功能罗列以及大体的软件设计
3、里程碑1：MVP版本，基本内网穿透功能（本视频）
4、里程碑2：HTTP的内网穿透，初步会结合apisix或者openresty来对接http功能
5、里程碑3：HTTP的身份认证
6、里程碑4：TCP的身份认证

SourceTree
https://github.com/CommunityToolkit/ColorCode-Universal

sourceinsight真的好用，查看c++代码的，很好用
SourceInsight_Portable_907552.rar
SourceInsight4.mhtml
在Linux 下经常需要链接一些 *.a的库文件，那怎么查看这些*.a 中包
含哪些文件、函数、变量：
        1. 查看文件：ar -t *.a
        2. 查看函数、变量：nm *.a

c 多进程 多线程练手项目
https://gitcode.com/sandisks/hatchCode/overview

sourceinsight c++代码，如何查看某个抽象类的子类
EA版本
Program Version:16.0.1604(Build:1604)-32 Copyright a Sparx Systems 1998-2022

ASPICE（Automotive Software Process Improvement and Capability Determination）是一种汽车软件开发过程评估和改进的国际标准。SYS.3级别是ASPICE模型中的一个等级，代表了系统开发过程的一组要求和指南。
SYS.3级别的ASPICE要求主要涉及系统架构设计和系统集成方面的内容。下面是SYS.3级别的一些核心要求：
1. 系统需求定义：定义系统需求和约束条件，确保对系统功能和性能的清晰理解。
2. 系统体系结构设计：基于系统需求，设计系统的软件和硬件体系结构，确保系统的可靠性、可扩展性和可维护性。
3. 接口定义和管理：定义和管理系统内部和外部的接口，确保不同组件之间的协调和通信。
4. 系统集成测试：执行系统集成测试，验证系统在各个组件和子系统集成后的功能和性能。
5. 安全和可靠性分析：进行安全性和可靠性分析，识别潜在的风险，并采取相应的措施来降低风险。

架构设计工具
https://blog.csdn.net/smith_chai/article/details/140844257
EA(Enterprise architect)，MATLAB Simulink，Visio，在Polarion上管理。此外还有rational的那一套。

poco http server有打印日志吗？
C++ 标准库（STL）本身并不直接提供读取 INI 文件（一种简单的配置文件格式，常用于存储设置和配置）的 API。但是，你可以通过几种方式来实现 INI 文件的读取：

手动解析：你可以编写自己的代码来解析 INI 文件。INI 文件通常包含键值对，这些键值对可能分布在不同的节（section）中。你可以使用 std::ifstream 来读取文件，并使用 std::string 和 std::stringstream 等工具来解析内容。
使用第三方库：有许多第三方库可以方便地读取 INI 文件，如 libini、inih（一个轻量级的 C 库，但也可以很容易地在 C++ 中使用）等。
使用现代 C++ 特性：如 std::filesystem（C++17 引入）来检查文件是否存在，以及 std::regex（C++11 引入）来辅助解析文件内容。
示例：手动解析 INI 文件
以下是一个简单的示例，展示了如何使用 C++ 标准库手动解析 INI 文件。请注意，这个示例仅用于教学目的，并未处理所有可能的 INI 文件格式（如注释、节嵌套等）。

```cpp
#include <iostream>  
#include <fstream>  
#include <sstream>  
#include <map>  
#include <string>  
  
std::map<std::string, std::map<std::string, std::string>> parseIniFile(const std::string& filename) {  
    std::map<std::string, std::map<std::string, std::string>> iniData;  
    std::string currentSection = "";  
    std::ifstream file(filename);  
    std::string line;  
  
    if (!file.is_open()) {  
        throw std::runtime_error("Failed to open file");  
    }  
  
    while (std::getline(file, line)) {  
        // 忽略空行和注释（这里假设注释以分号开头）  
        if (line.empty() || line[0] == ';') continue;  
  
        // 检查是否是节标题  
        if (line[0] == '[' && line.back() == ']') {  
            currentSection = line.substr(1, line.size() - 2);  
            continue;  
        }  
  
        // 尝试分割键值对  
        size_t pos = line.find('=');  
        if (pos != std::string::npos) {  
            std::string key = line.substr(0, pos);  
            std::string value = line.substr(pos + 1);  
            // 去除键和值两端的空白字符  
            key.erase(std::remove_if(key.begin(), key.end(), isspace), key.end());  
            value.erase(std::remove_if(value.begin(), value.end(), isspace), value.end());  
  
            // 存储键值对  
            iniData[currentSection][key] = value;  
        }  
    }  
  
    file.close();  
    return iniData;  
}  
  
int main() {  
    try {  
        auto ini = parseIniFile("example.ini");  
        for (const auto& section : ini) {  
            std::cout << "[" << section.first << "]" << std::endl;  
            for (const auto& keyValue : section.second) {  
                std::cout << keyValue.first << "=" << keyValue.second << std::endl;  
            }  
        }  
    } catch (const std::exception& e) {  
        std::cerr << "Error: " << e.what() << std::endl;  
    }  
  
    return 0;  
}
```
注意
这个示例没有处理所有可能的 INI 文件格式，如注释、引号内的空格、节嵌套等。
对于更复杂的 INI 文件，你可能需要编写更复杂的解析逻辑，或者使用第三方库。
考虑到性能和易用性，对于大型项目，使用成熟的第三方库通常是更好的选择。

poco，有读取.ini文件的代码

poco代码，Foundation.h  行号，宏__LINE__
using LineNumber = decltype(__LINE__);

c++ template方法，写在.h .hpp里面
分开template方法，.tpp

在 CMake 中，CMakeUserPresets.json 文件通常是手动创建的，然而你可以通过以下步骤和命令生成或配置 CMakeUserPresets.json 文件。
https://cmake.org/cmake/help/v3.20/manual/cmake-presets.7.html

CMakeUserPresets.json