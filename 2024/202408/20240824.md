# 20240824

static_cast

 UInt16 port;

        // 将 int 类型的 intPort 转换为 UInt16 类型的 port
        port = static_cast<UInt16>(intPort);


Visual Studio 2019已经集成了gtest

httpmockserver
https://github.com/seznam/httpmockserver
