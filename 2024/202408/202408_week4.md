# 202408_week4

## 原有内容


## 时间范围

- 202408 月第 4 周

## 本周记录

- 20240822：C++玩转模板之——函数萃取function traits；Eigen 线代库，发现里面大量使用了这种模式；自动驾驶开发之中间件-OpenPilot的cereal
- 20240823：基于Arrow的轻量线程池；语言只是一个工具，学习之前首先要想明白你学习的目的是什么，是要解决实际的项目问题还是要刷题学理论通过面试进大厂？ 如果是前者那么你甚至都不一定非要学C++，底层可以学个C语言，高级可以学python，C++能干的python基本都能干。如果实际项目中非要用C++，则学会基本语法即可，然后找点实际项目的代码，先阅读别人的代码，遇到不会的高级语法再去查资料，然后模仿别人尝试改代码，熟练之后就可以自己写写代码了，做几个项目之后你自然就掌握了，这比你捧着本转头书昏昏欲睡的啃效率高多了。；肯定算啊，但是前提是你得能发出来，system的论文很难的，工程量大，收稿少
- 20240824：static_cast；UInt16 port;；// 将 int 类型的 intPort 转换为 UInt16 类型的 port
- 20240825：不过学习数学，最好还是使用mathematica，因为matlab数值解太多，mma直接给你解析形式，更适合抄答案啦；开源搜索引擎框架和产品有很多，例如elasticsearch，sphinx，xapian，lucence，typesense，MeiliSearch 等，分别用不同的语言实现，具有类似但不完全相同的功能。准确来说不属于通用的搜索引擎，而是属于一种基于索引的文字检索系统。；C++开源搜索引擎xapian开发入门
- 20240826：std::make_unique是 C++14 中引入的函数，用于创建std::unique_ptr对象，用于管理动态分配的内存资源。；手动实现一个简单的make_unique函数；如果你想保持代码中的std::make_unique语法，但编译器不支持 C++14，你可以手动实现一个简单的make_unique函数。以下是一个可能的实现：
- 20240827：MySQL数据库，Read View只针对RC和RR级别；Flink和Spark的反压机制（Backpressure）是处理流处理中数据流量控制的重要机制，旨在防止系统因处理速度跟不上数据生成速度而导致的资源耗尽或性能下降。；向量数据库  Milvus数据库是开源的。Milvus是一款专为处理向量搜索任务而设计的开源向量数据库，它支持多种类型的向量，如浮点向量、二进制向量等，并且可以处理大规模的向量数据。Milvus在Apache 2 License协议下发布，并于2019年10月正式开源，是LF AI & DATA基金会的孵化项目。其源代码被托管于Github，社区活跃，支持多语言、多工具的Milvus生态系统，使得开发者可以方便地使用和扩展Milvus。
- 20240828：微信好友老邓头c++；acb0y2013；帮忙看了代码

## 整理补充

## 时间范围

- 202408 月第 4 周

## 本周记录

- 20240822：C++玩转模板之——函数萃取function traits；Eigen 线代库，发现里面大量使用了这种模式；自动驾驶开发之中间件-OpenPilot的cereal
- 20240823：基于Arrow的轻量线程池；语言只是一个工具，学习之前首先要想明白你学习的目的是什么，是要解决实际的项目问题还是要刷题学理论通过面试进大厂？ 如果是前者那么你甚至都不一定非要学C++，底层可以学个C语言，高级可以学python，C++能干的python基本都能干。如果实际项目中非要用C++，则学会基本语法即可，然后找点实际项目的代码，先阅读别人的代码，遇到不会的高级语法再去查资料，然后模仿别人尝试改代码，熟练之后就可以自己写写代码了，做几个项目之后你自然就掌握了，这比你捧着本转头书昏昏欲睡的啃效率高多了。；肯定算啊，但是前提是你得能发出来，system的论文很难的，工程量大，收稿少
- 20240824：static_cast；UInt16 port;；// 将 int 类型的 intPort 转换为 UInt16 类型的 port
- 20240825：不过学习数学，最好还是使用mathematica，因为matlab数值解太多，mma直接给你解析形式，更适合抄答案啦；开源搜索引擎框架和产品有很多，例如elasticsearch，sphinx，xapian，lucence，typesense，MeiliSearch 等，分别用不同的语言实现，具有类似但不完全相同的功能。准确来说不属于通用的搜索引擎，而是属于一种基于索引的文字检索系统。；C++开源搜索引擎xapian开发入门
- 20240826：std::make_unique是 C++14 中引入的函数，用于创建std::unique_ptr对象，用于管理动态分配的内存资源。；手动实现一个简单的make_unique函数；如果你想保持代码中的std::make_unique语法，但编译器不支持 C++14，你可以手动实现一个简单的make_unique函数。以下是一个可能的实现：
- 20240827：MySQL数据库，Read View只针对RC和RR级别；Flink和Spark的反压机制（Backpressure）是处理流处理中数据流量控制的重要机制，旨在防止系统因处理速度跟不上数据生成速度而导致的资源耗尽或性能下降。；向量数据库  Milvus数据库是开源的。Milvus是一款专为处理向量搜索任务而设计的开源向量数据库，它支持多种类型的向量，如浮点向量、二进制向量等，并且可以处理大规模的向量数据。Milvus在Apache 2 License协议下发布，并于2019年10月正式开源，是LF AI & DATA基金会的孵化项目。其源代码被托管于Github，社区活跃，支持多语言、多工具的Milvus生态系统，使得开发者可以方便地使用和扩展Milvus。
- 20240828：微信好友老邓头c++；acb0y2013；帮忙看了代码
