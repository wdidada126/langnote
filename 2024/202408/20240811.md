# 20240811

【360 C/C++代码规范推荐-哔哩哔哩】 https://b23.tv/qsH5oy4

https://github.com/Qihoo360/safe-rules
https://zh-google-styleguide.readthedocs.io/en/latest/google-cpp-styleguide/contents.html
当然了，C++圈子中最广泛认可与现代的仓库是：https://github.com/isocpp/CppCoreGuidelines



我给推荐三个视频：
【现代C++详解（98,11,14,17）】 https://www.bilibili.com/video/BV1Ea411u7qH/?share_source=copy_web&vd_source=ad69e43810b50c6f36db9d3d27fe06cb。
【linux系统编程】 https://www.bilibili.com/video/BV14d4y1t7Qe/?share_source=copy_web&vd_source=ad69e43810b50c6f36db9d3d27fe06cb
【企业级C++网络开发】 https://www.bilibili.com/video/BV1R5411o77p/?share_source=copy_web&vd_source=ad69e43810b50c6f36db9d3d27fe06cb
这是位真正的大佬，对于C++的理解很深刻。昨天和他聊了一下，复制几段昨天大佬说的对C++的理解，说的太好了。这三个视频真正体现了大佬的思想，算是目前我见过的C++板块最好的教程了。

对于C++：永远是开发体系大于语法本身，把C++视作一个工具。在学习整个体系之前，有个基本的工具是必须的，但若是工具本身的复杂程度远远超过了实际工作所需的范围，则很难理解工具为什么被设计成这样，只能死记硬背。此外，一个工具缺乏场景去检验，也很难真正真正学会使用这个工具，纯靠想想是学不好的。如果在实际开发场景中真的需要一个语法了，再去学这个语法，才可以说真正掌握了。

为什么先录这三个课程，这三门课直接构成了基本开发体系，绝大多数项目都是建立在这三个的基础之上的。这三门课是绝对的核心。

最后是怎么学C++语法本身：对于C++语言，每个语法都要进行分类，明白每个语法在C++语言本身中的位置比明白语法本身更加重要。
C++语法按照思路不同可以被分为三个部分：
部分一，面向过程部分：也就是C语言部分，包括一个程序必备的部分，比如程序的启动，结束，执行过程中的内存变化等，这些也是面向过程部分的真正核心。C的核心不是C本身。理解程序执行过程，C自然就明白了，对于一位真正的C++高手，是可以看懂C++的任何报错与调试信息的。

部分二：面向对象部分：也就是封装，继承，多态、极大的简化程序设计流程，让程序更加接近人的思维。

部分三：泛型编程：也就是抽象部分，这一部分可以极大提高代码的复用性，在库程序中用的比较多。这部分等有了一定的代码基础再学吧，泛型编程在大型项目中才有使用价值。

在真正项目中，这三部分的组合有时看个人的理解，这时C++就变成一门哲学了。

这些话说的非常好，贴出来可以多看一看，作为学习方向的指导。
对我自己而言，C++语言层面上的话，我仿佛作为一个C++爱好者一样，就语言语法特性，乃至新特性，是十分有探究，或研究底层实现，或看看源码，但实际上这似乎也只限于语言层面了，至少这块。看过我视频的应该都有感觉，我实际上对于一些思想什么的闭口不谈。如果你看过我的视频应该也能注意到，我推荐过很多up主，以弥补我教程上有很多没讲，或讲的依旧不够深入的，如协程那一块，模块什么的，我都提过看一些别的。
实际上，就目前而言，我还没见到将本视频全部看完且将试卷做出来的人。我所讲述的现代C++教程，十分纠结于概念，和各种情况，我实际上个人无所谓于你到底喜不喜欢，因为喜不喜欢它也存在。如果对于你而言，你对现代C++无感，或者你对复杂的C++世界不胜其烦，那或许可以先看看上面推荐的，并且实际上我个人不喜欢营销号意义的细微知识，之前的视频也强调过，这也是我推荐上面那视频的原因之一。


【高频交易dragon的个人空间-哔哩哔哩】 https://b23.tv/Zby5cN7
更多更全视频教程[脱单doge]：
https://www.bilibili.com/video/BV1D84y1t76J/
https://www.bilibili.com/video/BV1r8411N75b/
https://www.bilibili.com/video/BV1Zj411r7eP/
https://b23.tv/nZV2G5L
第一个是比较详细的知识点，第二个是关于stl的专题，第三个就是比较常见的习题，第四个是新的现代cpp模板教程。

《C++ 从零开始构建后端技术栈》课程介绍：
https://hkrb7870j3.feishu.cn/docx/UPgTdnZjFoNCDXxuTxsc2HajnzI
《编译原理：C++ 手写解释器和虚拟机》课程介绍：
https://hkrb7870j3.feishu.cn/docx/FIxJdRLtZofQEPxrIxTcI2xNnDd
《C++ 从零手写数据结构》课程介绍：
https://hkrb7870j3.feishu.cn/docx/UcJ2dWarxopDwhxVB3xcITpZnig

视频中的代码和命令行都整理在这里：
https://github.com/0cch/moderncpp_public/blob/main/others/bilibili/cppcheck_1dC411a73g.md


60年代的fortran4就有复数了。
数据库oltp方向是不是岗位很少了？
https://www.zhihu.com/question/631983226/answer/3306153787

国内有没有教如何编写出一个编译器的课程?
https://www.zhihu.com/question/392127140/answer/3555181541

鸿蒙一直在招开发吧，看OD岗位一直有。
听说中间件是不容易量化产出， 做得好看不出来，做的不好一个故障就玩儿完，是这样吗
分类型和应用场景的中间件，有些中间件挂也可没关系。产出看企业所处的阶段，公司认可需要就是产出。

如何评价c++包管理工具conan?
https://www.zhihu.com/question/47298264/answer/3495712343



浦发银行
企业级架构专项工程整体规划与业务试点设计项目
中标候选人第1名：建信金融科技有限责任公司，投标报价：7920.32万元；

好多系统不支持cpp 17 只能all in tbox了

c c++代码

https://www.zhihu.com/question/509541648/answer/3269615733?utm_psn=1804945700603162624

zig本来就是better c，设计之初就没有想过memory safety，而且比起c还是有很多好用的地方



拍照技术哪家强
当然是蓝厂，尤其夜景环境下的高光压制，一骑绝尘。



好多系统不支持cpp 17 只能all in tbox了

c c++代码

https://www.zhihu.com/question/509541648/answer/3269615733?utm_psn=1804945700603162624

zig本来就是better c，设计之初就没有想过memory safety，而且比起c还是有很多好用的地方



g++ -o myprogram myprogram.cpp $(pkg-config --cflags --libs log4cpp)

pkg-config --cflags --libs log4cpp
-pthread -I/usr/local/include -L/usr/local/lib -llog4cpp



skia
impeller 2d引擎
https://github.com/flutter/engine/tree/main/impeller


vscode不开源
code-oss开源
vscodium是vscode开源封装
vsc插件大多开源，ms组件不开源

这些是法律上区分
其实rider写c++也不错

快手那种农村厂弟，天天臆想别人本硕博家庭和他们一样追魂夺命流水线呢

DigitalOcean 可以提供 GPU 服务器（旗下的 Paperspace 平台），提供包括 H100、A100、A6000、A5000 等一系列 GPU，资源按秒计费结算。部分 GPU 服务器价格可参考如下截图，如需了解更多，可点击链接，联系 DigitalOcean 中国区独家战略合作伙伴卓普云。

构建系统中最复杂的就是依赖关系和条件编译，实际上就是正则表达式解析。Python不调第三方库也能做到，就是比较慢。
SConS是纯Python写的，用sqlite记录文件md5而不是时间戳。配置和编译一次搞定，比CMake好用多了。可惜好多IDE只支持CMake。

之前也看网上有人说过, 有些特定的代码, clang编译出来是会比gcc性能差, 反之gcc很少出现比clang差的, 但是clang的提示,编译速度等等比gcc好太多了. 现在高版本的gcc错误提示好多了, 以前的错误提示和tm天书一样
理论上x64性能天花板还是clang，不过那个clang的另一个名字叫icx/icpx
不好说，现在服务器上可能是aocc和aocl了
intel libm的威力
链接libc++试试 clang配libstdc++产物有时确实不太好

C语言的stdlib.h等头文件里的函数是由对应平台的编译器实现的吗？
https://www.zhihu.com/question/377511463/answer/1101641424

c++
具有广播和延迟计算的C++张量
https://github.com/xtensor-stack/xtensor

（1）包管理 vcpkg 并不是首创者，pkg-config 也可以把开发者从包管理的痛苦中解放出来。
（2）premake、xmake 都具有 project generation 的能力，这一点 CMake 没有优势。
（3）"cmake愿意对各种corner case进行优化"，这句话存疑，至少 C++20 的特性 CMake 支持的很差，即使那些特性编译器都可以一键使用了。
（4）Lua 也是纯C/C++混合汇编的，不觉得LuaJIT比CMake差在哪里。

vcpkg安装完 都在install 这个目录， 就类似于 系统目录下 include/lib share bin 这些目录， 像make scons 你直接引入就好了， conda很多缺少包都是那vcpkg跑上传他们自己的仓储的，xmake机制也可以直接调用vxpkg仓储， 具体原理没研究过

find_package(log4cpp)是找liblog4cpp.so还是log4cpp.cmake文件？
find_package(log4cpp) 在 CMake 中是用来查找并加载一个名为 log4cpp 的外部项目的配置信息的。CMake 会根据这个命令的行为来查找和配置这个外部库，但具体查找什么，取决于几个因素，主要包括：

CMake的查找策略：find_package 的行为可以通过多种方式被自定义，包括指定模块路径（CMAKE_MODULE_PATH）、使用 Config 模式或 Module 模式等。默认情况下，CMake 会先尝试在 Config 模式下查找，即查找 <PackageName>Config.cmake 或 <lower-case-package-name>-config.cmake 文件（在这个例子中是 log4cppConfig.cmake 或 log4cpp-config.cmake）。如果找不到，它会尝试在 Module 模式下查找，即查找 <PackageName>.cmake 文件（在这个例子中是 log4cpp.cmake）。
已安装的 log4cpp 的配置：log4cpp 库本身是否提供了 CMake 配置文件（如 log4cppConfig.cmake 或 log4cpp.cmake），或者是否有其他方式通过 CMake 暴露其配置（例如，通过包管理工具安装的 log4cpp 可能会有特定的包配置文件）。
环境变量和缓存：CMake 查找包时还会考虑环境变量（如 CMAKE_PREFIX_PATH）和 CMake 缓存中的信息。
关于 liblog4cpp.so，这是 log4cpp 库的动态链接库文件，而不是 CMake 配置文件。find_package 不会直接查找这个库文件，但它可能会通过 CMake 配置文件中的指令来定位这个库文件，并设置相应的变量（如 log4cpp_LIBRARIES）来指向这些库文件。

简而言之，find_package(log4cpp) 主要是在查找 log4cppConfig.cmake、log4cpp-config.cmake 或 log4cpp.cmake 这样的 CMake 配置文件，而不是直接查找 liblog4cpp.so。然而，一旦找到并加载了这些配置文件，它们就会配置 CMake 以找到并使用 liblog4cpp.so（或相应的静态库）和其他相关的库文件。

如果你在使用 CMake 并希望为 log4cpp 库（或任何自定义库）生成 log4cppConfig.cmake、log4cpp-config.cmake 或 log4cpp.cmake 这样的配置文件，以便其他项目可以使用 find_package(log4cpp) 来找到并链接到你的库，你需要手动编写这样的配置文件，并将其安装到合适的位置。

以下是一个简单的示例，展示如何为 log4cpp 编写 log4cppConfig.cmake 文件，并将其安装到你的安装目录中：

1. 编写 log4cppConfig.cmake
首先，在你的 CMake 项目中创建一个 log4cppConfig.cmake 文件。这个文件将设置一些变量，以便其他项目可以找到和使用 log4cpp 库。

```cmake
# log4cppConfig.cmake  
# Set the include dir  
set(LOG4CPP_INCLUDE_DIRS "include")  
  
# Set the library dirs and libraries  
if(WIN32)  
    set(LOG4CPP_LIBRARY_DIRS "lib")  
    set(LOG4CPP_LIBRARIES "liblog4cpp.dll.a" CACHE FILEPATH "log4cpp library")  
else()  
    set(LOG4CPP_LIBRARY_DIRS "lib")  
    set(LOG4CPP_LIBRARIES "liblog4cpp.so" CACHE FILEPATH "log4cpp library")  
endif()  
  
# Define the target  
if(NOT TARGET log4cpp::log4cpp)  
    add_library(log4cpp::log4cpp UNKNOWN IMPORTED)  
    set_target_properties(log4cpp::log4cpp PROPERTIES  
        INTERFACE_INCLUDE_DIRECTORIES "${LOG4CPP_INCLUDE_DIRS}"  
        IMPORTED_LOCATION "${LOG4CPP_LIBRARY_DIRS}/${LOG4CPP_LIBRARIES}"  
    )  
endif()  
  
# Optionally, set some version information  
set(LOG4CPP_VERSION "1.2.3")
```
注意：上面的路径（如 include 和 lib）应该根据你的项目结构进行调整。此外，Windows 示例中使用了 .dll.a，这实际上是 MinGW 或其他工具链在 Windows 上生成的导入库文件，你可能需要根据你的编译器和库的实际情况进行调整。

2. 在 CMakeLists.txt 中安装 log4cppConfig.cmake
然后，在你的主 CMakeLists.txt 文件中，使用 install 命令来安装 log4cppConfig.cmake 文件，并确保它被安装到 CMake 能够找到它的地方（通常是 <prefix>/lib/cmake/<PackageName> 或 <prefix>/share/cmake/<PackageName>）。

cmake
# 在你的 CMakeLists.txt 中  
# ... 其他 CMake 指令 ...  
  
# 安装 log4cppConfig.cmake  
install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/log4cppConfig.cmake"  
        DESTINATION "lib/cmake/log4cpp"  
)  
  
# 安装头文件和库文件（示例）  
install(DIRECTORY "include/log4cpp" DESTINATION "include")  
install(TARGETS log4cpp LIBRARY DESTINATION "lib" ARCHIVE DESTINATION "lib")
注意：log4cpp 目标（TARGETS log4cpp）在上面的 install 命令中被引用，但你需要确保你的 CMake 项目中确实定义了这个目标。如果 log4cpp 是一个外部库，你可能不需要安装它（因为用户可能已经安装了它），而只是需要安装 log4cppConfig.cmake 以便其他项目可以找到它。

3. 使用 find_package(log4cpp)
现在，当你的库和配置文件都被正确安装后，其他项目可以使用 find_package(log4cpp) 来找到并使用你的 log4cpp 库。

cmake
# 在其他项目的 CMakeLists.txt 中  
find_package(log4cpp REQUIRED)  
  
# 使用 log4cpp  
```
target_link_libraries(my_target PRIVATE log4cpp::log4cpp)


#include <mutex>
#include <iostream>

class Singleton {
private:
    static Singleton* instance;
    static std::mutex mtx;//标记位置1
    Singleton() {}
    Singleton(const Singleton&) = delete;
    Singleton& operator=(const Singleton&) = delete;

public:
    static Singleton* getInstance() {
        if (instance == nullptr) {
            mtx.lock();
            if (instance == nullptr) {
                instance = new Singleton();
            }
            mtx.unlock();
        }
        return instance;
    }
};

// 初始化静态成员变量
Singleton* Singleton::instance = nullptr;
std::mutex Singleton::mtx; //标记位置2 

```

这段代码 标记位置1 标记位置2有什么区别？为什么一个是声明，一个是定义初始化

在你的代码中，std::mutex mtx; 在类 Singleton 内部和类外部的出现确实扮演了不同的角色，这主要涉及到静态成员变量的声明与定义（包括初始化）的区别。

标记位置1
```cpp
static std::mutex mtx; // 标记位置1
```
在类 Singleton 内部，static std::mutex mtx; 是一个声明。它告诉编译器，这个类将有一个静态的 std::mutex 类型的成员变量 mtx。然而，这个声明本身并不为 mtx 分配内存或初始化它。在类内部，你通常只能声明静态成员变量，而不能在这里直接定义（即分配内存和初始化）它们，因为静态成员变量是类的所有实例共享的，它们的生命周期贯穿整个程序运行期间，而类的定义本身并不直接关联到任何特定的实例或作用域。

标记位置2
```cpp
std::mutex Singleton::mtx; // 标记位置2
```
在类外部，std::mutex Singleton::mtx; 是一个定义（同时完成了初始化）。这里，你实际上为 Singleton 类的静态成员变量 mtx 分配了内存，并进行了初始化（对于基本类型，如 std::mutex，如果没有提供初始值，编译器会使用默认构造函数进行初始化）。这个定义通常放在类的实现文件（.cpp 文件）中，或者在类的声明文件（.h 或 .hpp）的末尾，但通常建议在 .cpp 文件中进行定义，以避免头文件重复包含可能导致的重复定义问题。

总结
声明（在类内部）：告诉编译器这个类有一个静态成员变量，但不分配内存或初始化。
定义（在类外部）：为静态成员变量分配内存，并（可选地）进行初始化。这是必须的，因为静态成员变量需要具体的存储位置。
为什么需要这种区分？
静态成员变量需要这种声明与定义的区分，主要是因为它们的存储和作用域特性。静态成员变量不属于任何类的实例，而是属于类本身，所有实例共享同一个静态成员变量。因此，它们的定义（即内存分配和初始化）必须发生在类的外部，以确保它们在整个程序中只有一个定义，而声明则用于在类的内部引用这些成员变量。


```
    std::string s; // 默认初始化，调用std::string的默认构造函数，s是一个空字符串  
    std::cout << "s is empty: " << s.empty() << std::endl; // 输出：s is empty: 1 
```
inline函数，编译器决定是否替换，不一定替换，不是谷歌开发规范里面说的，一定已替换

brpc，是编译.a静态库
log4cpp，需要pthread，不知道需要添加pthread依赖，就是报错
未定义引用 pthread_create()函数
java，未引入库的jar，报错classnotfound


在C++中，析构函数（destructor）和纯虚函数（pure virtual function）的处理方式确实有所不同，这解释了为什么virtual ~BasePageDao();需要实现而virtual T getOne(int id) = 0;不需要。

析构函数
析构函数是特殊的成员函数，它在对象生命周期结束时自动被调用，用于执行清理工作（如释放资源）。对于基类中的虚析构函数，它必须有一个定义（即使是一个空的定义），因为当通过基类指针删除派生类对象时，需要调用派生类的析构函数来确保正确清理。如果基类中的虚析构函数没有定义，那么在尝试实例化模板类（特别是当模板参数是派生自该基类的类型时）时，链接器会报错，因为它无法找到析构函数的实现。

纯虚函数
纯虚函数是一种特殊的虚函数，它在基类中被声明但没有定义。它的目的是强制派生类提供该函数的实现。纯虚函数通过在函数声明的末尾添加= 0来指定。由于纯虚函数没有定义，因此它们不需要（也不应该有）在基类中提供实现。相反，它们的存在是为了创建一个抽象基类（abstract base class, ABC），该类不能被直接实例化，但可以用作派生类的接口。

示例
```cpp
// BaseCrud.h  
class BaseCrud {  
public:  
    virtual ~BaseCrud() {} // 基类中的虚析构函数，提供空实现  
  
    // 纯虚函数，强制派生类实现  
    virtual T getOne(int id) = 0;  
  
    // 其他成员函数...  
};  
  
// BasePageDao.h  
template<class T>  
class BasePageDao : public BaseCrud {  
public:  
    virtual ~BasePageDao() {} // 模板基类中的虚析构函数，同样提供空实现  
  
    // 派生类可能需要实现的其他函数...  
};  
  
// ArticleDaoSqlpp11.h  
#include "BasePageDao.h"  
#include "Rest/Bean/Article.h"  
  
namespace Rest {  
namespace Dao {  
    class ArticleDaoSqlpp11 : public BasePageDao<Rest::Bean::Article> {  
    public:  
        // 必须实现纯虚函数  
        virtual Rest::Bean::Article getOne(int id) override;  
  
        // 其他成员函数...  
    };  
}  
}  
  
// ArticleDaoSqlpp11.cpp  
#include "ArticleDaoSqlpp11.h"  
  
Rest::Bean::Article Rest::Dao::ArticleDaoSqlpp11::getOne(int id) {  
    // 实现获取单个Article的逻辑  
}
```


在这个示例中，BaseCrud 是一个包含虚析构函数和纯虚函数的抽象基类。BasePageDao 是一个模板基类，它继承自 BaseCrud 并提供了虚析构函数的实现（尽管是空的）。ArticleDaoSqlpp11 是 BasePageDao 的一个派生类，它必须实现 getOne 纯虚函数，因为它是从 BaseCrud 继承而来的抽象接口的一部分。

date 3.0.1 要求c++17，否则time_zone报错
cmakelistx.txt里面有
conan date页面我没找到相关要求

maven 库要求java11，如果用java8编译，如何报错？

```
brew list log4cpp
/usr/local/Cellar/log4cpp/1.1.3/bin/log4cpp-config
/usr/local/Cellar/log4cpp/1.1.3/include/log4cpp/ (53 files)
/usr/local/Cellar/log4cpp/1.1.3/lib/liblog4cpp.5.dylib
/usr/local/Cellar/log4cpp/1.1.3/lib/pkgconfig/log4cpp.pc
/usr/local/Cellar/log4cpp/1.1.3/lib/ (2 other files)
/usr/local/Cellar/log4cpp/1.1.3/share/aclocal/log4cpp.m4
```
log4cpp mac平台，log4cpp cmake组织代码，link log4cpp库文件失败
https://github.com/edidada/cpptestlog4cpp
手动g++制定lib文件夹路径成功

你遇到的错误可能是由于`time_point_cast`的使用方式不正确，或者是因为包含的头文件不完整。要解决这个问题，你可以按照以下方式修改代码：

### 1. 确保包含正确的头文件
确保你包含了`<chrono>`和`<date/date.h>`（如果你在使用 Howard Hinnant 的 `date` 库）。例如：

```cpp
#include <chrono>
#include <iostream>
#include <date/date.h>
```

### 2. 使用 `time_point_cast` 正确转换时间
`time_point_cast` 是 C++17 中引入的，用于在不同的时间点类型之间进行转换。正确的使用方式如下：

```cpp
#include <iostream>
#include <chrono>
#include <date/date.h>

int main() {
    using namespace std::chrono;
    auto now = system_clock::now();

    // 使用 system_clock::duration 进行转换
    auto now_c = time_point_cast<system_clock::duration>(now);

    // 如果你使用 date 库，需要这样转换
    auto now_date = date::floor<system_clock::duration>(now);

    std::cout << "Time point cast successful.\n";

    return 0;
}
```

### 解释

1. C++17 标准库：`time_point_cast<Duration>(time_point)` 是标准库的一部分，可以将一个时间点转换为指定精度的时间点。

2. `date` 库：如果你在使用 `date` 库，则可以使用 `date::floor<Duration>(time_point)` 来实现类似的转换。

如果你只是使用标准的 C++ 库，那么你只需要 `time_point_cast<system_clock::duration>(now)`；如果你在使用 `date` 库，那么你可以使用 `date::floor` 函数来实现类似的效果。

这两个方法都应该能解决你遇到的问题。

在 CMake 和 Conan 中，`Component target` 和 `Target` 是构建和链接项目时的关键概念，用于表示项目中的特定部分（组件）和最终生成的可执行文件或库。

### 1. Target（目标）
在 CMake 中，`Target` 是一个泛指的术语，表示可以构建的实体，比如可执行文件、静态库、共享库等。目标通常是通过 `add_executable()` 或 `add_library()` 这样的命令定义的。在 CMake 文件中，目标可以像下面这样声明：

```cmake
add_executable(MyApp main.cpp)
add_library(MyLib mylib.cpp)
```

一旦定义了目标，你可以使用 `target_link_libraries()` 函数来链接依赖的库到该目标。例如：

```cmake
target_link_libraries(MyApp MyLib)
```

### 2. Component Target（组件目标）
在 Conan 中，`Component target` 是用于描述一个库中不同部分的概念。一个库可以包含多个组件，每个组件可能代表该库的一部分功能。例如，一个库可能包含核心功能、扩展模块、工具等，这些都可以被视为组件。每个组件可以被独立使用或链接。

- Component target：这个目标通常代表库中的某个特定组件。例如，对于 `date` 库中的时区支持部分，可以定义为一个组件目标 `date::date-tz`。

- Target：在库中可能有多个组件被合并为一个最终的目标，例如 `date::date` 代表 `date` 库的核心部分。

### 3. 在 `date` 库中的应用
- `date::date`：这是 `date` 库的核心功能部分的目标。它提供了日期和时间的基本处理功能。

- `date::date-tz`：这是 `date` 库中的时区支持组件。时区支持可能依赖于额外的数据和功能，因此它作为一个独立的组件目标存在。

### 4. 在 CMake 中使用
在使用 Conan 管理的依赖时，CMake 会识别并引入这些目标。你可以在 CMakeLists.txt 中通过 `target_link_libraries()` 函数链接这些目标：

```cmake
# 链接核心 date 库功能
target_link_libraries(MyApp date::date)

# 如果需要时区支持，链接 date-tz 组件
target_link_libraries(MyApp date::date-tz)
```

### 总结
- Target 是在 CMake 中表示构建目标的通用术语，可以是可执行文件、库等。
- Component Target 是 Conan 用于细分库中的不同功能模块的概念，可以让你只链接和使用库的特定部分。

在你的情况下，`date::date-tz` 和 `date::date` 分别代表 `date` 库的时区支持组件和核心功能部分。你可以根据需要在 CMake 中链接这两个组件，以确保你的应用程序包含所需的功能。

msu编码器大赛
https://www.openh264.org/

有各个平台编译好的库
https://sourceforge.net/projects/openh264.mirror/files/v2.4.1/

git clone https://github.com/cisco/openh264.git
cd openh264
git checkout v2.4.1
meson builddir
ninja -C builddir
meson test -C builddir -v

Android source code :
external/opencore/codecs_v2/video/avc_h264/enc
So library :
adb shell -> cd  system/lib ---> libopencoreanthor.so

推荐开源项目：MediaCodecEncodeH264 - 高效的Android H.264编码库
项目简介
是一个基于Android MediaCodec API实现的高效H.264视频编码库。此项目由张鹏飞（sszhangpengfei）开发并开源，旨在为开发者提供一种更简单、高效的方案来进行H.264编码操作，特别适合需要处理实时视频流或进行视频录制的应用。

https://github.com/AnJoiner/FFmpegCommand

一般的FFmpeg编译之后也会有6～7M左右，再加上编译一些第三方音视频处理库的话（如：fdk-aac、mp3lame、libx264等等），可能达到10多M，这样就造成了APP的臃肿，所以说对于APP上使用的应用程式应该秉承这样一个原则：

https://juejin.cn/post/6949916105271935007

https://www.videolan.org/vlc/

https://www.videolan.org/developers/x264.html
https://www.videolan.org/developers/x262.html
https://www.videolan.org/developers/x265.html

git clone https://code.videolan.org/videolan/x264.git

Software using x264
Avidemux
ELDER
ffdshow
ffmpeg
GordianKnot
Handbrake
LiVES
MeGUI
MEncoder
Bencos (formerly RealAnime)
StaxRip
VLC media player

https://juejin.cn/post/6949916105271935007


除了使用MediaCodec进行编码之外，另外一种比较流行的方案就是使用ffmpeg+x264/openh264进行软编码，ffmpeg是用于一些视频帧的预处理。这里主要是使用x264/openh264作为视频的编码器。

openh264(https://github.com/cisco/openh264)则是由思科开源的另外一个h264编码器，项目在2013年开源，对比起x264来说略显年轻，不过由于思科支付满了h264的年度专利费，所以对于外部用户来说，相当于可以直接免费使用了，另外，firefox直接内置了openh264，作为其在webRTC中的视频的编解码器使用。


微信公众号
WeMobileDev

微信开源组件WCDB

https://github.com/obsproject/obs-studio

https://obsproject.com/

https://docs.obsproject.com/

https://github.com/obsproject/obs-studio/wiki/install-instructions#building-obs-studio
https://github.com/obsproject/obs-studio/wiki/build-instructions-for-linux