# 20240821
所谓PImp是非常常见的隐藏真实数据成员的技巧，核心思路就是用另一个类包装了所要隐藏的真实成员，在接口类中保存这个类的指针。
在C++中，"PImp"（可能是一个简写或特定领域的术语，不常见于标准C++术语中）可能指的是一种用于实现接口和实现分离的技术，特别是通过指针来实现（Pointer to IMPlementation，简称PImpl）。这种技术常用于隐藏类的具体实现细节，增强编译时的独立性和减少编译依赖。下面我将介绍如何使用C++封装一个使用PImpl手法的类。

步骤 1: 定义接口类
首先，定义一个接口类（也称为抽象基类），这个类声明了将要被实现的接口（即纯虚函数）。

cpp
// IMyInterface.h  
#ifndef IMYINTERFACE_H  
#define IMYINTERFACE_H  
  
class IMyInterface {  
public:  
    virtual ~IMyInterface() {}  // 虚析构函数保证正确的析构  
    virtual void doSomething() = 0;  // 纯虚函数，表示接口  
};  
  
#endif // IMYINTERFACE_H
步骤 2: 定义实现类
然后，在另一个文件中定义具体的实现类，该类继承自接口类，并实现接口中的所有纯虚函数。

cpp
// MyInterfaceImpl.h  
#ifndef MYINTERFACEIMPL_H  
#define MYINTERFACEIMPL_H  
  
#include "IMyInterface.h"  
  
class MyInterfaceImpl : public IMyInterface {  
private:  
    // 类的私有成员  
  
public:  
    void doSomething() override;  // 实现接口  
    // 其他成员函数  
};  
  
#endif // MYINTERFACEIMPL_H  
  
// MyInterfaceImpl.cpp  
#include "MyInterfaceImpl.h"  
#include <iostream>  
  
void MyInterfaceImpl::doSomething() {  
    std::cout << "Doing something..." << std::endl;  
}
步骤 3: 使用PImpl封装
现在，我们将使用PImpl手法来封装实现类。这通常意味着在另一个类中，我们使用一个指向实现类的指针来持有具体的实现。

cpp
// MyClass.h  
#ifndef MYCLASS_H  
#define MYCLASS_H  
  
#include "IMyInterface.h"  
#include <memory>  // 对于智能指针  
  
class MyClass {  
private:  
    std::unique_ptr<IMyInterface> m_pImpl;  // 指向实现的智能指针  
  
public:  
    MyClass();  // 构造函数  
    ~MyClass();  // 析构函数  
  
    void doAction();  // 通过接口调用实现  
};  
  
#endif // MYCLASS_H  
  
// MyClass.cpp  
#include "MyClass.h"  
#include "MyInterfaceImpl.h"  
  
MyClass::MyClass() : m_pImpl(std::make_unique<MyInterfaceImpl>()) {}  
  
MyClass::~MyClass() {}  
  
void MyClass::doAction() {  
    m_pImpl->doSomething();  // 通过接口调用实现  
}
优点
隐藏实现细节：用户只需要知道接口，而不需要知道具体的实现。
减少编译依赖：因为用户代码只包含接口头文件，而不需要包含实现类头文件，这可以减少编译时间并降低编译错误的风险。
增强可扩展性和可维护性：可以在不改变接口的情况下修改实现类，或者在接口不变的情况下添加新的实现类。
通过使用PImpl手法，你可以使你的C++代码更加模块化、更加易于管理和维护。

https://zhuanlan.zhihu.com/p/338227526



https://github.com/Ikaros-521/Learning-Notes
extern "C" {}
        告诉C++编译器按照C语言的方式声明函数，这样C++就可以调用C编译器编译出的函数了（C++目标文件
可以与C目标文件合并生成可执行程序）。
        如果C想调用C++编译出的函数，需要将C++函数的定义用extern "C"包括一下。
        注意：如果两个函数名一样，一定会冲突。



在C++17标准中，协程（Coroutine）并没有直接被包含在标准库中，而是作为一个提议（Proposal）在后续的C++标准（特别是C++20）中被引入。然而，你可以通过一些编译器支持（如GCC的co_await、co_yield、co_return等关键字的扩展）以及第三方库（如Boost.Coroutine2）来体验协程的概念。

不过，为了给你一个接近C++20协程特性的示例，我将展示一个假设的、使用C++20协程库的简单例子。请注意，这个例子需要使用支持C++20的编译器（如GCC 10及以上版本，或者MSVC的某个支持C++20的版本）。

首先，这里是一个简单的生成器函数，它使用C++20的协程特性来生成一系列整数：

cpp
#include <coroutine>
#include <iostream>
#include <vector>
#include <memory>

// 协程的返回类型，用于生成整数
struct Generator {
    struct promise_type {
        int value;

        Generator get_return_object() {
            return Generator{std::coroutine_handle<promise_type>::from_promise(*this)};
        }
        std::suspend_never initial_suspend() { return {}; }
        std::suspend_always final_suspend() noexcept { return {}; }
        void return_void() {}
        void unhandled_exception() { std::terminate(); }
        std::suspend_always yield_value(int v) {
            value = v;
            return {};
        }
    };

    std::coroutine_handle<promise_type> coro;

    Generator(std::coroutine_handle<promise_type> h) : coro(h) {}
    ~Generator() {
        if (coro) coro.destroy();
    }

    bool moveNext() { return coro.resume(), !coro.done(); }
    int currentValue() const { return coro.promise().value; }
};

// 协程函数，生成一个整数序列
Generator generateSequence() {
    for (int i = 0; i < 5; ++i) {
        co_yield i;
    }
}

int main() {
    auto seq = generateSequence();
    while (seq.moveNext()) {
        std::cout << seq.currentValue() << std::endl;
    }
    return 0;
}

注意：这个示例代码是假设性的，因为它基于C++20的协程特性，但在C++17中并不直接可用。在C++17中，你需要依赖编译器的特定扩展或者第三方库来实现类似的协程功能。

如果你正在使用C++17并且想体验协程，你可能需要查看如Boost.Coroutine2这样的库，或者使用支持C++20特性的编译器来编译和运行上述代码。

从无栈协程到C++异步框架
https://zhuanlan.zhihu.com/p/568518673

c++友元函数报错例子
https://www.zhihu.com/question/31121134

友元函数没有this指针，则参数要有三种情况:
①要访问非static成员时，需要对象做参数，
②要访问static成员或全局变量时，则不需要对象做参数，
③如果做参数的对象是全局对象，则不需要对象做参数,
没有对象做参数的，需要exter 声明一下即可


智能化弹性伸缩方案 AHPA
https://zhuanlan.zhihu.com/p/584518584


标准是Coming soon，不是What's new.
C++标准是下阶段编译器的开发计划，而不是新版本编译器的发布说明。发布C++20，意味着C++20标准就是编译器在2020~2023期间的开发计划，此时C++17的特性应该是完全可用的了。在2020~2023期间全力制作C++20特性。到了C++23发布之时，意味着C++23就是2023~2026期间的编译器开发计划，此时C++20特性应该基本全部完成。现在才2022年，本来就没到C++20特性全部完成的时间点啊。


面向对象使用的轻量化高并发Linux c++网络库kikilib
https://github.com/YukangLiu/kikilib

https://zhihu.com/column/c_1371105465062076416



https://github.com/edidada/kikilib

C++环境下，可以通过matplotlibcpp库来实现折线图等图表的绘制。

clang-format 是 LLVM 开发的用于格式化 C/C++/Java/JavaScript/Objective-C/Objective-C++/Protobuf 等多种语言代码的工具，借助 clang-format 可以实现代码仓库的风格统一，提升开发效率，本文将阐述使用该工具进行代码风格管理的基本步骤。

leveldb写的根本就不像cpp 看看里面的迭代器判断valid next prev明明是java看看rocksdb 这才是真正的cpp
c++ 风格的迭代器用起来很爽，但实现起来很麻烦，还是基础库用的多。例如，c++ 迭代器通常支持拷贝，你试试把 leveldb 迭代器改成支持拷贝，就知道有多难搞了。


https://systems.cs.columbia.edu/projects/kvm-arm/


Arm Open Source
https://developer.arm.com/



使用 TiDB 列存引擎进行实时数据分析
 
PingCAP 实时分析 / 资深技术总监
马晓宇，PingCAP 实时分析组负责人，负责 TiDB 列存引擎 TiFlash 的研发。曾担任 Quantcast SQL 平台和网易杭研大数据基础架构组 Tech Lead。关注分布式系统以及数据库。

课程简介
案例背景:
当业务不断发展和变复杂，数据不断增长，用户的数据平台架构被迫引入各种复杂的组件以解决平台的扩展性以及不同场景对数据应用的不同需求，例如批量计算的报表需求，高速高并发的数据存取，或者介于两者之间的综合需求，异或对数据的实时分析。而在引入不同组件的同时，数据在平台间流转也变得更加困难，时效性和一致性也不断降低。我们是否有可能找到一个方案，让架构尽可能简化？

解决思路:
随着 TiDB 新版发布的 TiFlash 是一套高性能的列存引擎，它补足了 TiDB 在 HTAP 场景下行存分析性能偏弱的缺点。和以往的列存设计不同，TiFlash 是专为实时分析场景设计的。它拥有匹配行存的实时更新数据能力，可以作为可随意拔插的列存镜像实时同步在线数据。配合 TiDB 优化器的智能选择功能，它不但可以让用户实时查询在线交易数据而不必担心影响业务稳定，也可以当做查询加速器为 TiDB 分析场景提速，使得整个体系同时拥有行存和列存的性能优势。这样的架构，可以很大程度满足不同场景的多种需求，简化用户的平台架构以及运维成本。

成果：
本次分享将会介绍当前 TiDB HTAP 体系的案例分享以及如何实际解决用户的痛点。

目标收益
1. 了解可实时更新列存的设计
2. 了解分布式一致性的异构复制体系设计
3. 了解 TiDB 独特的行列混合 HTAP 架构优势


C++为什么提供move函数？
或者说，compiler为什么不能向程序员隐藏这个功能，由compiler自己选择什么情况下move？而什么情况下，需要程序员去指定move？
我大概明白问题的意思，但是编译器的优化不是万能的。左值和右值本来就是编译器自己判断的。但是当你想强制把左值当成右值时就需要调用move。也就是说移动语义大部分情况下是自动的，只有个别情况下才需要手动move。对左值move转移走以后可能把原来的值清空了。这个结果是程序可见的，不能编译器自己处理。因为编译器的优化不能改变程序逻辑。


https://github.com/edidada/uWebSockets

cpp17
https://github.com/uNetworking/uWebSockets

https://zhuanlan.zhihu.com/p/590634684
https://github.com/wanghenshui/cppweeklynews


有最新版的Effective Modern C++, 讲的是11到14

就拿数据挖掘来说，基本就是几大‘门派’，UIUC韩组，CMU christos还有他的学生Jure，ASU huan组，UIC 菲利普Yu组。你可以看看现在北美DM圈子哪一个没有跟他们沾上边？


个人比较熟悉而且做过的是 1 。简单来说就是找一些开源项目，去打 deb/rpm 或者是 AUR 包。由于需要符合发行版的 convention ，很多时候不得不需要 patch Makefile ，或者 reconfigure 。找的开源项目可以是比较复杂使用了 autotools 的，这样就可以照着 autotools 的很多 guide （然而不幸的是没有哪个写得真的很好的……）参照实例来理解。还可以试着找官方源里已经有的包，自己打个新版或者有 custom patch 的，然后再对照官方的 rules/spec 来看看。Debian/Fedora 仓库里很多包的构建都有些“巧妙”（dirty）的 hack ……2 的话就更是需要自己编译安装一堆项目，使用的 toolchain 千奇百怪。不过有个缺点是只要“装上就行”，可能不像 1 那样需要追求 elegant 、 uniform ，需要改构建过程的机会更少（大部分时候就是 follow document 打几条命令）。不过由于我其实没有完整做过，这也只是我的猜想而已。

作者：马宏菩
链接：https://www.zhihu.com/question/34982971/answer/62768494
来源：知乎
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。


做完卡耐基梅隆大学的计算机系统导论的全部实验

工具类的东西，大部分功能是不用的。入门的话要结合应用场景，经常用的多查资料。Linux 下的东西很零散的，没有一本书可以讲的很全。

c++ std 容器是非侵入性质的，数据结构和算法课程里面是侵入性质的，性能高点


cpp 编译原理
https://github.com/humeng1832596901/toy_interpreter


在12306出现之前，线下购票柜台工作人员使用的TRS系统也是全国联网的

sbrk()函数
brk, sbrk - change data segment size

SYNOPSIS
#include <unistd.h>
int brk(void *addr);
void *sbrk(intptr_t increment);

只是开源了可视化和数据协议部分，因为我们也在做类似的工作，坦白的说，这部分技术难度并不大，主要是工程方面需要开发工作量
https://avs.auto/#/
官方地址是avs.auto，主要分可视化部分streetscape.gl和数据协议部分xviz

https://github.com/aurora-opensource/xviz
XVIZ is a protocol for real-time transfer and visualization of autonomy data. 


我安装了一次破解版，然后再次安装更新版本，报错了。怎么办？
另外，多问一句用过的人，understand不支持自动补全是么？


AMD为什么要把FSR开源

https://missing-semester-cn.github.io/


需要多个流的话，为什么当初没有选用 sctp 呢？为什么 sctp 没能在公网上流行？sctp 保证不了最坏情况吗？
google有一篇关于quic的论文专门讨论了这个问题，简而言之互联网上有海量的nat设备只支持TCP和udp，因此在互联网上大规模引入两者之外的协议在实践中几乎是不可能的
sctp也有二十多年了，真的遗憾，本来是一个很好的协议。

c++特别是内存安全的一系列规范，内存上的 Bug 是很难定位的。

https://github.com/cpp-best-practices/cppbestpractices


指针（包含指针数组、数组指针、函数指针、指针的指针

多态分为静态多态和动态多态，动态多态就是我们常说的virtual，在运行时才知道其具体类型；静态多态除了传统的template外，自cpp17也引入了variant

根据2024年的排名，美国计算机科学（CS）领域排名前四的大学通常根据不同的排名机构略有不同，但以下是四所常被认为在计算机科学领域顶尖的大学：

1. 麻省理工学院 (MIT)
2. 斯坦福大学 (Stanford University)
3. 加州大学伯克利分校 (UC Berkeley)
4. 卡内基梅隆大学 (Carnegie Mellon University)

这些大学在计算机科学领域都拥有非常强的研究和教育实力，吸引了全球顶尖的学生和学者。每年不同的排名机构（如 U.S. News、QS 世界大学排名等）可能对它们的排名顺序稍有不同，但这四所大学几乎总是名列前茅。

https://github.com/YukunJ/Turtle/blob/main/README_CN.md

Turtle是一个基于Linux的C++17轻量级网络库. 它将繁琐的套接字操作抽象为优雅可复用的类. 它支持自定义服务器的快速设立: 以回调函数的形式为每一个用户的TCP连接加入业务逻辑. 它同时支持HTTP协议的GET/HEAD请求与回复.

https://github.com/edidada/Turtle/


中国工商银行于 2014 年率先启动银行核心业务系统的分布式架构转型探索，自主研发了同业领先的分布式服务平台。
https://zhuanlan.zhihu.com/p/489778327

先设置输出，在设置输入

VS调试器功能确实不错，比gdb更直观友好这个确实，更好的调试器能更快地处理疑难病症。
不过，调试器的作用没有那么大，单元测试的用处远比调试器用处大；调试器还不如valgrind之类的东西用处大。只有代码出了问题，对智商要求超出预期、已经hold不住的时候，才成为必需品，因此调试器的使用率不高。
日常开调试器，形成对调试器的强烈依赖，以至于写一个只有寥寥几句非常简单的小程序，也必须开调试器的人，基本上都是写不出好代码的人。他们像探索宇宙一样使用调试器探索自己所写的代码，而不是构思自己的代码。一句话，他们连自己写的代码在干什么都不太清楚。
写代码之前要想好
处理疑难杂症还得是 xdbg

目前各个大厂都有自己的rtc技术，也都是webrtc私有化改出来的，比如腾讯叫trtc，百度的叫brtc，抖音drtc，快手叫krtc，网易叫wrtc等等吧

mvcc
https://github.com/rosedblabs/rust-practice/tree/main/mvcc


这就是简单的宏函数，各类大型软件系统，库里很常见

宏与模版结合起来比这个骚多了


https://github.com/rosedblabs/rust-practice/tree/main/mvcc

逻辑错误用log，看调用栈用gdb，单元测试用gtest，性能热点用gprof，这样就没有搞不定的bug
补充一条，内存错误用valgrind