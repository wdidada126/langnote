# 20240815

-DCMAKE_EXPORT_COMPILE_COMMANDS=ON
https://cmake.org/cmake/help/latest/variable/CMAKE_EXPORT_COMPILE_COMMANDS.html
This option is implemented only by Makefile Generators and Ninja Generators. It is ignored on other generators.

对于 Visual Studio，你可以安装 "CMake Tools for Visual Studio" 扩展，然后在项目设置中启用 "Generate CMake options" 选项。这将在构建过程中自动生成 compile_commands.json 文件。

要在CMake中设置选项直接生成`compile_commands.json`文件，你可以在运行CMake时添加`-DCMAKE_EXPORT_COMPILE_COMMANDS=ON`选项。这将在构建过程中生成一个名为`compile_commands.json`的文件，其中包含了所有编译命令的信息。

具体操作步骤如下：

1. 打开终端或命令提示符。
2. 导航到你的CMakeLists.txt所在的目录。
3. 运行以下命令：

```bash
cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -S <源代码目录> -B <构建目录>
```

其中，`<源代码目录>`是你的CMakeLists.txt所在的目录，`<构建目录>`是你希望生成构建文件的目录。例如：

```bash
cmake -S /path/to/your/source -B /path/to/your/build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
```

4. 构建项目：

```bash
cmake --build <构建目录>
```

例如：

```bash
cmake --build /path/to/your/build
```

5. 完成后，你会在构建目录中找到`compile_commands.json`文件。



Clion编译

```shell
-- Could NOT find OpenSSL, try to set the path to OpenSSL root folder in the system variable OPENSSL_ROOT_DIR (missing: OPENSSL_CRYPTO_LIBRARY) (found version "1.1.0i")
-- Could NOT find APR (missing: APR_INCLUDE_DIR APR_LIBRARY) 
-- Could NOT find APRUTIL (missing: APRUTIL_INCLUDE_DIR APRUTIL_LIBRARY) 
-- Could NOT find Apache2 (missing: APACHE2_INCLUDE_DIR) 
-- Could NOT find MySQL (missing: MYSQL_INCLUDE_DIR MYSQL_LIBRARY) 
-- Could NOT find PostgreSQL (missing: PostgreSQL_LIBRARY) (found version "9.6.3")
```

MYSQL_INCLUDE_DIR MYSQL_LIBRARY


cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -G "Unix Makefiles" -S D:\git\github\poco -B D:\git\github\poco\cmake-build-debug -DMYSQL_INCLUDE_DIR=D:/mysql-8.0.26-winx64/include -DMYSQL_LIBRARY=D:/mysql-8.0.26-winx64/lib/libmysql.lib



C:\Users\edida\AppData\Local\Programs\CLion\bin\cmake\win\x64\bin\cmake.exe -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -G "Unix Makefiles" -DEVENT__DISABLE_OPENSSL=ON -DEVENT__DISABLE_BENCHMARK=ON -DCMAKE_TOOLCHAIN_FILE=D:\git\github\vcpkg\scripts\buildsystems\vcpkg.cmake -S D:\git\github\testlibevent -B D:\git\github\testlibevent\cmake-build-debug


https://clangd.llvm.org/design/compile-commands

cmake generator的概念
https://cmake.org/cmake/help/latest/manual/cmake-generators.7.html#id7