# 20240901

nm -C --defined-only  build/Debug/blog_rest >~/rest_poco.nm.txt

00000000013075b8 V vtable for Rest::Dao::BasePageDao<Rest::Bean::Article>
00000000012c17c8 V vtable for Rest::Dao::UserDaoPoco
00000000012c1860 V vtable for Rest::Dao::AdminDaoPoco
00000000012c16d8 V vtable for Rest::Dao::ArticleDaoPoco
00000000012c1938 V vtable for Rest::Dao::UserDaoSqlpp11
00000000012c18c8 V vtable for Rest::Dao::AdminDaoSqlpp11
00000000012c13d8 V vtable for Rest::Dao::ArticleDaoSqlpp11
00000000012c1440 V vtable for 

tagged union
https://zhuanlan.zhihu.com/p/714978114

https://www.oreilly.com/library/view/c2/9787111633280/Text/chapter22.xhtml

enum class Color {
} 和enum Color {
}区别？

在C++中，`enum`和`enum class`都是用来定义枚举类型的方式，但它们在作用域限定性、隐式转换以及底层类型的指定等方面有所区别，具体分析如下：
1. 作用域限定性
   - enum：传统的`enum`定义的枚举值在相同作用域下是可见的，这可能会导致名称冲突。
   - enum class：枚举类`enum class`的枚举值在枚举类的作用域内是局部的，不会与其他作用域中的名称冲突，从而防止了命名空间污染。
2. 隐式转换
   - enum：枚举值可以隐式转换为其底层类型（通常是整数类型），并且可以与整数类型进行比较。
   - enum class：枚举值不会隐式转换为其底层类型，需要。
3. 底层类型的指定
   - enum：底层类型可以是`int`、`unsigned int`、`char`等整数类型，默认情况下是`int`。
   - enum class：除了传统的`enum`支持的类型外，还可以是任何整数类型，包括枚举类本身不具备的类型（例如`long`、`unsigned long long`等）。
4. 枚举值访问
   - enum：枚举值可以直接使用枚举值的名称。
   - enum class：必须通过作用域解析运算符`::`来访问，例如`EnumClassName::EnumValue`。
5. 类型安全程度
   - enum：不是完全类型安全的，可以与其他整型类型进行比较和赋值。
   - enum class：类型安全，不能将其值与其他类型进行比较或赋值，除非显式转换。

针对上述分析，提出以下几点建议：
- 当您希望避免枚举值的名称冲突时，考虑使用`enum class`；
- 如果您打算将枚举值与整数进行直接比较或赋值，而不希望进行额外的类型转换，可以考虑使用传统`enum`；
- 在需要跨编译单元共享枚举常量时，`enum class`支持前置声明，可以减少编译依赖，提高编译效率；
- 选择枚举类型的底层类型时，对于`enum class`，您有机会指定非默认的底层类型，以节省存储空间或表示更大的范围；
- 当您的代码库已经使用了传统`enum`并且没有特定需求迁移到`enum class`，可以继续使用`enum`以避免不必要的改动。
总的来说，`enum class`提供了更严格的类型检查和更好的作用域限定性，有助于避免名称冲突和意外的类型转换错误。在现代C++编程中，`enum class`被广泛推荐用于代替传统的`enum`，特别是在追求强类型安全性和清晰的命名空间管理的场景下。

在C++中，虚函数是通过虚函数表（vtable）来实现的。每个包含虚函数的类都有一个虚函数表，其中存储了该类的虚函数地址。当调用一个对象的虚函数时，程序会根据对象的实际类型查找相应的虚函数表，然后调用表中的函数。
要在Ubuntu上查看C++项目的虚函数及其实现类的虚表，可以使用`nm`命令和`objdump`命令。首先，需要编译项目并生成可执行文件或共享库。然后，使用以下命令查看虚函数表：
1. 使用`nm`命令查看符号表：
```bash
nm -C --defined-only your_executable_or_shared_library
```
这将显示项目中所有符号的列表，包括虚函数。虚函数通常以`vtable for`开头，后面跟着类名。例如：
```
0000000000000000 T vtable for YourClassName
```
2. 使用`objdump`命令查看虚函数表的具体内容：
```bash
objdump -p -t your_executable_or_shared_library
```
这将显示项目中所有类型的详细信息，包括虚函数表。虚函数表通常以`VTT for`开头，后面跟着类名。例如：
```
Contents of section '.rodata':
 0000000000000000 VTT for YourClassName
```
要查看特定类的虚函数表，可以使用`grep`命令过滤输出：

```bash
objdump -p -t your_executable_or_shared_library | grep "VTT for YourClassName"
```
这将显示特定类的虚函数表的内容，包括虚函数的地址。