# 202409_week3

- 202409 月第 3 周

## 本周记录

- 20240915：补充了当日日志，暂未提炼出明确主题。
- 20240916：补充了当日日志，暂未提炼出明确主题。
- 20240917：在 C++11 标准中引入了多线程支持，包括了 `std::thread` 类和相关的头文件。在 Mac 平台上，你可以使用 Clang 或者 GCC 编译器来编译支持 C++11 的代码。；在 C++11 标准之前，进行多线程编程可能需要依赖于操作系统提供的线程库，比如 POSIX 线程库。在 Mac 平台上，它支持 POSIX 线程，你可以使用 `<pthread.h>` 头文件来开启多线程编程。；以下是一个简单的示例代码，展示了如何在 C++11 之前使用 POSIX 线程库在 Mac 平台上开启多线程编程：
- 20240918：补充了当日日志，暂未提炼出明确主题。
- 20240919：oracle sql develope，insert update delete数据之后，如何提交数据；oracle sql develope有提交按钮；在SQL查询中，WHERE、GROUP BY、和ORDER BY子句通常用于对数据进行筛选、分组和排序。这些子句在SQL语句中的顺序很重要，但它们并不是按照它们在语句中出现的顺序来执行的。了解这些子句的逻辑执行顺序对于编写有效和准确的查询至关重要。
- 20240920：补充了当日日志，暂未提炼出明确主题。
- 20240921：补充了当日日志，暂未提炼出明确主题。

## 整理补充

## 时间范围

- 202409 月第 3 周

## 本周记录

- 20240915：补充了当日日志，暂未提炼出明确主题。
- 20240916：补充了当日日志，暂未提炼出明确主题。
- 20240917：在 C++11 标准中引入了多线程支持，包括了 `std::thread` 类和相关的头文件。在 Mac 平台上，你可以使用 Clang 或者 GCC 编译器来编译支持 C++11 的代码。；在 C++11 标准之前，进行多线程编程可能需要依赖于操作系统提供的线程库，比如 POSIX 线程库。在 Mac 平台上，它支持 POSIX 线程，你可以使用 `<pthread.h>` 头文件来开启多线程编程。；以下是一个简单的示例代码，展示了如何在 C++11 之前使用 POSIX 线程库在 Mac 平台上开启多线程编程：
- 20240918：补充了当日日志，暂未提炼出明确主题。
- 20240919：oracle sql develope，insert update delete数据之后，如何提交数据；oracle sql develope有提交按钮；在SQL查询中，WHERE、GROUP BY、和ORDER BY子句通常用于对数据进行筛选、分组和排序。这些子句在SQL语句中的顺序很重要，但它们并不是按照它们在语句中出现的顺序来执行的。了解这些子句的逻辑执行顺序对于编写有效和准确的查询至关重要。
- 20240920：补充了当日日志，暂未提炼出明确主题。
- 20240921：补充了当日日志，暂未提炼出明确主题。
