# 20240902

既然scanf和strcpy等函数会被MSVC编译器报不安全，那么C语言教材为什么还讲这些函数？
https://www.zhihu.com/question/493813623/answer/2252862710
我直接把这篇知乎回答的**核心观点 + 最精炼结论**整理给你，**一句话讲透为什么教材还在讲 scanf / strcpy**。

# 核心结论（最直白）
**C 语言教材讲 scanf、strcpy，不是因为它们安全，而是因为它们是标准、通用、教学最干净的函数。**
微软报“不安全”只是**微软自己的行为**，不是 C 语言标准，更不是全世界通用规则。

---

# 详细原因（来自这篇知乎回答）
## 1. **不安全警告是微软 MSVC 自己加的，不是 C 标准**
- `scanf`、`strcpy` 是 **C89/C99/C11 标准库函数**，全球通用。
- **微软觉得不安全，于是标为 deprecated**，但 **GCC、Clang 都不这么干**。
- 换句话说：
  **只有 VS 会报错，Linux、Mac、嵌入式全都正常用。**

## 2. **安全版函数（_s 结尾）不标准、不通用、不可移植**
微软推的 `scanf_s`、`strcpy_s`：
- **不属于 C 标准强制要求**
- **Linux 没有，嵌入式没有，跨平台不能用**
- **C11 只是可选支持**，很多编译器干脆不实现
- **不同平台行为不一致**

教材不可能教**只有 Windows 能用、别的地方编译不过**的函数。

## 3. **教学必须教最基础、最通用、最干净的语法**
- `strcpy` 逻辑干净：复制字符串，一看就懂。
- `strcpy_s` 参数复杂、带长度、带错误处理，**不适合教学**。
- 教材目标是**教原理、教逻辑、教C语言本质**，不是教 Windows 编程。

## 4. **安全不是靠函数，而是靠用法**
回答里强调：
**不存在绝对安全的函数，只存在安全的写法。**
- 就算用 `strcpy_s`，缓冲区长度填错照样溢出。
- 真正的安全来自：
  - 清楚缓冲区大小
  - 边界检查
  - 良好编程习惯

`strcpy` 本身是**高度优化、高效**的底层函数，不是“垃圾函数”。

## 5. **C 语言哲学：相信程序员**
C 语言设计理念：
**给你最大权限，相信你知道自己在做什么。**
`strcpy`、`scanf` 就是这种哲学的体现。

微软 `_s` 函数偏向“安全枷锁”，**和 C 语言精神不符**。

---

# 最终精炼答案（可直接背诵）
1. **scanf/strcpy 是国际 C 标准，全球通用；**
2. **不安全警告只是微软自家编译器行为，不是标准；**
3. **安全版 _s 函数不可移植，Linux/嵌入式都不能用；**
4. **教材教标准、教原理，不教平台私货；**
5. **安全靠用法，不靠函数名。**

---

如果你需要，我可以给你一段**面试/作业可直接抄写的标准答案**。

ISSCC和所谓计算机体系结构四大顶会（ASPLOS、HPCA、MICRO、ISCA）有什么区别吗？
openshift

paho-mqttpp3

同时开启coroutines与module特性
https://docs.spring.io/spring-integration/reference/mqtt.html
Spring Boot 提供了用于 MQTT 消息传递的 `spring-boot-starter-mqtt` 起步依赖。通过使用这个起步依赖，您可以很容易地在 Spring Boot 应用程序中集成 MQTT 客户端功能。
首先，在您的 Spring Boot 项目的 `pom.xml` 文件中添加 `spring-boot-starter-mqtt` 依赖：
```xml
<dependency>
    <groupId>org.springframework.integration</groupId>
    <artifactId>spring-integration-mqtt</artifactId>
    <version>6.3.3</version>
</dependency>
```

接下来，您可以创建一个 MQTT 客户端的配置类，并在其中配置 MQTT 连接信息和相关的 bean。以下是一个简单的示例：
```java
import org.eclipse.paho.client.mqttv3.MqttConnectOptions;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.integration.annotation.ServiceActivator;
import org.springframework.integration.mqtt.core.MqttPahoClientFactory;
import org.springframework.integration.mqtt.core.MqttPahoMessageDrivenChannelAdapter;
import org.springframework.messaging.MessageHandler;

@Configuration
public class MqttConfig {

    private static final String MQTT_BROKER = "tcp://localhost:1883";
    private static final String CLIENT_ID = "spring-boot-client";

    @Bean
    public MqttConnectOptions mqttConnectOptions() {
        MqttConnectOptions options = new MqttConnectOptions();
        options.setServerURIs(new String[]{MQTT_BROKER});
        options.setAutomaticReconnect(true);
        // 设置其他连接选项
        return options;
    }

    @Bean
    public MqttPahoClientFactory mqttClientFactory() {
        DefaultMqttPahoClientFactory factory = new DefaultMqttPahoClientFactory();
        factory.setConnectionOptions(mqttConnectOptions());
        return factory;
    }

    @Bean
    @ServiceActivator(inputChannel = "mqttInputChannel")
    public MessageHandler mqttInputHandler() {
        MqttPahoMessageDrivenChannelAdapter adapter = new MqttPahoMessageDrivenChannelAdapter(CLIENT_ID, mqttClientFactory(), "your-topic");
        adapter.setCompletionTimeout(5000);
        adapter.setConverter(new DefaultPahoMessageConverter());
        adapter.setQos(1);
        return adapter;
    }
}
```
在这个配置类中，我们定义了 MQTT 连接选项以及 MQTT 客户端工厂。然后，通过 `MqttPahoMessageDrivenChannelAdapter` 创建了一个消息驱动的通道适配器，用于订阅指定的主题并处理接收到的消息。
请注意，您需要根据您的实际情况修改 MQTT 代理的地址、客户端 ID 以及订阅的主题等信息。
这样，您就可以使用 `spring-boot-starter-mqtt` 起步依赖在 Spring Boot 应用程序中实现 MQTT 客户端功能。
https://github.com/spring-projects/spring-integration-samples/blob/main/basic/mqtt/src/main/java/org/springframework/integration/samples/mqtt/Application.java

std容器列表
std::array,std::bitset, std::deque, std::list, std::map, std::queue, std::set, std::stack, std::string, std::tuple, std::unordered_map,std::unordered_set, std::vector，std::priority_queue

有个项目要关RTTI，cereal搞得要死要活搞不定，msgpack轻松解决了。
https://uscilab.github.io/cereal/index.html

https://github.com/felixguendling/cista

Rust mqtt
https://github.com/eclipse/paho.mqtt.rust

合肥产业园二期
中国银联系列公司的子公司众多，涵盖了多个领域，以下是一些主要的子公司及其简介：
1. 银联国际
简介：银联国际是中国银联负责运营国际业务的子公司，致力于拓展银联卡境外受理网络，扩大银联卡发行和使用，开展创新支付的跨境应用，提升银联品牌的国际影响力。
业务：通过与全球2000多家机构合作，银联国际的受理网络已延伸至全球181个国家和地区，境外77个国家和地区发行了超1.9亿张银联卡。此外，银联国际还积极推动移动支付服务的全球化，已有多个国家和地区的钱包接入银联网络。
高层：银联常务副总裁蔡剑波兼任银联国际董事长，李晓峰担任副董事长、首席执行官。
2. 银联商务
简介：银联商务是中国银联旗下的综合性支付服务提供商，为商户提供包括POS收单、互联网支付、移动支付等在内的全方位支付解决方案。
业务：银联商务的服务范围广泛，覆盖了零售、餐饮、交通、医疗等多个行业，为众多商户提供了安全、便捷的支付服务。
3. 银联数据
简介：银联数据是中国银联旗下的专业数据处理和金融服务公司，为金融机构提供包括数据处理、风险管理、信息咨询等在内的多项服务。
业务：银联数据凭借其强大的数据处理能力和专业的金融知识，为金融机构提供了高效、精准的服务，助力金融机构提升业务水平和风险管理能力。
4. 其他子公司
除了上述三个主要子公司外，中国银联还拥有上海联银创投、北京银联金卡科技有限公司、中金金融认证中心等多家子公司，这些子公司在各自的领域内发挥着重要作用，共同推动中国银联的发展。
总结
中国银联系列公司的子公司遍布多个领域，包括国际业务、支付服务、数据处理等，这些子公司凭借各自的专业优势和创新能力，共同构成了中国银联强大的业务体系和服务网络。通过不断拓展和创新，中国银联将继续为全球用户提供更加安全、便捷、高效的支付服务。
请注意，以上信息可能随时间发生变化，建议访问中国银联官网或相关权威机构网站以获取最新信息。