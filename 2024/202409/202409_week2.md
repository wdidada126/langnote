# 202409_week2

- 202409 月第 2 周

## 本周记录

- 20240908：补充了当日日志，暂未提炼出明确主题。
- 20240909：左耳听风；在Oracle中，我们可以使用ROWNUM伪列和子查询来实现分页。而在MyBatis中，我们可以通过编写动态SQL来实现分页。；以下是Oracle SQL的分页示例：
- 20240910：c，没新建文件的api；在C语言中，没有专门用来创建文件的内置函数或API。但是，你可以使用标准库中的fopen函数来创建一个新文件。如果文件已经存在，fopen会覆盖它。如果文件不存在，fopen会创建一个新文件。；下面是使用fopen创建文件的示例代码：
- 20240911：oracle数据库；<insert id="insertData" parameterType="com.example.YourEntity">；INSERT INTO your_table_name (column1, column2, create_time)
- 20240912：要查看某个commit ID是在哪个分支提交的，可以使用以下Git命令：；将`<commit-id>`替换为您要查询的commit ID。这个命令会列出所有包含该commit ID的分支。
- 20240913：java代码；检查好之后再提交，复制黏贴的代码往往错误；屹通ares项目
- 20240914：devv.ai

## 整理补充

## 时间范围

- 202409 月第 2 周

## 本周记录

- 20240908：补充了当日日志，暂未提炼出明确主题。
- 20240909：左耳听风；在Oracle中，我们可以使用ROWNUM伪列和子查询来实现分页。而在MyBatis中，我们可以通过编写动态SQL来实现分页。；以下是Oracle SQL的分页示例：
- 20240910：c，没新建文件的api；在C语言中，没有专门用来创建文件的内置函数或API。但是，你可以使用标准库中的fopen函数来创建一个新文件。如果文件已经存在，fopen会覆盖它。如果文件不存在，fopen会创建一个新文件。；下面是使用fopen创建文件的示例代码：
- 20240911：oracle数据库；<insert id="insertData" parameterType="com.example.YourEntity">；INSERT INTO your_table_name (column1, column2, create_time)
- 20240912：要查看某个commit ID是在哪个分支提交的，可以使用以下Git命令：；将`<commit-id>`替换为您要查询的commit ID。这个命令会列出所有包含该commit ID的分支。
- 20240913：java代码；检查好之后再提交，复制黏贴的代码往往错误；屹通ares项目
- 20240914：devv.ai
