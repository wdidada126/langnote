# 20240326

java
a.foreach();
会有空指针异常

判空
```sql
	<select id="getPayrollCntId"  parameterType="map" resultType="map">
		select sum(PAYOFF_RECORD.STAFF_NUM) as PAYROLL_EMP_NUM
			,sum(PAYOFF_RECORD.STAFF_AMT) as PAYROLL_EMP_AMT
			,PAYOFF_RECORD.ETP_ID
		from ${APP_MARKET}.MKT_TENA_INFO_TD TENA_INFO
			inner JOIN ${FUYAO_USER}.T_ENTERPRISE_INFO_TD F_ETP_INNFO
		ON F_ETP_INNFO.APP_ID = TENA_INFO.CODE and TENA_INFO.TENA_ID = #{TENANT_ID}
			inner JOIN
			${APP_MARKET}.MTK_JX_SHOW_PAYOFF_RECORD PAYOFF_RECORD on PAYOFF_RECORD.ETP_ID = F_ETP_INNFO.ETP_ID
			group by PAYOFF_RECORD.ETP_ID
		order by PAYROLL_EMP_NUM
	</select>
```
租户
机构
