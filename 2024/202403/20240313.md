# 20240313

mysql
cs架构
连接管理
磁盘（文件管理
数据读写，提高效率，多线程/多进程？
事务
acid
原子性，一致性，隔离性，持久性
隔离级别
ru
rc
rr
s

日志

磁盘文件

索引，提高查询销量，空间换时间
索引失效
隐士转换
临时表
sql性能优化


什么时候生成redo log
什么时候生成undo log
什么时候删除，什么时候使用？

隔离级别
ru
rc
rr
s

脏读，幻读，不可重复度
什么现象
怎么解决


数据事务并发带来的问题：

脏读(Drity Read)：事务A更新记录但未提交，事务B查询出A未提交记录。

不可重复读(Non-repeatable read): 事务A读取一次，此时事务B对数据进行了更新或删除操作，事务A再次查询数据不一致。

幻读(Phantom Read): 事务A读取一次，此时事务B插入一条数据事务A再次查询，记录多了。

不可重复读和幻读区别：不可重复读在于记录的值，幻读在于记录的数量。


程序出现的bug
@Transactional修饰多线程，事务失效，导致数据不一致

多线程访问线程不安全的类
两个double变量相减，结果小数位数好多，不是数学上正确的结果

大学，计算机，代码实验思路

数据库，有代码，缺少了一部分，然你填进去

知识点：理论知识点梳理
使用的三方库，熟悉
构建工具，熟悉
编程语言基本语法，熟悉




写代码遇到的问题，排错思路

屹通 微前端项目，走网关
api不匹配，遇到这个问题排错思路

资料要重复看，不要捡了芝麻丢了西瓜
it资料  javaguild
mysql
脏读，幻读，不可重复读，概念没有记住啊

mysql 锁
锁的粒度
整个数据库，表，某一行（元祖


RR 级别幻读是，先查询，然后自己更改，再查询，这个就产生幻读了
而且 一般在事务里面查询，也不会加forupdate
就是单纯的mvcc

mysql，不建议用字符来表示时间
屹通大面积用字符来表示时间
