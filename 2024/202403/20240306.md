# 20240306


轻松玩转 MySQL 8.0 源码编译
https://www.modb.pro/db/50258
注意boost这个库

idea可以打开多个文件所在的文件夹
mybatis log free idea插件，打印mysql执行的sql
反思，在中行，一天的工作没成果，被劝退

sudo apt install autoconf-archive -y
sudo apt install linux-libc-dev -y

cmake依赖
find_package()
target_link()
是在xxxVersion.cmake中定义

maven
a
group
version

都是在pom.xml中定义的


https://www.gnu.org/savannah-checkouts/gnu/autoconf/manual/autoconf-2.72/index.html

autoconf (GNU Autoconf) 2.69
possibly undefined macro: m4_equote如何解决


`m4_equote` 是 Autoconf 宏文件中定义的宏之一。它用于在自动配置过程中对字符串进行转义，以便在生成的脚本中正确处理特殊字符。

在 Autoconf 中，宏文件（通常以 `.m4` 扩展名结尾）包含一系列宏定义，用于生成配置脚本。这些宏可以在 `configure.ac` 文件中使用，以定义变量、检测系统特性和生成配置文件等。

`m4_equote` 宏的作用是对字符串进行转义，以便在生成的脚本中使用这些字符串时，特殊字符能够正确解释。特殊字符如引号、反斜杠等可能会干扰生成的脚本的语法，因此在使用这些字符串之前，需要进行转义。

例如，假设在 `configure.ac` 文件中有以下语句：

```autoconf
AC_DEFINE([MY_STRING], [This is a "quoted" string], [Description])
```

在进行自动配置时，`m4_equote` 宏将会对字符串 `[This is a "quoted" string]` 进行转义，确保生成的配置脚本中的字符串是正确的。转义后的字符串可能如下所示：

```autoconf
AC_DEFINE([MY_STRING], [This is a \"quoted\" string], [Description])
```

这样，在生成的配置脚本中使用 `MY_STRING` 时，引号字符被正确解释。

总之，`m4_equote` 宏在 Autoconf 的宏文件中用于对字符串进行转义，以确保生成的配置脚本正确处理特殊字符。


sudo ln -s /usr/bin/autoconf2.64 /usr/bin/autoconf
sudo ln -s /usr/bin/autoheader2.64 /usr/bin/autoheader
sudo ln -s /usr/bin/autom4te2.64 /usr/bin/autom4te
sudo ln -s /usr/bin/autoreconf2.64 /usr/bin/autoreconf
sudo ln -s /usr/bin/autoscan2.64 /usr/bin/autoscan
sudo ln -s /usr/bin/autoupdate2.64 /usr/bin/autoupdate

编译odb
./configure 报错，
configure.ac:12: error: possibly undefined macro: m4_equote
      If this token and others are legitimate, please use m4_pattern_allow.
      See the Autoconf documentation.
configure.ac:12: error: possibly undefined macro: m4_ifdef
autoconf版本 2.69，我找了半天文档，没找到m4_equote这个宏，哪位大佬指导下？
