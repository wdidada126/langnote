# 20240302
周六休息

笑死了，翻墙被这样说
Developers from China cares more about their privacy protections. They never use their real IP address.We noticed that developers from China have a better network foundation because we don't have enough IPs to use personally.

我简单考考你，
1.设计一个tps为2000的购物网站，你要从哪些角度考虑。
2.建设一个类似阿里云的云服务平台，怎么设计调度系统？
3.设计一个高频的股票量化交易系统，你又会采用什么架构？ 以上三题能回答出来一个，你都绝不会找不到工作

下面三个问题都是经典的超大规模系统设计题目，我会结合 2025 年最新的业界实践（阿里、字节、腾讯、阿里云、蚂蚁、Jane Street、Two Sigma、Google Cloud 等）给你最硬核、最接近真实落地的设计思路。

### 1. 设计一个TPS 2000+ 的购物网站（双11级别准备）

目标不是 2000 TPS，而是“峰值 100w+ TPS 还能稳”（2000 TPS 只是热身）。

核心考虑角度（按优先级排序）：

| 维度             | 关键设计点（2025 版）                                                                                   | 代表实践                          |
|------------------|---------------------------------------------------------------------------------------------------------|-----------------------------------|
| 流量削峰         | 全链路限流（网关、Nginx、应用）、令牌桶+漏桶、降级熔断、排队页（大促等待室）、异步下单架构            | 淘宝“红细胞”排队系统、字节令牌服务 |
| 读路径极致优化   | 全站静态化 + CDN + Edge Node（Vercel/Cloudflare Workers）、BigTable/ TiDB + Redis 多级缓存、预热      | 淘宝 99.99% 请求走 CDN            |
| 写路径异步化     | 下单 → 异步写本地 Binlog → 异步落库（RocketMQ/Kafka/Pulsar），最终一致性，库存异步扣减（本地+Redis 预扣） | 阿里“异步确保成功”架构            |
| 库存扣减         | 行锁 → Redis 预扣 → 本地内存限流兜底（单机 10w/s）→ 异步回补，超卖了就补偿/补货                       | 蚂蚁“本地限流+异步扣减”           |
| 数据库抗压       | MySQL → TiDB/PolarDB-X（分布式），读写分离 + 分库分表（订单号雪花 ID），热点订单走独立集群            | 淘宝订单库 10w+ 分片              |
| 缓存架构         | Redis Cluster + 多级缓存（L1本地缓存 Heap/Offheap + L2 Redis + L3 TiKV），缓存穿透用布隆+空值缓存      | 字节抖音 90% 读走本地缓存         |
| 秒杀/大促专属    | 独立秒杀系统（Go + Lua + Redis Cell 限流），动态扩容 TKE/Serverless，压测要到真实 10 倍流量            | 拼多多百亿补贴秒杀系统            |
| 链路追踪/降级    | OpenTelemetry + Hystrix/Sentinel/Resilience4j 全链路熔断，关键链路金丝雀 + 影子表                     | 阿里鹰眼、腾讯河图                |
| 容器&调度        | K8s + HPA/VPA + 单元化（多活），机房级故障自动熔断                                                      | 阿里 Sigma + ACK 多活             |

真实双11峰值：2024 年阿里已经做到 78 万 TPS（官方公布），2025 年预计破 100 万，核心就是“读静态化 + 写异步化 + 库存本地化”。

### 2. 建设一个类似阿里云的云服务平台，调度系统怎么设计？
阿里云飞天调度叫 Fuxi → 伏羲 → 天鹰（2025 版），字节叫 Volcano + SuperBatch，Google 是 Borg → Omega → Kubernetes。
2025 年最先进的调度系统架构（你要做阿里云级别，必须这么干）：

| 层级            | 系统名称（自研）      | 核心功能和技术亮点                                                                                  | 代表开源/业界实现            |
|-----------------|-----------------------|-----------------------------------------------------------------------------------------------------|------------------------------|
| 全局资源管理    | Galaxy（资源大脑）    | 万级机房、百万节点资源抽象，超卖比 300%-500%，异构资源（GPU/CPU/存储）统一建模                     | Kubernetes + Koordinator     |
| 任务抽象        | Job = DAG + Gang Scheduling + Elastic | 支持 AI 训练（万卡 AllReduce）、Serverless、Batch、Streaming 统一调度                              | Volcano + Kubernetes Batch   |
| 调度器          | Pangu Scheduler（两级调度） | 1. 全局调度（每 10s 一次，优化放置 + 装箱 + 碎片整理）<br>2. 局部调度（Binpack + Spread + Affinity） | Kubernetes 默认 + Karmada    |
| 弹性伸缩        | AutoScale + Predict  | 基于 Prophet + LSTM 预测 1 小时后负载，预扩容，GPU 训练支持弹性卡数（像 OpenAI Triton）             | Karpenter + Ray              |
| 干扰检测&迁徙   | ChaosBlade + Live Migration | CPU/内存/IO/网络干扰打分，自动冷迁移，GPU 训练支持 Checkpoint + vGPU 迁移                         | Alibaba ECI + Google GKE     |
| 多租户隔离      | 单元化 + Namespace + Quota + FairShare | 硬隔离（机房单元）、软隔离（FairShare 调度算法），大客户独占集群                                    | 阿里云 ACK One 多活          |
| 万卡训练专用    | Jigsaw（集体调度）    | 万卡 GPU 任务整体调度（Gang + Topology Aware），故障自动补卡，支持 3D-Torus 网络优化                | 阿里云天鹰 + 字节火山引擎    |
| Serverless 冷启动| Knix（预热池 + Sandbox） | FaaS 保持 10w 沙箱预热，冷启动 <50ms，支持 GPU Serverless                                          | AWS Lambda + Azure Container Apps |

真实案例：阿里云 2025 年已经支持单集群 10 万卡 GPU 训练（光子超算集群），调度延迟 <3s，资源利用率 68%+（业界最高）。

### 3. 设计一个高频股票量化交易系统（延迟 < 100μs 级别）

这已经不是普通互联网架构了，是真正的“军备竞赛”领域，玩家只有 Jane Street、Citadel、Two Sigma、Jump Trading、盈透等。

2025 年最硬核的高频量化架构（毫秒已经出局，微秒才是王道）：

| 模块             | 技术选型（2025 版）                                      | 延迟级别         | 关键点                                           |
|------------------|---------------------------------------------------------|------------------|--------------------------------------------------|
| 行情接收         | 交易所直连光纤 + FPGA 解析（上海 300、深圳 800）         | 1-5 μs           | 所有行情必须 FPGA 预处理，丢包率 < 0.0001%       |
| 订单网关（OMS）  | 纯 C++17/23 + lock-free queue + RDMA                    | < 10 μs          | 跳过内核（Kernel Bypass），DPDK/Solarflare Onload |
| 风控&策略引擎    | FPGA（策略硬化） + CPU（复杂策略）                      | FPGA < 500ns     | 简单策略全放 FPGA（做市、套利），复杂策略 PIN CPU 核心 |
| 核心数据结构     | 平铺内存（FlatBuffer/Cap'n Proto）+ 自定义内存池         | 零拷贝           | 禁止 new/delete，全部预分配                      |
| 通信             | RDMA + Infiniband/200Gbps RoCE v2                       | < 1μs            | 跨机器共享内存一样快                             |
| 存储&回测        | ClickHouse（实时） + KDB+（历史 TiCK 数据）             | 毫秒级查询       | KDB+ 仍是王者，单机 10 亿条数据查询 < 10ms       |
| 部署方式         | 交易所同机柜托管（Colocation），光纤距离 < 50 米         | 物理极限         | 上海金桥、深圳龙华机柜租金百万级/年               |
| 容错             | 三套独立系统热备 + 自动切换（Active-Active-Active）     | 切换 < 50ms      | 一旦主系统延迟异常，自动切换                     |
| 编程范式         | C++23 + Rust（新模块） + Verilog（FPGA）                | 极致性能         | 所有热点路径禁止虚函数、STL                     |

真实延迟数据（2025 年国内一线量化私募）：
- 行情收到 → 策略决策 → 订单发出：平均 35μs（顶级团队 < 15μs）
- 订单到交易所：光纤 + 网关 < 80μs（上海 300 最快）

总结一句话：
- 购物网站：流量越大越要“读静写动 + 异步 + 削峰”
- 云平台调度：两级调度 + 预测弹性 + 干扰迁徙 + 万卡集体调度
- 高频量化：能用 FPGA 绝不用 CPU，能直连绝不走公网，能省 1μs 就值 1000 万

这三个系统代表了互联网、民用云计算、金融极致性能的三个顶点，做一个顶一个烧钱烧人烧头发，但也顶一个吃十年。

cmake 组织的c++的，关联vcpkg，自动下载依赖
“两地三中心”是中国人民银行在2002年提出、2004年开始逐步实施的有关央行系统数据中心布局的战略部署，也被称为“一主两备”或“一个同城加两个异地”。它是指在北京建立两个数据中心，同时在天津和上海各建立一个数据中心。
在这个方案中，北京的两个数据中心采用双活模式，天津和上海的数据中心则采用异步方式（近同步）复制北京的数据，但不对用户开放。这样做的目的是保证当任何一个地方发生灾难时，其他的地方还可以对外提供服务。
这个方案的实施得到了花旗银行的支持。花旗银行的现金管理平台采用了Oracle真正应用集群（RAC）来整合位于纽约和伦敦的两个数据中心，并使用Oracle Data Guard作为第三点故障切换。这种多点集群技术可以提供更高的可用性，使系统在遇到故障时能够自动进行切换，从而最大限度地减少了系统的停机时间。

“两地三中心”的开源方案主要涉及确保高可用性和灾难恢复的策略。在开源领域，可以利用多个软件和工具组合来实现这一目标。以下是构建“两地三中心”的开源解决方案的一些组件和技术：
1. 分布式文件系统：
   - GlusterFS 或 Ceph：这些分布式文件系统支持数据复制和分布到多个物理位置，提供容错和灾难恢复功能。
2. 数据库复制：
   - MySQL/MariaDB：使用主从复制或组复制（Group Replication）功能来同步数据到不同数据中心。
   - PostgreSQL：通过流复制（Streaming Replication）或逻辑复制实现多地点数据同步。
   - MongoDB：提供副本集（Replica Sets）和分片（Sharding）来支持跨多个数据中心的数据复制。
3. 负载均衡和故障转移：
   - HAProxy 或 NGINX：这些负载均衡器可以在多个数据中心之间分配流量，并在一个数据中心发生故障时将流量重定向到其他中心。
   - Keepalived 或 VRRP：用于实现高可用性，通过虚拟路由冗余协议保持一个虚拟IP地址，当主服务发生故障时，可以自动切换到备份服务。
4. 消息队列系统：
   - Apache Kafka：集群可以分布在多个数据中心，Kafka的副本机制可以保证消息的持久性和高可用性。
5. 配置管理和自动化部署：
   - Ansible、Puppet、Chef 或 SaltStack：这些工具可以帮助自动化配置和管理跨多个数据中心的服务。
6. 监控和告警：
   - Prometheus 结合 Grafana 或 Zabbix：监控系统状态，一旦检测到问题，可以触发告警并采取相应措施。
7. 全局负载均衡器：
   - DNS解决方案（如Anycast DNS）：将用户请求导向最近的数据中心。
   - 硬件负载均衡器（如F5 BIG-IP）：配置在各个数据中心前端，配合DNS实现全球服务器负载均衡（GSLB）。
实施“两地三中心”的开源方案通常需要对以上技术进行综合运用，并根据实际业务需求和预算限制进行定制。重要的是要定期测试灾难恢复计划，确保在实际发生灾难时，系统能够按预期进行切换和恢复。

Anycast DNS

bind9
quagga