# 202403_week1

## 时间范围

- 202403 月第 1 周

## 本周记录

- 20240301：国企诚邀音视频领域专家加入；职位描述；1、基于WebRTC开源框架，进行音视频引擎的开发，包括采集渲染优化、音视频处理、编解码、QoS等。
- 20240302：周六休息；笑死了，翻墙被这样说；Developers from China cares more about their privacy protections. They never use their real IP address.We noticed that developers from China have a better network foundation because we don't have enough IPs to use personally.
- 20240303：周日；qt 6.4.3；需要cpp17
- 20240304：解析urls.js文件，正则匹配，正则不熟悉啊；conda；conan考虑了c++的编译选项
- 20240305：conan2.0.5以上，下载；conan_provider.cmake；DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="conan_provider.cmake" -DCONAN_COMMAND="D:\dev_tools\Conan\conan\conan.exe"
- 20240306：cmake；target link；在CMake中，xxx::xxx 这种命名格式通常是由库的项目通过CMake配置脚本（例如 xxxConfig.cmake 或通过 install(EXPORT ...) 生成的 xxxTargets.cmake）定义的导入目标（imported target）。这个命名不是直接写在 CmakeLibraryInstConfig.cmake.in 模板文件中的，而是由库的构建系统生成的。
- 20240307：自己搜索信息；知网；论文检索
