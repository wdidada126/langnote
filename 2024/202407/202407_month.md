# 202407_month

## 原有内容


mysql server 多线程阻塞同步，实现事物，要么同时成功，要么同时失败
分布式事务
xid，牺牲性能
tcc 手动处理，存储临时数据 commit rollback
Java wait notify

在Linux中，C和C++提供了一些类似Java中`wait()`和`notify()`的线程同步机制，主要是基于线程库`pthread`。

### 在C中使用`pthread`库
1. 条件变量 (`pthread_cond_t`):
   - `pthread_cond_wait(pthread_cond_t *cond, pthread_mutex_t *mutex)`: 等待条件变量，会释放互斥锁，并使线程进入阻塞状态，直到条件变量被唤醒。
   - `pthread_cond_signal(pthread_cond_t *cond)`: 唤醒等待在条件变量上的一个线程。
   - `pthread_cond_broadcast(pthread_cond_t *cond)`: 唤醒等待在条件变量上的所有线程。
2. 互斥锁 (`pthread_mutex_t`):
   - `pthread_mutex_lock(pthread_mutex_t *mutex)`: 锁定互斥锁。
   - `pthread_mutex_unlock(pthread_mutex_t *mutex)`: 解锁互斥锁。
### 在C++中使用`std::mutex`和`std::condition_variable`
1. 条件变量 (`std::condition_variable`):
   - `std::condition_variable::wait(std::unique_lock<std::mutex>& lock)`: 等待条件变量，会释放互斥锁并阻塞线程。
   - `std::condition_variable::notify_one()`: 唤醒一个等待线程。
   - `std::condition_variable::notify_all()`: 唤醒所有等待线程。
2. 互斥锁 (`std::mutex`):
   - `std::mutex`: C++标准库提供的互斥锁。
这些函数和类可以用于实现线程之间的同步和通信，类似于Java中的`wait()`和`notify()`。您可以根据您的需求选择适合的线程同步机制。

在Windows平台上，C和C++提供了一些用于线程同步的API，类似于Java中的`wait()`和`notify()`。主要的概念是使用事件对象或互斥对象来实现线程之间的同步。
### 在Windows平台上使用WinAPI
1. 事件对象：
   - `CreateEvent`: 创建一个事件对象。
   - `SetEvent`: 将事件对象设置为有信号状态，唤醒等待该事件的线程。
   - `ResetEvent`: 将事件对象设置为无信号状态。
   - `WaitForSingleObject`: 等待事件对象变为有信号状态。
   - `WaitForMultipleObjects`: 等待多个事件对象中的任何一个变为有信号状态。
2. 互斥对象：
   - `CreateMutex`: 创建一个互斥对象。
   - `WaitForSingleObject`: 等待互斥对象的所有权。
   - `ReleaseMutex`: 释放互斥对象的所有权。
### 在C++中使用`std::mutex`和`std::condition_variable`
如果您在Windows上使用C++，也可以使用C++标准库提供的线程同步工具：
1. 条件变量 (`std::condition_variable`):
   - `std::condition_variable::wait(std::unique_lock<std::mutex>& lock)`: 等待条件变量。
   - `std::condition_variable::notify_one()`: 唤醒一个等待线程。
   - `std::condition_variable::notify_all()`: 唤醒所有等待线程。

2. 互斥锁 (`std::mutex`):
   - `std::mutex`: C++标准库提供的互斥锁。
这些工具可以帮助您实现在Windows平台上类似于Java中`wait()`和`notify()`的线程同步机制。根据您的需求和环境，可以选择适合的线程同步工具。

c语言中操作内容的
malllocl free
java new gc回收内存
c++ new delete
操作网络

jdk jvm的功能
gc
家在.class文件
jni
javah

java Lockapi 使用native封装了
pthread_cond_signal

linux
win
mac操作系统提供了哪些随着操作系统安装就提供的库
linux pthread m
mac kqueue 图形api
win iocmp 图形api

pthread 是Linux（以及大多数类Unix系统）上实现多线程编程的标准库。
kqueue 是macOS（及某些类Unix系统）上用于高效处理IO事件的机制。
IOCP（I/O Completion Ports）是Windows上用于高效处理并发I/O请求的机制。
图形API在不同操作系统上有所不同，但都有各自的高效解决方案，如Windows的Direct3D、Linux的OpenGL/Vulkan，以及macOS的Metal。

qt封装了图形界面api
轮子哥 office 封装图形api

c代码，struct MHD_Daemon *daemon; struct MHD_Daemon daemon; 有*没有*有什么区别
在C语言中，struct 关键字用于定义结构体类型，而结构体变量（或称为实例）是这种类型的一个或多个实例。在您给出的例子中，struct MHD_Daemon *daemon; 和 struct MHD_Daemon daemon; 之间的主要区别在于它们如何声明和使用结构体变量。
struct MHD_Daemon *daemon;
这行代码声明了一个指向 struct MHD_Daemon 类型的指针 daemon。这意味着 daemon 本身不存储 struct MHD_Daemon 类型的数据，而是存储了一个地址，这个地址指向了某个 struct MHD_Daemon 类型的实例（或者它可以是 NULL，表示不指向任何实例）。
要使用这个指针访问 struct MHD_Daemon 的成员，你需要先让这个指针指向一个有效的结构体实例。这通常通过分配内存给这个结构体实例，并将返回的地址赋值给这个指针来完成，比如使用 malloc 函数：
```c
daemon = malloc(sizeof(struct MHD_Daemon));  
if (daemon != NULL) {  
    // 现在可以使用 daemon 指针来访问和修改结构体成员  
}
```
注意，使用完 malloc 分配的内存后，你应该使用 free 函数来释放它，以避免内存泄漏。

struct MHD_Daemon daemon;
这行代码声明了一个 struct MHD_Daemon 类型的变量 daemon。这意味着 daemon 直接存储了 struct MHD_Daemon 类型的所有数据。你可以直接通过 daemon 变量来访问和修改它的成员，而不需要通过指针解引用。
这种方式更加直接和简单，但它有一个限制：daemon 变量只在它被声明的作用域内有效，并且它的大小是固定的，由 struct MHD_Daemon 的定义决定。
总结：
使用指针时，你可以动态地分配和释放内存，这在你不知道需要多少 struct MHD_Daemon 实例或者需要在多个函数间共享同一个实例时非常有用。
直接声明结构体变量时，内存管理更加简单直接，但可能受限于作用域和固定大小的限制。
在选择哪种方式时，你应该根据你的具体需求来决定。
在C++中，如果你想根据目标操作系统是否为Mac来条件性地包含某个头文件，你不能直接在C++代码中直接检测操作系统类型，因为C++标准本身并不提供这样的功能。但是，你可以利用预处理器指令来根据编译时定义的宏来判断。
对于Mac系统，Xcode和其他基于Clang的编译器通常会定义一些特定的宏，比如__APPLE__和__MACH__。因此，你可以使用这些宏来条件性地包含头文件。
下面是一个示例，展示了如何只在Mac系统上包含某个头文件：
```cpp
#if defined(__APPLE__) && defined(__MACH__)  
#include "SomeMacSpecificHeader.h" // 只在Mac上包含这个头文件  
#endif  
```
// 其他的包含和代码...
在这个例子中，__APPLE__宏表示代码正在Apple平台上编译，而__MACH__宏进一步确认这是Mac OS X或iOS等基于Mach内核的Apple系统。通过结合这两个宏，你可以确保只有在Mac系统上才会包含SomeMacSpecificHeader.h。
请注意，这种方法依赖于编译器在编译时定义的宏，这些宏可能会因编译器、编译选项或目标平台的微小变化而有所不同。然而，对于大多数基于Clang的Mac编译器来说，__APPLE__和__MACH__宏是可靠的。
另外，如果你正在使用CMake等构建系统，你也可以在CMakeLists.txt文件中根据目标操作系统来条件性地设置编译选项或包含目录，而不是在源代码中硬编码这些检查。这种方法更加灵活和强大，因为它允许你在不修改源代码的情况下针对不同的平台和编译器配置进行构建。

## 整理补充

## 本月概览

- 本月共整理 31 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 周度脉络

- 202407_week1：- 20240701：git clone https://github.com/apache/zookeeper.git；cd zookeeper/src/c；protoc --cpp_out=. path/to/your_proto_file.proto
- 202407_week2：- 20240708：select sum(money) from  ENT_COMMISSION_STRANS_TD where COMMISSION_ID = "XZDF202210140021";；sum单列可以；`SUM`函数可以在没有`GROUP BY`的情况下使用，但它会对整个结果集进行聚合。这意味着如果你不使用`GROUP BY`，`SUM`函数会计算所有行中指定列的总和，而不会分组。
- 202407_week3：- 20240715：void swap(Statement& other) noexcept;；Article getOne(int id) override;；Service Mesh 解决了以下几个微服务架构中常见的问题：
- 202407_week4：- 20240722：kaldi.md；以下是一些可能涉及幂等性相关内容的书籍：；《幂等分析（英文）/国外优秀数学著作原版系列》：作者为（苏）V.P.马斯洛夫、S.N.森博思奇，由哈尔滨工业大学出版社于 2019 年 1 月出版。这本书是数学领域的著作，可能从数学角度深入探讨幂等分析的相关理论和概念。

## 本月高频主题

- git clone https://github.com/apache/zookeeper.git
- cd zookeeper/src/c
- protoc --cpp_out=. path/to/your_proto_file.proto
- std::move 是 C++11 版本中引入的一个重要新特性，它属于 C++11 引入的移动语义（Move Semantics）的一部分。移动语义旨在提高大型对象（特别是那些涉及资源管理的对象）的复制效率，通过允许资源从一个对象“移动”到另一个对象，而不是进行昂贵的复制操作来实现。这一机制主要通过右值引用（right-value reference）和移动构造函数（move constructor）以及移动赋值操作符（move assignment operator）来实现。
- 以下是关于 std::move 和 C++11 版本的详细归纳：
- 版本归属：
- /c++后端开发技术栈：https://zhuanlan.zhihu.com/p/697190930
- c++基础知识
- 今天给大家介绍的是一款国内的国产编程神器，远超微软GitHub Copilot。关键它还是完全免费。
- 在C++11及其之后的版本中，关键字 `default` 和 `delete` 可以用于方法的声明，以显式地定义特殊成员函数（如构造函数、析构函数、拷贝构造函数、拷贝赋值运算符、移动构造函数和移动赋值运算符）的行为。它们提供了对类的默认行为的控制，以满足不同的设计需求。
- 关键字 `default` 用于告诉编译器为类生成一个默认的实现。例如，如果你不需要自定义的构造函数、析构函数或拷贝/移动操作，可以使用 `default` 来让编译器自动生成这些方法的默认实现。
- 关键字 `delete` 用于禁止特定的函数实现。这在禁止类的某些拷贝或移动操作时非常有用。例如，如果你希望一个类是不可拷贝的，可以将拷贝构造函数和拷贝赋值运算符声明为 `delete`。
