# 202407_week2

## 时间范围

- 202407 月第 2 周

## 本周记录

- 20240708：select sum(money) from  ENT_COMMISSION_STRANS_TD where COMMISSION_ID = "XZDF202210140021";；sum单列可以；`SUM`函数可以在没有`GROUP BY`的情况下使用，但它会对整个结果集进行聚合。这意味着如果你不使用`GROUP BY`，`SUM`函数会计算所有行中指定列的总和，而不会分组。
- 20240709：再问一个cmake include的问题，./include/a.h ./include/b.h ./example/main.cc 还有其他代码文件，是新建./CMakeLists.txt还是 ./example/CMakeLists.txt；正儿八经的项目，两个cmake都要做，root下面那个存共用的变量，example下面的才是一个编译目标；要查看Linux内核的头文件信息，你可以通过以下几种方法进行查找和查看：
- 20240710：问个c++ include问题的，假设有/usr/local/include /usr/local/include/include/muduo/net.h ,我现在要引用net.h，是；/usr/local/include/；include添加到头文件路径，然后
- 20240711：知乎网友：搞不懂生命周期，就别写c++；程序启动就存在，程序退出就销毁；您引用的这句话非常精辟，它道破了理解C++生命周期的核心——对象的生存期由其创建和销毁的时机决定。这位知乎网友用最极端的例子（程序启动和退出）来强调这个概念的重要性。
- 20240712：sql.md；leveldb.md；自动化测试工具 JetBrains Aqua IDE 正式发布
- 20240713：The CMake policy CMP0091 must be NEW, but is ''；cmake如何解决 win clion；这个问题是由于CMake版本的兼容性问题导致的。
- 20240714：conan github，codespace忘记设置conan profile了；在 C++ 项目中，将代码分解到多个文件中通常是最佳实践，因为它有助于组织代码、提高可维护性和可读性。然而，有时候将所有代码放在一个文件中也有其合理性和实际需求。以下是一些将代码集中在一个文件中的常见原因，以及其优缺点。；项目规模小：对于小型项目或简单的示例程序，将所有代码放在一个文件中可能更加方便，因为它减少了文件管理的复杂性。
