# 202407_week1

## 原有内容

上个周末，买c++教程
https://marketplace.visualstudio.com/items?itemName=1ndrew100.forceUTF8NoBOM

CodeGeeX是一款基于大模型的智能编程助手，它可以实现代码的生成与补全、自动为代码添加注释、自动解释代码、自动编写单元测试、实现代码审查Code Review、自动修复代码fixbug、自动生成commit message完成git提交，以及在不同编程语言的代码间实现互译、针对技术和代码问题的智能问答等丰富的功能。

帮助开发者显著提高工作效率，CodeGeeX支持100+种编程语言，适配多种主流IDE平台，包括Visual Studio Code，JetBrains IDEs，Visual Studio，HBuilderX，DeepIn-IDE等。
https://www.cnblogs.com/1312mn/p/18211377

C:\Users\edida\AppData\Local\Programs\CLion\bin\cmake\win\x64\bin\cmake.exe -DCMAKE_BUILD_TYPE=Debug -DCMAKE_MAKE_PROGRAM=C:/Users/edida/AppData/Local/Programs/CLion/bin/ninja/win/x64/ninja.exe -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES=conan_provider.cmake -DCONAN_COMMAND=D:\dev_tools\Conan\conan\conan.exe -G Ninja -S D:\git\github\rest_poco -B D:\git\github\rest_poco\cmake-build-debug-visual-studio-64
-- CMake-Conan: first find_package() found. Installing dependencies with Conan
-- CMake-Conan: Checking if a default profile exists
C:\Users\edida\.conan2\profiles\default

CMakeLists.txt没有
find_package()函数，会被报错的

## 整理补充

## 时间范围

- 202407 月第 1 周

## 本周记录

- 20240701：git clone https://github.com/apache/zookeeper.git；cd zookeeper/src/c；protoc --cpp_out=. path/to/your_proto_file.proto
- 20240702：std::move 是 C++11 版本中引入的一个重要新特性，它属于 C++11 引入的移动语义（Move Semantics）的一部分。移动语义旨在提高大型对象（特别是那些涉及资源管理的对象）的复制效率，通过允许资源从一个对象“移动”到另一个对象，而不是进行昂贵的复制操作来实现。这一机制主要通过右值引用（right-value reference）和移动构造函数（move constructor）以及移动赋值操作符（move assignment operator）来实现。；以下是关于 std::move 和 C++11 版本的详细归纳：；版本归属：
- 20240703：/c++后端开发技术栈：https://zhuanlan.zhihu.com/p/697190930；c++基础知识；今天给大家介绍的是一款国内的国产编程神器，远超微软GitHub Copilot。关键它还是完全免费。
- 20240704：在C++11及其之后的版本中，关键字 `default` 和 `delete` 可以用于方法的声明，以显式地定义特殊成员函数（如构造函数、析构函数、拷贝构造函数、拷贝赋值运算符、移动构造函数和移动赋值运算符）的行为。它们提供了对类的默认行为的控制，以满足不同的设计需求。；关键字 `default` 用于告诉编译器为类生成一个默认的实现。例如，如果你不需要自定义的构造函数、析构函数或拷贝/移动操作，可以使用 `default` 来让编译器自动生成这些方法的默认实现。；关键字 `delete` 用于禁止特定的函数实现。这在禁止类的某些拷贝或移动操作时非常有用。例如，如果你希望一个类是不可拷贝的，可以将拷贝构造函数和拷贝赋值运算符声明为 `delete`。
- 20240705：kotlin的协程和空指针检查太爽了，用了就回不去了；starrocks.md
- 20240706：svg图片不会模糊；做得烂也比什么都没做好；github地址:https://github.com/Meta11ic0/ghjson
- 20240707：IO多路复用的原理；感谢up主大大的讲解，非常精彩。我将视频整理成了完整的笔记。基本囊括了所有知识点，并且含有配图。希望能够帮助到大家。；链接  https://www.processon.com/view/link/5f36856b5653bb06f2ce529f
