# 20240720
在 CMake 中，include_directories() 命令用于指定编译器在搜索头文件（.h, .hpp 等）时应该包含的目录列表。这对于确保你的项目能够找到它所依赖的库的头文件非常重要。然而，需要注意的是，include_directories() 并不是用来包含 CMake 脚本或模块的，它专门用于处理 C/C++ 源代码编译时的头文件包含路径。

这里有一个 include_directories() 的基本用法示例：

cmake
# 假设你的项目依赖一个位于 /usr/local/include/mylib 的库  
include_directories(/usr/local/include/mylib)  
  
# 或者，如果你使用的是相对路径（相对于当前 CMakeLists.txt 的位置）  
# 假设 mylib 的头文件位于你的项目的一个子目录中  
include_directories(path/to/mylib)  
  
# 现在，在你的源代码中，你可以直接使用 #include <mylib/header.h>  
# 而不需要指定完整的路径
然而，随着 CMake 的发展，推荐使用 target_include_directories() 命令来代替 include_directories()，特别是在处理更复杂的项目时。target_include_directories() 允许你为特定的目标（如可执行文件或库）指定头文件搜索路径，这样可以更精细地控制哪些目标包含哪些头文件目录。

以下是 target_include_directories() 的一个示例用法：

cmake
# 创建一个可执行文件目标  
add_executable(my_app main.cpp)  
  
# 为 my_app 目标指定头文件搜索路径  
target_include_directories(my_app PRIVATE /usr/local/include/mylib)  
  
# 或者，如果你使用的是相对路径  
target_include_directories(my_app PRIVATE path/to/mylib)  
  
# 现在，只有在编译 my_app 时，CMake 才会将这些目录添加到头文件搜索路径中
在这个例子中，PRIVATE 关键字指定了这些包含目录仅对 my_app 目标可见，这有助于减少不同目标之间的依赖和潜在的编译问题。CMake 还支持 PUBLIC 和 INTERFACE 关键字，它们提供了更细粒度的控制，允许你指定哪些目录应该被链接到该目标的库所共享，或者仅作为接口传递给使用该目标的其他目标。


Blazing fast terminal file manager written in Rust, based on async I/O.
https://github.com/sxyazi/yazi

Yazi：一个百万并发的C++后台服务框架、网络编程、后端开发、后台开发
https://hkrb7870j3.feishu.cn/docx/UPgTdnZjFoNCDXxuTxsc2HajnzI

https://github.com/yespon/yazi
c++后端
-std=c++11
make组织的

https://github.com/edidada/testlibjpeg

# README

## mac

`brew install libjpeg`

```cpp
#include <jpeglib.h>
#include <stdio.h>
```

改成
```cpp
#include <stdio.h>
#include <jpeglib.h>
```

好的,非常感谢你提供了更多代码细节。我已经仔细分析了你的代码,并发现了一个需要调整的地方。

之前你提到的头文件包含顺序问题是正确的。在 C/C++ 中,头文件的包含顺序是很重要的,因为头文件之间可能存在相互依赖关系。

在你的代码中,应该先包含 `<stdio.h>` 头文件,然后再包含 `<jpeglib.h>` 头文件。这样可以确保 `FILE` 类型在使用 `jpeglib.h` 中的函数时已经被正确定义。

所以你的代码应该修改为:

```cpp
#include <stdio.h>
#include <jpeglib.h>
```

这样就可以解决之前提到的 `'FILE' has not been declared` 的错误问题了。

除此之外,请确保你已经正确地配置了 libjpeg 库的包含和链接路径,如我之前在 CMake 示例中提到的那样。如果问题仍然存在,请继续提供更多信息,我会继续帮助你排查。


include(CheckTypeSize): 用于检查特定类型在当前系统上的大小。常用于确定整数、浮点数等基本类型的大小。
include(CheckFileOffsetBits): 用于检查文件偏移量类型的位数,例如 off_t 类型。
include(Macros): 包含一些常用的 CMake 宏,比如 include_directories() 和 link_directories()。
include(CheckVariableExists): 用于检查某个变量是否存在。
include(CheckSymbolExists): 用于检查某个符号(函数、变量等)是否存在。
include(CheckStructHasMember): 用于检查某个结构体是否包含指定的成员。
include(CheckCSourceCompiles): 用于编译并运行一小段 C 代码,检查是否能成功编译和运行。
include(CheckPrototypeDefinition): 用于检查某个函数的原型定义是否正确。
include(CheckFunctionKeywords): 用于检查某些关键字(如 inline、restrict 等)是否被编译器支持。
include(CheckConstExists): 用于检查某个常量是否存在。
include(AddCompilerFlags): 用于添加编译器标志。
include(VersionViaGit): 用于从 Git 仓库中提取项目版本信息。

find_package(Git)

libevent源代码，有cmake文件夹，各种.cmake文件

gcc编译器扩展

dubbo没有类似ares框架的.flw文件

郑州银行也用nas
fastdfs
mongo

Android ndk开发，是复制一份c/c++的源代码，

c/c++的库，有编译好的文件供下载。
spring官网有编译好的jar包下载，依赖关系是没人管的
mybatis官网也有jar包下载
opencv有编译好的.h文件，库文件下载

maven工具，pom.xml可以说吗依赖关系，依赖是固定版本的
conan管理c/c++依赖，.py conandata.yml也说明了依赖
xmake .lua文件，也定义了依赖

Java Web后端代码，因为是部署在服务器上的，服务器版本，JDK版本都是开发人员选择的，事先知道的，所以不存在判断程序环境的代码
libevent需要兼容不同os，不同编译器，是否存在某个Linux的系统调用，不同api版本

