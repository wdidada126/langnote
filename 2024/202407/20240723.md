# 20240723

  typedef basic_string<char>    string;   
    return string(path);


  typedef basic_ostringstream<char> 	ostringstream;
    ostringstream buf;

zk c库编译

pom.xml可以定义依赖

cmake的CMakeLists.txt如何处理依赖

D:\git\github\rest_poco\.github\workflows


sudo apt update
apt-cache showpkg libboost-all-dev
