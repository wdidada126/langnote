# 202407_week3

## 时间范围

- 202407 月第 3 周

## 本周记录

- 20240715：void swap(Statement& other) noexcept;；Article getOne(int id) override;；Service Mesh 解决了以下几个微服务架构中常见的问题：
- 20240716：enum怎么在class的public部分？；在 C++ 中，枚举类型可以在类的公共部分声明，以便枚举成员可以被类的用户访问。你的代码是正确的，但为了完整性和清晰性，可以展示一个完整的示例，演示如何在类的公共部分声明枚举类型。；HTTPResponse::HTTP_OK？
- 20240717：`inline`关键字和`Data_API`宏在C++中具有不同的用途和作用。以下是它们的详细说明：；`inline`关键字告诉编译器将函数的代码嵌入到每个调用该函数的地方，而不是进行函数调用。这有助于减少函数调用的开销，尤其是对于频繁调用的小函数。编译器可能会忽略`inline`建议，特别是如果函数的代码很复杂或较大。；示例：
- 20240718：以前是京东的？，后来变成博云；做证券外包；大门面试京东了4轮，挂了
- 20240719：zip -r Crow.zip ./Crow；鉴定为不如llama3 我们公司付费就用gpt4，不付费就用llama3的儿子们，怎么会去搞Mistral；比烂是吧？Bazel就是谷歌overdesign的产物。
- 20240720：五种拷贝控制操作：拷贝构造函数、拷贝赋值运算符、移动构造函数、移动赋值运算符、析构函数。举例说明；在C++中，拷贝控制操作是管理对象复制和移动以及资源释放的重要机制。这包括拷贝构造函数、拷贝赋值运算符、移动构造函数、移动赋值运算符和析构函数。下面，我将通过例子来说明这五种操作：；拷贝构造函数（Copy Constructor）
- 20240721：cpp primer；从零开始写内核；小彭老师带您一同自主研发静态反射！本期我们介绍了介绍了traits思想，实现了boost同款宏PP_FOREACH，自动为所有自定义类建立反射，并注册上名字，最后成功结合jsoncpp序列化和反序列化了自定义对象。还介绍了成员指针的特殊语法，实现了成员变量和成员函数的重载，还能用于自动注册pybind11对象，只需添加一行宏即可，快来学习并引入到你的项目中吧！
