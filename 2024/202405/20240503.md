# 20240503

ubuntu 24 docker安装

docker run -i -t --name ubuntu24 ubuntu bash
docker start ubuntu24
docker exec -i -t ubuntu24 bash


ubuntu 24安装gcc
Update System

apt update
apt install build-essential
apt-get install manpages-dev

Install the GCC package dependencies.


To check GCC version

gcc --version
