# 202404_week3

## 时间范围

- 202404 月第 3 周

## 本周记录

- 20240415：c cpp代码用understand；java代码用idea；ares框架，idea不支持啊
- 20240416：操作系统_原理与实现2.md；架构师，画图；三种策略
- 20240417：数据字典；软件工程中的核心概念；大话软件工程
- 20240418：在你的代码中，你使用了 split("|") 方法来按照竖线 | 分割字符串 emp。然而，split 方法使用的分隔符是一个正则表达式，而 | 在正则表达式中是一个特殊字符，表示“或”的意思。因此，直接使用 "|" 作为分隔符实际上并不会按照你期望的那样工作。；为了正确按照竖线 | 分割字符串，你需要对 | 进行转义，这样它就会被当作一个普通的字符来处理。你可以通过在 | 前面加上一个反斜杠 \ 来实现转义，但在Java字符串中，反斜杠本身也是一个特殊字符，所以你需要使用两个反斜杠 \\ 来表示一个实际的反斜杠。；正确的代码应该是：
- 20240419：补充了当日日志，暂未提炼出明确主题。
- 20240420：string_view 可能为空，但是是string对象的引用；国外计算机lab；用c++17 20，相对硬核点
- 20240421：中山大学编译原理课程实验（完全重构版本）；gradescope；中山大学 编译器
