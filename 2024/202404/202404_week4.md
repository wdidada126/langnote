# 202404_week4

## 时间范围

- 202404 月第 4 周

## 本周记录

- 20240422：就是把你的启动脚本换行符改一下就行；改成unix；要不在linux上是dos格式的，用不了
- 20240423：string_view和string的区别在于string_view只能被访问而不能被修改( refer to a constant contiguous sequence),，而string可以追加、删除和修改字符；telnet mails.jlu.edu.cn smtp 这条命令尝试使用 Telnet 协议来连接到 mails.jlu.edu.cn 这个地址的 SMTP（简单邮件传输协议）端口。；SMTP（Simple Mail Transfer Protocol）是一种用于在服务器之间发送和接收电子邮件的 TCP/IP 标准协议。它是一种应用程序层协议，是通过 Internet 进行电子邮件通信（传输和传递电子邮件）的最常见和最常用的协议之一。
- 20240424：在Windows系统中，如果您尝试删除一个文件或目录时出现 "Failed to delete" 的错误，并且想要查看是哪个程序占用了该文件或目录，可以使用以下方法：；使用资源监视器：资源监视器是Windows系统自带的一个工具，可以查看系统中的进程和文件使用情况。按下Win + R键打开运行对话框，输入 "resmon" 并按下回车键打开资源监视器。在资源监视器中，选择 "CPU" 标签，然后在 "Associated Handles" 部分中输入您要删除的文件或目录的路径，即 "D:\work\yitong\codes\B_CLOUD_MP_CONSOLE\target"。它将显示正在使用该文件或目录的进程列表。；使用第三方工具：您还可以使用一些第三方工具来查看哪个程序占用了文件或目录。例如，"Process Explorer" 是一款功能强大的进程管理工具，可以显示系统中所有的进程，并提供详细的信息，包括打开的文件和句柄。您可以下载并运行 "Process Explorer"，然后使用该工具来搜索并查找占用目标文件或目录的进程。
- 20240425：目前在 MSSQLServer、Oracle、DB2 等主流数据库中都提供了对开窗函数的支持,不过非常遗憾的是MYSQL5暂时还未对开窗函数给予支持。；mysql8有；这种 一般查 比如  找谁工资最高，谁工资第二高。会比较好
- 20240426：哇哦，一次性甩出 Karpathy 三件套！这三位就是 2023-2025 年纯 C/CUDA 大模型训练/推理圈的“神级开源项目”圣三家，基本把“让每个人都能在裸金属上跑大模型”这件事推到了极致。下面我给你一个 2025 年底最硬核、最新的对比和现状总结（已经帮你把三个仓库都拉到最新 commit 看完了）。；| 项目          | llm.c（2024.4 开始）                  | llama2.c（2023.7）                  | minGPT（2020-2023）                     |；|---------------|---------------------------------------|-------------------------------------|-----------------------------------------|
- 20240427：补充了当日日志，暂未提炼出明确主题。
- 20240428：A fast reverse proxy to help you expose a local server behind a NAT or firewall to the internet.；git clone -b v0.57.0 https://github.com/fatedier/frp.git；cd frp
