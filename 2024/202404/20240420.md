# 20240420

string_view 可能为空，但是是string对象的引用

国外计算机lab
用c++17 20，相对硬核点

db
huadb

硬盘，缓存，内存，cpu cache速度不匹配
Page啥的
b+树



计算机网络
cs144

https://github.com/meta-llama/llama3


云原生计算基金会（Cloud-Native Computing Foundation，CNCF）批准将Strimzi作为孵化项目，以简化 Kubernetes 上 Apache Kafka 的部署。Strimzi 提供了一种 Kubernetes 原生的方式，通过一组扩展了 Kubernetes API 的 Operator 与 Kafka 进行交互，使得在 Kubernetes 上配置、部署和运维 Kafka 更加容易。
在确定 Strimzi 满足孵化需求并且高效实现了 Kubernetes 的operator模型后，技术监督委员会批准了该项目。Strimzi 的孵化凸显了该项目的技术可行性，并为 CNCF 提供了一个机会，用来支持和协调Kafka以及其他事件驱动架构的部署、管理和运维。
Strimzi 项目的开发人员构建了 Kubernetes operator和其他API以增强 Kafka 集群管理的效率。这些特性包括实现安全控制、自动化运维和迁移功能。例如，使用kubectl和GitOps管理 Kubernetes 上的 Kafka。

群里的佬们，刚才面试，有一个问题： 如果Redis集群全部挂掉了，怎么解决？  没答上来， 想来找找答案
服务降级用es兜底也行
全挂掉了，那就用数据库兜底吧
然后靠程序限流
等着redis集群恢复
在网关上做个监控，redis如果不能用的时候，先返回一个错误，重启之后，再放开。
找相关运维啊，如果有几十个上百个节点，我们普通开发敢重启吗
0. 通过自动探测接口、自动化监控告警系统发现问题
1. 打开降级开关， 降级使用本地缓存、或者备用 Redis 集群，先抗一会儿
2. 排查 Redis 集群所涉及的机器， 看出了什么问题，能否快速解决和恢复。如果可以，解决，并重启 Redis 集群，重启时暂不提供服务，需要进行缓存预热，预热结束关闭降级开关，Redis 集群才开始提供服务。如果不能快速解决和恢复，部署新的 Redis 集群，进行缓存预热，预热结束后通知客户端重新连接到新的Redis 集群，Redis 集群才开始提供服务。如果对数据一致性要求高，集群恢复期间，可暂停对写请求的处理。如果能容忍短暂的数据不一致，则不用管。
3. 编写故障处理报告，按时间线详细叙述整个故障处理过程。深入调查故障的根本原因，针对性地给出整改方案，防止类似问题再次发生。
总结：快速发现问题、有预案降级（降级开关，预先开发降级方案的代码、缓存预热代码、切换新集群代码等）、快速恢复、问题复盘与改进。
我还遇到过更绝的问题，数据库全挂了，redis也全挂了，mq也全挂了，无论啥啥啥都全挂了怎么办的问题这种难题我真不会。