# 202404_week2

## 时间范围

- 202404 月第 2 周

## 本周记录

- 20240408：在Java中，"this逃逸"是指在一个对象还没有完全构造完成时，其引用（即this引用）被发布到其它线程，或者能够以其它方式被外部访问到。这可能导致对象的状态在不同线程间不一致，从而引发并发问题或逻辑错误。；以下是关于Java中this逃逸问题的详细解释：；this逃逸的原因
- 20240409：Semaphore是共享锁的一种实现，它默认构造AQS的state值为permits，你可以将permits的值理解为许可证的数量，只有拿到许可证的线程才能执行。；java知识点内部联系；java内置锁，syn eait notify
- 20240410：补充了当日日志，暂未提炼出明确主题。
- 20240411：消息队列；企业表，很多表不光存储了企业id，也存储了企业名称，企业名称可以更改；消息队列推送企业新名称
- 20240412：当查询第100页时SQL执行慢，这通常是因为随着页码的增加，需要扫描的数据量也在增加，导致查询性能下降。为了优化这种情况，你可以考虑以下几个策略：；分页优化：；使用LIMIT和OFFSET进行分页时，随着OFFSET的增加，性能会下降。这是因为数据库需要扫描更多的行来跳过前面的记录。考虑使用“seek method”进行分页，即基于上一次查询的最后一个ID或其他唯一标识进行下一页的查询。
- 20240413：hustub；mac编译不了
- 20240414：屹腾工作总结；ares代码，不能断点调试，直接打印日志；哪儿打印日志？
