# 20240416

架构师，画图


三种策略
https://javaguide.cn/database/redis/3-commonly-used-cache-read-and-write-strategies.html

Cache Aside Pattern（旁路缓存模式）
Cache Aside Pattern 是我们平时使用比较多的一个缓存读写模式，比较适合读请求比较多的场景。

Read/Write Through Pattern（读写穿透）Read/Write Through Pattern 中服务端把 cache 视为主要数据存储，从中读取数据并将数据写入其中。cache 服务负责将此数据读取和写入 db，从而减轻了应用程序的职责

Write Behind Pattern（异步缓存写入）Write Behind Pattern 和 Read/Write Through Pattern 很相似，两者都是由 cache 服务来负责 cache 和 db 的读写。


redis的基本数据类型

string
list
hash
set
sorted set /zset
bitmap
bitfeild
geo
hyperlog
stream

sql exaplin执行的结果

type

const
ref
equal_ref
range 没记住

没有执行BaseRouter.execute()

	@RequestMapping(value = { "/common/file/checkDownload/**" }, method = RequestMethod.GET)
	public void checkDownload(HttpServletRequest req, HttpServletResponse resp) {
		logger.warn("--common/file/download--【{}】", req.getRequestURI());
		BusinessContext ctx = (BusinessContext) ContextHolder.getContext().getBusinessContext();
		String etpid = ctx.getSessionObject("SESS_ETP_CUST_NO");
		String tenaAccToken = ctx.getSessionObject("SESS_TENA_ACC_TOKEN");
		String sessTenaId = ctx.getSessionObject("SESS_TENA_ID");
		logger.info("checkDownload_etpid:{},tenaAccToken:{},sessTenaId:{}",etpid,tenaAccToken,sessTenaId);
		download(req,resp);
	}

https://test-ecs.hrs361.com/cloud-gateway/common/file/checkDownload/appmkt/excel/awhiteList/%E4%BC%81%E4%B8%9A%E7%99%BD%E5%90%8D%E5%8D%95%E5%AF%BC%E5%85%A5%E6%A8%A1%E6%9D%BF.xlsx

Mysql b+数 红黑树 平衡术 b树 b-树
