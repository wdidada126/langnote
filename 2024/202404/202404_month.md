# 202404_month


javaGuide

java CLH

db log

this escape problem

## 整理补充

## 本月概览

- 本月共整理 30 篇日记。
- 已结合周报回填当月重点主题，便于后续继续补充。

## 本月高频主题

- 定时任务
- 0 0 0/2 * * ?
- 不是
- git 把某个commit id的提交diff出来，应用到其他分支
- 要将某个特定 commit id 的提交差异（diff）应用到另一个分支，你可以使用 git cherry-pick 命令。下面是如何做到这一点的步骤：
- 找到你想要应用的 commit id：
- javaGuide
- double精度问题，是因为double在计算机中是小数 指数表示的
- 用BigDecimal
- Mac系统中查看子类的快捷键与Windows系统一致，同样是`Ctrl + Alt + B`。
- 在Mac系统中，CLion的快捷键操作几乎与Windows系统相同，只是在具体快捷键的表示上有所区别，Windows系统中使用的`Ctrl`键，在Mac系统中通常对应为`Cmd`（Command）键。
- 在Java中，"this逃逸"是指在一个对象还没有完全构造完成时，其引用（即this引用）被发布到其它线程，或者能够以其它方式被外部访问到。这可能导致对象的状态在不同线程间不一致，从而引发并发问题或逻辑错误。

## 整理补充

## 周度脉络

- 202404_week1：- 20240401：定时任务；0 0 0/2 * * ?；不是
- 202404_week2：- 20240408：在Java中，"this逃逸"是指在一个对象还没有完全构造完成时，其引用（即this引用）被发布到其它线程，或者能够以其它方式被外部访问到。这可能导致对象的状态在不同线程间不一致，从而引发并发问题或逻辑错误。；以下是关于Java中this逃逸问题的详细解释：；this逃逸的原因
- 202404_week3：- 20240415：c cpp代码用understand；java代码用idea；ares框架，idea不支持啊
- 202404_week4：- 20240422：就是把你的启动脚本换行符改一下就行；改成unix；要不在linux上是dos格式的，用不了
- 202404_week5：- 20240429：补充了当日日志，暂未提炼出明确主题。

## 本月高频主题

- 定时任务
- 0 0 0/2 * * ?
- 不是
- git 把某个commit id的提交diff出来，应用到其他分支
- 要将某个特定 commit id 的提交差异（diff）应用到另一个分支，你可以使用 git cherry-pick 命令。下面是如何做到这一点的步骤：
- 找到你想要应用的 commit id：
- javaGuide
- double精度问题，是因为double在计算机中是小数 指数表示的
- 用BigDecimal
- Mac系统中查看子类的快捷键与Windows系统一致，同样是`Ctrl + Alt + B`。
- 在Mac系统中，CLion的快捷键操作几乎与Windows系统相同，只是在具体快捷键的表示上有所区别，Windows系统中使用的`Ctrl`键，在Mac系统中通常对应为`Cmd`（Command）键。
- 在Java中，"this逃逸"是指在一个对象还没有完全构造完成时，其引用（即this引用）被发布到其它线程，或者能够以其它方式被外部访问到。这可能导致对象的状态在不同线程间不一致，从而引发并发问题或逻辑错误。
