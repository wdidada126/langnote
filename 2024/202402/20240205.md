# 20240205

thunderbird
mac，就是firefox登录

thundermail有账号吗？可以自动同步添加的邮箱账户

Asio是head only
跨平台的

cow也是head only？

windows Clion编译cow库的代码，设置
-DCMAKE_TOOLCHAIN_FILE=D:/git/github/vcpkg/scripts/buildsystems/vcpkg.cmake
之后，vcpkg自动下载编译依赖

DCMAKE_TOOLCHAIN_FILE是CMake中的一个参数，用于指定交叉编译工具链文件的路径。它告诉CMake使用特定的工具链进行交叉编译，以在某个平台上开发和构建另一个平台的代码。通过使用这个参数，可以灵活地配置构建类型和交叉编译环境，以满足不同的开发需求。
DCMAKE_TOOLCHAIN_FILE的主要作用是定义和配置工具链。这包括指定编译器、链接器和其他工具的路径，设置目标平台的特定编译和链接标志，定义目标平台的系统名称、处理器类型等信息。
通过使用DCMAKE_TOOLCHAIN_FILE，可以简化跨平台编译的过程，使得开发者能够轻松管理复杂的工具链设置，同时保持代码的可移植性和可维护性。
在实际应用中，工具链文件可能会更加复杂，包括设置各种编译器标志、链接选项等。但其核心思想始终是为了简化和标准化跨平台编译的过程。

    std::atomic`<int>` init_count(0);

asio

Traits 的中文翻译是“特性”。
Type Traits 的中文翻译是“类型特性”。
Unary/Binary Traits 的中文翻译是一元/二元特性。

C++ Traits 是一种模板元编程技术，它提供了一种在编译时处理类型和特性的机制。Traits 通常用于根据类型特性提供类型转换、查询和操作的功能。

Traits 可以分为两类：

1. Type Traits：用于处理类型。它们在编译时检查类型的属性，并根据这些属性执行类型转换或生成新的类型。
2. Unary/Binary Traits：用于处理表达式。它们提供了一元或二元运算符的特性和行为，以便在编译时进行类型转换和优化。

使用 Traits 可以帮助我们实现一些功能，例如：

* 类型转换：根据给定的条件或属性，将一个类型转换为另一个类型。
* 类型查询：检查类型的属性，例如是否是某个特定类型或是否具有某个特性。
* 类型操作：基于类型的属性，执行特定的操作或生成新的类型。

Traits 的主要优点是它们可以在编译时执行操作，避免了运行时的开销。此外，由于 Traits 是在编译时处理的，因此它们可以提供更好的类型安全性和性能优化。

在C++中，"Type Traits"是一个模板元编程的库，它提供了一种机制来查询和操作类型和表达式的属性。Type Traits特别用于在编译时确定关于类型的信息，并基于这些信息进行各种类型的转换、检查和操作。

Type Traits的主要用途包括：

1. 类型检查：确定一个类型是否是某种特定类型（例如，是否是POD类型，是否是数组，等等）。
2. 类型转换：根据给定的条件或属性，将一个类型转换为另一个类型。
3. 生成新类型：基于现有类型的属性，生成新的类型。
4. 辅助模板：为其他模板提供有关类型的额外信息。

例如，C++标准库中的 `std::is_integral_v<T>`就是一个Type Trait，它检查给定的类型 `T`是否是整数类型。

Type Traits为模板元编程提供了强大的工具，使得在编译时进行复杂的类型操作和决策成为可能。

head only的库，可以直接引入头文件，也可以编译成库引入？

公司单位，中庸，意味着没有差的指标，不会得罪人。提拔评优的时候有优势。

github action

```shell
name: CI

on:
  push:
    branches: [master]

jobs:
  build:
    strategy:
      matrix:
        compiler: [gcc, clang]
```

```shell
name: asio CI

on:
  push:
    branches: [ master, citest-* ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        build-type: ['sanity']
        runs-on: [ubuntu-20.04, ubuntu-22.04, macos-latest]
        compiler: [g++-7, g++-8, g++-9, g++-10, g++-11, g++-12, clang++-10, clang++-14, g++]
        cxx-std: ['c++11', 'c++14', 'c++17', 'c++2a', 'c++20']
        separate-compilation: ['', '--enable-separate-compilation']
        optim-level: ['-O0']
```

./autogen.sh
./configure ${{ matrix.separate-compilation }} ${{ matrix.with-boost }}
make && make check

git clone https://github.com/chriskohlhoff/asio.git
cd asio
git checkout asio-1-29-0
./autogen.sh
./configure
make && make check

const std::unordered_map<std::string, std::string> mime_types{
      {"shtml", "text/html"},
      {"htm", "text/html"});

`std::unordered_map`和 `std::map`是C++标准库中提供的两种关联容器，它们的主要区别在于内部实现、排序方式、空间复杂度、时间复杂度以及多线程访问的支持。

1. **内部实现**：`std::map`的内部数据存储方式为平衡二叉查找树（如红黑树），因此数据是按key有序存储的。而 `std::unordered_map`的内部数据存储方式为哈希表，数据是无序的。
2. **排序方式**：`std::map`是有序的，它基于红黑树实现，按照键的排序顺序进行存储。而 `std::unordered_map`是无序的，它基于哈希表实现，不对元素进行排序，而是根据键的哈希值将元素存储在不同的存储桶中。
3. **空间复杂度**：`std::unordered_map`需要额外的空间存储哈希表，因此通常比 `std::map`使用更多的内存。
4. **时间复杂度**：`std::map`的性能和平衡二叉查找树相同，插入、删除和查找操作的平均时间复杂度为O(log n)。而 `std::unordered_map`的性能和哈希表相关，插入、删除和查找操作的平均时间复杂度为O(1)，但在最坏情况下可能达到O(n)。
5. **多线程访问**：两者都不是线程安全的，因此在多线程环境中访问这些容器时，需要采取适当的同步措施，如使用互斥锁或其他线程安全的数据结构。

总结来说，`std::unordered_map`和 `std::map`各有其优缺点，选择使用哪种容器取决于你的具体需求，如是否需要排序、对空间复杂度和时间复杂度的要求以及是否需要在多线程环境中使用。

没有std::ordered_map

asio官方GitHub仓库没有cmake组织，是有.sh脚本和make组织的
vcpkg Conan xmake是如何组织asio代码的？


```shell
ibqodeMacBook-Pro:LangNote ibqo$ xrepo search asio
The package names:
    asio: 
      -> asio-1.24.0: Asio is a cross-platform C++ library for network and low-level I/O programming that provides developers with a consistent asynchronous model using a modern C++ approach. (in xmake-repo)
ibqodeMacBook-Pro:LangNote ibqo$ xrepo install asio
note: install or modify (m) these packages (pass -y to skip confirm)?
in xmake-repo:
  -> asio 1.24.0 
please input: y (y/n/m)
y
  => download https://github.com/chriskohlhoff/asio/archive/refs/tags/asio-1-24-0.tar.gz .. ok
  => install asio 1.24.0 .. ok
```