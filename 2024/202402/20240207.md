# 20240207

Go有个招人场景的成本问题，不然字节现在的Java也不会崛起的这么快
单纯就是因为golang简单，节约员工成本。刚开始为了简单用的是python，后来性能不行了才换的go，go是主流，但java和python其实也不少
招人还好，语言不是门槛，很多开发之前都是写java的，来了字节才开始学golang。工作具体用什么语言还是看团队，算法那边基本都是python，像是物流这种整个团队都是从阿里挖过来的就用java，剩下的基本都是golang


clang(llvm) github action编译6小时自动结束了


要将 CentOS 7 上的 `.ssh` 文件夹的所有权限修改为 `wdidada` 用户及其用户组，您可以按照以下步骤操作：

1. **更改文件夹所有者**：
   使用 `chown` 命令将 `.ssh` 文件夹的所有者更改为 `wdidada` 用户和用户组。

   ```bash
   sudo chown -R wdidada:wdidada ~/.ssh
   ```

   这里的 `-R` 选项表示递归更改，意味着文件夹内的所有文件和子文件夹的所有者也会被更改。

2. **设置文件夹权限**：
   使用 `chmod` 命令设置 `.ssh` 文件夹的权限。通常，`.ssh` 文件夹的权限设置为 `700`，这意味着只有所有者可以读取、写入和执行该文件夹。

   ```bash
   sudo chmod 700 ~/.ssh
   ```

   对于 `.ssh` 文件夹内的 `authorized_keys` 文件，通常的权限设置为 `600`，以确保只有所有者可以读取和写入该文件。

   ```bash
   sudo chmod 600 ~/.ssh/authorized_keys
   ```

   如果您的 `.ssh` 文件夹内还有其他重要文件，确保它们的权限设置得当，以保护它们的安全性。

3. **验证更改**：
   使用 `ls -ld` 命令查看 `.ssh` 文件夹的权限和所有者是否已更改。

   ```bash
   ls -ld ~/.ssh
   ```

   输出应该类似于：

   ```bash
   drwx------ 2 wdidada wdidada 4096 Oct 23 10:00 /home/username/.ssh
   ```

   这里 `drwx------` 表示文件夹的权限，`2` 表示文件夹内的文件数量，`wdidada` 是文件夹的所有者和用户组。

4. **测试**：
   以 `wdidada` 用户身份登录，并尝试访问 `.ssh` 文件夹及其内容，以确保权限设置正确。

   ```bash
   su wdidada
   cd ~/.ssh
   ls -l
   ```

   您应该能够顺利访问 `.ssh` 文件夹并列出其中的文件。

请注意，如果您正在通过 SSH 远程连接到服务器，并且您正在尝试更改当前用户的 `.ssh` 文件夹权限，那么您可能不需要使用 `sudo`，因为您已经有足够的权限了。但是，如果您正在尝试更改其他用户的 `.ssh` 文件夹权限，那么您可能需要使用 `sudo` 或者以该用户的身份登录。


注意，
[wdidada@10-23-29-39 ~]$ chmod 600 ~/.ssh/id_ed25519_edidada_gitee
[wdidada@10-23-29-39 ~]$ chmod 600 ~/.ssh/id_ed25519_edidada_gitee.pub
不能用sudo


ssh-keygen -f "/root/.ssh/known_hosts" -R "106.75.209.6"
