# 20240218

odb编译，需要
gcc-plugin

github action可以设置定时任务，例子https://github.com/edidada/odb

俄语学习
https://t.me/yandex360blog

    List`<BillDiffLocalData>` importBillList = billDiffDAO.queryExistBillData(etpId, monthStr);

    LOGGER.info("--生成对比数据--queryBillInsuranceContent,importBillList:{}", JSON.toJSONString(importBillList));

    Set`<String>` set2 = importBillList.stream().map(BillDiffLocalData::getUSER_ID).collect(Collectors.toSet());

java.lang.ClassCastException: java.util.HashMap cannot be cast to xxx.yyy.zzz.monthlybalance.model.BillDiffLocalData
	at java.util.stream.ReferencePipeline$3$1.accept(ReferencePipeline.java:193) ~[?:1.8.0_231]
	at java.util.ArrayList$ArrayListSpliterator.forEachRemaining(ArrayList.java:1382) ~[?:1.8.0_231]
	at java.util.stream.AbstractPipeline.copyInto(AbstractPipeline.java:482) ~[?:1.8.0_231]
	at java.util.stream.AbstractPipeline.wrapAndCopyInto(AbstractPipeline.java:472) ~[?:1.8.0_231]
	at java.util.stream.ReduceOps$ReduceOp.evaluateSequential(ReduceOps.java:708) ~[?:1.8.0_231]
	at java.util.stream.AbstractPipeline.evaluate(AbstractPipeline.java:234) ~[?:1.8.0_231]
	at java.util.stream.ReferencePipeline.collect(ReferencePipeline.java:499) ~[?:1.8.0_231]
	at xxx.yyy.zzz.monthlybalance.service.impl.BillDiffServiceImpl.compareEtpAndMonth(BillDiffServiceImpl.java:48) ~[classes/:?]
    qubie?
.collect(Collectors.toSet());
.collect();
.collect(Collectors.toList());

/:? 是微信特殊符号


sql GROUP BY在where后面

    WHERE ESSM.ETP_ID = #{etpId} AND ESSM.month like CONCAT('%',#{month},'%')
        GROUP BY
        ESSM.ETP_ID,
        ESSM.MONTH


怀习惯，新增或者修改代码，直接跑，不检查代码，然后各种低级错误
