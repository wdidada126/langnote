# 20240229

编译docker k8s
nginx
envoy

https://dev.mysql.com/doc/refman/5.7/en/json-creation-functions.html

MySQL 5.7引入了对JSON数据类型的支持，同时也提供了一些用于处理JSON数据的函数。以下是一些常用的MySQL 5.7中的JSON处理函数：
1. JSON_OBJECT：用于创建一个JSON对象。

   ```sql
   SELECT JSON_OBJECT('name', 'John', 'age', 30);
   -- 输出：{"name": "John", "age": 30}
   ```
2. JSON_ARRAY：用于创建一个JSON数组。
   ```sql
   SELECT JSON_ARRAY('apple', 'banana', 'orange');
   -- 输出：["apple", "banana", "orange"]
   ```
3. JSON_EXTRACT：用于提取JSON对象中的特定属性或数组元素。
   ```sql
   SELECT JSON_EXTRACT('{"name": "John", "age": 30}', '$.name');
   -- 输出：John
   ```
4. JSON_SET：用于更新或添加JSON对象中的属性。
   ```sql
   SELECT JSON_SET('{"name": "John", "age": 30}', '$.age', 35);
   -- 输出：{"name": "John", "age": 35}
   ```
5. JSON_REMOVE：用于从JSON对象中删除属性。
   ```sql
   SELECT JSON_REMOVE('{"name": "John", "age": 30}', '$.age');
   -- 输出：{"name": "John"}
   ```
6. JSON_CONTAINS：用于检查JSON数组或对象是否包含指定的值。
   ```sql
   SELECT JSON_CONTAINS('["apple", "banana", "orange"]', 'orange');
   -- 输出：1
   ```
7. JSON_ARRAYAGG：用于聚合行中的JSON值，将它们组合成一个JSON数组。
   ```sql
   SELECT JSON_ARRAYAGG(name) FROM fruits;
   -- 输出：["apple", "banana", "orange"]
   ```
这些是MySQL 5.7中的一些常用的JSON处理函数。请注意，这些函数在较新的MySQL版本中可能会有所变化。建议在使用之前查阅MySQL官方文档以获取最新的函数列表和用法说明。


合肥公租房
三年到五年，提价
五年之后必须搬走

祥春，紫蓬山


docker-ce -> moby
https://mobyproject.org/

https://dockercommunity.slack.com/

要在使用 Maven 命令行运行时取消执行测试，您可以使用 `-DskipTests` 参数。这将告诉 Maven 跳过执行测试阶段。

以下是使用 `mvn` 命令行取消运行测试的示例：

```bash
mvn clean install -DskipTests
```

在上述示例中，`clean install` 是示例的构建目标，您可以根据您的需求替换为其他构建目标，例如 `package`, `deploy` 等。`-DskipTests` 参数告诉 Maven 跳过执行测试阶段。

使用该命令后，Maven 将跳过执行测试并继续执行其他构建阶段。

请注意，使用 `-DskipTests` 参数将跳过所有测试阶段，因此请确保在真正需要跳过测试时使用此参数。如果您只想跳过某个特定的测试类或方法，可以使用 `@Ignore` 注解或其他适当的方法在测试代码中进行跳过。


https://github.com/edidada/grpc-java1-3-0
跨语言是伪需求？

https://github.com/grpc/grpc-java/tree/v1.39.0
