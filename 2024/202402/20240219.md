# 20240219

https://zhuanlan.zhihu.com/p/590901624

免费、不用注册、不用公网 IP、同步速度也挺理想
https://zhuanlan.zhihu.com/p/627920819?utm_id=0
Syncthing 真的非常适合用来搭建个人全平台工作流，跨平台软件 + Syncthing 的搭配在日常使用中简直爽到飞起，比目前市面上各种同步方案都好用

比如 Obsdian + Syncthing 解决了图床问题，笔记中的图片、视频等资源统一放在 Obsdian 库目录下的某个文件夹里，笔记中使用相对路径对资源进行引用，然后用 Syncthing 同步到其他设备，在手机平板上就能直接查阅完整的笔记了，并且在手机上也能对笔记进行修改，修改后的数据也会同步到其他设备

我个人软件中，Krita/Xmind/Keepass/WPS/Obsdian/RIME/Joplin + Syncthing 就实现了安卓设备、桌面设备的无缝协同

其实所有的跨平台的本地存储软件，都可以用 Syncthing 来做同步，最重要的是所有数据都只存储在个人设备中，同步过程也是加密的，安全、不担心隐私泄露


春节期间，昨晚使用安卓版ss，没有使用混淆，使用vps科学上网，被禁

检测ip是否可以被访问
https://tcp.ping.pe/

妙妙屋，有shadowrockets的app账号
https://miao.114514house.com/user/tutorial?os=mac&client=clashx##


吴婷婷，说日企半导体公司，用oracle erp

trojan

server端，安卓客户端clash？

八合一脚本
https://github.com/fscarmen/sing-box/


mybatis
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties>
 		<property name="schema.piecework" value="PIECE_WORK_UAT" />
		<property name="schema.hrbase" value="HRBASE_UAT2" />
		<property name="schema.fuyaouser" value="fuyao_user_dev" />
	</properties>
	<!-- sql print -->
	<settings>
		<setting name="logImpl" value="SLF4J" />
		<setting name="logPrefix" value="dao." />
		<setting name="cacheEnabled" value="true" /><!-- 默认是false：开启二级缓存 -->
	</settings>
	

</configuration>
```
注意schema.piecework
schema.hrbase


```xml
	<!-- 根据员工查询工资表信息 -->
	<select id="queryPayrollList" parameterType="map" resultType="map">
		SELECT SE.ID,SE.TOTAL_SALARY, B.NAME, WS.TITLE, WS.MONTH, WS.ID AS SALARY_ID
		 FROM ${schema.piecework}.W_SALARY_TD WS, ${schema.piecework}.W_SALARY_EMPLOYEE_TD SE
			LEFT JOIN ${schema.hrbase}.T_CUSTOM_INFO_TD B ON SE.CREATE_ID = B.EMP_NO
			WHERE SE.EMP_NO=#{userId} AND WS.ID=SE.SALARY_ID AND SE.ETP_ID=#{etpId}
		<if test="confirmState != '' and confirmState != null">
			 AND SE.CONFIRM_STATE=#{confirmState}
		</if>
		<if test="sendState != '' and sendState != null">
			 AND SE.SEND_STATE=#{sendState}
		</if>
		<if test="month != '' and month != null">
			AND SE.MONTH=#{month} 
		</if>
	</select>

```
