# 202402_week4

## 时间范围

- 202402 月第 4 周

## 本周记录

- 20240222：编译mysql，因为boost不对，失败；boost这个库的应用很广？mysql用，trantlib这个金融库也用？
- 20240223：补充了当日日志，暂未提炼出明确主题。
- 20240224：此外，NobePay还支持一些需要3D验证的支付渠道，比如一些土耳其区的在线商城。通常只有少数银行的实体信用卡才能通过这种验证，所以NobePay的兼容性也非常值得称赞。；总结一下：；充值方式：支付宝、微信、USDT
- 20240225：周日休息
- 20240226：github codespace不用了要删除，算磁盘占用的；February
- 20240227：这个异常 `java.sql.SQLException: The used SELECT statements have a different number of columns` 通常发生在使用 `UNION` 或 `UNION ALL` 操作符来合并两个或多个 `SELECT` 语句的结果时。这个错误表明你尝试合并的 `SELECT` 语句返回的列数不匹配。；例如，如果你尝试执行以下查询：；这个查询会失败，因为第一个 `SELECT` 语句返回两列，而第二个 `SELECT` 语句只返回一列。在使用 `UNION` 或 `UNION ALL` 时，所有 `SELECT` 语句必须选择相同数量的列，并且相应的列必须具有兼容的数据类型。
- 20240228：GitKraken；7天试用，试用期过了之后不能打开github私有仓库；linux依赖
