# 202402_week3

## 时间范围

- 202402 月第 3 周

## 本周记录

- 20240215：补充了当日日志，暂未提炼出明确主题。
- 20240216：至于编码能力，最好是毕业后到公司里学习，编程是一种技能，需要通过参与实际的项目来打磨。；运行 `sudo chmod 777 -R /*` 命令会对根目录（`/`）及其所有子目录和文件应用递归的权限更改。；具体地说，命令中的选项和参数含义如下：
- 20240217：补充了当日日志，暂未提炼出明确主题。
- 20240218：odb编译，需要；gcc-plugin；github action可以设置定时任务，例子https://github.com/edidada/odb
- 20240219：大内网战略(4)：多设备间文件同步 Syncthing；免费、不用注册、不用公网 IP、同步速度也挺理想；Syncthing 真的非常适合用来搭建个人全平台工作流，跨平台软件 + Syncthing 的搭配在日常使用中简直爽到飞起，比目前市面上各种同步方案都好用
- 20240220：在 MyBatis 的 `mybatis-config.xml` 文件中，您可以定义变量并在配置文件的其他地方使用这些变量。以下是一个示例，展示如何在 `mybatis-config.xml` 中定义和使用变量：；上述示例中，我们在 `<properties>` 元素中定义了一个名为 `myVariable` 的变量，并将其值设置为 "Hello, World!"。然后，在 `<mapper>` 元素的 `resource` 属性中，我们使用 `${myVariable}` 来引用该变量，并将其作为 XML 映射文件的路径。；您可以根据自己的需求定义和使用更多的变量。在配置文件的其他位置，您可以使用 `${变量名}` 的形式来引用变量。
- 20240221：,"sorts": [；{；"column": "name",
