# 202401_week3

## 时间范围

- 202401 月第 3 周

## 本周记录

- 20240115："C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Auxiliary\Build\vcvarsall.bat x64"；cmd bat；call "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
- 20240116：java编译后可以编译得到grpc-core-1.24.0-sources.jar之类的文件；no-nonsense；不废话
- 20240117：libevent；xlnt；<libevent.h>
- 20240118：windows  alt +tab edge浏览器只显示一个，不展示不同tab
- 20240119：github codespace每个月只有120 hour core；才19号就用完了；ssh-keygen -f "/root/.ssh/known_hosts" -R github.com
- 20240120：Bad owner or permissions on /home/wdidada/.ssh/config；[wdidada@iZbp12v2gd23fwmbl3hk4nZ ~]$ ls -l ~/.ssh/config；rw-rw-r-- 1 wdidada wdidada 164 Jan 20 11:00 /home/wdidada/.ssh/config
- 20240121：carplay，是苹果开发的，利用手机做运算，汽车显示。；ssh -c 有哪些cipher；ssh -Q cipher 是用来查询 SSH 客户端支持的加密算法套件的，而 ssh-keygen -t 是用来指定生成 SSH 密钥对时所使用的算法类型的。这两者虽然都与加密有关，但它们的用途和输出信息是不同的。
