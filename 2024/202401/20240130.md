# 20240130

mkdir myconanprj && cd myconanprj
conan new cmake_exe -d name=mypkg -d version=0.1


## conan2
conan new cmake_exe -d name=mypkg -d version=0.1 -f

conan graph info  .

借鉴qt的conanfile.py文件
    def requirements(self):
        self.requires("zlib/1.2.11")
