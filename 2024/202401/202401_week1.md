# 202401_week1

## 时间范围

- 202401 月第 1 周

## 本周记录

- 20240101：招聘；奇瑞控股集团有限公司；maven设置jdk版本
- 20240102：objdump.md；c++程序的调试的信息（Visual Studio中的pdb文件，对于MAC和Unix系统，位于库本身中）；Bazel Build：Bazel是一把双刃剑
- 20240103：单一字段搜索，根据联想词搜索；汉字的；可以考虑用ES
- 20240104：RAII，中文翻译RAII，全称Resource Acquisition Is Initialization，中文翻译为“资源获取即初始化”，是一种C++编程技术。它是由Bjarne Stroustrup，C++之父提出的，用于管理程序在运行过程中所使用或申请的有限资源，如内存、网络套接字等。；不同于其他编程语言（例如Java, Python, Node, Go等具有垃圾回收功能的语言），C++需要程序员自己处理资源的申请和释放，以避免诸如内存泄漏等问题。RAII技术利用了C++的一个特性，即当创建一个类对象时，会自动调用构造函数；当对象超出作用域时，会自动调用析构函数。这样，我们可以将资源的生命周期与某个对象的生命周期绑定在一起，只要对象能正确地析构，就不会出现资源泄漏问题。；RAII（Resource Acquisition Is Initialization）是一种C++ RAII（Resource Acquisition Is Initialization）是一种C++编程技术，用于管理资源，如内存、文件句柄等。当一个对象被创建时，它会自动获取所需的资源；当对象被销毁时，它会自动释放这些资源。这样可以确保资源的正确分配和释放，避免资源泄漏。
- 20240105：同样是点击Win10左下角的开始菜单图标，然后点击进入“设置”→“个性化”→“开始”标签下的“偶尔在’开始’菜单中显示推荐应用”的复选框关闭即可；windows开始菜单搜索，取消搜索建议；为什么Linux下要把创建进程分为fork()和exec()(一系列函数)两个函数来处理?
- 20240106：ppt和示例代码好像是这个：；aosp那套构建系统太复杂了,Make+Kati+Soong,希望bazel能早日替代；Android用bazel构建了
- 20240107：sudo apt update 更新本地文件；更新之后deb包404不见了；注意类比maven和apt
