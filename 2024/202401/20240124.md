# 20240124

xlnt依赖库

要在 CLion IDE 中关联 `xlnt` 库的源代码，您可以按照以下步骤进行操作：
1. 确保您已经成功地获取了 `xlnt` 库的源代码。如果您尚未获取源代码，可以从 `xlnt` 的官方代码库或其他可靠来源获取。
2. 打开 CLion IDE，创建一个新的项目或打开现有项目。
3. 在 CLion 的项目视图中，找到 `xlnt` 库的源代码目录。这通常是包含 `xlnt` 源代码的文件夹。
4. 右键单击该目录，然后选择 "Mark Directory as"（将目录标记为） -> "Sources Root"（源代码根目录）。这将告诉 CLion 该目录是源代码的根目录。
5. 确保您的项目中包含了正确的头文件和库文件路径，以便编译器能够找到 `xlnt` 库的头文件和库文件。
6. 在您的代码中，使用 `#include` 指令包含 `xlnt` 库的头文件。例如：
   ```cpp
   #include <xlnt/xlnt.hpp>
   ```
7. 现在您应该能够在 CLion IDE 中调试和浏览 `xlnt` 库的源代码了。您可以设置断点、跟踪代码执行，并查看 `xlnt` 库的源代码。
请注意，您需要确保您的项目正确配置了 `xlnt` 库的依赖项和编译选项。这可能包括链接正确的库文件、定义适当的编译宏等。根据您的项目设置和 `xlnt` 库的要求，您可能需要进行一些额外的配置。

cd "C:\Users\edida\Documents\WeChat Files\wxid_fop5od0dhpvp21\FileStorage\File\2024-01"
mvn install:install-file -DgroupId=cn.com.yitong.mail -DartifactId=sendmail-sdk -Dversion=1.0.1 -Dpackaging=jar -Dfile=sendmail-sdk-1.0.1.jar


unqualified id
不符合要求的id


public static final String 变量，定义在interface中，不是在class中

https://github.com/bxcodec/go-clean-arch

https://blog.cleancoder.com/uncle-bob/2011/11/22/Clean-Architecture.html

https://github.com/sandisks/TeamTalk

编译

以前复杂sql不会写，有了例子，就会写了
ifnull()
concat()

bash脚本，不知道有判断，循环条件

c++ 标准，23了
cppreference上一对类似conan vcpkg的工具类
cmake跟autoconf竞争
.cmake文件
enum class，不是enum
开发需要用.h文件，运行不需要.h文件
cmake脚本的一对command
maven assemble插件，跟cmake install()一样，挨个添加文件，添加文件夹


在SQL中，不同的数据库系统有不同的函数来拼接字符串。以下是一些常见数据库系统的拼接字符串函数：

MySQL: 使用 CONCAT() 函数。

```sql
SELECT CONCAT('Hello', ' ', 'World');
```
SQL Server: 使用 + 运算符。

```sql
SELECT 'Hello' + ' ' + 'World';
```
Oracle: 使用 || 运算符或 CONCAT() 函数。

```sql
SELECT 'Hello' || ' ' || 'World' FROM DUAL;
```
或者

```sql
SELECT CONCAT('Hello', ' ', 'World') FROM DUAL;
```
PostgreSQL: 使用 || 运算符。

```sql
SELECT 'Hello' || ' ' || 'World';
```
SQLite: 使用 || 运算符。

```sql
SELECT 'Hello' || ' ' || 'World';
```
当使用字符串拼接时，请注意数据类型，确保您正在连接的数据是字符串类型。如果不是，您可能需要使用函数将其转换为字符串，例如在MySQL中使用 CAST() 或 CONVERT()。


http resp contentType

application/x-gzip

centos 7.9 源码编译安装gcc5.5，/bin/sh: line 3: cd: host-x86_64-unknown-linux-gnu/libcc1: No such file or directory
报错如何解决

CC1 是 GCC 编译器套件中的一个 C 语言编译器前端，负责将 C 语言源代码转换为中间代码。在编译过程中，CC1 主要完成词法分析、语法分析和语义分析等任务，将源代码中的文本信息转换为结构化的抽象语法树（AST），以便后续的编译器后端进行优化和目标代码生成。
gcc编译好之后要大量测试

MySQL 5.7 支持的 SQL 版本主要是 SQL:2003，同时也兼容一些早期的 SQL 标准。具体来说，MySQL 5.7 支持大部分 SQL:2003 的功能，但可能不完全符合所有的 SQL:2003 规范。

