# 20240107

sudo apt update 更新本地文件
更新之后deb包404不见了
注意类比maven和apt
maven可以设置多个远程仓库
阿里
华为
官方

conan -r可以设置多个
maven gradle都是设置固定版本，npm是设置版本范围

conandata.yml

https://github.com/conan-io/conan-center-index/blob/master/recipes/grpc/all/conandata.yml
https://github.com/conan-io/conan-center-index/

pip，npm，goget，mvn，nuget 这些我都用，最稳定的的确是maven。
glide是Golang的包管理工具，是为了解决Golang依赖问题的。 为什么需要glide？ 原因很简单，Go 语言原生包管理的缺陷。罗列一下golang的 get 子命令管理依赖有很多大缺陷：

conan  doc
proprietary

conan2对于cmake是无侵入的，不用再cmakelists.txt中调用特定的函数。conan_setup

maven对于依赖库是深度优先遍历还是广度优先遍历
mave对于依赖版本号冲突为什么不能保存
xmake可以报错

能拉取源码的平台很有限，绝大多数依赖的是 github.com
不能区分版本，以至于令开发者以最后一项包名作为版本划分
依赖 列表/关系 无法持久化到本地，需要找出所有依赖包然后一个个 go get
只能依赖本地全局仓库（GOPATH/GOROOT），无法将库放置于局部仓库（$PROJECT_HOME/vendor）
govendor 是 go 语言依赖管理工具

cmake打包，需要指定cpp源文件有哪些
maven是指定某个文件夹

最近在重撸C++，现在已经有C++版本的channel、WaitGroup，蛮多模仿Go的语句出现，说明C++也没有完全故步自封，她也是在不断吸取其他语言的长处，在不断进步的。

七牛的许式伟已经搞了一个go+
最近在重撸C++，现在已经有C++版本的channel、WaitGroup，蛮多模仿Go的语句出现，说明C++也没有完全故步自封，她也是在不断吸取其他语言的长处，在不断进步的。

权限框架
shrio
spring sexurity

不完全一样，一般意义上的权限更多的是用户角色权限三大板块，而鉴权一般特指c端用户是否登录系统

conan.io
打包已经存在的库



ubuntu 如何查看版本
sudo lsb_release -a

ubuntu如何更新firefox

sudo apt-get update
sudo apt-get install firefox  --fix-missing

vs code
sudo apt-get install code -y

JPA不仅可以写HQL，还可以写Native SQL，配合QueryDSL使用，开发效率比MyBatis高多了。


看看佳通轮胎 外企
我前同事搞大数据的，说里面很好
那边4.30就下班了

你说的这是招行卡部，不是信用卡中心
招行只有一个信用卡中心

阿里云放弃IoT，缩减混合云，具体多少人被干掉，就不知道了
Minecraft 也不一定要 JRE 才能玩，有基岩版，C++ 写的。

微服务架构（MicroServices Architecture，MSA）：微服务架构可以看做是面向服务架构和分布式服务架构的拓展，使用更细粒度的服务（所以叫微服务）和一组设计准则来考虑大规模的复杂系统架构设计。也可以把它看做是SOA的一种衍生，所以总体来说，微服务架构是一种架构设计思想，也是一种做系统的方法论。

他破冰行动蔡永强 特别像哈利波特第一集的斯内普教授，人民的名义里的达康书记，放出来先迷惑观众的。
https://www.zhihu.com/question/485718919/answer/2967555685?utm_psn=1704150981976231938

说的难听一点，官场提拔是交易，而不是奖励。
把你提到领导岗位上，你要就要付出与之相符的价值。有的人能办事、有的人能搞钱、有的人能背锅，有的人有宣传意义、有的人有统战价值，你首先具备了以上的价值，然后才能获得提拔的机会，这是一种交易。
多数人理解的提拔是，我认真干活，我俯首甘为孺子牛，领导应该用提拔来奖励我的付出，但凡有这样思想的基本告别提拔了。
所以，如果有人认识到了自己就是个普通的小公务员，没挂着天线、没贴着标签、也没什么超人的情商和能力，不想提拔就是很正常的事，这是对自己的定位有清晰的认识。

haproxy
api网关

要是有架构师跟我说顺序一致性场景用kafka，我可能会抽他

mingw有工具reimp，操作操作都能支持

微服务网关 分类

那个圆某自身眼界就很窄，也就搞一些小应用玩具，真正的高性能分布式系统，都是C/C++开发的，本身system方向就是CS所有大类中最难的那个，国内基本只有985和强211高校有sys方向的课题组。还说C++程序员除了那点技术什么都不会，自己从考公出国移民什么都懂，笑死，我让他给CCF里面那几个systems方向的A会和A刊的编辑和大佬们说这句话，他就不理我了。

● 项目官方网站：
https://trpc.group/
● Github主仓库：
https://github.com/trpc-group
● Github插件仓库：
https://github.com/trpc-ecosystem

看到一篇文章上面讲，conda安装的tensorflow要比pip安装的tensorflow完善。
我用conda install的类库，可不可以用pip uninstall呢？反过来呢？这样会不会有什么问题？

https://docs.conan.io/en/1.62/developing_packages/package_dev_flow.html

conan new
conan build
conan test
conan install
conan create
conan package
conan export-pkg
conan source

可以直接下载网络上的库文件
添加清单信息

grpc需要protoc
这个工具，conan支持复制protoc到制定文件夹
poco需要zlib库，把以来信息导入到一个json文件

conan search grpc
现在想想2019年，自己在电脑上编译grpc，没必要啊

学知识不成体系
maven文档都没看完整
git也没看吧

https://www.slideshare.net/chenshuo/muduo-network-library

MySQL从4.1开始支持SQL的子查询。这个技术可以使用SELECT语句来创建一个单列的查询结果，然后把这个结果作为过滤条件用在另一个查 询中。例如，我们要将客户基本信息表中没有任何订单的客户删除掉，就可以利用子查询先从销售信息表中将所有发出订单的客户ID取出来，然后将结果传递给主 查询，如下所示： 
DELETE FROM customerinfo WHERE CustomerID NOT in (SELECT CustomerID FROM salesinfo ) 　 　使用子查询可以一次性的完成很多逻辑上需要多个步骤才能完成的SQL操作，同时也可以避免事务或者表锁死，并且写起来也很容易。但是，有些情况下，子查 询可以被更有效率的连接（JOIN）.. 替代。例如，假设我们要将所有没有订单记录的用户取出来，可以用下面这个查询完成： SELECT * FROM customerinfo WHERE CustomerID NOT in (SELECT CustomerID FROM salesinfo ) 　　如果使用连接（JOIN）.. 来完成这个查询工作，速度将会快很多。尤其是当salesinfo表中对CustomerID建有索引的话，性能将会更好，查询如下： SELECT * FROM customerinfo LEFT JOIN salesinfoON customerinfo.CustomerID=salesinfo. CustomerID WHERE salesinfo.CustomerID IS NULL 　　连接（JOIN）.. 之所以更有效率一些，是因为 MySQL不需要在内存中创建临时表来完成这个逻辑上的需要两个步骤的查询工作

资料多，资料看了不用记不住的

Linux 4.4.0内核源码分析TCP实现.pdf：
链接: https://pan.baidu.com/s/1kTQ3lyNzjttI0xSYYqSwCQ 提取码: rtbn

讲稿：https://p0kt65jtu2p.feishu.cn/docx/HQR3dZsPoo9z9ExZk63cYgovn6c
硬核课堂主页：https://hardcore.feishu.cn/wiki/wikcn9i4sfdTkxVX0DYrMMbNBpd

本课程主要讲解llama2模型与llama模型的区别以及支持中文化改造
【【训练自己的llama2】太强了！llama2医疗大模型，医疗应用llm应用现状及微调医疗大模型！对话机器人/大语言模型-哔哩哔哩】 https://b23.tv/hDUxqYh

autosar

apache spark 和 ambari spark 或 Cloudera 驱动包是不同的

中电信量子 二面
Nacos zk 区别，从cap的角度说明
Zk cp
nacos cp ap都支持

项目架构
翼支付的架构
讯飞的架构
Erp的架构

写代码 v字形if 避免

软通动力 苏州轨道交通，xc低代码平台

迷信，出租屋租客怀孕，房子不租了
说是在那屋生会借走房子主人的财运，按这个道理，生娃都是在医院生啊，医院还这么赚钱，怕啥？

sql每科成绩大于90分的人数

要计算每科成绩都大于90分的人数，需要先获取每科的成绩数据，然后对数据进行筛选。以下是一种可能的SQL查询语句，假设成绩数据存储在名为"scores"的表中，表结构包含科目名称（subject）和成绩分数（score）：
SELECT COUNT(DISTINCT student_id)
FROM scores
WHERE subject IN (SELECT DISTINCT subject FROM scores) AND score > 90;
这个查询语句中，首先使用子查询获取所有科目的名称，然后在外部查询中使用条件筛选出每科成绩大于90分的记录。最后使用COUNT函数统计满足条件的学生的数量。
请注意，具体的查询语句可能因数据库结构和表设计而有所不同，上述查询语句仅提供了一种参考方案。

Java的AQS（AbstractQueuedSynchronizer）是一个同步组件框架，它提供了一种基于队列的、可扩展的线程同步机制。在AQS中，ConditionObject是用于实现线程间同步的条件对象。
ConditionObject是AQS的一个内部类，它封装了一个等待队列（Waiting Queue），用于存储等待某个条件满足的线程。ConditionObject提供了一些方法，可以用于在特定条件下控制线程的等待和通知。
下面是ConditionObject的一些常用API及其作用：
await()：该方法使当前线程进入等待状态，直到被唤醒或等待超时。当线程调用await()方法时，它会被放入ConditionObject的等待队列中，并释放锁。其他线程可以调用signal()或signalAll()方法来唤醒等待队列中的线程。
signal()：该方法唤醒等待队列中的一个线程，使其继续执行。被唤醒的线程将重新尝试获取锁并执行后续代码。
signalAll()：该方法唤醒等待队列中的所有线程，使它们重新尝试获取锁并执行后续代码。
awaitUninterruptibly()：该方法使当前线程进入等待状态，直到被唤醒或等待超时。与await()不同的是，该方法不会响应中断，即使线程被中断也不会提前返回。
awaitUntil(Date deadline)：该方法使当前线程进入等待状态，直到被唤醒、等待超时或达到指定的截止时间。该方法可以设置一个时间限制，超过该时间限制后线程将抛出TimeoutException异常。
使用ConditionObject可以实现灵活的线程同步机制，例如在某个条件满足时唤醒等待的线程，或者在一定时间内等待某个条件满足等。这些功能可以应用于各种多线程场景，如生产者-消费者问题、资源分配等。

两台主机 复制文件
scp
查询文件内容

scp是Linux系统下的一个用于远程复制文件的命令。其基本语法如下：
scp [参数选项] [源文件] [目标地址]
其中，参数选项和源文件都是可选的，目标地址是必须的。
以下是一些常用的参数选项：
-r：递归复制整个目录。
-P：指定端口号。
-C：开启压缩传输。
-v：显示详细的传输过程。
以下是一些常用的示例：
从本地复制文件到远程服务器：
scp /path/to/local/file user@remote_server:/path/to/remote/directory
这将把本地的文件复制到远程服务器的指定目录。
从远程服务器复制文件到本地：
scp user@remote_server:/path/to/remote/file /path/to/local/directory
这将从远程服务器的指定文件复制到本地的指定目录。
递归复制整个目录：
scp -r /path/to/local/directory user@remote_server:/path/to/remote/directory
这将递归复制本地的整个目录到远程服务器的指定目录。
使用端口号进行传输：
scp -P 22 user@remote_server:/path/to/remote/file /path/to/local/directory
这将使用SSH端口22进行文件传输。
请注意，在使用scp命令时，需要确保目标地址和源文件的路径正确，并且需要提供目标服务器的用户名和密码（如果需要的话）。

讯飞金融
助贷，风控

RedisJSONRedisJSON[2] 是 Redis 数据库的扩展功能，提供对 JSON 数据的全面支持。它允许在 Redis 中存储、更新和检索 JSON 数据，就像处理普通数据一样。Redis JSON 还支持 JSONPath 语法，使用户能够方便地选择和更新文档中的元素。这些文档以二进制数据形式存储在树状结构中，实现了快速访问子元素。此外，Redis JSON 提供了针对不同 JSON 数据类型的原子操作，确保数据的完整性和一致性。它还与 Redis 的搜索和查询功能无缝集成，使用户能够轻松索引和查询 JSON 文档。这使得 Redis JSON 成为处理和管理 JSON 数据的强大工具，同时充分发挥了 Redis 数据库的性能和灵活性。鉴于 JSON 功能的普适性和广泛的接受程度，Apache Kvrocks 也期望实现对于 Redis JSON 命令的支持，以使得用户可以更容易的处理 JSON 文档。我们目前初步地实现了对于 JSON 数据类型的基础处理能力的脚手架以及对其中最基础的命令（JSON.SET[3] 以及 JSON.GET[4]）的支持，但仍然存在大量的命令等待被实现。因此，我们发起了一个 tracking issue[5]，并号召社区的大家一起参与进来，共同完善对于 JSON 的支持。我们特别欢迎没有参与过的新人借此机会成为开源贡献者，以借此机会逐步了解开源社区的工作机制以及 Kvrocks 的内部实现。

八股文面试
资料链接：https://pan.baidu.com/s/1NQZDW-9_VCnEUXcXp_G-bA?pwd=9987 提取码：9987

讯飞工作，广州出差，在办公室不工作查公积金，违反工作纪律

trpc为何低调开源？
https://github.com/trpc/trpc

美亚有部门要加班的

二面
简历写得不好

讯飞消费者面试
乐大威

大客户 不能有bug
国内，海外

多语种

堆和栈的区别
临时变量 栈
