# 202406_week4

## 时间范围

- 202406 月第 4 周

## 本周记录

- 20240622：输入js代码，输出js的ast；A small, fast, JavaScript-based JavaScript parser；js写的编译器
- 20240623：补充了当日日志，暂未提炼出明确主题。
- 20240624：华为ddm也是支持分区的；mysql8 分区 窗口 概念理解 举例说明；详细解释一下 MySQL 8 中的分区和窗口函数的概念,并提供一些示例说明。
- 20240625：窗口函数与聚合函数不同的是，聚合函数一组只能返回一行，而窗口函数每组可以返回多行；window function；aggregate function
- 20240626：在 MyBatis 的 XML 映射文件中，<![CDATA[ ... ]]> 通常用于包含那些可能包含 XML 特殊字符（如 <、>、&、"、' 等）的文本内容，以避免 XML 解析器错误地解释这些字符。；具体来说，以下是一些使用 <![CDATA[ ... ]]> 的情况：；包含 XML 特殊字符：当 SQL 片段中包含这些特殊字符时，使用 CDATA 可以确保它们被原样保留并传递到数据库，而不是被 XML 解析器解析。
- 20240627：sqlpp11 有命令行工具，依赖python；MySQL Connector/C；MySQL Connector/C++
- 20240628：std::vector<std::unordered_map<std::string, std::string>> data = new std::vector 补全代码；在C++中，`new`运算符用于动态分配内存并返回指向该内存的指针。然而，在你的代码中，`data`的类型是`std::vector<std::unordered_map<std::string, std::string>>`，而不是指针类型。你可以使用直接初始化的方式来创建一个空的`std::vector<std::unordered_map<std::string, std::string>>`对象。以下是补全代码的示例：；通过以上代码，你创建了一个名为`data`的空`std::vector<std::unordered_map<std::string, std::string>>`对象，可以在之后的代码中使用。
