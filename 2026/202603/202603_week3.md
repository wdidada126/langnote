# 202603_week3

## 原有内容


## 时间范围

- 202603 月第 3 周

## 本周记录

- 20260315：RelayPulse 并不是一个 AI 编程工具，而是一个专门用于 监控大模型 API 中转服务 的开源项目。；简单来说，它就像是一个“照妖镜”，专门用来检测市面上那些提供 AI 模型（如 GPT、Claude 等）接口的中转服务商是否真的靠谱。；核心功能
- 20260316：金融支付风控系统是一个融合了实时计算、规则引擎、机器学习与大数据的复杂体系。其核心目标是在毫秒级延迟内，精准识别并拦截欺诈交易，同时平衡用户体验与合规要求。；以下是针对架构设计、核心算法及开源框架的详细拆解：；一、 系统架构设计：分层防控体系
- 20260317：git restore --staged 是 取消暂存文件 的命令。它的作用是：将已添加到暂存区（Staging Area）的文件，移回工作区（Working Directory），但保留文件的修改内容。；工作原理图示；工作区 (Working Directory)  <-- 暂存区 (Staging Area)  <-- 本地仓库 (Local Repository)
- 20260318：性能提升 - 更好的垃圾回收和JIT优化；新特性 - 记录类、模式匹配、密封类等；长期支持 - Oracle官方LTS版本
- 20260319：Playwright 自动化测试；自动化 / Playwright / Puppeteer；sdd skill编程
- 20260320：git remote remove edidadaoutlook；git remote add edidadaoutlook git@edidadaoutlook.com:edidadaoutlook/rest_poco.git；git remote add edidadaoutlook git@edidadaoutlook.com:edidadaoutlook/mycalcbyantlr.git
- 20260321：在 WSL 环境下加速安装 Rust，核心是同时配置 rustup（工具链安装）与 cargo（依赖下载）的国内镜像，以下是一步到位的标准方案。；在当前 WSL 终端中执行以下命令，设置临时镜像环境变量：；随后直接执行安装命令：

## 整理补充

## 时间范围

- 202603 月第 3 周

## 本周记录

- 20260315：RelayPulse 并不是一个 AI 编程工具，而是一个专门用于 监控大模型 API 中转服务 的开源项目。；简单来说，它就像是一个“照妖镜”，专门用来检测市面上那些提供 AI 模型（如 GPT、Claude 等）接口的中转服务商是否真的靠谱。；核心功能
- 20260316：金融支付风控系统是一个融合了实时计算、规则引擎、机器学习与大数据的复杂体系。其核心目标是在毫秒级延迟内，精准识别并拦截欺诈交易，同时平衡用户体验与合规要求。；以下是针对架构设计、核心算法及开源框架的详细拆解：；一、 系统架构设计：分层防控体系
- 20260317：git restore --staged 是 取消暂存文件 的命令。它的作用是：将已添加到暂存区（Staging Area）的文件，移回工作区（Working Directory），但保留文件的修改内容。；工作原理图示；工作区 (Working Directory)  <-- 暂存区 (Staging Area)  <-- 本地仓库 (Local Repository)
- 20260318：性能提升 - 更好的垃圾回收和JIT优化；新特性 - 记录类、模式匹配、密封类等；长期支持 - Oracle官方LTS版本
- 20260319：Playwright 自动化测试；自动化 / Playwright / Puppeteer；sdd skill编程
- 20260320：git remote remove edidadaoutlook；git remote add edidadaoutlook git@edidadaoutlook.com:edidadaoutlook/rest_poco.git；git remote add edidadaoutlook git@edidadaoutlook.com:edidadaoutlook/mycalcbyantlr.git
- 20260321：在 WSL 环境下加速安装 Rust，核心是同时配置 rustup（工具链安装）与 cargo（依赖下载）的国内镜像，以下是一步到位的标准方案。；在当前 WSL 终端中执行以下命令，设置临时镜像环境变量：；随后直接执行安装命令：
