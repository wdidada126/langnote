# 20260315


## 本地网络与 WSL 连接配置

### 当前检测结果（2026-03-15）

Windows 主机 IPv4：

- `vEthernet (WSL (Hyper-V firewall))`：`172.18.176.1`
- `WLAN`：`192.168.31.174`
- `以太网`：`10.7.74.169`
- `Loopback`：`127.0.0.1`

WSL (Ubuntu-24.04) IPv4：

- `172.18.176.57`

端口监听：

- `0.0.0.0:20800` 处于 `LISTEN` 状态（PID `18452`）

WinHTTP 代理：

- 当前为 `Direct access (no proxy server)`

说明：WSL 访问 Windows 本机 `127.0.0.1:20800` 不可达，需要使用 Windows 主机在 WSL 虚拟网卡上的地址 `172.18.176.1:20800`。

### Windows 侧检测命令

```powershell
Get-NetIPAddress -AddressFamily IPv4
Get-NetTCPConnection -LocalPort 20800 -State Listen
netsh winhttp show proxy
```

### WSL 侧检测命令

```bash
hostname -I
cat /etc/resolv.conf
```

### Ubuntu 24.04 代理配置（WSL）

设置当前终端环境变量：

```bash
export http_proxy=http://172.18.176.1:20800
export https_proxy=http://172.18.176.1:20800
```

持久化到 `~/.bashrc`：

```bash
echo 'export http_proxy=http://172.18.176.1:20800' >> ~/.bashrc
echo 'export https_proxy=http://172.18.176.1:20800' >> ~/.bashrc
source ~/.bashrc
```

APT 代理配置：

```bash
sudo tee /etc/apt/apt.conf.d/95proxy >/dev/null <<'EOF'
Acquire::http::Proxy "http://172.18.176.1:20800";
Acquire::https::Proxy "http://172.18.176.1:20800";
EOF
```

Git 代理配置：

```bash
git config --global http.proxy http://172.18.176.1:20800
git config --global https.proxy http://172.18.176.1:20800
```

连通性测试：

```bash
curl -I -x http://172.18.176.1:20800 http://example.com
curl -I -x http://172.18.176.1:20800 https://example.com
```

### Ubuntu 需要安装的常用包

```bash
sudo apt update
sudo apt install -y ca-certificates curl git build-essential
```

### Shadowsocks/obfs（Shadowrocket 兼容说明）

如果 Windows 已提供 `127.0.0.1:20800` 的 HTTP/HTTPS 代理，WSL 只需配置代理即可，不必单独安装 obfs 插件。

若需要在 Ubuntu 侧自建 Shadowsocks + obfs 客户端，可使用以下包：

```bash
sudo apt update
sudo apt install -y shadowsocks-libev simple-obfs
```

示例（需替换为你的服务端信息）：

```bash
obfs-local -s <server_ip> -p <server_port> -l 1080 --obfs http --obfs-host <host>
```

然后将 WSL 代理指向本地端口：

```bash
export http_proxy=http://127.0.0.1:1080
export https_proxy=http://127.0.0.1:1080
```

### 源码编译（可选）

```bash
sudo apt update
sudo apt install -y git autoconf automake libtool pkg-config libssl-dev
git clone https://github.com/shadowsocks/simple-obfs.git
cd simple-obfs
./autogen.sh
./configure
make
sudo make install
```

我来教你**彻底取消并删除**这个代理设置，一步到位，直接复制命令执行即可！

## 一键取消代理（立即生效）
```bash
# 取消当前终端的代理
unset http_proxy
unset https_proxy
unset HTTP_PROXY
unset HTTPS_PROXY

# 从配置文件删除代理行（永久删除）
sed -i '/http_proxy/d' ~/.bashrc
sed -i '/https_proxy/d' ~/.bashrc

# 重新加载配置
source ~/.bashrc
```

## 验证是否已取消
执行下面命令，**什么都不输出**就说明成功取消：
```bash
echo $http_proxy
echo $https_proxy
```

### 完整说明
你之前运行的这两行：
```bash
echo 'export http_proxy=http://172.18.176.1:20800' >> ~/.bashrc
echo 'export https_proxy=http://172.18.176.1:20800' >> ~/.bashrc
```
作用是**给系统全局设置代理**，所有网络请求都会走这个代理。
我给你的命令会：
1. **立刻清除当前终端代理**
2. **永久删除配置文件里的代理设置**
3. **重启生效**

### 总结
1. 复制上面**第一段一键命令**直接运行
2. 用 `echo $http_proxy` 验证，无输出=取消成功
3. 之后 npm、git、curl 都会走正常直连网络


