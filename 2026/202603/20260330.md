# 20260330

git@github.com:edidadaoutlook/learn_books.git

# 1. 下载并解压
tar -xzf openssl-3.2.1.tar.gz
cd openssl-3.2.1

# 2. 配置
./config --prefix=/usr/local/openssl --openssldir=/usr/local/openssl shared zlib

# 3. 编译
make

# 4. 安装
sudo make install
