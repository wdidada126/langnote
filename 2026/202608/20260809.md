# 20260809


Redis公司（Redis Inc.）在2026年5月16日正式发布的官方产品，全称就是 Redis Iris。

它不是博主包装的概念，而是一个专为AI Agent（人工智能代理）设计的实时上下文引擎，旨在解决大模型在实际应用中面临的“数据碎片化、上下文不新鲜、无记忆”等核心痛点。

根据官方信息，Redis Iris的核心能力可以总结为下表：

核心能力 解决的问题 关键功能/组件
统一、实时的上下文 数据分散在不同系统（CRM、订单等），导致Agent回答基于过时、碎片化的信息。 Redis Data Integration：实时从业务系统拉取最新数据灌入Redis。 Context Retriever：为Agent提供清晰的、基于业务实体（如客户、订单）的数据访问路径。
让上下文“可导航” Agent只能进行简单的查询，无法像人一样在关联数据中“探索”和“推理”。 构建业务实体之间的连接，让Agent能理解“这个订单属于哪个客户”这类关系，而不是孤立的查询。
让上下文“复合”增长 Agent每次对话都是“失忆”的，无法记住用户偏好或历史决策。 Agent Memory：提供工作记忆（当前会话）和长期记忆（跨会话的用户偏好、历史决策），让每次交互都能建立在过往基础上。
极低的延迟 检索和推理步骤多，耗时过长，导致Agent在高并发下容易超时或崩溃。 Redis Search：在Redis速度下过滤和检索运营上下文。 LangCache：缓存语义相似的提示词和响应，避免重复计算，保证<250ms P95的查询延迟。

总结与建议

Redis Iris并非一个简单的数据库，而是一套建立在Redis之上的、面向AI Agent全生命周期管理的完整基础设施层。它的核心创新在于将实时数据集成、向量/语义检索、短期与长期记忆整合到同一个实时运行环境中。

如果你正在从事AI Agent开发，并面临数据不实时、无记忆、延迟高等生产环境问题，值得深入研究。建议你直接访问官网，查看“State of context engineering”报告和“Context engineering maturity model”评估模型，这比任何二手解读都更权威和前沿。你之前看到的视频，正是对这套官方技术的解读，现在可以放心学习了。

https://redis.io/iris/