# 202604_week3

## 时间范围

- 202604 月第 3 周

## 本周记录

- 20260415：这道题是**分布式系统高可靠告警**经典面试题，考的是：**可靠性、一致性、实时性、复杂度、成本、运维成本**的权衡。；我直接给你**面试标准答案版**，结构清晰、好背、有对比、有结论。；**同步调用 + 重试 + 熔断降级**
- 20260416：先给你一句话结论：；NameServer 不存消息，只存路由（Topic → Broker 地址），无 Raft、无持久化、不丢消息（因为本来就不存）。；Broker 才存消息（CommitLog）：
- 20260417：包含：字符串校验完整规则 + 数字类型完整校验项 + 注解写法 + 全局异常返回统一JSON + 不通过直接抛业务异常；基于 SpringBoot 2.7+/3.x 原生 `jakarta.validation`（SpringBoot3已替换javax.validation）；所有前端传入参数必须后端强校验，不信任前端
- 20260418：export http_proxy=http://127.0.0.1:10800;；export https_proxy=http://127.0.0.1:10800;；export http_proxy=http://127.0.0.1:7899;
- 20260419：我给你一次性讲清楚：；这段是 前端登录退出接口的请求报文，我会告诉你：；1）每个字段含义
- 20260420：conan.txt解决不了冲突；conanfile.py可以；conan list "qt/*"
- 20260421：Windows PowerShell 里的 curl 根本不是真 curl，是 Invoke-WebRequest 的别名，非常难用。；本文全部针对 Kafka 3.0~3.9，区分旧版 eager / 新版增量协作重平衡，附完整日志检索命令。；重平衡（Consumer Group Rebalance）是消费者组分区自动分配机制，由 Broker 端 Group Coordinator 协调。
