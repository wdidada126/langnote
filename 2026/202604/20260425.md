# 20260425

npx @react-native-community/cli init MyRNiOSApp --version 0.72.0


CocoaPods

CocoaPods = iOS/macOS 的依赖管理工具
React Native iOS 项目必须用它管理 Pods
CocoaPods 版本太低或者 Ruby 环境不好，会导致 pod install 失败，影响编译

If you need to have ruby first in your PATH, run:
  echo 'export PATH="/usr/local/opt/ruby/bin:$PATH"' >> /Users/ibqo/.bash_profile

For compilers to find ruby you may need to set:
  export LDFLAGS="-L/usr/local/opt/ruby/lib"
  export CPPFLAGS="-I/usr/local/opt/ruby/include"

For pkg-config to find ruby you may need to set:
  export PKG_CONFIG_PATH="/usr/local/opt/ruby/lib/pkgconfig"
  