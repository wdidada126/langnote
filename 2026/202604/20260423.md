# 20260423
https://freesql.com/

# 📚 Oracle FreeSQL 左侧 Navigator 库说明（完整翻译+用途，2026最新官方样本库）
你打开的是 Oracle 官方公网在线免费SQL环境 FreeSQL / LiveSQL，左边下拉列表全是Oracle官方自带标准测试样本Schema（示例库），全球Oracle教学、面试、练手通用，我逐个给你讲清楚用途、表结构、适合练什么SQL。

## 完整列表逐行解释
| 库名(缩写) | 中文全称 | 核心用途 & 适合练习SQL | 业务场景 |
| --- | --- | --- | --- |
| Academic (AD) | 学术教育库 | 学生、课程、成绩、专业、班级数据；适合基础SQL、单表查询、分组统计 | 教务、学生成绩系统 |
| Analytic Views (AV) | 分析视图库 | Oracle数据仓库、多维分析、Cube聚合、BI报表；适合OLAP分析、分组聚合、窗口函数 | 大数据报表、数据仓库（对应你FineReport报表） |
| Customer Orders (CO) | 客户订单库 | 客户、商品、订单、订单明细；最适合练 多表JOIN关联查询 | 电商订单、CRM客户系统（你之前MySQL JOIN慢的对应场景） |
| Human Resources (HR) | 人力资源库（你当前选中） | 员工、部门、职位、薪资、地点；Oracle最经典入门库，练基础SQL、自关联、子查询 | 人事HR、员工管理系统 |
| Order Entry (OE) | 销售订单库 | 完整电商进销存：客户、订单、仓库、产品、库存；适合复杂多表关联、外键约束 | 企业进销存、订单业务系统 |
| Olympic Data (OLYM) | 奥运会赛事库 | 历届奥运会、运动员、国家、奖牌、项目；适合范围查询、排序、统计、分组 | 大数据统计、赛事分析 |
| Projects (PROJECTS) | 项目管理库 | 项目、任务、员工、进度、成本；适合关联查询、层级递归查询 | PMP项目管理、工单系统 |
| EMP and DEPT (SCOTT) | 斯科特经典入门库 | Oracle全世界最古老、最经典入门库：员工表emp、部门表dept；面试必背入门SQL | SQL零基础入门、面试基础题 |
| Sales History (SH) | 销售历史数据仓库 | 千万级海量销售历史数据；适合分区表、大数据查询、性能优化、Explain Plan执行计划 | 大数据风控、AML反洗钱、海量数据查询（你之前Oracle分区表、ES优化查询完全对应） |



## 🔥 重点库详细说明（你学习优先级）
### 1. SCOTT (EMP DEPT) —— 入门首选
全世界Oracle入门第一个库，只有2张表：
- `dept` 部门表
- `emp` 员工表
适合：select基础、where条件、group by分组、join关联、子查询、面试基础SQL

### 2. HR 人力资源（你当前选中）
Oracle官方标准HR库，包含：员工、部门、工作岗位、薪资、国家、地址
适合：多表join、自关联、函数、条件查询，企业业务SQL练手首选

### 3. SH 销售历史库（最适合你现在学习）
完全对应你之前学习的知识点：
- Oracle分区表
- 海量大数据查询
- 多表JOIN性能优化
- Explain Plan执行计划（查看查询耗时）
- Oracle→ES数据同步宽表优化

### 4. AV 分析视图库
对应你FineReport报表、Oracle分析函数、窗口函数、数据仓库OLAP。



## 一句话总结
左边列表 = Oracle官方免费、现成、可直接写SQL练手的测试数据库
从零基础入门 → 大数据性能优化 → 报表分析全覆盖，是你学Oracle、练SQL、面试刷题的全部素材库。

需要我给你每个库最经典的入门SQL查询语句，直接复制粘贴到中间Worksheet运行吗？

# 安卓 Gradle 多环境（dev/sit/uat/prod）最标准、最稳定、2026最新方案
完全对标你之前用的 Maven Profile，但这是 Gradle 安卓官方用法，能做到：
- 打包时自动切换环境
- 代码里直接拿环境变量
- XML / BuildConfig 都能用
- 命令行：`./gradlew assembleDevDebug`

我直接给你全套可复制配置，一步到位！



# 1. 先在 app/build.gradle 里配置 4 个环境（核心）
```gradle
android {
    compileSdk 35

    // 👇 定义多环境：dev / sit / uat / prod
    flavorDimensions "env" // 固定维度名
    productFlavors {
        dev {
            buildConfigField "String", "ENV", "\"dev\""
            buildConfigField "String", "API_HOST", "\"https://dev-api.xxx.com\""
        }
        sit {
            buildConfigField "String", "ENV", "\"sit\""
            buildConfigField "String", "API_HOST", "\"https://sit-api.xxx.com\""
        }
        uat {
            buildConfigField "String", "ENV", "\"uat\""
            buildConfigField "String", "API_HOST", "\"https://uat-api.xxx.com\""
        }
        prod {
            buildConfigField "String", "ENV", "\"prod\""
            buildConfigField "String", "API_HOST", "\"https://api.xxx.com\""
        }
    }

    // 👇 必须开启 BuildConfig（新版 Gradle 默认关闭）
    buildFeatures {
        buildConfig true
    }
}
```



# 2. 代码里直接使用（Java / Kotlin 都能用）
```kotlin
// 自动生成的类，不需要自己写
String env = BuildConfig.ENV;             // dev / sit / uat / prod
String host = BuildConfig.API_HOST;       // 对应环境接口地址
```

完全等价于 Maven profile 切换！



# 3. 打包命令（你最关心）
```bash
# dev 包
./gradlew assembleDevDebug
./gradlew assembleDevRelease

# sit
./gradlew assembleSitDebug
./gradlew assembleSitRelease

# uat
./gradlew assembleUatDebug
./gradlew assembleUatRelease

# prod
./gradlew assembleProdDebug
./gradlew assembleProdRelease
```



# 4. 对应 Android Studio 切换环境
右上角 `Build Variants`
直接选择：
- devDebug
- sitDebug
- uatDebug
- prodDebug

点 Run 运行就是对应环境！



# 5. 最关键对比（你之前用 Maven，现在看秒懂）
| Maven | Gradle 安卓 |
|---|---|
| `-P dev` | `assembleDev` |
| `-P sit` | `assembleSit` |
| profile 变量 | BuildConfig 字段 |
| application-dev.yml | flavor 配置 |

功能一模一样，只是安卓 Gradle 官方叫 productFlavors



# 🎯 终极总结
你要的 dev/sit/uat/prod 4套环境自动切换
Gradle 安卓用 productFlavors 实现，就是 Maven Profile 的等价方案！

---

需要我再给你：
多环境 + 不同包名 + 不同图标 + 不同配置文件 的完整版吗？
企业级上线标准配置！

test dev sit prod 这些profile

mvn clean package -P dev -DskipTests
mvn clean package -P dev -DskipTests
mvn -q clean package -Pdev -DskipTests
mvn -q -pl gateway-module/gateway-service -am clean compile
mvn -q -pl gateway-module/gateway-service -am spring-boot:run -Dspring-boot.run.profiles=dev
mvn -q -pl gateway-module/gateway-service -am spring-boot:run "-Dspring-boot.run.profiles=dev"
mvn -q -pl gateway-module/gateway-service -am install -DskipTests

-Pdev 和 -P dev 无任何区别
加不加空格，maven 都认
-q 只是减少日志，不影响打包

# 我给你讲最直白、最本质、不绕弯的区别！
## 一句话总结
这俩完全不是一个东西，但可以联动！



# 1. 先给你最清晰定义（背会就懂）
## ① `application.yml` 里的
```yaml
spring:
  profiles:
    active: dev
```
叫：SpringBoot 环境配置
作用：决定项目启动时加载哪个配置文件
- `application-dev.yml`
- `application-prod.yml`
运行时生效
跟 Maven 没关系


## ② `mvn clean package -P dev`
叫：Maven 构建 Profile
作用：决定打包时用什么参数
编译/打包时生效
跟 Spring 没关系



# 2. 它们的真正关系（90%的人都搞混）
## 默认情况下：
它们互相独立，各玩各的！
你写 `active: dev`，不影响 maven 打包
你用 `-P prod`，不影响 spring 读配置



# 3. 企业级真正用法（联动起来）
我们会让 Maven 把环境变量传给 SpringBoot！

## 做法：
### 1）pom.xml 里定义
```xml
<profile>
    <id>dev</id>
    <properties>
        <env>dev</env>
    </properties>
</profile>
```

### 2）application.yml 里写成
```yaml
spring:
  profiles:
    active: @env@
```

### 3）然后打包
```bash
mvn clean package -P dev
```

### 最终效果：
Maven 的 `-P dev`
→ 把 `env=dev` 传给 Spring
→ Spring 自动激活 `dev` 环境



# 4. 超级直观对比
| 名称 | 属于谁 | 作用 | 时机 |
|---|---|---|---|
| `spring.profiles.active` | SpringBoot | 选配置文件 | 项目运行时 |
| `-P dev` | Maven | 构建参数 | 打包编译时 |



# 5. 你最容易踩的坑
### 坑1：
你在 yml 写 `active: dev`
但用 `mvn -P prod` 打包
→ 最终运行还是 dev 环境！

### 坑2：
你想打包 prod 版本
必须要么：
- yml 写 `active: prod`
要么
- 用 `@env@` + `mvn -P prod`



# 🎯 终极结论（你记这句就够）
## Spring profiles = 运行时环境
## Maven profile = 编译打包时环境
## 可以联动，但默认互不影响！



需要我给你配置一套企业级标准联动方案吗？
打包时自动把环境传给 Spring，再也不会错！
