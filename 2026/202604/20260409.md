# 20260409

在 Python 工业级项目里，单纯用 pip + requirements.txt 是不够的，缺少版本锁定、环境隔离、依赖解析、构建发布等能力。

下面给你最主流、最生产级、企业真正在用的工具，按成熟度排序：


# 1. Poetry（最推荐，工业级标准）
## 用途
- 环境隔离
- 依赖管理
- 版本精确锁定
- 自动打包发布
- 无手动写 requirements.txt

## 核心文件
- `pyproject.toml` 依赖声明
- `poetry.lock` 精确锁定（类似 package-lock.json）

## 常用命令
```bash
poetry install          # 安装全部依赖
poetry add requests     # 安装包
poetry remove requests  # 删除包
poetry export -f requirements.txt --output requirements.txt --without-hashes  # 导出给老项目用
```

## 优点
- 依赖解析最强
- 环境隔离干净
- 适合生产、微服务、后台项目
- 大厂主流

# 2. PDM（现代Python包管理，速度快）
## 用途
- 比 Poetry 更快
- 标准 PEP 规范
- 适合大型项目

## 命令
```bash
pdm add requests
pdm install
pdm export -o requirements.txt
```

## 优点
- 速度快
- 兼容性强
- 适合大型团队

# 3. Pipenv（Pip + Virtualenv 合体）
## 用途
- 早期工业级工具
- 简单、稳定

## 文件
- `Pipfile`
- `Pipfile.lock`

## 命令
```bash
pipenv install
pipenv install requests
pipenv lock -r > requirements.txt
```

## 优点
- 简单稳定
- 适合中小型项目

# 4. uv（Rust写的超级快工具）
## 用途
- 最快的依赖安装
- 适合大型 monorepo、企业级项目

## 命令
```bash
uv add requests
uv sync
```

## 优点
- 极快
- 适合大型项目

# 企业级总结（你直接选）
## ✔ 新项目、后台服务、微服务 → Poetry（最推荐）
## ✔ 大型项目、追求速度 → PDM / uv
## ✔ 老项目兼容 → Pipenv

# 我可以直接给你
- 生成 `pyproject.toml`
- 生成 `poetry.lock`
- 导出 `requirements.txt`
- 一键构建 Docker 生产环境
