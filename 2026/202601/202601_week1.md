# 202601_week1

## 原有内容


## 时间范围

- 202601 月第 1 周

## 本周记录

- 20260101：元旦三天假；深入浅出hyperscan.md；计算机存储系统领域的最顶级会议FAST
- 20260102：元旦三天假；优先图（Precedence Graph）；Nachos（全称Not Another Completely Heuristic Operating System）是一款基于MIPS R2/3000指令集虚拟机构建的教学操作系统骨架
- 20260103：Gentoo Linux；我用的Gentoo Linux，所以我不用apt或者pacman，而是使用emerge.但是呢，Gentoo Linux上的更新流程是，从git仓库更新gentoo.git和各个overlay，然后跑依赖计算emerge -uDUav @world，然后portage包管理器自动地逐个在tmpfs内存盘/tmp/portage里面编译包，编译完了安装到硬盘，包管理器自动追踪soname依赖会不会被破坏。然后手动cd到/usr/src/linux, 跑git fetch upstream && git rebase -i upstream ，然后make LLVM=1 -j32再sudo make install && sudo make modules_install，最后重启。如果要更新的包比较多，或者libc、编译器有更新，那就提前跑sudo zfs snapshot data/gentoo@20260101 瞬间创建一个快照。如果有包编译不过，或者测试不过，我可以在tmpfs里分析源码和编译日志，修改编译环境或者源码。如果有装上去的包报错或者段错误，我可以开-O0 -g重新编译包，然后上lldb、strace、改源码加printf精确分析程序行为。确定问题之后，要么改/etc/portage下面的配置，要么加patch，要么改ebuild脚本。由于我使用的musl libc和LLVM libc++和clang/flang/lld/LLVM tools比较特殊，因此我安装的1500多个包中大约30%的包需要处理。Gentoo甚至支持部分包直接用git仓库的最新版，也可以手动进行二分测试，然后把部分commit下载作为patch安装。如果启动不了，我可以在initramfs里重启到同一个ZFS存储池里安装的archlinux，再去修Gentoo Linux，或者直接从ZFS快照里捞文件/直接回退。如果掉盘了/电脑爆炸了/丢了，我可以从另一个电脑的机械硬盘里的ZFS存储池直接ssh root@nas zfs send -RVv data/gentoo@20260101 | sudo zfs receive -Fuv data/gentoo就可以把之前的快照下载回来。当然要定期反方向send快照过去。 相反的事情，我的Windows加了beta通道，目前版本是26220.7523，但是由于AB测试的存在，我根本不知道这个版本号意味着什么。Windows的补丁包是一个整体，我无法单独更新其中的一个部分。我没有源码，很难调试Windows内部问题。Windows也没有将ReFS作为系统盘的默认格式，不支持全盘快照备份。就比如我使用的一台笔记本最近不知道更新了什么，屏幕偶尔闪屏，但是Linux并不闪，明显是软件问题，但我压根无法排查到底是哪个Windows更新导致了这个问题。；如果是为了学操作系统原理，或者想手写一个操作系统，没必要研究Linux kernel 0.11，因为太古老了。推荐学xv6，MIT用于教学的Unix系统，2万行代码，涉及保护模式、中断、多进程：https://github.com/mit-pdos/xv6-riscvgithub.com/mit-pdos/xv6-riscv 用qemu模拟器运行和调试，还有配套视频（YouTube或B站搜xv6）。
- 20260104：您之前使用 --depth 1 只克隆了最新提交，现在想要下载完整的提交历史，有以下几种方法：；一、方法1：使用 --unshallow 获取完整历史；cd postgres
- 20260105：IPC::Run 是一个Perl模块，用于运行和管理子进程。PostgreSQL 的 TAP 测试框架需要它。；msgfmt是gettext工具，用于编译 .po文件为 .mo文件，PostgreSQL需要它来处理国际化支持。WxWidgets也要用gettext；SimpleDB_Chinese_V1_0.pdf
- 20260106：2026年1月5日，内蒙古农村商业银行发布新一代核心系统群核心业务系统项目、统一柜面系统项目的公开招标公告，项目采购限价分别为2500万元、1000万元。招标文件获取时间截止至1月12日下午17点，开标时间为1月26日上午9点半。；以信用卡核心系统为例，股份制银行的核心系统基本都自主可控，也就是自己独立研发迁移了，现在国有大行以及11家股份制银行里基本就剩华夏银行还在银数外包了。；中国共有12家全国性股份制商业银行，而非11家。这12家银行包括：招商银行、兴业银行、浦发银行、中信银行、民生银行、光大银行、平安银行、华夏银行、广发银行、浙商银行、渤海银行、恒丰银行。
- 20260107：PostgreSQL数据库内核分析；计算机科学中的数学；id生成器 搞个来源的来用

## 整理补充

## 时间范围

- 202601 月第 1 周

## 本周记录

- 20260101：元旦三天假；深入浅出hyperscan.md；计算机存储系统领域的最顶级会议FAST
- 20260102：元旦三天假；优先图（Precedence Graph）；Nachos（全称Not Another Completely Heuristic Operating System）是一款基于MIPS R2/3000指令集虚拟机构建的教学操作系统骨架
- 20260103：Gentoo Linux；我用的Gentoo Linux，所以我不用apt或者pacman，而是使用emerge.但是呢，Gentoo Linux上的更新流程是，从git仓库更新gentoo.git和各个overlay，然后跑依赖计算emerge -uDUav @world，然后portage包管理器自动地逐个在tmpfs内存盘/tmp/portage里面编译包，编译完了安装到硬盘，包管理器自动追踪soname依赖会不会被破坏。然后手动cd到/usr/src/linux, 跑git fetch upstream && git rebase -i upstream ，然后make LLVM=1 -j32再sudo make install && sudo make modules_install，最后重启。如果要更新的包比较多，或者libc、编译器有更新，那就提前跑sudo zfs snapshot data/gentoo@20260101 瞬间创建一个快照。如果有包编译不过，或者测试不过，我可以在tmpfs里分析源码和编译日志，修改编译环境或者源码。如果有装上去的包报错或者段错误，我可以开-O0 -g重新编译包，然后上lldb、strace、改源码加printf精确分析程序行为。确定问题之后，要么改/etc/portage下面的配置，要么加patch，要么改ebuild脚本。由于我使用的musl libc和LLVM libc++和clang/flang/lld/LLVM tools比较特殊，因此我安装的1500多个包中大约30%的包需要处理。Gentoo甚至支持部分包直接用git仓库的最新版，也可以手动进行二分测试，然后把部分commit下载作为patch安装。如果启动不了，我可以在initramfs里重启到同一个ZFS存储池里安装的archlinux，再去修Gentoo Linux，或者直接从ZFS快照里捞文件/直接回退。如果掉盘了/电脑爆炸了/丢了，我可以从另一个电脑的机械硬盘里的ZFS存储池直接ssh root@nas zfs send -RVv data/gentoo@20260101 | sudo zfs receive -Fuv data/gentoo就可以把之前的快照下载回来。当然要定期反方向send快照过去。 相反的事情，我的Windows加了beta通道，目前版本是26220.7523，但是由于AB测试的存在，我根本不知道这个版本号意味着什么。Windows的补丁包是一个整体，我无法单独更新其中的一个部分。我没有源码，很难调试Windows内部问题。Windows也没有将ReFS作为系统盘的默认格式，不支持全盘快照备份。就比如我使用的一台笔记本最近不知道更新了什么，屏幕偶尔闪屏，但是Linux并不闪，明显是软件问题，但我压根无法排查到底是哪个Windows更新导致了这个问题。；如果是为了学操作系统原理，或者想手写一个操作系统，没必要研究Linux kernel 0.11，因为太古老了。推荐学xv6，MIT用于教学的Unix系统，2万行代码，涉及保护模式、中断、多进程：https://github.com/mit-pdos/xv6-riscvgithub.com/mit-pdos/xv6-riscv 用qemu模拟器运行和调试，还有配套视频（YouTube或B站搜xv6）。
- 20260104：您之前使用 --depth 1 只克隆了最新提交，现在想要下载完整的提交历史，有以下几种方法：；一、方法1：使用 --unshallow 获取完整历史；cd postgres
- 20260105：IPC::Run 是一个Perl模块，用于运行和管理子进程。PostgreSQL 的 TAP 测试框架需要它。；msgfmt是gettext工具，用于编译 .po文件为 .mo文件，PostgreSQL需要它来处理国际化支持。WxWidgets也要用gettext；SimpleDB_Chinese_V1_0.pdf
- 20260106：2026年1月5日，内蒙古农村商业银行发布新一代核心系统群核心业务系统项目、统一柜面系统项目的公开招标公告，项目采购限价分别为2500万元、1000万元。招标文件获取时间截止至1月12日下午17点，开标时间为1月26日上午9点半。；以信用卡核心系统为例，股份制银行的核心系统基本都自主可控，也就是自己独立研发迁移了，现在国有大行以及11家股份制银行里基本就剩华夏银行还在银数外包了。；中国共有12家全国性股份制商业银行，而非11家。这12家银行包括：招商银行、兴业银行、浦发银行、中信银行、民生银行、光大银行、平安银行、华夏银行、广发银行、浙商银行、渤海银行、恒丰银行。
- 20260107：PostgreSQL数据库内核分析；计算机科学中的数学；id生成器 搞个来源的来用
