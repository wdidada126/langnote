# Pingora


Pingora的URL鉴权机制是一种网络安全措施，它允许只有特定的URL能够通过校验，从而控制对资源的访问权限。这种机制通过嵌入在URL中的验证信息（如时间戳、签名、用户ID等）来实现，以确保只有经过授权的用户才能访问特定的资源。以下是对Pingora URL鉴权的详细分析：

一、原理
确定鉴权参数：选择用于鉴权的参数，这些参数通常包括用户ID、请求时间戳、请求的资源路径等。
生成签名：使用哈希算法（如HMAC、SHA256）和密钥对选定的参数进行加密，生成签名。这个签名将作为验证用户身份和访问权限的重要依据。
构造URL：将签名和其他参数添加到URL中，形成带有鉴权信息的完整URL。
验证签名：当用户请求带有鉴权信息的URL时，服务器会解析URL中的参数，并使用相同的签名算法和密钥重新生成签名。然后，服务器会将重新生成的签名与URL中的签名进行比对，以确保请求的合法性。
二、应用场景
Pingora的URL鉴权机制广泛应用于多种业务场景，包括但不限于：

文件下载：生成带有鉴权信息的下载链接，确保只有授权用户能够获取文件。
API访问：对于需要鉴权的API接口，使用URL鉴权来保护资源，防止未授权的访问。
临时链接：生成有效期有限的访问链接，确保链接在一定时间内有效，以防止链接被分享给未授权用户。
支付链接：在电子商务平台中，生成带有鉴权的支付链接，确保支付请求的安全性。
三、安全性提升
为了进一步提升Pingora URL鉴权的安全性，可以采取以下措施：

选择强健的签名算法：如SHA256等，确保签名的安全性。
加入时间戳和随机数：在URL中加入时间戳和随机数，确保每次请求的唯一性。服务器在验证签名时，应检查时间戳是否在有效范围内，并验证随机数是否已使用过，以防止重放攻击。
安全存储密钥：确保签名所用的密钥安全存储，避免在前端代码中暴露。密钥的泄露将直接导致鉴权机制的失效。
记录鉴权日志：记录鉴权的成功与失败情况，以便后续分析和审计。通过监控异常访问行为，可以及时发现并阻止潜在的攻击。
四、注意事项
在使用Pingora URL鉴权时，需要注意以下几点：

URL长度限制：注意URL的长度限制，尤其在参数较多时，可以考虑使用POST请求或短链接。
时间同步：服务器的时间需要准确，以避免因时间戳不一致导致的鉴权失败。
定期审计和更新：定期对鉴权机制进行审计和更新，及时发现并修复潜在的漏洞。
综上所述，Pingora的URL鉴权机制是一种有效的网络安全措施，通过嵌入在URL中的验证信息来控制对资源的访问权限。在实际应用中，需要结合具体场景和需求进行合理设计和实现，以确保用户的数据安全和系统的稳定运行。

Cloudflare表示，多年来在NGINX的使用上遇到了很多限制，包括损害性能的架构，某些类型的功能难以添加等。在Rust编写的Pingora上使用，消耗的CPU和内存能减少约70%和67%，除了性能优势外，还很安全。

https://www.zhihu.com/answer/2683364208

就拿国内公司环境往国外公司硬套。Nginx 的问题文中说的很清楚了，无论是 request per worker 导致 CPU 负载不均衡的问题还是 tls cache miss 的问题都是业界有目共睹的，随便一搜就是各种相关提问，而 Nginx的架构导致根本无法完全规避这些问题。

“在nginx已经足够强劲” 强劲还能省出 70% CPU 和 67% 内存的吗 

git clone https://github.com/cloudflare/pingora.git
cd pingora

env:
  BUILD_TYPE: Release
  CARGO_TERM_COLOR: always


“request per worker 导致 CPU 负载不均衡的问题”通常出现在多线程或多进程服务器环境中，其中“worker”指的是处理请求的线程或进程。当每个worker处理的请求数量不均衡时，就会导致CPU负载不均衡，进而影响服务器的整体性能和响应时间。以下是对这一问题的详细描述：

一、问题背景
在多线程或多进程服务器架构中，通常会将请求分配给多个worker进行处理。这些worker负责执行请求所需的操作，并返回结果。然而，如果请求分配不均衡，就会导致某些worker过载，而另一些worker则处于空闲状态。

二、导致CPU负载不均衡的原因
请求分配策略不当：
如果请求分配策略没有考虑到worker的当前负载情况，就可能导致某些worker被分配过多的请求，而其他worker则请求不足。
常见的分配策略包括轮询、随机分配等，但这些策略可能无法在所有情况下都保证负载均衡。
请求处理时间差异：
不同类型的请求可能需要不同的处理时间。例如，一些请求可能涉及复杂的计算或数据库操作，而另一些请求则可能只是简单的数据检索。
如果所有请求都按照相同的优先级进行处理，就可能导致处理时间较长的请求占据过多的CPU资源，从而影响其他请求的响应时间。
系统资源限制：
服务器的CPU、内存等资源是有限的。当某个worker处理的请求过多时，可能会耗尽其所在的CPU核心资源，从而导致该核心上的负载过高。
同时，如果内存不足，也可能导致频繁的磁盘I/O操作，进一步加重CPU的负担。
三、影响与后果
性能下降：
CPU负载不均衡会导致服务器的整体性能下降。过载的worker可能无法及时处理新的请求，从而导致请求堆积和响应时间延长。
资源浪费：
空闲的worker意味着CPU资源的浪费。这些资源本可以用于处理其他请求，但由于请求分配不均衡而无法得到充分利用。
用户体验受损：
对于用户来说，他们可能会遇到请求超时、响应缓慢等问题。这不仅会影响他们的使用体验，还可能导致他们对服务器的信任度降低。
四、解决方案与建议
优化请求分配策略：
采用更智能的请求分配策略，如基于worker当前负载的动态分配算法。这种算法可以根据worker的实时负载情况动态调整请求分配，以确保负载均衡。
引入负载均衡器：
在服务器前端引入负载均衡器，根据worker的负载情况动态地将请求分配给不同的worker。这有助于减轻单个worker的负载压力，提高整体的负载均衡性。
监控与调整：
实时监控服务器的CPU负载情况，并根据监控结果进行调整。例如，当发现某个worker的负载过高时，可以手动将部分请求转移到其他worker上。
优化请求处理流程：
对请求处理流程进行优化，减少不必要的计算和I/O操作。这有助于缩短请求的处理时间，从而减轻worker的负载压力。
增加硬件资源：
如果服务器的硬件资源不足，可以考虑增加CPU核心数、内存等硬件资源。这有助于提高服务器的处理能力，从而更好地应对高并发请求。
综上所述，“request per worker 导致 CPU 负载不均衡的问题”是一个复杂而重要的问题。通过优化请求分配策略、引入负载均衡器、监控与调整、优化请求处理流程以及增加硬件资源等措施，可以有效地解决这一问题，提高服务器的整体性能和用户体验。


TLS（传输层安全协议）中的“cache miss”问题是一个相对复杂且具体的技术议题，它主要涉及到TLS会话的缓存和检索机制。以下是对TLS cache miss问题的详细解释：

一、TLS会话缓存机制
TLS会话缓存是一种优化机制，用于存储和检索TLS会话的密钥和其他相关信息。当客户端和服务器建立TLS连接时，它们会协商一套加密密钥，这些密钥用于保护后续的数据传输。为了加速后续的连接建立过程，这些密钥和相关信息可以被缓存在客户端和/或服务器上。

二、TLS cache miss的含义
在TLS环境中，“cache miss”指的是当尝试从缓存中检索TLS会话信息时未能找到匹配项的情况。换句话说，当客户端或服务器尝试利用缓存中的信息来加速TLS连接的建立时，如果缓存中没有存储与当前请求相匹配的会话信息，就会发生cache miss。

三、TLS cache miss的原因
TLS cache miss可能由多种原因引起，包括但不限于：

缓存未命中：最直接的原因是缓存中根本没有存储与当前请求相匹配的TLS会话信息。这可能是因为这是一个全新的连接，或者之前的会话信息已经被清除或过期。
缓存容量限制：如果TLS会话缓存的容量有限，那么当新的会话信息被添加时，旧的会话信息可能会被覆盖或删除。这可能导致在后续尝试检索这些被删除的会话信息时出现cache miss。
会话标识符不匹配：TLS会话通常使用唯一的会话标识符来区分不同的会话。如果客户端或服务器在尝试检索会话信息时使用了错误的会话标识符，那么就会发生cache miss。
缓存一致性问题：在多服务器环境中，如果TLS会话缓存不是集中管理的，那么不同的服务器之间可能会出现缓存不一致的情况。这可能导致在一个服务器上能够检索到的会话信息在另一个服务器上却无法检索到。
四、TLS cache miss的影响
TLS cache miss会对TLS连接的建立速度和整体网络性能产生负面影响。具体来说：

增加连接建立时间：当发生cache miss时，客户端和服务器需要重新进行TLS握手过程，这会增加连接建立的时间。
增加网络负载：重新进行TLS握手需要传输额外的数据，这会增加网络的负载。
降低用户体验：对于用户来说，长时间的连接建立过程可能会导致不佳的体验，特别是在需要频繁建立TLS连接的应用场景中。
五、解决TLS cache miss的方法
为了解决TLS cache miss问题，可以采取以下措施：

增加缓存容量：通过增加TLS会话缓存的容量，可以减少因缓存容量限制而导致的cache miss。
优化缓存管理策略：采用更智能的缓存管理策略，如LRU（最近最少使用）算法，可以更有效地利用缓存空间，减少不必要的cache miss。
集中管理缓存：在多服务器环境中，采用集中管理的TLS会话缓存可以确保缓存的一致性，减少因缓存不一致而导致的cache miss。
使用会话恢复机制：TLS协议提供了会话恢复机制，如会话标识符和会话票据，这些机制可以在一定程度上减少cache miss的发生。通过合理使用这些机制，可以加速TLS连接的建立过程。
综上所述，TLS cache miss问题是一个需要关注的技术议题。通过了解其原因、影响以及解决方法，可以更好地优化TLS连接的建立过程，提高网络性能和用户体验。
